# 02 — Scorecard (r6; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `03875ed`)

1. Good design is innovative — Score: 2/3 (r5 2, Δ0)
   Evidence: [C8] identity-free publish, no-author-column storage, browser-held ownership keys and category-only reports re-verified with zero drift (`api/client.ts`, `packages/backend/**`, `WhyPage.tsx` byte-identical since `5d810d8`); every pattern this round touched (in-flow empty state, two-row actions, `:focus` ring, `aria-describedby`, `?date=` deep link, persisted SW signature) is conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing shipped is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1.

2. Good design makes a product useful — Score: 2/3 (r5 2, Δ0)
   Evidence: [S17][W9] next lesson 1 screen / 0 taps; stream 1 tap; share 4 screens / 3 taps to the editor (4 taps to a cursor), `activeElement` BODY on landing; every r5 blocker verified gone — [R1] the empty-day answer is on screen at 320 and 390, [R6] the composer no longer fails open, [A-R5-A1] retries no longer scroll the filters away, [R11] the never-listed CTA is 220×44. New detours on adjacent surfaces: [R6-1] every recovery affordance ("Try again") is 77×36 on 8 surfaces and reactions/`···` are 38 px on the stream; [R6-8] at 320×568 on a lessons day the P3 block's centre belongs to the nav and P5 is below the fold on landing; [R6-6] the composer's own retry and the reaction toggle drop focus to BODY.
   Justification: All three primary tasks complete on-screen and no path requires a wrong-screen detour (rules out 1); the chooser → History → editor path still adds steps, the recovery and reaction targets shrank below the touch floor, and the smallest supported viewport needs a scroll to reach an afternoon lesson, so not "fewest possible steps; no decoy actions" (rules out 3).

3. Good design is aesthetic — Score: 1/3 (r5 1, Δ0)
   Evidence: The system holds — [V2] 97.82 % on-ladder at 320/390/430 with 0 author-level off-ladder values, desktop 92.78 %; [V4] 43 colours, `tokens.css` byte-identical for three commits; [V3] type unchanged; [V5] floor 4.66/5.83, 0 failures; [V11] 0 overlaps; [V17] login unchanged. Rendered inconsistencies on `03875ed`: [R6-3] three status chips with opposite meanings render as one pill (1 distinct pair of 3, both boots); [R6-2] four theme cards, one selected, render identically; [R6-1] five button-height families on the mobile surfaces (36/38/44/48/81; r5 four) with "Try again" at 36 beside 44 px compose actions; [R6-10] 0 px between the foot caption and the empty line, a "foot" that renders above the timeline, a 20 px rule under a 22 px one; [R6-11] the empty day stated in prose and six ghost labels. Orphans: [S4] 1 duplicate selector (r5 0), 6 dangling comments (r5 4), 1 orphaned descendant, 2 consumer-less classes, 5 pre-existing classes without rules.
   Justification: Anchor 1 is "3–5 inconsistencies OR one jarring violation" — five rendered inconsistencies, two of which (chips that cannot be told apart, a picker that cannot show its choice) are not minor, so anchor 2's "≤2 minor inconsistencies" is exceeded on count and severity, and anchor 3's "no orphan styles" is exceeded by the duplicate selector and inert rules alone; 1 by count, not by tie.

4. Good design makes a product understandable — Score: 2/3 (r5 2, Δ0)
   Evidence: Fixed and verified: [C6] "Save school login" submits "Save login" with the choice in the form; [C7] P1–P6 explained above the timeline and on error; one not-found sentence, one Sync clause, two-sentences-for-one-idea count 0; composer and entity page agree on the same id; titles name every route including entity/404/login; Reconnect appears only when it can act. Remaining unclear control: [R6-2] the Settings background picker exposes `aria-pressed` but shows no selected state — a sighted user cannot read which surface is chosen. Copy residue: [R6-12] "No lessons **today**" on a Saturday, 404 "Nothing is listed at this address." for a routing error, "1 stored key no longer match", "Your notes and posts" vs "&"; [R6-4] a focus ring that appears on an ordinary tap now means nothing in particular; [R6-7] the skip link lands invisibly. Jargon: none unexplained ([C7]).
   Justification: Every primary action is identifiable without help (not 0); exactly one control carries a state the user cannot read, which matches anchor 2's "1 control needs a tooltip", while anchor 1's "2–3 controls unclear; jargon present" does not hold (jargon count 0, one control); the copy residue and the meaningless ring bar anchor 3's "names every primary control correctly", so 2.

5. Good design is unobtrusive — Score: 1/3 (r5 2, Δ−1) — tie 1↔2, settled lower
   Evidence: [W4][V10] 0 idle animations under both preferences; [W5] 0 interrupts on load; [W6] the SW no longer re-downloads ~99 KB after a cold start (0 B) and evicts silently; [W10] landings never steal focus or scroll on a normal load and retries move the page 0 px; [V9] overlays own the viewport. Against: [R6-4] `.focus-landing:focus` paints the 3 px accent ring around the whole content region — the feed stream, the Home hero card, a 1780 px History list — whenever the student taps plain text inside it, on 7/7 surfaces, pixel-identical to the retry ring and with nothing to signal; [W4] the 460 ms `settle` still replays on every route; [S5] a primary-styled Share still sits in the stream header (now 36 px); [R6-13] two concurrent `/api/entities` on every entity-targeted compose; [R6-11] six ghost "Free" labels under the empty sentence.
   Justification: Whether an accent ring drawn around the content on an ordinary tap is "decoration competing with content" (anchor 1) or chrome that is "visible but quiet" (anchor 2) is a genuine judgment — it is a misfiring indicator rather than ornament, but it is not quiet and it appears on the primary reading surface with no cause the student can see; the tie-breaker settles it at 1. Nothing dominates the content (not 0).

6. Good design is honest — Score: 2/3 (r5 2, Δ0)
   Evidence: [C5] the import disclosure now names exactly the two real triggers (account creation, Sync now — the only two `syncTimetable` callers) and a real pull issues 0 `POST /api/sync`; [C8] seven privacy claims re-mapped 1:1 with zero drift; [C4] 0 dark patterns by the rubric list (the Save-login default-on box is inside a user-initiated flow and disclosed twice in the same view); [C6] titles, Delete bodies, Disconnect body, Sync clause, survivor links all true; [C9] `auth.ts:30-31` corrected. Against: [C3] one degree-word inflation ("strongly protected", `WhyPage.tsx:73`); [R6-12] "No lessons today" asserted on non-today dates; [C-R6-4b] a dialog body that would misstate its premise if reached; [C9] `ExperiencePost.tsx:137-139` describes a focus return that does not happen (code comment, not user-facing).
   Justification: Anchor 1 requires "2+ inflations OR one dark pattern" and neither holds; anchor 2's "≤1 minor inflation" is met; the "today" claim on other days and the one inflation bar anchor 3's "every claim, badge, and label maps 1:1", so 2.

7. Good design is long-lasting — Score: 2/3 (r5 2, Δ0)
   Evidence: [V3][V4] single humanist face, one accent per surface, no gradients, 43 colours held for three commits, spacing 97.82 % tokenised; [W4] the staggered per-route entrance choreography and frosted translucent surfaces remain the one plausible trend marker. Durability facts from Visual: three of five CSS deletions this round removed rules with live consumers because dead selectors were co-located with live ones; the restored chip rules depend on source order alone.
   Justification: One dated marker matches anchor 2 exactly; no skeuomorph residue or trend typography (bars 1); the entrance signature bars 3.

8. Good design is thorough down to the last detail — Score: 1/3 (r5 1, Δ0)
   Evidence: Fixed and verified: [R1][R2][R4][R5][R6][R7][R9][R11][R12][R13][S-R13][A-R5-A1][A-R5-A4] — the in-flow empty state, unwrapped 44 px compose rows, the Save dialog's verb and checkbox, the pointer/touch ring 24/24 with 0 scroll, the composer's error state, entity/composer agreement, the 220×44 CTA, the SW's seeded eviction, `h2#privacy` ring, the alert outside the live region; [A1] 0 contrast failures ×2 boots, [A2] 126/126 stops ringed, [A8] 0 under reduce, [S4] `tsc` 0. Rough states on the worst instance: **focus** — [R6-4] the ring fires on any tap on 7/7 surfaces, [R6-5] `···` returns focus 0/3, [R6-6] composer retry and reaction toggle → BODY, [R6-7] the skip landing paints 0 pixels, `/history` is a dead-end; **selected / status** — [R6-2] the theme picker shows no choice, [R6-3] Hidden reads as Published; **touch** — [R6-1] every "Try again" 77×36 and 34 sub-44 instances (r5 2); **error / edge** — [R6-9] the Save submit 66 px below the fold at 320, [R6-8] a lesson block under the nav at 320, [R6-14] `aria-describedby` 2/6, the hidden date input's name, the live region that never announces; **hygiene** — [R6-10] duplicate selector, six dangling comments, 0 px caption spacing.
   Justification: Three-plus states are rough on their worst instance (a focus system that fires without cause and still loses the trigger, a selected state with no visual, a status chip with no tone, a recovery affordance below the touch floor on every error surface), which is past anchor 2's "1 state missing or rough"; the state framework exists on every surface and every r5 blocking condition verified fixed, so not 0.

9. Good design is environmentally friendly — Score: 3/3 (r5 3, Δ0)
   Evidence: [W1] app JS 85,691 B br / 88,484 B wire, under 100 KB (+0.7 %); [W4][V10] 0 idle animations on 11 screens × 2 preferences; [W8] prefers-color-scheme honoured pre-FCP in 6/6 boot contexts with 0 later mutations; [W4] prefers-reduced-motion → 0 on every route; [W6] SW cold-start refetch 0 B (r5 ~99 KB), activate eviction 5/5 stale gone and 10/10 current kept, one navigation shell; [W10] CLS 0; [W2] 0 long tasks in 25 loads. Relayed, outside the anchor: [R6-13] one duplicate in-flight `/api/entities`; [R6-15] unused fonts, admin CSS, `sw.js` header, cache-integrity trust.
   Justification: All four of anchor 3's conditions verify live for the fourth round running and the SW resource defects relayed in r5 are repaired; the remaining relay items do not touch the anchor, so 3.

10. Good design is as little design as possible — Score: 2/3 (r5 2, Δ0)
    Evidence: [S1] census unchanged (−1: a Reconnect that could not act is gone); [S3] families 15 (+1: the survivor link in two shapes); [S4] 0 uncommented literals, `.view` one rule. Candidates for a removable element on one screen: the `···` menu wrapping the single item "Report" (`ExperiencePost.tsx`, 1 menu on 1 surface); on an empty Timetable day, the sentence or the six ghost "Free" labels that say the same thing ([R6-11]); in the `showPicker` fallback, the date button beside the date input ([A-R6-11], rare branch). Latent: header Share + interleaved invite at ≥9 posts; the unnamed `aria-live` wrapper adds a level and no affordance.
    Justification: No page is dominated by decoration or duplicated affordances (not 0); at most two same-screen elements could be removed without breaking a task, which is anchor 2's "≤2 removable elements"; the empty day now saying the same thing in two grammars on the primary screen removes r5's tie toward anchor 3's "every element earns its place", so 2 without a tie.

**Total: 18/30** (r5 19/30, Δ−1; r4 18/30; r3 16/30; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #5 (1↔2). Principles scored with no tie: #1, #2, #3, #4, #6, #7, #8, #9, #10. Sensitivity: granting #5 upward gives 19/30 — still below the REFINE threshold — so the verdict does not depend on the tie.
