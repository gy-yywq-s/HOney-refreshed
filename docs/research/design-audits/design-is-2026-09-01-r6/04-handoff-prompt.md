# 04 — /make-plan handoff (r6)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 03875ed). Current design failed audit at 18/30 with critical gaps in principles #3 aesthetic, #5 unobtrusive and #8 thorough (each scored 1/3; no principle scored 0).

Verdict paragraph (quoted from 03-verdict.md):
> The r5 handoff verified as built almost everywhere it could be measured — "No lessons today" renders in flow at `[16,190,358,23]` and `[16,208,288,23]` and hit-tests to itself, appearing only on genuinely empty days once the harness was run in the student's timezone; the P1–P6 sentence sits above the timeline and survives the error branch; the compact composer is two rows of 44 px with no wrapped label at four viewports; the composer refuses to mount an editor when the registry fails and says the same thing as the entity page about the same delisted id, with the same survivor link; "Save school login" submits "Save login" with the stay-connected choice in the form and an `aria-describedby` body; Reconnect appears only when the portal token has expired; the import disclosure names exactly the two triggers that exist and a real pull issues no `POST /api/sync`; every "Try again" lands focus on a named region with a visible ring by mouse, touch and keyboard and moves the page 0 px; the skip link reaches `#main` on every route; `h2#privacy` lands with a ring; every route including entity pages, the 404 and login carries its own title; and the service worker now evicts 5/5 seeded stale hashes at activate, keeps the Dash chunk and all seven fonts, persists its index signature and re-downloads 0 B after a cold start — but the same wave shipped a CSS sweep that deleted the coarse-pointer 44 px floor so that every "Try again", every reaction and the `···` trigger are now below the touch target on the primary surfaces, deleted the theme picker's selected-state rule alongside a dead sibling so a student cannot see which background is chosen, restored the status-chip tones above their base rule so "Hidden" renders exactly like "Published", made the landing ring fire on any ordinary tap on plain text inside all seven landings, and wrote an outside-click guard that is defeated by the very `tabindex=-1` containers the previous fixes introduced; so HOney moves from 19 to 18/30 — #3 and #8 stay at 1 on defects of the fix wave's own making, #5 drops to 1 on a tie over the misfiring ring — and the audit reports the number it measured.

Why redesign and not refine: total 18/30 is below the REFINE threshold of 20; #3 and #8 are each at 1 on measured counts and #5 at 1 on a tie settled downward — granting the tie upward gives 19, still REDESIGN — so this prompt is the REDESIGN template even though every move is a finish-the-detail move on a layer whose tokens, type, colour, spacing, motion budget, weight, trust architecture and (this round) service worker all verify live.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 320×600, 390×620, 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decision, not up for re-litigation: timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), the disclosure is now accurate, and the plan must keep it so. Verify everything in a real browser against the deployed app, not from screenshots alone. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop, reducedMotion})`; probe scripts `r6-{struct,vis,copy,wf,a11y}-*.js` with `.log` files; `matrix.js` = 9-device regression) can be re-run to confirm each fix. IMPORTANT: the harness runs in UTC, where this account's lessons fall outside the 09:00–20:00 timeline window and the Timetable always looks empty — create contexts with `timezoneId: "Asia/Shanghai"` (see `r6-struct-timetable-cst.js`, `r6-vis-tz3.js`, `r6-a11y-tz.js`) whenever a measurement involves lesson blocks. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying; a reaction tap is allowed only if undone. The test account has 0 ownership keys and a connected, valid portal token — seed `HOney.experiences.keys` / `honey.portal.cred` sentinels in localStorage (as the r5/r6 probes did) to reach Mine's populated/error branches and the saved-login UI; fulfil `/api/experiences/mine` with `published`/`blocked`/`revoked` rows (see `r6-vis-chips.js`) to render the three status chips; rewrite the `/api/me` body to `portalTokenValid:false` to reach the Reconnect branch.

Preserve from current design (MUST NOT be touched — these verified live on 03875ed):
- Spacing system as it renders: 215 spacing lines, 177 on `var(--sp-*)` (208 usages), 4 literal-px lines and all 4 commented (components.css:523,665; features.css:1179,1225-1226); rendered rhythm at 320/390/430 = 853 of 872 values on the 4/8/12/16/20/24/32/44 ladder (97.82 %), desktop 92.78 %; 0 author-level off-ladder values on mobile. `.timeline__empty { display:flex; align-items:center; gap:var(--sp-2); margin:0 0 var(--sp-2) }` in flow (features.css:438-449) — keep it in flow, add the top margin (see Discard). Regression check: re-run the rendered spacing census (`r6-vis-typespace.js`) at 320/390/430/1280; on-ladder share must not drop below 97 % mobile / 92 % desktop and no 2/9/10/14/18/22/26/30 px value may appear.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, one fractional (27.95 at 430 only); 10 off-ramp `font-size` declarations. Regression check: rendered-font-size census, nothing below 12 px, no new off-ramp declaration.
- Palette and accent discipline: 43 colour literals, `tokens.css` blob 1d70d963a91a5bfbeb17928b599185286ad8f836 byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; contrast floor 4.66 stone / 5.83 night on `.home-head__stale`, 0 enabled text below 4.5:1 on either boot (468 nodes each); the landing ring 5.81–6.13 against inside/parent/page on stone, 8.36–9.28 on night; Save dialog checkbox label 5.14/6.11; `.timeline__empty` and `.timetable-foot` 5.34/8.07. Regression check: rerun `r6-a11y-contrast.js` / `r6-vis-contrast*.js` on stone and on a fresh `colorScheme:dark` context with no stored theme; 0 enabled text below 4.5:1; every focus ring ≥3:1.
- Zero idle motion: `document.getAnimations().length === 0` at 2.5 s on 11 screens × both preferences; `infinite` keyframes only on the pull spinner and skeleton; 6 `@keyframes`; `.view` ONE rule (components.css:525) carrying `settle .46s var(--ease) backwards` — do not split it; prefers-reduced-motion collapses durations to 1e-06 s; CLS 0 on cold /timetable, /home, /experiences in both timezones. Regression check: `r6-wf-anim.js`, `r6-wf-cls.js`; `grep -n '\.view' apps/web/src/styles/*.css` returns one `.view {` block.
- Weight: app JS 85,691 B br / 88,484 B wire / 273,120 raw, CSS 9,211 br / 40,577 raw, Dash chunk 4,406 br loaded only on /dash (0 of 14 student loads); 12 requests cold /home (3 data), 3 warm to /experiences; 0 long tasks in 25 loads; no new dependency. Regression check: curl br sizes under 100 KB; DashPage never fetched on student routes; request counts unchanged; `GET /api/entities` exactly once per entity page.
- Service worker as it now is (public/sw.js, 4,828 B): `liveAssetSet` reading cached bodies first and resolving Vite's relative `import("./Chunk-hash.js")` against the importing file (sw.js:21-40); `activate` → `evictStaleAssets()` with no argument (sw.js:66-74); `/__index-sig` persisted in the cache and compared whole on every navigation (sw.js:57-65,110-118); content-type guard refusing 200-HTML under /assets/; `/api/` never cached; navigations network-first; one `/` shell; offline shell 76–89 ms. Verified: 5/5 seeded stale hashes evicted at activate, 10/10 current assets (index JS, index CSS, Dash chunk, 7 fonts) kept, 0 B refetched after `ServiceWorker.stopWorker` ×3, JS-only and CSS-only signature mismatches both evict, exact match does no work. Regression check: re-run `r6-wf-sw1.js` (mode real), `r6-wf-sw3.js`, `r6-wf-sw4.js` after any sw.js change; keep the cache name unless a deliberate flush is intended.
- Boot script honouring prefers-color-scheme pre-FCP in six contexts with stored-choice-wins and 0 later `<html>` mutations (index.html:14-33).
- Route-settle containment: lesson modal, report dialog, Save, Reconnect and delete-confirm overlays equal the viewport at 320/390/1280, nav not hit-testable beneath, sheet padding on the ladder, 0 internal overlaps; focus trap + Esc + opener restore 6/6.
- Focus system: 3 px accent outline offset 3 on 126/126 Tab stops across 14 route states; `useRetryFocus` (lib/useRetryFocus.ts) arming only from the eight "Try again" handlers, focusing with `{preventScroll:true}` — landings never focus on a normal load, never in the Tab order, scroll 0 px on retry (24/24); named landing regions "Now and next" (HomePage.tsx:66), "Day timeline" (TimetablePage.tsx:182), "Lessons" (HistoryPage.tsx:107), "Everything listed" (ExplorePage.tsx:88), "Your notes and posts" (MinePage.tsx:130 — unify the spelling, see Discard), "<name> experiences" (EntityPage.tsx:87); landings are the result lists, not the filters (`landing.contains(control) === false` everywhere); feed `#feed-panel[role=tabpanel][tabindex=-1]` with the `role=alert` banner and the skeleton as direct children OUTSIDE the inner `aria-live` div (FeedPage.tsx:107-116,157,170); `h2#privacy` ring via `.section-title[id]:focus` (features.css:1289-1292); `#main[tabIndex=-1]` (AppLayout.tsx:120) so the skip link lands 12/12 — keep the landing, add the ring (see Discard); the `.focus-landing` ring rule's colour, width, offset and radius (3 px accent, offset 3, radius 12) — keep the look, change the trigger (see Discard); `···` `aria-haspopup="menu"`, `aria-controls`, `aria-expanded`, Escape → trigger, Tab-out close, Shift+Tab keep-open, ArrowUp/Down handler (ExperiencePost.tsx:144-152).
- Timetable: `.timeline__empty` `<p role="status">` as flow child 0 of the "Day timeline" landing, before `.timeline` (TimetablePage.tsx:299-304) — present only when the day has no lessons in the 09:00–20:00 window (verified in Asia/Shanghai), absent on lessons days; `.timetable-foot` "P1–P6 are the school's six lesson periods. Last synced …" above the timeline (TimetablePage.tsx:147-151), present in the error branch; `?date=YYYY-MM-DD` deep link (TimetablePage.tsx:30-34) that Home's "Open timetable" uses for the chip's day (HomePage.tsx:93-96) — keep the link, tighten the guard (see Discard); the `.daynav__note` Sync clause 24 px under "Sync now", present on error.
- Compose: the registry-pending skeleton with no editor; the registry-error branch (ComposePage.tsx:206-217: h1 + `role=alert` + "Try again", no editor) — keep it, add the landing (see Discard); the delisted branch consulting `useNames()` and offering "Open the current entry for {name}" as a 44 px primary button (ComposePage.tsx:221-258); the never-listed branch "Nothing is listed at this address, so nothing can be shared about it." + "Find someone or something" 220×44; `@media (max-height:620px) and (max-width:960px) { .compose-actions { flex-direction:row; flex-wrap:wrap } .compose-actions .btn { flex:1 1 0 } .compose-actions .btn:first-child { flex-basis:100% } }` (features.css:1260-1276) — Share `[16,315,288,44]`, Keep private `[16,367,140,44]`, Cancel `[164,367,140,44]` at 320×568, one line box per label at 320×568/320×600/390×620/390×844.
- Entity: never-listed ids → h1 "Nothing is listed at this address." + `btn btn--primary` "Find someone or something" `[16,111,220,44]` (EntityPage.tsx:76-83), title "Not found · HOney"; delisted ids → name, "This entry is no longer listed." + `.entity-survivor` "Open the current entry for {name}" 349×72; per-entity titles (EntityPage.tsx:70-72).
- Settings: "Reconnect" rendered only when `connection.connected && !connection.portalTokenValid` (SettingsPage.tsx:150-154); "Delete account…" (:101) and "Delete imported data…" (:215); the import row "…imported from the school portal when your account is created, and again whenever you press Sync now. Delete them below at any time." (:199-204) — the two named triggers are the only two `syncTimetable` callers (packages/backend/src/routes/auth.ts:32, routes/data.ts:17) and a real pull issues 0 `POST /api/sync`; the Sync clause " · Sync now signs in again with your saved school login if the portal session expired." byte-identical at SettingsPage.tsx:118 and TimetablePage.tsx:144; `ReconnectDialog` passing `describedBy="school-dialog-body"` and `submitLabel={purpose === "save" ? "Save login" : "Reconnect"}` with the stay-connected checkbox inside the form before the submit for both purposes, checked by default when saving (ReconnectDialog.tsx:36-60; SchoolLoginForm.tsx:13-14,65-68); `Modal`'s `describedBy` prop (Modal.tsx:11,15,57) — extend it to the other four call sites (see Discard).
- Login: `.login { height:100dvh; flex-direction:column; overflow-y:auto }`; wordmark top 32/152/215/149; checkbox 22×22 in the form before the submit; submit bottom 536 < 568 at 320×568; skip link "Skip to sign-in"; title "Sign in · HOney"; footnote "…created on first sign-in. Your timetable comes along with it."
- Titles: per-route `document.title` "<Name> · HOney" on all 10 AppLayout routes, "ChenJenny · HOney"-style entity titles, "Not found · HOney" on the 404 and never-listed entity pages, "Sign in · HOney" on /login, mutating correctly on SPA navigation.
- PTR: idle disc unnamed inside `aria-hidden="true"`, `aria-label="Refreshing"` only while refreshing (PullToRefresh.tsx:98-103); 1 `GET` per pull, never `POST /api/sync`; `not_eligible` note `role="status"` (ExperiencePost.tsx:273).
- Explore: complete-set rule (four sections with counts at 390 and 320, "Food 0" rendered, search only filters); `aria-label="Filter by name"`.
- Clean build: `tsc --noEmit --noUnusedLocals` = 0; all 11 classes deleted in 03875ed score 0 hits in the served JS; `.btn--ghost` intact (components.css:92-94).
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:229,351-353), no author column (packages/backend/src/db/database.ts:90-121), 6 = 6 = 6 report categories, "no free text is collected" ↔ `{category}` only, the HOney ID line (SettingsPage.tsx:72-75), WhyPage "design boundary, not magic", contract.ts:12-16, the corrected comment routes/auth.ts:30-31.

Discard (structural patterns causing the failures):
- Compound-rule deletion that took live selectors with dead ones: `@media (pointer: coarse) { .btn--small, .react-btn, .sort-toggle__btn, .usermenu__button { min-height: 44px } }` (components.css:838-843 at 487c1c4) was removed whole because `.sort-toggle__btn` was dead, leaving `.btn--small { min-height: 36px }` (components.css:112-116) and `.react-btn { min-height: 38px }` (:225-240) to govern on touch — every "Try again" 77×36 on all 8 error surfaces (r5 77×44), reactions 49×38, `···` 44×38, feed header Share 59×36 / "Find someone or something" 183×36 / "Your notes & posts" 131×36, Timetable "History" 67×36 / "Sync now" 79×36, Mine "Forget it" 75×36, `/history?select=1` "Select" 62×36 ×24; 10 distinct controls / 34 instances at 390×844 and 320×568 (r5: 2). Likewise `.option-grid button[aria-pressed="true"], .segmented button[aria-pressed="true"] { background: var(--ink); color: var(--surface) }` (components.css:779-783 at 487c1c4) was removed whole because `.segmented` was dead, so the four ThemeControls.tsx:22-31 buttons compute identically on stone and night and the orphaned `… span` descendant (components.css:780) matches nothing. Caused failure on principles #3, #8 and #2.
- Modifier rules placed before their base rule: `.chip--ok/--danger/--muted` (components.css:195-206) sit above `.chip { background: var(--soft); color: var(--accent) }` (:207-217) at equal specificity; "Published", "Hidden" and "Removed" render one (background, colour) pair on both boots (`.chip--private` after `.chip` renders correctly — proof it is ordering). Caused failure on principles #3 and #8.
- A landing ring bound to any `:focus`: `.focus-landing:focus, .focus-landing:focus-visible { outline: 3px solid var(--accent); … }` (features.css:1281-1288) on `tabindex=-1` containers means an ordinary mouse or touch tap on plain text — Home hero "Next", Timetable "No lessons today" / hour labels (358×620), History "September 2026", Explore "Teachers", Mine's h1, a feed post body, Entity "Teacher" — focuses the container and paints the full ring on 7/7 surfaces with no retry in play, pixel-identical to the genuine retry ring, around regions up to 1780 px. Caused failure on principles #5 and #8.
- An outside-click guard that treats landings and `#main` as controls: ExperiencePost.tsx:135-142 skips the focus return when `target.closest("button, a, input, textarea, select, [tabindex]")` matches — post text is inside `div.feed-stream[tabindex="-1"]`, empty space inside `main#main[tabindex="-1"]` (added in the same commit), so 0/3 outside-click variants return focus to `···` (post text → the feed container, a control → BODY, background → `main`); the comment at :137-139 describes behaviour that does not occur. Caused failure on principle #8.
- Retry and toggle handlers with no landing: the composer's registry-error "Try again" (ComposePage.tsx:213) calls `entities.reload()` without arming a landing → `activeElement` BODY after every retry; the reaction toggle leaves focus on BODY by Enter and by pointer (ExperiencePost.tsx reaction handler). Caused failure on principle #8.
- Skip target with no visible landing: `#main[tabindex=-1]` takes focus on 12/12 routes but the only ring is the UA `outline: 1px auto` on a box `[0,0,390,844]` — edge-pixel sampling shows 0 visible ring pixels; no rule targets `#main:focus` or `.main:focus`; `/history`'s 1780 px landing holds 0 focusables so Tab leaves to the nav; the 404 has no skip link and no `#main`. Caused failure on principle #8 (and #4: the student cannot see where the skip went).
- A 620 px canvas against a 568 px viewport with a fixed nav: on a lessons day at 320×568 (Asia/Shanghai), P3 `button.lesson-block [79,462,216,84]` has its centre under `nav.mobile-nav` (top 494; `elementFromPoint` → `a.mobile-nav__item.is-active`) and P5 `[79,631,216,84]` is 147 px below the fold on landing; at 390×844 all three blocks hit-test to themselves. Pre-existing (never rendered in r1–r5, which ran in UTC), first measured r6; the compose screen already solves it by not rendering the nav. Caused failure on principle #2 (and #8).
- Dialog content that pushes its submit out of the first fold: at 320×568 the "Save school login" sheet is `[0,45,320,523]` with `scrollHeight 611`, the 132 px stay-connected block (caveat wrapped to 5 lines) sits between the fields and the submit `[16,590,288,44]`, bottom 634 > 568 — reachable only by scrolling the sheet. Caused failure on principle #8.
- Two grammars for one fact on the primary screen's empty day: `.timeline__empty` "No lessons today" (15px/600, role=status) at y 190 directly above six `span.timeline__ghost-free` "Free" labels (12px/500) at y 229–762, all co-visible at 390; on lessons days only the free periods render. Kept #10 from 3; contributed to #5.
- Caption and empty line butting at 0 px: `.timetable-foot` bottom 190 = `.timeline__empty` top 190 (features.css:439-449 gives the empty line no top margin; the foot has no bottom margin) — two muted lines of different size and weight touch; the element is still class-named `timetable-foot` while rendering above the timeline (TimetablePage.tsx:147-151). Caused failure on principle #3.
- Hygiene residue the sweep created: `.timeline__empty svg` declared twice back to back (features.css:451-454 at 20 px — dead; :455-459 at 22 px wins) — the only duplicate selector in the served sheet (r5 0); six dangling comments (r5 4), three created by this commit (components.css:44 "Hover lift…", foundations.css:142 "Reading serif…", :189 "Numbered action-card grid") and three surviving (components.css:803, foundations.css:191, features.css:1253); five button-height families on mobile (36/38/44/48/81). Caused failure on principle #3.
- Copy that asserts "today" on any date and a date guard that accepts impossible dates: TimetablePage.tsx:302 renders "No lessons today" on `?date=2026-08-22` under the heading "Saturday, August 22, 2026"; TimetablePage.tsx:348-350 validates `?date=` by shape only, so `?date=2026-13-45` renders "Sunday, February 14, 2027". The 404 (NotFoundPage.tsx:11-12) now says "Page not found / Nothing is listed at this address." — the directory's sentence on a routing error. MinePage.tsx:196 "1 stored key no longer match a post on the server."; MinePage.tsx:130 region "Your notes and posts" vs h1/title "Your notes & posts"; ReconnectDialog.tsx:37 "Sign in again to refresh the school connection…" is unreachable (Settings passes `sessionExpired={!portalTokenValid}` and renders Reconnect only when it is false). Caused failure on principles #4 and #6.
- Duplicate in-flight data call: `compose?entityKey=` issues two concurrent `GET /api/entities` (t=0 and t=+2 ms) — ComposePage.tsx:54-57 `useApi(() => api.entities(), [effectiveEntityKey])` with no cache key beside the newly imported `useNames()` (ComposePage.tsx:99 → shared.tsx:161, key "entities"); lib/useApi.ts:38-70 has no in-flight dedupe. Resource defect relayed under principle #9 (anchor still met); intrusion under #5.
- AT residue: `Modal`'s `describedBy` passed at 1 of 5 call sites — delete-account, delete-data (Modal.tsx:94 ConfirmDialog), report (ExperiencePost.tsx:320), lesson (TimetablePage.tsx:422) and theme (ThemeControls.tsx:46) dialogs have `aria-describedby: null`; no dialog sets `inert`/`aria-hidden` on the background; the hidden date input still carries `aria-label="Pick a date"` inside `aria-hidden="true"` (TimetablePage.tsx date picker) and the `showPicker` fallback shows two named date affordances; the entity error-branch region is named "Teacher experiences" (kind-as-name fallback, EntityPage.tsx:63-68,87); the feed's `aria-live` div exists only in the items branch and is replaced wholesale on tab change and load-more (0 mutations observed — nothing is announced); Explore's four named sections are nested inside the "Everything listed" region; the unnamed `aria-live` wrapper adds a DOM level (FeedPage.tsx:157,170); `banner`/`contentinfo` 0 everywhere. Caused failure on principle #8 (and #4).
- Relay, anchor #9 met: `sw.js` served `cache-control: public, max-age=14400` (the r5 relay asked for `no-cache` via the hostd manifest; not done); `liveAssetSet` trusts cached bodies — seeding 1-byte bodies for the 10 current assets makes activate evict 9/10 of them (sw.js:33-34); `/__index-sig` is a permanent non-asset entry never cleaned (53 B); 123,968 B of unused font subsets and 1,201 B of admin CSS ship in the student bundle; `/assets/*` lacks `immutable`; static `theme-color="#edf0f1"` (index.html:6) outside the boot palette.
- Latent same-screen duplicate (code only, 1 post live): header Share + interleaved invite co-render at ≥9 posts (FeedPage.tsx:33,43,159-166, `SHARE_PROMPT_EVERY = 8`); the `···` menu wrapping the single item "Report" (tie-register item under #10).

Top 4 moves from the audit (verbatim):
1. #3 aesthetic + #8 thorough + #2 useful — Reverse the four regressions the orphan sweep introduced, selector by selector, and verify each on screen. Restore the coarse-pointer floor for the live classes — `@media (pointer: coarse) { .btn--small, .react-btn { min-height: 44px } }` (or raise the base `.btn--small` 36 px at `components.css:112-116` and `.react-btn` 38 px at `:225-240` to 44 on all pointers) — so every "Try again" (77×36 on 8 error surfaces), every reaction (49×38), the `···` trigger (44×38), the feed header buttons, Timetable "History"/"Sync now" and the 24 History "Select" rows are ≥44 px again at 390×844 and 320×568; restore `.option-grid button[aria-pressed="true"] { background: var(--ink); color: var(--surface) }` (deleted from `components.css:779-783` with `.segmented`) and delete the orphaned `… span` descendant at `:780`, verified by computed style on the pressed card vs the others on stone and night; move `.chip--ok/--danger/--muted` (`components.css:195-206`) **after** `.chip` (`:207-217`) or raise their specificity, verified with three synthesised Mine rows showing three distinct (background, colour) pairs; delete the dead `.timeline__empty svg { 20px }` rule (`features.css:451-454`) and the three dangling comments this commit created (`components.css:44`, `foundations.css:142,189`) plus the three surviving ones (`components.css:803`, `foundations.css:191`, `features.css:1253`); give `.timeline__empty` a top margin (`--sp-2` or `--sp-3`) so it no longer butts against `.timetable-foot` at 0 px, and rename the class that now renders above the timeline. Before any further CSS deletion, run the selector-level reverse sweep: for every selector in a compound rule, grep the TSX and the served JS for its class tokens and attribute selectors; delete only the selectors with 0 consumers, never the whole rule. Evidence: [R6-1][R6-2][R6-3][R6-10][S-R6-2][V14][V-R6-5][V-R6-6].
2. #5 unobtrusive + #8 thorough — Make the landing ring mean "you were brought here", and make focus go where the fixes say it goes. Replace `.focus-landing:focus` (`features.css:1281-1288`) with a state the retry hook sets — `useRetryFocus` (`lib/useRetryFocus.ts:11`) sets `data-landed` on the element it focuses and clears it on `blur`; style `.focus-landing[data-landed]:focus` (keep `:focus-visible` for keyboard) — so an ordinary tap on plain text in the feed stream, the Home hero, the History list or any other `tabindex=-1` landing paints nothing (today 7/7 surfaces paint the full 3 px ring on such a tap, pixel-identical to the retry ring). Fix the `···` outside-click guard (`ExperiencePost.tsx:135-142`) so a `tabindex="-1"` container does not count as a control — e.g. `closest('button, a, input, textarea, select, [tabindex]:not([tabindex="-1"])')` — and verify 3/3 outside-click variants return focus to `···` on Feed, Entity and Mine; correct the comment at `:137-139`. Give the composer's registry-error retry a landing (arm `useRetryFocus` on the target card or the editor; `ComposePage.tsx:213` → BODY today) and keep focus on the reaction button after a toggle (BODY today by Enter and by pointer). Give `#main:focus` the app ring or move the skip target to the page's first heading so the skip link lands visibly (UA `1px auto` on a viewport-sized box = 0 visible pixels today); give `/history`'s browse landing something focusable or focus its first row (0 focusables in a 1780 px landing). Evidence: [R6-4][R6-5][R6-6][R6-7][A-R6-1][A-R6-3][A-R6-4][A-R6-5][A-R6-6].
3. #2 useful + #8 thorough — Make the smallest supported viewport hold a lesson day and a dialog. At 320×568 on a day with lessons the P3 block `[79,462,216,84]` has its centre under `nav.mobile-nav` (top 494) and P5 `[79,631,216,84]` is 147 px below the fold on landing: either hide the fixed nav on the timetable screen the way the compose screen does (`nav` box `[0,0,0,0]` there), or scale the 620 px canvas / 84 px bands under `@media (max-height: 620px)` so 09:00–18:00 fits above the nav, and verify `elementFromPoint` at every rendered lesson block's centre is the block itself at 320×568 and 390×844 in `timezoneId:"Asia/Shanghai"`. In the "Save school login" dialog at 320×568 the submit opens 66 px below the fold (`[16,590,288,44]`, sheet `scrollHeight 611` vs `clientHeight 522`): shorten the checkbox caveat to one sentence at ≤360 px or move the caveat under the submit, so the submit is inside the first fold. Evidence: [R6-8][R6-9][S-R6-12][V13][V-R6-4].
4. #4 understandable + #6 honest + #9 relay — One true sentence per state, and no duplicate work. Make the empty sentence date-aware ("No lessons on Saturday 22 August" / "No lessons today" only when the selected date is today; `TimetablePage.tsx:302`) and validate `?date=` as a real calendar date (`:348-350` accepts `2026-13-45` and renders 14 February 2027); give the 404 its own sentence instead of the directory's ("Nothing is listed at this address." now serves three surfaces, one of which is a routing error); fix "1 stored key no longer match" (`MinePage.tsx:196`) and unify "Your notes and posts" with "Your notes & posts" (`:130`); remove or reach the dead "refresh" reconnect body (`ReconnectDialog.tsx:37`); drop `aria-label` from the `aria-hidden` date input and make the fallback show one date affordance; name the entity error-branch region from the directory before falling back to the kind; pass `describedBy` at the remaining four `Modal` call sites and set `inert` on the background; dedupe `GET /api/entities` on `compose?entityKey=` by sharing the `"entities"` cache key (`ComposePage.tsx:54-57` vs `shared.tsx:161`) or adding in-flight dedupe to `useApi`; serve `sw.js` with `cache-control: no-cache` (still `max-age=14400`). Evidence: [R6-12][R6-13][R6-14][R6-15][S-R6-4][S-R6-5][C-R6-L2][C-R6-L3][C-R6-4b][A-R6-8][A-R6-11][S-R6-7][W-R6-5].

Redesign principles in priority order:
1. #8 thorough — success: every "Try again", reaction, `···`, feed-header button, Timetable action and History "Select" row measures ≥44 px tall in a `hasTouch` context at 390×844 and 320×568; the pressed theme card computes a different background/colour from the other three on stone and night; three synthesised Mine chips render three distinct (background, colour) pairs; a mouse click on plain text inside each of the seven landings paints no ring, while a pointer, touch and keyboard "Try again" still paints the 3 px accent ring 24/24 with 0 scroll; all three `···` outside-click variants return focus to the trigger on Feed, Entity and Mine; the composer's retry and the reaction toggle leave focus on a control, never BODY; the skip link's landing paints a visible ring (or lands on a ringed heading) on 12/12 routes; `/history` Tab from the landing stays in content; `aria-describedby` resolves on 6/6 dialogs; `tsc` 0; 0 duplicate selectors; 0 dangling comments.
2. #3 aesthetic — success: button heights on the mobile surfaces collapse to two families (44 for actions, 48 for the login submit) plus the 81 px theme cards; `.timeline__empty` sits ≥8 px under `.timetable-foot`; the empty day shows one statement of emptiness (sentence with the ghosts, or ghosts alone with the status text visually hidden — one grammar, `role=status` kept); the rendered spacing census stays ≥97 % mobile / ≥92 % desktop with 43 colours and the type set unchanged; 0 element-pair overlaps; every class the served JS emits has a rule that actually applies.
3. #5 unobtrusive — success: no accent ring appears on the content region on any tap that is not a retry landing; one `settle` per route remains the only entrance motion; `compose?entityKey=` issues one `GET /api/entities`; 0 interrupts on load; the SW adds 0 requests to a warm navigation.
4. #2 useful — success: at 320×568 in Asia/Shanghai on a 3-lesson day every rendered lesson block's centre hit-tests to itself on landing and the afternoon block is reachable without the nav covering it; the Save dialog's submit is inside the first fold at 320×568; the three primary tasks keep their current step counts (1/0, 1 tap, 4 screens / 3 taps).
5. #4 understandable + #6 honest — success: the empty sentence names the day it describes; `?date=` rejects impossible dates; the 404 has its own sentence; "match/matches" agrees; one spelling of "Your notes & posts"; no unreachable dialog body; the import disclosure stays exactly as it is.
6. #9 environmentally friendly (keep at 3) — success: bundle under 100 KB wire, 0 idle animations, dark boot honoured pre-FCP, reduced-motion 0; the SW battery (`r6-wf-sw1/3/4.js`) still passes after any sw.js change; `sw.js` served `no-cache`.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — the retry-landing state model (armed → landed → cleared) replacing `:focus`; the timetable screen's relationship to the fixed nav at ≤620 px height (hide it as compose does, or scale the canvas); the Save dialog's stack order at 320 (fields → submit → caveat, or a one-sentence caveat); the empty day's single grammar; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): a lessons day at 320×568 from landing to tapping the 13:30 block; Feed → tap a post body (no ring) → tap "Try again" after an error (ring) → tap outside `···` (focus back on `···`); Settings → Appearance → the pressed card visibly pressed; Mine with Published / Hidden / Removed chips in three tones; "Save school login…" at 320×568 with the submit in the first fold; skip link → visible landing.
- States checklist (empty, loading, error, success, focus, disabled) per screen, with the focus column split into keyboard / pointer-retry / pointer-plain-tap, the touch-target column listing every control's height in a `hasTouch` context, and the selected-state column for the theme picker and the chips.
- Migration path for users currently on the old design: none for routes or data; ship a `sw.js` byte change only if the SW is edited, and re-run the seeded-eviction battery with real bodies (`r6-wf-sw1.js` mode real) and the cold-start probe (`r6-wf-sw3.js`) before calling it done.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (44 px floor on every `.btn--small`/`.react-btn` in a touch context at 390 and 320; pressed theme card distinct; three chip tones distinct; no ring on a plain tap in 7/7 landings, ring on retry 24/24; `···` focus return 3/3; composer retry and reaction focus never BODY; skip landing visible; P3 block hit-testable at 320×568 in Asia/Shanghai; Save submit in the first fold at 320×568; empty sentence date-aware; one `GET /api/entities` on compose; rendered spacing census ≥97 %; `getAnimations()` 0; contrast on a genuine dark boot; SW battery green).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the `:focus`-bound ring, the `[tabindex]` control guard, the whole-rule CSS deletion and the 36/38 px base heights on touch must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the touch-target census in a `hasTouch` context, the plain-tap ring test on all seven landings, the spacing census, the dark-boot contrast probe, the 320×568 timetable sweep in Asia/Shanghai and the SW seeded-eviction battery, after every CSS, focus or SW change.
- Deleting a compound rule because one of its selectors is dead — that is how this wave removed the 44 px touch floor and the theme picker's pressed state; delete selectors, not rules, and grep the served JS for every class token and attribute selector first.
- Adding a rule without checking the cascade — the chip modifiers were restored above their base rule and shipped inert; put modifiers after the base or raise specificity, and verify by computed style, not by reading.
- Widening a selector to fix one modality — `.focus-landing:focus` fixed the pointer retry and broke every plain tap; bind the ring to the retry state, not to focus.
- Adding a `tabindex="-1"` container without re-testing every handler that inspects `closest("[tabindex]")` — `#main` and the landings defeated the `···` guard.
- Measuring the Timetable in UTC — the harness's default clock hides every lesson block; any measurement involving lessons must use `timezoneId:"Asia/Shanghai"`.
- Fixing a surface without fixing its siblings — enumerate every `Modal` call site, every `useApi` consumer of `/api/entities`, every `.btn--small` and `.react-btn` site before changing one.
- Claiming a change in the commit message that the diff does not contain — verify each move on the deployed app before reporting it done.
````
