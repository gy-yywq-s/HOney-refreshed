# 01 — Evidence (r6, HOney @ `03875ed`, deployed at https://honey.gaelisus.com)

Consolidated from five evidence subagents (Structural, Visual, Copy & Honesty, Weight & Friction, Accessibility — each run on `model: "opus"`), plus the orchestrator's own reading of `git diff 487c1c4..03875ed`. Every claim below carries a probe/screenshot or `file:line` citation; findings without a citation were rejected. Anchor ids: r5 ids ([S1], [V2], [R1]…) are reused where the same thing was re-measured; r6-new anchors are `[X-R6-n]`; the r6 defect register is `[R6-n]` at the end. Served bundle `index-D47LYHws.js` (273,120 B raw) / `index-CFkg1jp9.css` (40,577 B) confirmed on first load by all five passes; `sw.js` byte-identical to repo (sha256 `03e35160…4c41`), cache `honey-v3`, served `max-age=14400`.

Probe scripts and screenshots: `/root/claude-work/design-audit/r6-{struct,vis,copy,wf,a11y}-*.js|.log|.png` (Structural 13 scripts + 34 png; Visual 17 + 122; Copy 9 + 24; W&F 19; A11y 15 + 61). Harness timezone is UTC; every Timetable-shaped number was re-measured under `timezoneId:"Asia/Shanghai"` (`r6-struct-timetable-cst.js`, `r6-vis-tz*.js`, `r6-a11y-tz.js`, `r6-wf-cls*.js`).

Hard limits honoured by all passes: nothing published, no report submitted, no Sync now / Reconnect / Save login / Disconnect / Delete / Sign out / Turn off pressed (dialogs opened and Cancelled/Escaped only); two reaction taps in total, each immediately undone and re-verified (`aria-pressed=false`, count 0); token-expired branch produced by rewriting the `/api/me` body via `page.route`; `/home/honey/.secrets/` never read; no token printed. Every browser closed; 147 stray chromium processes left by the usage-limit kill were terminated before the resumed passes ran.

---

## Structural (S)

- **[S1] Census @390×844 (visible/total)** — identical to r5 on every surface except Settings **18/25** (r5 19/26; "Reconnect" now gated on `!portalTokenValid`, `SettingsPage.tsx:150-154`): Home 9/15; Timetable 10/17; Feed 15/21; Explore 26/32; Compose default 7/13, `?entityKey=` 10/16, `?lessonId=` 10/16, editing 6/16, delisted 6/12, never-listed 6/12; Mine empty 6/12, seeded 7/13 (+"Forget it"); History 8/14, `?select=1` 24 × "Select" (now `<button>` + `navigate()`, `HistoryPage.tsx:137-144`); Entity listed/delisted/never-listed 6/12; Login 5/5; 404 **1/1** (no skip link, no `#main`). Timetable/Compose @320×568 same as @390. Net control change r5→r6: **−1**. (`r6-struct-census.log`)
- **[S2] Depth** — Feed `maxInMain` **11** (r5 10, +1) from the new bare `<div aria-live="polite">` (`FeedPage.tsx:157,170`) [S-R6-1]; Home 7, Timetable 9, Explore 9, Settings 9, History 9, Mine 6, Compose 6.
- **[S3] Repeated-affordance families: 15** (r5 14). New #15: "Open the current entry for {name}" in two shapes — `EntityPage.tsx:110` bare `a.entity-survivor` 349×72 vs `ComposePage.tsx:245-250` `a.btn.btn--primary` 237×44. "Try again" ×8 and `role=alert` ×8 (composer added, `ComposePage.tsx:206-217`). Resolved into one wording: the Sync clause (2 sites, byte-identical), the not-found sentence (3 sites), the delisted sentence (2 sites). Landings 7/7 named/roled (Feed = tabpanel). Landing granularity unchanged: Home hero 358×217, Timetable 358×651, History 358×**1780**, Explore 358×**1254**, Mine whole `.stack` 358×273, Entity whole `.stack` 358×391, Feed 358×219. "Find someone or something" as a CTA on 4 surfaces. Header Share + interleaved invite at ≥9 posts still latent (`FeedPage.tsx:33,43,159-166`).
- **[S4] Hygiene** — `tsc --noEmit --noUnusedLocals` **0**. Same-context duplicate selectors **1** (r5 0): `.timeline__empty svg` at `features.css:451` (20 px, dead) and `:455` (22 px, wins; live svg 22×22) [S-R6-10]. Forward sweep (CSS class with 0 hits in the served JS): 9 of 214, of which 7 are template-interpolated (`chip--*`, `swatch--*`) → genuine **2** (`.reveal`, `.is-in`; r5 10). Reverse sweep (class emitted with no rule): the 5 pre-existing (`.home`, `.home-head`, `.home-voices`, `.login__card`, `.post__dot`), unchanged; `.chip--ok/--danger/--muted` now have rules (`components.css:195-206`) — but see [V14]; `.btn--ghost` present (`:92-94`). **Selector-level reverse sweep [S-R6-2]** — deletions in `03875ed` with a surviving consumer: (1) `.option-grid button[aria-pressed="true"], .segmented button[aria-pressed="true"] { background: var(--ink); color: var(--surface) }` deleted while `ThemeControls.tsx:22-31` renders `.option-grid` buttons with `aria-pressed`; the descendant `.option-grid button[aria-pressed="true"] span` survives orphaned at `components.css:780` (buttons contain `<i>` + `<strong>`, no `<span>`); (2) `@media (pointer: coarse) { .btn--small, .react-btn, .sort-toggle__btn, .usermenu__button { min-height: 44px } }` deleted while `.btn--small` has 30 TSX emit sites / 22 exact-token hits in the served JS and `.react-btn` 10 / 5 (`.usermenu__button` keeps its own 44 px at `:596`; `.sort-toggle__btn` 0). All other deleted classes (`.action-card`, `a.card`, `.lift`, `.btn--secondary`, `.section-head__copy`, `.reading`, `.stat*`, `.primary .card-index/.card-copy`, `.report-option*`, `.segmented`) score 0 hits in `index-D47LYHws.js` and leave 0 residual CSS refs — the class-level sweep itself was clean. Dangling comments **6** (r5 4): 3 new from this commit's own deletions (`components.css:44` "Hover lift…", `foundations.css:142` "Reading serif…", `:189` "Numbered action-card grid"), 3 surviving (`components.css:803`, `foundations.css:191`, `features.css:1253`); 2 removed. Uncommented px literals in spacing declarations **0** (90 px now commented, `features.css:1179`). `.view` ONE rule (`components.css:525`).
- **[S5] Feed** — header Share shown loading + normal, hidden on error and empty (`FeedPage.tsx:33`); empty state one CTA "Share the first one"; tabs `#tab-my-classes`/`#tab-school` `aria-controls="feed-panel"` resolve; `role=tabpanel` = `div.feed-stream.focus-landing#feed-panel[tabindex=-1]`, `aria-labelledby` follows selection; the `role=alert` banner and the skeleton are direct children of the panel, **outside** the inner `aria-live` div (`r6-struct-feedmenu.log`).
- **[S6] Explore** — complete-set rule holds at 390 and 320: 4 sections with counts ("Teachers 6 / Courses 8 / Places 5 / Food 0 + Nothing here yet."), 19 rows; `zzzz` → 4 sections at 0 with "Nothing by that name."
- **[S7] Composer** — pending: skeleton `role=status aria-label="Loading"`, no editor. **Registry error** (`/api/entities` aborted, real and bogus key): h1 + `role=alert` `[16,63,358,122]` + "Try again" `btn--ghost btn--small` `[33,136,77,36]`; no textarea/Share/Keep private/Cancel/skeleton; "Try again" refetches exactly once; **focus after retry = BODY** in 4/4 cases (no `useRetryFocus` landing, `ComposePage.tsx:213`) [S-R6-6]. Compact rows at 320×568 / 320×600 / 390×620: Share `[16,315,288,44]` / `[16,347,288,44]` / `[16,386,358,44]`, Keep private `[16,367,140,44]` / `[16,399,140,44]` / `[16,438,175,44]`, Cancel `[164,367,140,44]` / `[164,399,140,44]` / `[199,438,175,44]`; 390×844 column y552/604/656; all 44 px, `getClientRects().length === 1` on every label (12/12), `elementFromPoint` = self 12/12, nav clear (`r6-struct-compose.log`, `r6-vis-compose-rows-*.png`).
- **[S8] Timetable** — `.timeline__empty` is a `<p role="status">`, **flow child index 0** of `div.focus-landing[role=region][aria-label="Day timeline"]`, before `div.timeline`; `position: static`. Stack @390×844 (empty day): `.daynav [16,16,358,106]` → `h1 [16,138,358,22]` → `p.timetable-foot [16,172,358,18]` → `p.timeline__empty [16,190,358,23]` → `div.timeline [16,221,358,620]` (bottom 841); `nav.mobile-nav [10,770,370,64]`; scrollHeight 937. @320×568: foot 2 lines `[16,172,288,36]` → empty `[16,208,288,23]` → timeline `[16,239,288,620]`; nav `[10,494,300,64]`. `elementFromPoint` at the text centre = the status node's own SPAN at both viewports; overlaps daynav/foot/nav 0; below fold 0. **CST re-run** (`r6-struct-timetable-cst.log`): on a 3-lesson day (2026-09-02, 2026-08-24) `.timeline__empty` **absent**; on the 0-lesson day (2026-08-22) present — the presence predicate is correct for a real Asia/Shanghai user. Directly measured cost of the in-flow block: `.timeline` top 190 → 221 (+31 px), scrollHeight 906 → 937 @390; 208 → 239, 918 → 949 @320. Canvas under the nav on landing: lessons@390 40 px, empty@390 71 px (P6 band `[73,755,300,84]`, its "Free" label overlaps the nav by 4 px), lessons@320 334 px, empty@320 365 px. P1–P6 foot present on error (`"P1–P6 are the school's six lesson periods."`, `[16,172,358,18]`); the error branch renders `role=alert [16,190,358,122]` + "Try again" 77×36, landing absent until success. `.timeline__now` never rendered (needs 09:00–20:00 local; runs were at 06:03 CST).
- **[S-R6-12] 320×568, lessons day, on landing** (CST, both probed dates; confirmed independently by Visual [V13] and A11y): `button.lesson-block` 216×84 — P1 `[79,209,216,84]` OK; **P3 `[79,462,216,84]` centre `elementFromPoint` = `a.mobile-nav__item.is-active`** (the fixed nav owns the point; only the top 32 px of the block is exposed); **P5 `[79,631,216,84]` fully below the fold** (147 px). @390×844 all three blocks (282×84) hit-test to themselves. Pre-existing (r1–r5 never rendered a lesson block), first observed r6.
- **[S-R6-13] Empty-day double grammar** — the six free-period markers `span.timeline__ghost-free` (23×12, "Free") render under the sentence: @390 at y 229/313/482/566/650/762 (P1–P6, all six co-visible with "No lessons today" at y 190); @320 three of six visible. Lessons days render only the free periods (3 each). `[role=status]` nodes: empty day `["div.ptr__disc","p.timeline__empty"]`, lessons day `["div.ptr__disc"]`.
- **[S-R6-3] WITHDRAWN** — the Structural pass's initial finding "No lessons today renders on days with lessons" was a UTC-harness artefact (this account's lessons fall at 01:00–07:00 UTC, outside the 09:00–20:00 render window). Residual note only: the predicate is time-window-based (`TimetablePage.tsx:211-212,244-245,288-290`), so a lesson wholly outside 09:00–20:00 local would still read "No lessons today" — unobserved on real data.
- **[S-R6-4] "today" on non-today dates** — `?date=2026-08-22` → h1 "Saturday, August 22, 2026", date button "Sat 22 Aug", status "No lessons **today**" (`TimetablePage.tsx:302`); confirmed in CST at both viewports. Pre-existing string, first flagged r6.
- **[S-R6-5] `?date=` guard is shape-only** — `?date=garbage` → today ✓; `?date=2026-13-45` → "Sunday, February 14, 2027" (regex `/^\d{4}-\d{2}-\d{2}$/`, `TimetablePage.tsx:348-350`; `Date` rolls over). Home "Open timetable" `href="/timetable?date=2026-09-02"` for chip "Tomorrow · 01:00" lands on "Pick a date (Wed 2 Sept)", and `/api/timetable?date=2026-09-02` contains the chip's lesson id 1335340 — [C-M5] fixed.
- **[S9] Entity / composer agreement** — composer `?entityKey=room:r_d48ff4b2f68a` → "This entry is no longer listed, so nothing can be shared about it." + `btn--primary` "Open the current entry for 213" `[37,164,237,44]` → `compose?entityKey=room:r_360`; entity page same id → title "213 · HOney", "This entry is no longer listed." + `a.entity-survivor` `[16,111,349,72]` → `/experiences/room/r_360`; composer `teacher:t_doesnotexist` → "Nothing is listed at this address, so nothing can be shared about it." + "Find someone or something" `[37,164,220,44]`; entity `t_doesnotexist` → title "Not found · HOney", h1 "Nothing is listed at this address." + `btn--primary` `[16,111,220,44]` (r5: 189×22). `EntityPage.tsx:70-72` sets `document.title` in a `useEffect` with no dependency array. Entity error branch: region name **"Teacher experiences"** (kind-as-name fallback, `EntityPage.tsx:63-68,87`) [S-R6-7].
- **[S10] Settings** — valid token: 13 buttons, **no Reconnect**; `/api/me` rewritten `portalTokenValid:false` → Reconnect appears. "Delete account…" `[37,818,152,44]`; "Delete imported data…" `[37,1568,193,44]`. Import row verbatim without "pull to refresh". Connection verbs 5 (+1 conditional).
- **[S11] ··· menu** — items `["Report"]` (one). Outside tap after open: (a) plain post text → `activeElement` **`div.feed-stream.focus-landing`**; (b) another control → **BODY**; (c) empty space → **`main#main`**. **0/3 return to `···`** (r5 0/3, different nodes). Cause: `ExperiencePost.tsx:135-142` guard `target.closest("button, a, input, textarea, select, [tabindex]")` matches `div.feed-stream[tabindex=-1]` and `main#main[tabindex=-1]` (added by the same commit, `AppLayout.tsx:120`), so the `setTimeout(focus)` never runs [S-R6-9 → R6-5]. ArrowDown/Up implemented (`:144-152`) but unobservable with one item (A11y proved it cycles with an injected second item). Escape → trigger ✓; Tab-out closes with fresh `aria-expanded` ✓.
- **[S12] `#main`** — `tabindex="-1"` on all 8 authed routes; skip link → `main#main` 8/8 (r5 BODY on 4); no app rule targets `#main:focus` (UA ring only). 404: no skip link, no `#main`.
- **[S13] `.btn--ghost`** — rule present; live "Try again" `background rgba(0,0,0,0)`, `color rgb(35,43,49)`, box `[54,216,77,36]`, `min-height: 36px`.
- **[S-R6-8] Touch targets @390, coarse pointer** — **33 sub-44 elements** over 10 surfaces (32 genuine + the `aria-hidden` date input): "Try again" `btn--ghost btn--small` **77×36** (8 sites); `/history?select=1` "Select" **62×36 ×24**; feed header Share 59×36, "Find someone or something" 183×36, "Your notes & posts" 131×36; Timetable "History" 67×36, "Sync now" 79×36, "Back to today" 36 tall; reactions `react-btn` 49×**38** ×2; `···` `react-btn--more` **44×38**; Mine "Forget it" 75×36. r5 [A6]: 2 genuine. (`r6-struct-targets.log`)
- **[S-R6-9] Theme picker** — Stone (`aria-pressed=true`), White, Mist, Night compute identically: `background rgba(255,255,255,0.72)`, `color rgb(35,43,49)`, `border rgba(35,43,49,0.14)`, `outline none`, `font-weight 400`, `box-shadow none` (`r6-struct-optiongrid.png`).
- **[S17] Step counts** — next lesson 1 screen / 0 taps (hero `[16,95,358,217]` above the fold); stream 1 tap; share via History 4 screens / 4 taps to a typing cursor (Select 62×36), via Explore 5 / 5; `activeElement` BODY after every route change (unchanged).

## Visual (V)

- **[V1] CSS spacing** — 215 spacing lines (r5 220), 177 with `var(--sp-*)` (r5 181), 208 occurrences (r5 212); literal-px lines **4**, **4/4 commented** (`components.css:523,665`, `features.css:1179,1225-1226`; r5 3/4).
- **[V2] Rendered census** (10 screens × 4 viewports, `r6-vis-typespace.js`) — @320 `1(1) 4(188) 8(177) 12(289) 13(8) 16(120) 20(68) 24(11) 90(10)` = **853/872 = 97.82 %**; @390 97.82 %; @430 97.82 %; desktop **887/956 = 92.78 %** (r5 97.83 / 92.81). Every off-ladder value attributed (90/96/110 nav clearance, 13 ×8 the commented 12+1 tap zone, 1 ×1 UA padding on the hidden date input; desktop 64/216/249 rail geometry). **Author-level off-ladder on mobile 0; no 2/9/10/14/18/22/26/30 px anywhere.** The in-flow empty state adds `margin 0 0 8px; gap 8px` — on ladder.
- **[V3] Type** — @320 `{12,13,15,16,20,24,26,28}`, @390 `{12,13,15,16,20,22,24,26,28}`, @430 + `27.95`, @1280 `{12,13,15,16,20,28,30,32,34,36}`, login `{13,16,17}`; floor 12; off-ramp `font-size` declarations **10** (r5 11; `.stat strong` deleted). Identical sets to r5.
- **[V4] Colour** — **43** literals; `tokens.css` blob `1d70d963…` byte-identical at `fd522c3`, `487c1c4`, `03875ed`; one accent per surface; warm only on `--danger`/`--ok`.
- **[V5] Contrast (live, alpha-composited; stone / genuine dark boot)** — per-surface lowest enabled: home **4.66/5.83** (`.home-head__stale`), timetable 5.34/7.27, feed 5.34/8.07 (A11y 6.74), explore/entity/compose/history 5.34/8.07, mine/settings 5.34–5.66/7.26, Save & Reconnect dialogs **5.14/6.11** (checkbox label), report dialog 5.05/7.27, login 4.87/6.92, feed-error 4.77/7.03. **0 enabled text below 4.5:1 on either surface** (A11y: 468 nodes per boot). New elements: `.timeline__empty` 5.34/8.07; `.timetable-foot` 5.34/8.07; compose actions 13.26/15.27; Save dialog body 5.63/7.27, submit 13.26/15.27; never-listed h1 and CTA 13.26/15.27; chips 5.62 ×3 / 7.01 ×3 (identical because inert, see [V14]); `.timeline__ghost-free` 5.63/7.27. Focus-landing ring vs inside/parent/page 5.81–6.13 stone, 8.36–9.28 night on 7/7 surfaces.
- **[V6] States** — empty ✓ (Timetable/Feed/Explore/History/Entity/Mine/never-listed), loading skeleton ✓ ×8, error `role=alert` ✓ ×8 incl. the **new composer registry-error branch** (`r6-vis-compose-registryerr-390.png`), success/disabled (compose actions disabled until typing; login submit "Signing in…" not exercised), **focus after a POINTER "Try again": ring painted 7/7** (`outline 3px solid rgb(51,102,124)`, offset 3, radius 12, `:focus-visible false`; `r6-vis-ring-crop-390.png`). **[V-R6-1] regression**: "Try again" 77×**36** on all error surfaces (r5 77×44) — `.btn--ghost` intact, height lost to [V-R6-5].
- **[V7] Timetable empty-state geometry** — `[16,190,358,23]` @390, `[16,208,288,23]` @320, `[16,218,398,23]` @430; text centre → SPAN; overlap nav/canvas/hours 0/null/null; distance from viewport bottom 631/337/691; svg 22×22 (1 px baseline offset at 320). Foot `[16,172,358,18]`/`[16,172,288,36]`/`[16,200,398,18]`, present on error with the alert below it. Day with lessons (`?date=2026-08-26`, CST): empty absent, lessons render. **[V-R6-2]** the empty day states the fact twice (prose + six ghost "Free" labels at y 229–762; no pixel collision). **[V-R6-3]** `.timetable-foot` bottom 190 = `.timeline__empty` top 190 — **0 px** between a 13px/400 and a 15px/600 muted line; the element class-named `timetable-foot` renders above the timeline (DOM index 2 vs 3).
- **[V8] Compose** — see [S7]; gaps 8 px on ladder; footnote `[16,423,288,55]` bottom 478 < nav 494 at 320×568; `.main` maxScroll 0 at every compact height; desktop row one line.
- **[V9] Modal containment** — overlay `[0,0,390,844]` / `[0,0,1280,800]` / `[0,0,320,568]`; sheet padding `20 16 24` / `24`; nav never hit-testable beneath; 0 internal overlaps in 5 dialogs. Save dialog @390: body `[16,356,358,96]` → fields → `.stay-connected [16,644,358,112]` (checkbox `[37,661,22,22]`, `accent-color rgb(51,102,124)`) → submit "Save login" `[16,776,358,44]`. **[V-R6-4] @320×568 the submit opens 66 px below the fold**: sheet `[0,45,320,523]`, `scrollHeight 611` vs `clientHeight 522`, submit `[16,590,288,44]` bottom 634 > 568; scroll-reachable (`overflow-y:auto`, `scrollTop 89`); cause: the caveat wraps to 5 lines (132 px block) at 320 (`r6-vis-savedialog-320x568.png`). Report dialog: six `btn--ghost btn--block` 358×44, 0 inputs (deleting `.report-option` was clean).
- **[V10] Idle motion** — `getAnimations().length` **0 at 2.5 s on 11 screens × 2 preferences**; at settle 0 except settings 1 / history 24 finite `rise-in` (0 by 2.5 s); under `reduce` 0 at settle everywhere. `.view` ONE rule; `@keyframes` 6; `infinite` 2 (pull spinner, skeleton).
- **[V11] Overlap sweep** — 4 viewports × 10 screens: `scrollWidth == innerWidth` everywhere; 0 per-element overflow; **0 element-pair overlaps** (incl. the empty state, compose rows, CTA, landings); 1 ellipsis unchanged (`.post__provenance`).
- **[V12] Touch targets (first-load census, 11–12 routes)** — 8–9 unique sub-44 classes / 10 instances at both 390×844 and 320×568: History 67×36, Sync now 79×36, feed Share 59×36, Find 183×36, Your notes 131×36, Forget it 75×36, reactions 49×38 ×2, `···` 44×38, Try again 77×36 ×7, plus pre-existing Why "Back to Experiences" 133×22. Never-listed CTA **220×44** (fixed). **[V-R6-5] cause**: `components.css:838-843` at `487c1c4` (`@media (pointer: coarse) { .btn--small, .react-btn, .sort-toggle__btn, .usermenu__button { min-height: 44px } }`) deleted; base `.btn--small` 36 px (`components.css:112-116`), `.react-btn` 38 px (`:225-240`). Consistent with [S-R6-8] (Structural counts instances over more states).
- **[V13] Lesson-day geometry, Asia/Shanghai** — confirms [S-R6-12] with identical rects: 320×568 P3 `[79,462,216,84]` centre → `A.mobile-nav__item.is-active`; P5 `[79,631,216,84]` 147 px below the fold; 390×844 all clear. Not caused by this commit (620 px canvas vs the 90 px clearance at `features.css:1179` on a 568 px viewport).
- **[V14] Chips — [R3] REFUTED in rendering.** Mine synthesised with `published`/`blocked`/`revoked` rows: `.chip--ok` "Published", `.chip--danger` "Hidden", `.chip--muted` "Removed" all render `rgba(35,43,49,0.05)` / `rgb(51,102,124)` (stone) and `rgba(255,255,255,0.06)` / `rgb(143,194,212)` (night) — **1 distinct (bg, colour) pair among 3**. Cause: the modifiers were inserted at `components.css:195-206` **above** the base `.chip { background: var(--soft); color: var(--accent) }` at `:207-217`; equal specificity, later wins (`.chip--private` after `.chip` does render — proof). A moderation-hidden post carries the same pill as a published one (`r6-vis-chips-390-stone.png`).
- **[V15] Orphans / dead rules / comments** — 11/11 deleted classes 0 hits, 0 residual refs; **[V-R6-6]** theme selected state gone (see [S-R6-9]); **[V-R6-7]** `.timeline__empty svg` declared twice (20 px dead, 22 px wins) — the only duplicate selector in the served sheet; dangling comments **6** (3 created by this commit); pre-existing unclosed comment at `components.css:46` swallowing the Buttons banner (`:47`).
- **[V16] Hash / skip landings** — `h2#privacy`: `3px solid rgb(51,102,124)` offset 3, `:focus-visible true`, `[37,16,316,24]` (`.section-title[id]:focus`, fixed). Skip → `#main`: UA `outline 1px auto rgb(16,16,10)`, offset 0 — no app rule for `#main:focus`.
- **[V17] Login** — wordmark top 32/152/215/149; checkbox 22×22 ×4; submit bottoms 536/637/680/614; fonts `{13,16,17}`; identical to r5.
- Visual per-principle list (#3): three status chips → one pill; four theme cards, one selected → identical; foot/empty 0 px; "foot" above the timeline; svg 20 vs 22; 1 px glyph baseline at 320; **five button-height families** on mobile (36/38/44/48/81; r5 four); Try again 36 beside 44 px compose actions. System held: 43 colours, 97.82 % ladder, type floor 12, 0 overlaps.

## Copy & Honesty (C)

- **[C1] Strings changed/added since `487c1c4`: 24, all LIVE-SEEN** (table in `r6-copy-*.log`); one string now dead: `ReconnectDialog.tsx:37` "Sign in again to refresh the school connection…" (Settings passes `sessionExpired={!portalTokenValid}` and renders Reconnect only when it is false → always the "session ended" body) [C-R6-4b]. Live totals: 273 distinct visible text lines, 20 `aria-label`s, 12 `document.title` values across 21 route states.
- **[C3] Inflations** — `WhyPage.tsx:73` "strongly protected" still live (file byte-identical); `:63` "where possible" hedge; new inflations **0**; superlatives 0.
- **[C4] Dark patterns (rubric)** — forced continuity 0 (login checkbox unchecked; reconnect mirrors `isAuthorized()`), hidden cost 0, fake scarcity 0, confirmshaming 0. **[C-R6-3a]** the Save-login checkbox is **checked by default** inside the user-initiated "Save school login…" flow, disclosed twice in the same view (body + label with the browser-storage caveat), uncheckable (measured).
- **[C5] Import disclosure — [R8] FIXED.** Settings row verbatim: "Timetable import — On for every HOney account since 1 September 2026 — your timetable and history are imported from the school portal when your account is created, and again whenever you press Sync now. Delete them below at any time." Exactly two `syncTimetable` callers in the backend: `routes/auth.ts:32` (`if (result.created)`) and `routes/data.ts:17` (`POST /api/sync`). Real CDP pull on `/timetable`, `/home`, `/history`, `/experiences`: **0 `POST`, 0 `POST /api/sync`** (1 `GET` each). Date claim consistent with migration 009 (`database.ts:244-253`). Login footnote unchanged and true.
- **[C6] Label→behaviour** — (a) "Save school login…" → title "Save school login", body via `aria-describedby="school-dialog-body"`, checkbox in form before submit, submit **"Save login"** ([R4] FIXED). (b) Reconnect absent on a valid token, present on the mocked expired one with body "The portal session ended…" (premise matches). (d) Sync clause identical at `SettingsPage.tsx:117-118` / `TimetablePage.tsx:143-145` (Settings prepends " · "); Timetable clause `[16,138,358,36]`, 24 px under Sync now, present on error. (e) One not-found sentence on 3 surfaces (`EntityPage.tsx:76`, `NotFoundPage.tsx:12`, `ComposePage.tsx:241` + purpose clause); 404 now says "Page not found / Nothing is listed at this address." — directory language on a routing error. (f) delisted: composer and entity agree ([R7] FIXED). (g) Titles: 10 routes + "Dash · HOney"; "ChenJenny · HOney", "213 · HOney", a 52-char course title; never-listed and 404 "Not found · HOney"; `/login` "Sign in · HOney"; SPA transitions correct ([R13] FIXED). (h) chip "Tomorrow · 01:00" ↔ `?date=2026-09-02` ↔ data ([C-M5] FIXED). (i) "Delete account…" → "Delete your HOney account?" / "This permanently removes your account and imported data. This cannot be undone."; "Delete imported data…" → "…You can import again with Sync now." (true, `data.ts:17`); Disconnect body true (`auth.ts:149-152`). (j) PTR idle unnamed inside `aria-hidden`; refreshing named ([R14] PTR FIXED). (k) `not_eligible` note `role="status"` (`ExperiencePost.tsx:273`; not observable live — 0 ineligible posts).
- **New mismatches** — **[C-R6-L1]** withdrawn in part with [S-R6-3]; the surviving half is [S-R6-4] ("today" on non-today dates). **[C-R6-L2]** `MinePage.tsx:196` "1 stored key no longer **match** a post on the server." (verb not pluralised; live with a seeded key). **[C-R6-L3]** `MinePage.tsx:130` region "Your notes and posts" vs h1/title "Your notes & posts". Explore `aria-label "Filter by name"` = placeholder "Filter by name…" ([C-M8] unchanged).
- **[C7] Jargon** — P1–P6 explained above the timeline, in viewport at both widths, on error, correct vs the six period `BANDS` ([R9] FIXED); Settings connection verbs 5 on a valid account (r5 6), 6 on the mocked expired one; "ownership key" / "scrambled at rest" glossed; two-sentences-for-one-idea count **0** (r5 2).
- **[C8] Drift** — `git log 487c1c4..03875ed` over `api/client.ts`, `packages/backend`, `packages/shared`, `WhyPage.tsx` → one comment line (`auth.ts:30-31`, now true). Seven core privacy claims re-mapped 1:1 (identity-free publish `client.ts:229,351-353`; no author column `database.ts:90-121`; 6 = 6 = 6 report categories; `{category}` only; HOney ID line; "design boundary, not magic"; `contract.ts:12-16`). `POST /api/consent` still writes a row nothing reads (unchanged residue).
- **[C9] Comments** — `ReconnectDialog.tsx:16-20` true on 5/6 clauses ("the same caveat the login shows" — the login adds "Turn it off in Settings." and says "Portal time-outs"); `auth.ts:30-31` TRUE; `portalCredentials.ts:8,16` TRUE; `Modal.tsx:11` true but `describedBy` passed at **1 of 5** call sites; **`ExperiencePost.tsx:137-139` FALSE live** (see [S11]); `features.css:440-442` and `:1260-1263` TRUE (measured).

## Weight & Friction (W)

- **[W1] Bundle** — `index-D47LYHws.js` **88,484 B wire (CDP) / 85,691 br / 273,120 raw** (r5 87,863 / 85,069 / 270,886; +0.7 %); CSS 10,250 wire / 9,211 br / 40,577 raw (−2.6 %); Dash chunk 4,406 br; 7 font subsets 152,708 B in dist, 1 fetched cold (latin 28,740), **123,968 B unused shipped** (unchanged); admin CSS in the student bundle 1,201 B; cold `/home` 154,970 B / 12 requests. `/assets/*` `max-age=14400` without `immutable`; Cloudflare RUM 11,131 B / 4 requests per cold load.
- **[W2] Requests** — cold `/home` 12 (3 data), `/experiences` 11 (2), `/timetable` 12 (2), `/settings` 10 (1), explore 12 (3), mine 12 (3), entity 13 (4); warm SPA → `/experiences` 3 (1); `GET /api/entities` once per entity page; DashPage on **0 of 14** student loads. **[W-R6-5] NEW: two concurrent `GET /api/entities` on `compose?entityKey=`** (dispatched at t=0 and t=+2 ms, before the first resolves at +22/+58 ms) — `ComposePage.tsx:54-57` `useApi(() => api.entities(), [effectiveEntityKey])` with no cache key + the newly imported `useNames()` (`:99` → `shared.tsx:161`, key `"entities"`); `useApi` (`lib/useApi.ts:38-70`) has no in-flight dedupe. `?lessonId=` 13 (4); `/history` 12 (3); `/why` 10 (1).
- **[W3] Timing** (medians of 5, not load-controlled) — cold `/home` TTFB/FCP/TTI 48/240/**322** ms (r5 40/192/267); cold `/experiences` 55/284/553 (r5 46/228/445); warm SW reload FCP/TTI 136/161 (r5 108/130); **0 long tasks in 25 cold loads**. r5's claim that the ~99 KB no-store refetch caused the slow first warm reload is refuted: the refetch is now 0 B and reload #1 is still the outlier (FCP 312 / TTI 363).
- **[W4] Idle** — 0 at settle and 2.5 s on 10/11 screens × {cold, SPA} × {no-preference, reduce}; `/history?select=1` 1 *finished* `rise-in`; `.view` settle 460 ms `backwards` per route commit (1e-06 s under reduce); new surfaces (empty state, compose rows, ring, checkbox) have no animation; modal open 2 finite animations.
- **[W5] Interrupts on load** — 0 dialogs/banners/toasts/badges/`role=alert` at first paint and settle on 10 authed routes + `/login`; skeleton `role=status` at first paint only; the sole settled `[role=status]` is Timetable's in-flow empty sentence, now hit-testing to itself. **[W-R6-10]** PTR idle name removed (`aria-label null`, wrapper `aria-hidden`).
- **[W6] Service worker — all r5 [R12] claims VERIFIED with seeded fakes** (`r6-wf-sw1..5.js`): (a) live = repo, `honey-v3`, `/sw.js max-age=14400` (relay item not done). (b) `liveAssetSet` regexes now match Vite's relative `import("./DashPage-….js")` (1 match, resolved against the importing file) and 7/7 fonts → live set 10 URLs. (c) Activate with 10 real current bodies + 5 stale fakes + `honey-v2` seeded: **stale 5/5 evicted, current 10/10 survived (Dash + 7 fonts), `honey-v2` deleted, 0 `/assets/` refetches, 0 reported SW errors**. (d) `/__index-sig` present (`"…CFkg1jp9.css|…D47LYHws.js"`, text/plain, 53 B); after 3 CDP `ServiceWorker.stopWorker` rounds → **0 index JS/CSS fetches, 0 B** (r5 98,712–100,606 B per cold start). (e) Whole-signature compare: JS-only mismatch evicts, CSS-only mismatch evicts, exact match no work (0 extra requests). (f) Non-asset entries 2 (`/` + sig, 2,265 B), shells capped at one `/`, `/api/` 0, content-type guard refuses 200-HTML under `/assets/`, offline shell 76–89 ms. (g) 0 thrown errors. **[W-R6-11] caveat**: with 1-byte (corrupt) cached bodies seeded, activate evicts 9/10 current assets — the live set now trusts cached-body integrity. **[W-R6-12]** `/__index-sig` is a second permanent non-asset entry never cleaned (53 B).
- **[W7] PTR** — feed 636 ms, timetable 556 ms; exactly 1 `GET` per pull, 0 `POST /api/sync`; no native reload; sub-threshold 0; 0 animations after.
- **[W8] Boot theme** — `data-surface` set pre-FCP in 6/6 contexts (50.8–257 ms), 0 later `<html>` mutations, stored choice wins both directions, garbage → stone; static `theme-color="#edf0f1"` residue unchanged.
- **[W9] Compose path** — feed → Share → chooser → History → editor **4 screens / 3 taps**, `activeElement` BODY; Explore path 3/2, BODY. Compact rows verified (see [S7]).
- **[W10] New-cost hunt** — landings never steal focus/scroll on a normal load (7/7 BODY, scrollTop 0, 1 landing per route); retry scroll Δ **0 on 6/6 reachable surfaces** (Explore's −162 is the browser clamping when the error content collapses at t=16 ms, before focus at t=32 ms; r5 History +174 / Explore +278 gone) [W-R6-7]; `?date=` deep link 0 added requests, 0 redirects; composer registry error = real error state, retry 1 request; **CLS 0** on cold `/timetable` (UTC and Asia/Shanghai, lesson blocks rendered), `/home`, `/experiences` — the 31 px offset is a difference between two states, not a within-load shift.

## Accessibility (A)

- **[A1] Contrast** — 468 enabled text nodes per boot, **0 failures** on stone and on a genuine dark boot (`colorScheme:"dark"`, stored theme removed, `surface:"night"` verified); floors 4.66 / 5.83 identical to r5; focus ring ≥3:1 on 7/7 landings (5.81–6.13 stone, 8.36–9.28 night).
- **[A2] Focus order** — 148 observations / 126 real stops over 14 route states; **126/126 ringed** `3px solid rgb(51,102,124)` offset 3, `:focus-visible` true on all. Timetable (lessons day, CST) 13 stops incl. 3 `lesson-block` 282×84; Settings 19 (no Reconnect); History browse 8 (no focusable rows); never-listed CTA now a 220×44 stop; 404 1 stop, no skip link.
- **[A3] Keyboard reachability** — YES for: skip (12/12 → `#main`), lesson modal trap/Esc/opener, scope tabs + arrows, `···` open → Report → Escape → trigger, Tab-out closes, Shift+Tab keeps open, report trap, Save / Reconnect / Delete ×2 trap + Esc + opener restore, date button + fallback, compose actions. **NO**: `···` outside-click focus return (0/3, [A-R6-4]); **[A-R6-5] reaction toggle drops focus to BODY** by Enter and by pointer (undone). ArrowUp/Down implemented but unobservable with one item [A-R6-12].
- **[A4] Landmarks** — `banner` 0, `contentinfo` 0 on every route (unchanged); `navigation` 1 @390, `main` 1 unnamed. Regions with computed names, **all six claimed names verbatim**: "Now and next", "Day timeline", "Lessons", "Everything listed", "Your notes and posts", "ChenJenny experiences"; Feed 0 regions (tabpanel named by `aria-labelledby`). **[A-R6-7]** nested landmarks new: Explore's four named sections inside "Everything listed"; Entity's `<header>` inside its region.
- **[A5] Skip link** — 12/12 authed + `/login`; absent on 404. `#main` receives focus 12/12 with `hash="#main"`, scrollTop 0. **[A-R6-3] the landing is visually silent**: UA `outline auto 1px rgb(16,16,16)` on a box `[0,0,390,844]` = the viewport — edge-pixel sampling of the screenshot returns page background at every sampled point (**0 visible ring pixels**). Next Tab stays in content on every route.
- **[R5][A-R5-A1..A4] Retry landings, 7 surfaces × {mouse, touch, keyboard}** — ring `solid 3px rgb(51,102,124) off 3` in **24/24** cells; `:focus-visible` false for mouse/touch, true for keyboard; **scroll Δ 0 on 24/24**; landings are the result list only (`landing.contains(control) === false` for every search/filter/daynav/scope-switch); 6/7 named regions (Feed = tabpanel, `aria-label` null). Next Tab: Home → Open timetable, Timetable free day → **nav** (dead end), Timetable lessons day (CST) → `lesson-block` ✓, **History → nav (dead end: 0 focusables in the 1780 px landing)**, Explore → row, Mine → Share, Feed → context link, Entity → Share. Ring geometry unchanged (History 1780 px, Explore 1254 px — only two edges on screen).
- **[A-R6-1] NEW — the ring is a false positive on ordinary clicks.** `.focus-landing:focus` (`features.css:1281-1288`) matches any focus, and every landing is a `tabindex=-1` container: `page.mouse.click` on plain non-interactive text — Home `SPAN "Next"`, Timetable `SPAN "No lessons today"` / `"09"` (358×620 on a lessons day), History `H2 "September 2026"`, Explore `H2 "Teachers"`, Mine `H1`, Feed post `P`, Entity `DIV "Teacher"` — focuses the container and paints the full ring on **7/7 surfaces** with no error and no retry in play (`fv:false`); `crop-r6-a11y-clickring-history.png` is pixel-identical to the genuine retry ring. `#main` also takes focus on such clicks but its `outline` computes `none` for pointer focus — only the seven landings misfire.
- **[A-R6-9] Feed live region** — split verified (alert and skeleton outside the polite div); but the live div exists only in the items branch (`liveCount 0` in error/loading) and is **replaced wholesale** on tab change and load-more: a MutationObserver on it recorded **0 mutations** in both — nothing is announced through it.
- **[A-R6-8] Dialogs (6)** — all `role=dialog aria-modal tabindex=-1`, named by `aria-label`, `aria-labelledby` null ×6; focus on open = the `.modal` container; trap 6/6; Escape restores the opener 6/6; background never `inert`/`aria-hidden`. `aria-describedby`: Save ✓ and Reconnect ✓ resolve to the distinguishing sentence; **Delete account, Delete imported data, Report, Lesson modal = null**. Save dialog: submit "Save login", checkbox in form before submit, 22×22, `checked:true`, label hit area 358×112 @390 / 288×132 @320. Reconnect (mocked expired): submit "Reconnect", checkbox `checked:false`.
- **[A6][A-R6-2] Touch targets** — 132 visible interactive elements at each of 390×844 and 320×568; **10 distinct sub-44 controls / 34 instances**, identical at both viewports (r5: 2 genuine): feed 6 (three `btn--small` 36 px, two reactions 49×38, `···` 44×38), timetable 2 (History 67×36, Sync now 79×36), Mine 1 (Forget it 75×36), Why 1 (pre-existing 133×22), `/history?select=1` **Select 62×36 ×24**; plus "Try again" **77×36 on all 8 error surfaces** (r5 77×44). Fixed: never-listed CTA 220×44; Save checkbox 22×22 with a ≥288×112 label. Chips unobservable on the real account ([R3] code-only for A11y; rendered by Visual [V14] with synthesised rows).
- **[A7] Semantics** — `role=alert` 8/8 with `liveAncestor null`; composer registry error `hasEditor:false` ([R6] FIXED) but retry → BODY [A-R6-6]; `.timeline__empty` `<p role=status>` in flow, hit-tests to its own SPAN at 390 and 320 ([R1] FIXED), absent on lessons days; `.timetable-foot` above the timeline and on error ([R9] FIXED) — residue: at 390 on a lessons day the point 20 px into the sentence returns `SPAN.timeline__hour` (an absolutely-positioned hour label bleeds over its left edge); PTR idle unnamed inside `aria-hidden` (fixed); `not_eligible` `role=status` source-only; **[A-R6-11] the hidden date input still carries `aria-label="Pick a date"` inside `aria-hidden="true"`** — the only such case app-wide — and the `showPicker` fallback still shows two named date affordances ([A-R5-A9] persists); `h2#privacy` ring on hash landing `[37,404,316,24]` ([S-R13] FIXED); 0 duplicate ids; 0 unnamed controls; titles per route incl. entity/404/login.
- **[A8] Reduced motion** — 12 routes × 2 preferences, 0 animations in all 24.

---

## Round-6 defect register (what is rough or wrong on `03875ed`, by anchor)

- **[R6-1] Coarse-pointer 44 px floor deleted** — `@media (pointer:coarse){ .btn--small, .react-btn, … { min-height:44px } }` removed (`components.css:838-843` @487c1c4 → `:829-834` now holds `.star-input__star` only) while `.btn--small` (30 emit sites) and `.react-btn` (10) are live. Live: "Try again" **77×36 on all 8 error surfaces** (r5 77×44), reactions 49×38, `···` 44×38, feed header Share/Find/Your notes 36 px, Timetable History/Sync now/Back to today 36 px, Mine "Forget it" 75×36, `/history?select=1` "Select" 62×36 ×24 — 10 distinct controls / 34 instances at 390×844 and 320×568 (r5 [A6]: 2 genuine). Sources [S-R6-8][S-R6-2][A-R6-2][V12][V-R6-1][V-R6-5].
- **[R6-2] Theme picker has no selected state** — `.option-grid button[aria-pressed="true"]` deleted with `.segmented` (`components.css:779-783` @487c1c4); all four Settings → Appearance buttons compute identically on stone and night; the surviving `… span` rule (`:780`) matches nothing (`<i>` + `<strong>`). Sources [S-R6-9][V-R6-6].
- **[R6-3] Chip tone rules restored but inert** — `.chip--ok/--danger/--muted` at `components.css:195-206` sit above `.chip` at `:207-217`; equal specificity → 1 distinct colour pair among Published/Hidden/Removed on both boots. Source [V14] (synthesised Mine rows; real account has 0 keys).
- **[R6-4] Landing ring on any ordinary tap** — `.focus-landing:focus` paints the 3 px accent ring around the whole landing (up to 1780 px) when plain text inside any of the 7 `tabindex=-1` landings is clicked, no retry involved; pixel-identical to the retry ring. Sources [A-R6-1] (7/7 with screenshots), Visual [V6] (ring rule), Structural E7 (boxes).
- **[R6-5] `···` outside-click still loses the trigger (0/3)** — the new guard `closest("button, a, input, textarea, select, [tabindex]")` (`ExperiencePost.tsx:135-142`) matches the `tabindex=-1` landing containers and `main#main[tabindex=-1]` (`AppLayout.tsx:120`, same commit), so the focus return is skipped; post text → `div.feed-stream`, control → BODY, empty space → `main`. The header comment `:137-139` is false. Sources [S11][A-R6-4][C9].
- **[R6-6] Focus dead-ends and drops** — composer "Try again" → BODY (no landing, `ComposePage.tsx:213`) [S-R6-6][A-R6-6]; reaction toggle → BODY by Enter and by pointer [A-R6-5]; `/history` landing holds 0 focusables (Tab → nav) [A-R5-A3 narrowed]; Timetable landing is a dead-end on free days only.
- **[R6-7] Skip-link landing invisible** — `#main` receives focus 12/12 but paints only the UA `1px auto` ring on a viewport-sized box: 0 visible pixels; no `#main:focus` rule. 404 still has no skip link and no `#main`. Sources [A-R6-3][S12][V16].
- **[R6-8] 320×568 lessons day (pre-existing, first observed)** — P3 `lesson-block [79,462,216,84]` centre hit-tests to `a.mobile-nav__item` (nav top 494); P5 `[79,631,216,84]` 147 px below the fold on landing; both probed dates; all clear at 390. Sources [S-R6-12][V13][A6].
- **[R6-9] Save dialog submit below the fold at 320×568** — submit `[16,590,288,44]`, bottom 634 > 568; scroll-reachable; the 132 px checkbox block this commit moved into the save path is the cause. Source [V-R6-4].
- **[R6-10] CSS hygiene residue** — duplicate selector 1 (`.timeline__empty svg` 20 vs 22 px); dangling comments 6 (+2, three created by this commit); 0 px between `.timetable-foot` and `.timeline__empty` (two muted lines of different size/weight butt together); the element class-named `timetable-foot` renders above the timeline; orphaned descendant `.option-grid button[aria-pressed="true"] span`; five button-height families (36/38/44/48/81). Sources [S4][S-R6-10][S-R6-11][V-R6-3][V-R6-7][V15].
- **[R6-11] Empty-day double grammar** — one `role=status` sentence "No lessons today" (15px/600) directly above six 12px "Free" ghost labels, all six co-visible at 390; on lessons days only the free periods render. Sources [S-R6-13][V-R6-2].
- **[R6-12] Copy residue** — "No lessons **today**" on any selected non-today date (`TimetablePage.tsx:302`) [S-R6-4]; `?date=2026-13-45` silently rolls to 2027-02-14 [S-R6-5]; 404 "Nothing is listed at this address." (directory language on a routing error); Mine "1 stored key no longer match" [C-R6-L2]; "Your notes and posts" vs "Your notes & posts" [C-R6-L3]; dead "refresh" reconnect body [C-R6-4b]; "strongly protected" [C3]; Explore aria = placeholder [C-M8].
- **[R6-13] Concurrent duplicate `GET /api/entities` on `compose?entityKey=`** — new with `useNames()` (`ComposePage.tsx:54-57,99`); `useApi` has no in-flight dedupe. Source [W-R6-5].
- **[R6-14] AT residue** — `aria-describedby` on 2 of 6 dialogs; `aria-labelledby` 0/6; background never `inert`; hidden date input `aria-label` inside `aria-hidden` and the two-affordance fallback [A-R6-11]; entity error-branch region "Teacher experiences" [S-R6-7]; feed live region replaced wholesale, 0 announcements [A-R6-9]; nested regions on Explore/Entity [A-R6-7]; unnamed `aria-live` div adds a DOM level [S-R6-1]; P1–P6 foot overlapped at its left edge by an hour label at 390 on lessons days [A7]; `banner`/`contentinfo` 0.
- **[R6-15] Relay (anchor #9 met)** — `sw.js` still `max-age=14400` and `honey-v3`; eviction trusts cached-body integrity [W-R6-11]; `/__index-sig` never cleaned [W-R6-12]; 123,968 B unused font subsets; 1,201 B admin CSS; `/assets/*` without `immutable`; static `theme-color` outside the palette.

## r5 handoff verification summary (live on `03875ed`)

VERIFIED: [R1] empty state in flow (390/320/430, CST-correct presence); [R2] compose two rows 44 px, no wraps (4 viewports); [R4] "Save login" + in-form checked checkbox + `aria-describedby`; [R5] pointer/touch/keyboard ring 24/24, scroll 0; [R6] composer registry-error state; [R7] composer/entity agreement on delisted and never-listed ids; [R8] import disclosure (2 real triggers, 0 `POST /api/sync` on pull); [R9] P1–P6 above the timeline, on error; [R11] CTA 220×44; [R12] SW activate eviction, Dash survival, `/__index-sig`, 0 B cold-start refetch, whole-signature compare; [R13] titles, one not-found sentence, one Sync clause; [R14] PTR name, `not_eligible` role (source), `h2#privacy` ring; [R16] "Delete account…"/"Delete imported data…"; [S-R13] skip → `#main`; [A-R5-A1] no landing scroll; [A-R5-A2] names 6/7; [A-R5-A4] alert outside the live region; [C-M5] Open timetable deep link; Reconnect gated on the token; 90 px literal commented; deleted classes clean 11/11; `.btn--ghost` intact; `tsc` 0.
PARTIAL: `aria-describedby` 2/6 dialogs; feed node still carries three jobs; Timetable/History dead-ends (History remains); ArrowUp/Down unobservable.
REFUTED: [R3] chips (rules present, inert by cascade); [R10] `···` outside-click (0/3, new cause); dangling comments (6, up from 4); duplicate selectors (1, up from 0); [A6] touch targets (regressed 2 → 34 instances); `sw.js` `no-cache` header (not done); [C3] "strongly protected" (unchanged); [C-M8] (unchanged); hidden date-input `aria-label` (unchanged).
WITHDRAWN by the audit: [S-R6-3] "No lessons today on days with lessons" (UTC harness artefact; presence predicate correct in Asia/Shanghai).

## Known gaps (all passes)

Harness timezone is UTC (`lib.js` sets no `timezoneId`); Timetable measurements were repeated under Asia/Shanghai but other routes were not; `.timeline__now` and `.nextlesson__wash` never rendered (runs at 06:03 CST). Mine's populated branch is synthesised (0 real ownership keys); chips rendered only with fulfilled `/api/experiences/mine` rows. Token-expired state is a rewritten `/api/me`. No dialog submitted, nothing published, no report sent — success states unexercised (as r1–r5). Timings not load-controlled. A real two-hash deploy was never staged (signature cases produced by rewriting `/__index-sig`). Screen-reader runtime announcement untestable (DOM wiring and mutation records only). 430×932 and 1280×800 censuses are Visual-pass-only. The session token expired twice mid-run; affected runs were discarded and repeated. The `sessionExpired:false` reconnect body and "Save login" with the box unchecked (which per `ReconnectDialog.tsx:61-62` would clear credentials) are source-traced only.
