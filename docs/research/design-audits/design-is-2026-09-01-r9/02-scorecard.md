# 02 — Scorecard (r9; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `eede644`)

1. Good design is innovative — Score: 2/3 (r8 2, Δ0)
   Evidence: [C-R9-19] identity-free publish, no-author-column storage, browser-held ownership keys and category-only reports re-verified with zero source drift for the eighth round (`api/client.ts:227-229,349-353`, `db/database.ts` mig. 003, `routes/experiences.ts:208`); everything this wave shipped — a body portal with `inert`, a generation-guarded cache write, a per-pill pending flag, a served-CSS parse gate, a `replaceState` per date change, a `scroll-margin-top` landing — is a conventional pattern.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1. No tie.

2. Good design makes a product useful — Score: 2/3 (r8 2, Δ0)
   Evidence: For: [W-R9-1][S-R9-11] a pull-to-refresh now sticks in both race orders; [S-R9-5][S-R9-6] a registry outage no longer hides a loaded feed or the entity's name; [S-R9-18][V-R9-5] a date step to a new day lands once; [S-R9-16][A-R9-20] retry never scrolls (0 px on every landing incl. the Timetable at 320×568); [S-R9-22][W-R9-10] a wrong address keeps the nav and costs one API call; [A-R9-9] the skip link reaches `#main` on 12/12 routes. Against: [S-R9-8][S-R9-9][A-R9-20] "Try again" on Mine, on Explore's directory failure and on a Timetable that fails again drops the keyboard user on `<body>` (3 of 10 error surfaces); [S-R9-13][W-R9-5] "Back to today" from a stepped-into page leaves the previous `?date=` in the address bar, and a reload of that link shows the wrong day; [S-R9-19][S-R9-20] a step onto a cached day inherits the previous day's scroll and a pull discards the landing; [W-R9-7] every date step re-fetches even a cached day (14 GETs over 13 steps); [W-R9-2][S-R9-11] one pull empties the cache for every screen; [A-R9-18][V-R9-17] at compact heights the first Tab lands mid-timeline past the skip link and the daynav; [A-R9-21] a second tap on a pending pill relocates focus to an unnamed tabpanel; [S-R9-29] History "Select" still lands the composer on `<body>`; [A-R9-20 required] no keyboard equivalent to refresh or load-more.
   Justification: All three primary tasks complete on-screen without a wrong-screen detour (rules out 1); the chooser path, a cursor tap, the re-fetch per step, the cache emptied by a pull and three retry surfaces that lose the user are adjacent steps and decoys, not "fewest possible steps; no decoy actions" (rules out 3). No tie.

3. Good design is aesthetic — Score: 2/3 (r8 0, Δ+2)
   Evidence: [V-R9-1][A-R9-1][O-R9-1] 0 of 117 controls carry an outline at rest on 60 route-loads across both boots, edge-band accent pixels 0/0/0/0, the theme grid unringed — r8's jarring violation is gone; [V-R9-2][O-R9-2] 42/42 mobile dialog opens are one sheet grammar (`20px 20px 0 0`, top border only, safe-area padding, `sheet-up`, flush bottom, palette intact through the portal), Reconnect scroll 6 px → 1 px; [V-R9-3] 0 orphan declarations; [V-R9-10..12] spacing 97.6–97.8 % / 92.6 % on-ladder, 43 colours, `tokens.css` byte-identical, type set unchanged; [V-R9-7] the 404 wears the same `h1.page-title` metrics as every other page; [V-R9-8] the lesson description costs no visible pixel. Remaining inconsistencies: [V-R9-9][S-R9-37] two disabled fills on one 44 px pair in the compose row (`btn--primary:disabled` grey vs ghost transparent); [V-R9-4] the skip ring's `border-radius:12px` matches no other rendered radius (cards 17, blocks 10, posts 0, buttons 99); latent/hygiene: [V-R9-9] `.react-btn:disabled` unstyled (never rendered), [V-R9-10] one new off-ladder literal (`scroll-margin-top:100px`), [V-R9-16] two adjacent identical `@media (max-height:620px)` blocks, [V-R9-2] a 1 px sheet scroll at 320×568.
   Justification: Anchor 3 requires "no orphan styles" — an orphan radius value and two fills for one disabled state bar it; anchor 2 is "≤2 minor inconsistencies across the audited surface", which is exactly the two rendered ones (the hour-label crossing [V-R9-4] is a focus-detail defect scored under #8, and the latent items are not rendered); nothing rendered is a jarring violation and the count does not reach 3, so not 1. No tie.

4. Good design makes a product understandable — Score: 1/3 (r8 1, Δ0)
   Evidence: Fixed and verified: [A-R9-1] a ring means focus again on 202/202 stops; [A-R9-6] only the tapped pill says pending; [C-R9-3][A-R9-12] the save dialog's button reads "Save login" / "Continue without saving" with the tick; [S-R9-22][A-R9-8] the 404 has the nav and a skip link that lands; [S-R9-10] one region named "Editor"; [C-R9-9] "1 experience" and a silent error channel; [C-R9-14] one locale, zero non-English tokens; [C-R9-6] a 29-character checkbox name; [A-R9-4] a punctuated lesson description; [V-R9-5][S-R9-15] the day heading and note in view on arrival when the target block is the first. Against, on `eede644`: [S-R9-13][S-R9-14][W-R9-5] after "Back to today" from a stepped-into page the address bar names the previous day (`?date=2026-09-04` over 2 September) — the control's result and the URL disagree on exactly the transition the r8 fix targeted; [S-R9-23] on `/experiences/new` or `/timetable/oops` a nav tab reads `aria-current="page"` under the heading "Page not found"; [A-R9-19][W-R9-8][S-R9-18] whenever today's target is a later lesson (after the first lesson ends) or a stepped-to day is sparse, the landing clamps to maximum scroll and the student arrives with no day name, no note and no stepper in view — r8's condition, now clock-dependent; [C-R9-3] the reconnect dialog's "Reconnect" saves or deletes the stored login by the tick with no change of label; [C-R9-10] the default tab's status ("Nothing has been shared yet") contradicts the visible sentence ("Nothing from your classes yet."); [C-R9-11][S-R9-26] load-more announces "24 experiences", a tab change re-announces an unchanged count; [C-R9-14] Home "Wednesday 2 September" beside Timetable "Wednesday, 2 September 2026"; [C-R9-7] "More options" opening "Post options" holding one item; [C-R9-17] "Couldn’t load" over "Could not reach"; [A-R9-21] a second tap on a pending pill moves focus silently; [C-R9-21] jargon / unclear labels 5 (r8 6), label→behaviour mismatches 4 (r8 4, all new).
   Justification: Anchor 1 is "2–3 controls unclear; jargon present": "Back to today" (URL says otherwise), the mis-current nav tab on a not-found page and the tick-dependent "Reconnect" are three controls whose state or result contradicts what they say, and five unclear labels are counted; that is past anchor 2's "1 control needs a tooltip". Every primary action is identifiable without help, so not 0. Closest to its upper boundary of any score this round but not a tie: the three contradicting controls are each measured, not inferred.

5. Good design is unobtrusive — Score: 2/3 (r8 1, Δ+1)
   Evidence: For: [V-R9-1][A-R9-1] the accent ring is painted only on focus (r8: ~25 rings per screen at rest); [W-R9-19] 0 dialogs / toasts / badges / alerts / live regions on load across 24 cells; [W-R9-3] a dialog opens with CLS 0 and no long task on 7/8, the page behind frozen and `inert`; [V-R9-5][W-R9-9] the compact landing is one instant step of 119–146 px that now keeps the heading and note on screen (first-lesson state); [V-R9-9] the untapped pill stays quiet; [W-R9-22][A-R9-15] reduced motion collapses every duration; [W-R9-21] the theme is right before first paint. Against: [W-R9-20] `/home` shifts its own footer at ~900 ms (CLS 0.095 once contention-free, 4/6 non-zero under contention; r8 0/18); [W-R9-18] a `width` transition on the next-lesson wash restarts every second for the length of every lesson; [A-R9-19][W-R9-8] the later-lesson landing and any sparse-day step scroll the heading, note and daynav out of the viewport ("motion that hides the chrome the student needs next", as r8 put it); [V-R9-5] the daynav is parked 106–127 px above the viewport on every compact arrival; [V-R9-4] the skip ring encloses the document-height `.view` so most of it is below the fold; [V-R9-15] the translucent floating nav and the per-route `settle` entrance remain.
   Justification: Anchor 2 is "chrome visible but quiet" — the nav, the entrance and the landing are visible but no decoration competes with the post text or the lesson blocks now that the rest ring is gone (rules out 1); a footer that shifts under the reader, a heading that leaves on arrival and a transition that never stops during a lesson keep the chrome from "receding" (rules out 3). No tie.

6. Good design is honest — Score: 2/3 (r8 2, Δ0)
   Evidence: [C-R9-19] nine privacy claims re-mapped 1:1 with zero source drift, 6 = 6 = 6 report categories, the import disclosure verbatim with exactly the two `syncTimetable` callers; [C-R9-1] both delete flows name the shared teacher, course, room and lesson rows and the claim is true at the row level; [C-R9-2] all four storage caveats and the notes bullet say the key shares the store, matching `portalCredentials.ts`; [C-R9-3] the save dialog's label follows the tick; [C-R9-8][W-R9-11] the backend comment matches both origin and edge; [C-R9-18] no composer is offered while the registry is unknown; [C-R9-21] dark patterns 0/7, inflations 1 ("strongly protected", `WhyPage.tsx:73`). Against: [C-R9-20] "Delete account" leaves the encrypted school login and its key in `localStorage`, unmentioned by either delete row or body; [C-R9-1] the account confirm body still omits "Published experiences stay"; [C-R9-4] "Continue without saving" performs a live portal sign-in it does not name; [C-R9-3] "Reconnect" saves or clears by the tick; [C-R9-5] one of three provenance rows unified, `shared.tsx` now mixing registers.
   Justification: Anchor 1 needs "2+ inflations OR one dark pattern" — one inflation, zero dark patterns; the saved login surviving account deletion and the unlabelled tick outcomes are omissions about what stays and what happens, not inflated value or a deceptive flow, so they bar anchor 3's "every claim maps 1:1" rather than adding to the inflation count; 2, no tie.

7. Good design is long-lasting — Score: 2/3 (r8 2, Δ0)
   Evidence: [V-R9-11][V-R9-12] single humanist face, one accent per surface, 43 colours and a byte-identical `tokens.css` for six commits, spacing 97.6 % tokenised; [V-R9-15] trend markers rendered: the per-route `settle` entrance and the floating translucent (`blur(12px)`) pill nav with sliding indicator — one soft-app-shell signature, as in r1–r8 — plus pill radii, 13+3 radius families, one soft shadow recipe, `color-mix()` tints; 0 gradients outside the skeleton; largest mobile type 28 px. Durability facts: [W-R9-13][W-R9-14] the new parse gate catches the two shapes that shipped in r8 and misses the same bug spelled with a class, an attribute, uppercase, a stray `}` or an unterminated `{`; [S-R9-8][A-R9-20] the entity fix was copied to Mine without its arming flag — the third round in which a sibling inherits the defect the original was written to cure.
   Justification: The entrance choreography and the translucent floating shell are one dated signature, matching anchor 2 exactly; the gate's blind spots and the copied defect are process facts, not trend markers; no skeuomorph residue or trend typography (bars 1); the signature bars 3. No tie.

8. Good design is thorough down to the last detail — Score: 1/3 (r8 1, Δ0) — tie 1↔2, settled lower
   Evidence: Fixed and verified: [A-R9-1][V-R9-1] focus is a distinguishing state on every control; [A-R9-3][V-R9-2] `inert` 7/7, trap 0 escapes over 60 presses per dialog, portal palette intact on a dark boot; [A-R9-9] the skip ring touches 0 content rows on 24 cells (r8 5/12); [A-R9-5] report `describedBy` no longer dangles; [A-R9-4][C-R9-16] a punctuated lesson description; [A-R9-10][S-R9-7] entity double-fail lands; [W-R9-1] refresh sticks; [V-R9-14] error states on Mine, Explore-directory and Entity; [V-R9-3][S-R9-34] 0 orphan declarations with a build gate; [S-R9-4] `tsc` 0; [A-R9-14] 0 contrast failures on both boots incl. the new 404 and the portaled dialog; touch 257/259 with the 2 sub-44 hidden. Worst instances on `eede644`: **focus** — [S-R9-8][S-R9-9][A-R9-20] three retry surfaces (Mine, Explore-directory, Timetable-fail-again) drop to `<body>`, [A-R9-17][S-R9-33] the report dialog returns focus to `<body>` on all four exits (1/7 dialogs), [A-R9-18][V-R9-17] the first Tab at compact heights skips the skip link and the daynav, [A-R9-21] a second tap on the pending pill parks focus on the tabpanel, [S-R9-29] History "Select" lands on `<body>`; **loading / error** — [V-R9-14] Compose-with-`lessonId` renders neither a skeleton nor a banner when `/api/history` stalls or fails; **pending** — [A-R9-21][S-R9-12] the second activation is swallowed with no visible or announced response; **disabled** — [V-R9-9] two fills, `.react-btn:disabled` unstyled; **empty** — [V-R9-14] Explore renders the error banner and four "Nothing here yet." together; **edge/spacing** — [A-R9-19] the later-lesson landing parks block 1 at y=−10 at 390×620 (r8 −6), [V-R9-4] the skip ring crosses the "20:00" hour label, [V-R9-6] the 16 px empty-day gap, [V-R9-2] a 1 px sheet scroll; **AT / copy** — [A-R9-4] the lesson `<dl>` still read after its description, [A-R9-7] two unnamed `role=status` nodes; **hygiene** — [S-R9-35] dangling comments 10 (r8 9), [W-R9-14] a gate that catches the instance, not the class, [A-R9-1] tap-highlight suppressed on `button, a` but not on inputs.
   Justification: Anchor 2 allows "1 state missing or rough"; here two families are missing on one compose branch (loading and error, [V-R9-14], single-pass but with the endpoint named) and four are rough on their worst instance (focus on five distinct paths, pending, disabled, empty), which reads as anchor 1's "2–3 states missing"; if the single-pass compose claim is discounted, 0 missing and 4 rough still exceed anchor 2's single allowance. Nothing is default-browser and no fourth family is absent, so not 0. Uncertain between 1 and 2; the tie-breaker settles it at 1.

9. Good design is environmentally friendly — Score: 2/3 (r8 3, Δ−1) — tie 2↔3, settled lower
   Evidence: [W-R9-15] app JS 86,624 B br / 89,051 zstd, CSS 9,312 br, 5 dependencies unchanged; [W-R9-21] prefers-color-scheme honoured pre-FCP 6/6 with 0 later `<html>` mutations; [W-R9-22][A-R9-15] reduced motion → 1e-06 s on 25/25 and `scroll-behavior:auto`; [W-R9-23] SW battery green (9/9 kept, 5/5 evicted, 0 B cold-start refetch, exact match no work, offline shell 79–117 ms, `/api/` never cached); [W-R9-16] 0 duplicate API paths on 15/15, warm navigation 3 requests / 1,808 B; [W-R9-25] 209 dead CSS bytes gone; [W-R9-18] idle cost 0 mutations / 0 requests / 0 long tasks over 20 s. Against the anchor: [W-R9-18] `document.getAnimations()` reports a **running** `width` transition on `/home` at 2.5 s during a lesson — `useNowTick(1000)` (`HomePage.tsx:21`) restarts `transition: width 1s` (`features.css:80`) every second for the length of every lesson (1/22 cells; r8 0/22 sampled outside a lesson; code unchanged since before r8). Relayed, outside the anchor: [W-R9-20] `/home` CLS 0.095; [W-R9-7] one GET per date step even for a cached day; [W-R9-26] the SWR map +2.46 MB over 20 steps, unbounded; [W-R9-2] a discarded body per abandoned in-flight view; [W-R9-25] 123,968 B never-requested font subsets and 1,201 B admin CSS in `dist`; [W-R9-23] the SW's eviction walk fetches the Dash chunk; [W-R9-24] the edge still rewrites `sw.js` to `max-age=14400`.
   Justification: Anchor 3 reads "initial JS <100KB, no idle animation, dark mode honored, prefers-reduced-motion respected" — three of four hold, and the fourth is now measured failing during school hours by the same `getAnimations()` probe the audit has used since r1; the transition is functional (a lesson-progress wash) and gated by reduced motion, which is anchor 2's "motion gated". Uncertain whether a progress transition is "idle animation" in the anchor's sense; the tie-breaker settles it at 2. This is not a wave regression — r8's 0/22 was sampled between lessons — but the audit scores what it measured.

10. Good design is as little design as possible — Score: 2/3 (r8 1, Δ+1)
    Evidence: [S-R9-1] census delta 0 on 16/17 surfaces (the 404 gains the shell's 4 nav items and skip link, which now do something); [S-R9-26] the error-branch status no longer duplicates the alert (r8's third removable gone); [S-R9-22] the 404's no-op skip link is gone (r8's second removable gone); [S-R9-34] `sheet-up` one grammar, 0 orphan declarations; [S-R9-4] `tsc` 0, consumer-less classes 0 adjusted. Removable elements on one screen now: [S-R9-25][C-R9-7] the `···` menu wrapping the single item "Report" — now named "Post options" under a trigger named "More options", still one element more than the item; [V-R9-14] Explore's four "Nothing here yet." rendered beneath the error banner. Latent: [S-R9-27] header Share + 2 invites at 24 posts (real feed 1 post); [S-R9-28] two classless `<div>` wrappers per post; [A-R9-7][W-R9-19] an always-mounted empty `role=status` disc; [S-R9-3] "Try again" 10, `role="alert"` 16, `h1.page-title` 16 — three families grown by this wave; [S-R9-35] dangling comments 10, individually repeated selectors 5.
    Justification: No page is dominated by decoration or duplicated affordances (not 0); two rendered elements are removable without breaking a task (the one-item menu, the four empties under a banner) — anchor 2's "≤2 removable elements"; the latent duplicates are code-level and the r8 rendered ones are gone, so not 1. No tie.

**Total: 18/30** (r8 15/30, Δ+3; r7 19/30; r6 18/30; r5 19/30; r4 18/30; r3 16/30; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #8 (1↔2), #9 (2↔3). Principles scored with no tie: #1, #2, #3, #4, #5, #6, #7, #10. Sensitivity, stated honestly: granting both ties upward gives 20/30 with no principle at 0 — the REFINE threshold — so for the first time in nine rounds **the verdict turns on the ties**. The rule is mechanical and both were genuinely uncertain: #8 because the only "missing" families are on one compose branch measured by one pass, with the rest rough rather than absent; #9 because the running animation is a functional progress wash that r1–r8 never sampled during a lesson. Downward: #4 is the score closest to its lower anchor (three measured contradicting controls) but is not a tie; no other no-tie score was close to its lower anchor.

Why r9 rose by 3: #3 recovered two points and #5 and #10 one each because the two parser-level CSS breakages are verifiably repaired on every surface and the r8 wave's no-op skip link and duplicated error announcement are gone; #4 and #8 held at 1 because the wave reproduced its own pattern once more (the entity arming fix copied to Mine without its flag; Explore's directory retry never arming; the report dialog's opener unmounted before the modal mounts; a `?date=` fix that compares against a value `replaceState` never updates; a heading-in-view fix that holds only while the target block is the first of the day; a `pointer-events:none` that lets a second tap fall through to the tabpanel; a compact landing that now moves the keyboard's starting point past the skip link); #9 lost a point to a measurement, not a change. The app is scored as it ships.
