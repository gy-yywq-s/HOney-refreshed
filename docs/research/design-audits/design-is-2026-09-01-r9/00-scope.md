# 00 — Scope (design-is audit, HOney, 2026-09-02, r9 — verification round)

## Audited artifact
- The DEPLOYED web app at https://honey.gaelisus.com — branch `integration/product-v2`, commit `eede644` ("web: a date step lands once like a cold load (settled only after this date was seen loading)"), five commits on top of `13cbcee` (r8's audited commit): `a6e7b44` ("css: repair two parser-level breakages; served-CSS parse gate in the build"), `c3a33fa` ("design-is r8 handoff: sibling contracts, cache write guard, pending per pill, inert dialogs, timetable URL/landing, copy"), `ee9ab58` ("web: 404 route as a shell sibling (was nested in the Dash Suspense); compact landing keeps the heading in view"), `eede644`. Served bundle `index-BDmmlJxi.js` / `index-BFZUVSQz.css` / `DashPage-C5s-hczQ.js` — matches `apps/web/dist/assets` built from HEAD (confirmed by `curl` of `/` before fan-out). `git diff --stat 13cbcee..eede644` touches 21 web source files (`App.tsx`, `Modal.tsx`, `ReconnectDialog.tsx`, `ExperiencePost.tsx`, `lib/format.ts`, `lib/useApi.ts`, `LoginPage`, `NotFoundPage`, `SettingsPage`, `TimetablePage`, `ComposePage`, `EntityPage`, `ExplorePage`, `FeedPage`, `MinePage`, `shared.tsx`, `components.css`, `features.css`, `foundations.css`, `package.json`, new `scripts/check-css.mjs`) plus `packages/backend/src/app.ts` (comment only) — 628 insertions / 77 deletions including the r8 audit artifacts; no `sw.js` change. Public-edge headers at fan-out: `sw.js` `cache-control: max-age=14400` (edge rewrite of the origin's `no-cache`; the edge is outside this repo), `/` `public, max-age=0, must-revalidate`. The commit messages claim to implement the full r8 handoff (moves 1–4).
- Source at `/home/honey/HOney-refreshed/apps/web`:
  - styles: `src/styles/` (tokens.css, foundations.css, components.css, features.css, admin.css); build gate `scripts/check-css.mjs` wired into `npm run build`
  - pages: `src/pages/` (HomePage, TimetablePage, DashPage, HistoryPage, SettingsPage, LoginPage, NotFoundPage, experiences/{FeedPage, ExplorePage, ComposePage, EntityPage, MinePage, WhyPage, shared})
  - components: `src/components/` (AppLayout, Modal, PullToRefresh, ReconnectDialog, SchoolLoginForm, ThemeControls …); `src/lib/useRetryFocus.ts`, `src/lib/useApi.ts`, `src/lib/motion.tsx`, `src/lib/format.ts`
  - stream components: `src/features/experiences/` (ExperiencePost.tsx, useFeedController.ts, useLoadMoreSentinel.ts)
  - backend (read for honesty mapping and header intent only): `packages/backend/src/app.ts`, `routes/auth.ts`, `routes/data.ts`, `services/accounts.ts`, `services/timetable.ts`, `services/importer.ts`, `db/database.ts`

## Round framing
This is ROUND 9 of an iterative audit. r1 16/30, r2 16/30, r3 16/30, r4 18/30, r5 19/30, r6 18/30, r7 19/30, r8 15/30 (all REDESIGN by the <20 rule). r8 audited `13cbcee`: the −4 came from two parser-level CSS breakages the r7 wave introduced (a dangling `button,` selector ringing every button at rest; a mis-nested `@media` killing the phone bottom-sheet skin) plus the new contradictions it shipped (entity retry armed on the wrong flag, a cache write above its cancellation guard, a mount-only `?date=` fix, a compact landing that scrolled the heading away, an unpluralised feed status, both pills pending, a 404 skip link that skipped nothing, "Save login" that clears, a locale moved not unified). The r8 handoff has been executed and deployed as `a6e7b44`→`c3a33fa`→`ee9ab58`→`eede644`. R9 (a) verifies every r8 move on the live app, (b) hunts for NEW defects the fixes introduced (the `Modal` portal to `document.body` + `inert` on `#root` — trap, Esc, opener restore, nav hit-testing, nothing inert inside the dialog, effect re-runs on parent re-render; the `.main:focus > .view` skip-link ring; the per-pill pending with `pointer-events:none`; the once-per-date landing contract incl. a step to a date cached from an earlier mount; the 404 inside the shell; the en-GB lock; the `scroll-margin-top:100px` literal now applying at every height; the `?date=today` rewrite on plain `/timetable`; the Mine registry retry armed on `mine.loading` while it reloads names — the r8 entity pattern reproduced), (c) re-measures the standard metric tables (rest-state outline census on 10 routes × 3 widths × 2 boots, mobile-sheet computed-style census on all 7 dialogs, rendered spacing census, type table, contrast on stone AND a genuine dark boot, touch targets in a `hasTouch` context at 390 and 320, idle-animation count, bundle size, `tsc --noUnusedLocals`, request counts, duplicate-selector and consumer-less-class counts, TSX→CSS reverse sweep, served-CSS parse, SW seeded eviction, compact-height scroll sampling + overlap census + h1-in-view at 320×568 / 360×620 / 390×620 in Asia/Shanghai). Scores are re-anchored from scratch against the same rubric — no credit for unverified fixes, no inflation for effort, tie-breaker lower; where an r8 blocking condition is verifiably gone, the anchor is re-read and the evidence scored as it stands.

Owner's product decisions (standing, not up for re-litigation): (1) timetable import is part of the account — there is NO consent gate and no switch by design; it is disclosed (Settings "Timetable import" row + login footnote). The audit scores the *disclosure* (visible, true, complete?), not the absence of a toggle. (2) The 620 px timetable canvas density on normal-height screens stays; the compact-height notches (540 at ≤620 px height, 450 at ≤360 px width), the static daynav and the once-per-date landing at ≤620 px heights are the deliberate degradation — the audit measures whether that degradation holds (hit-testing, legibility, overlaps, motion, heading in view), not whether it should exist.

## Primary user
A high-school student on their phone (installed PWA). Typical viewport 390×844; must hold down to 320×568.

## Primary tasks
1. Check today's timetable / next lesson.
2. Read the Experiences stream.
3. Share an experience.

## Constraints
- Quiet-humanist design language: single font (Source Sans 3), narrow cool palette, ONE muted blue-teal accent `#33667c` (night `#8fc2d4`), no warm tones except semantic danger/ok.
- No small ALL-CAPS titles (owner rule).
- Every finite option set fully displayed (owner rule — search only filters, never hides the set).
- Honest privacy copy — never overclaim anonymity.
- iOS wire compatibility — no API changes may be proposed.

## Evidence mode
- Dynamic behavior is first-class evidence: the live app was OPERATED in a real browser (playwright, authenticated via the audit harness at /root/claude-work/design-audit/, `lib.js` → `authedPage({viewport, desktop, reducedMotion})` returning `{browser, context, page}`; r9 taught `lib.js` to renew the access token from the refresh token instead of re-logging into the school portal every 30 minutes), not just screenshotted. Viewports: 390×844 (primary), 320×568, 320×600, 360×620, 390×620, 430×932, desktop 1280×800; `matrix.js` = 9-device regression. The harness runs in UTC, where this account's lessons fall outside the 09:00–20:00 window — every measurement involving lesson blocks uses a hand-built context with `timezoneId: "Asia/Shanghai"` (2026-09-02 and 2026-08-24 are 3-lesson days; 2026-08-22 and 2026-09-06 are empty). Probe scripts `r9-{struct,vis,copy,wf,a11y}-*.js` (+ `.log`) and screenshots `r9-*.png` retained in the harness directory; r8 probes re-run where the measurement is the same.
- Static code/CSS reading complements browser evidence.
- Five evidence subagents (Structural, Visual, Copy & Honesty, Weight & Friction, Accessibility), each run on `model: "opus"` per the owner's standing order (CLAUDE.md rule 4h); scoring and verdict by the orchestrator only.
- Hard limits honored: no publishing of experience posts (no "Share anonymously"/"Share as written"), no report submission, no state-changing Settings actions (Sync now / Reconnect / Save login / Continue without saving / Disconnect / Delete / Sign out / Turn off not pressed — dialogs opened and cancelled only), no secrets read or reproduced (`/home/honey/.secrets/` never opened, `.session.json` never printed or copied); reactions only if reverted in the same script or intercepted. Every browser launched is closed.

## Out of scope
- iOS app, admin pages (/admin, /dash internals), backend moderation logic, login portal internals, the public edge (hostd gateway / Cloudflare) header rewrite — recorded as a relay only.
