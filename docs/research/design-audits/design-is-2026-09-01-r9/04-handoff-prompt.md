# 04 — /make-plan handoff (r9)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit eede644). Current design failed audit at 18/30 with critical gaps in principles #4 understandable (1/3) and #8 thorough (1/3); no load-bearing principle scored 0, and two ties (#8 1↔2, #9 2↔3) were settled downward by the audit's tie-breaker — granted upward the total is 20 (REFINE), so this REDESIGN is one measured detail on each of two principles away from REFINE and every move below is a finish-the-contract move on a layer whose tokens, type, colour, spacing, motion budget, weight, trust architecture and service worker verify live for the seventh round running.

Verdict paragraph (quoted from 03-verdict.md):
> The r8 handoff verified as built on the two failures that cost r8 four points and on most of what it listed beside them — 0 of 117 controls carry an outline at rest on 60 route-loads across both boots and the ring differs from rest on 202/202 Tab stops, every one of 42 mobile dialog opens is a bottom sheet with `20px 20px 0 0`, a top border only, safe-area padding and `sheet-up`, the served CSS parses with 0 orphan declarations and a build gate that catches both shapes that shipped, the `Modal` portals to `document.body` with `#root` inert on 7/7 dialogs, 0 trap escapes over 60 presses each and the dark palette intact, the entity page renders its registry error above a loaded feed with no composer offered and lands a double-fail on the ringed region, the refreshed body wins the cache in both race orders, only the tapped pill says pending, the lesson dialog is described by one punctuated sentence, the skip-link ring sits in the shell gutter and touches 0 content rows on 24 cells, the 404 lives inside the shell with the nav and a skip link that reaches `#main`, the feed says "1 experience" and stays silent on error, every date in the app renders in en-GB with zero non-English tokens, "Save login" reads "Continue without saving" when unticked, both delete flows name the shared lesson rows and the claim is true at the row level, all four storage caveats say the key shares the store, the compact landing keeps the day heading and note on screen with block 1 at y≥94 whenever the first lesson is the target, a step to a never-seen day lands once, the backend comment states the origin/edge truth, and the trust architecture, tokens, type, spacing, weight, service worker and boot theme re-verify with zero drift — but the wave reproduced its own pattern once more: the entity arming fix was copied to Mine without its flag, so "Try again" on Mine drops focus to `<body>` exactly as the entity page did in r8; Explore's directory-only retry never arms because a cache hit keeps `entities.loading` false; a Timetable retry that fails again has no region to land on; the report dialog unmounts its opener before the modal mounts and returns focus to `<body>` on all four exits; the `?date=` fix compares against a `searchParams` value that `replaceState` never updates, so "Back to today" from a stepped-into page leaves the previous day in the address bar and a reload of that link shows the wrong day, while plain `/timetable` now pins a frozen `?date=`; the heading-in-view fix holds only while the target block is the first of the day — once the first lesson ends, or on any sparse day, the landing clamps to maximum scroll and the student arrives with no day name, no note and no stepper, block 1 at y=−10 at 390×620; the same programmatic scroll moves the keyboard's starting point so the first Tab at compact heights lands on a lesson block past the skip link and the daynav; `pointer-events:none` on the pending pill lets a second tap fall through to the tabpanel and park focus there; the reconnect dialog's "Reconnect" saves or deletes the stored login by the tick with no change of label; "Delete account" leaves the encrypted school login and its key on the device unmentioned; the 404 on a nested path marks a nav tab current; `/home` shifts its own footer at ~900 ms (CLS 0.095); and the next-lesson wash restarts a 1 s `width` transition every second for the length of every lesson, which the audit's own `getAnimations()` probe now catches for the first time — so HOney rises from 15 to 18/30: #3 recovers to 2, #5 and #10 recover to 2, #1, #2, #6, #7 hold, #4 and #8 hold at 1 on the reproduced pattern and the clock-dependent landing, #9 drops to 2 on a measurement rather than a change, and the audit reports the number it measured.

Why redesign and not refine: total 18/30 is below the REFINE threshold of 20; two ties (#8 1↔2, #9 2↔3) were settled downward and the audit rule is mechanical. Granting both upward gives 20 — the verdict turns on the ties this round, which is disclosed, not hidden. The moves are therefore sized as contract-finishing work, not structural change.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 320×600, 360×620, 390×620, 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decisions, not up for re-litigation: (1) timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), the disclosure is accurate (exactly the two `syncTimetable` callers that exist, both data types, both triggers), and the plan must keep it so; (2) the 620 px timetable canvas density on normal-height screens stays; the compact-height notches (540 px at ≤620 px height, 450 px at ≤360 px width), the static daynav and the once-per-date landing at ≤620 px heights are the deliberate degradation — fix how the degradation behaves (the day name in view in BOTH clock states, a landing that can be honoured when the target is the last block, the keyboard's starting point), not whether it exists. Verify everything in a real browser against the deployed app by computed style, geometry and `document.activeElement`, never by reading the diff. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop, reducedMotion})` returning `{browser, context, page}` and renews the access token from the refresh token; probe scripts `r9-{struct,vis,copy,wf,a11y}-*.js` and `r9-orch-check.js` with `.log` files and `r9-*.png` screenshots; `matrix.js` = 9-device regression) can be re-run to confirm each fix — `r9-a11y-landing.js` / `r9-a11y-loose*.js` reproduce the three `<body>` landings and the report-dialog focus drop, `r9-struct-date2.js` and `r9-wf-ttstep.js` the `?date=` mismatch, `r9-a11y-loose3.js` / `loose4.js` the clamped landing. IMPORTANT: the harness runs in UTC, where this account's lessons fall outside the 09:00–20:00 timeline window and the Timetable always looks empty — build contexts by hand with `timezoneId: "Asia/Shanghai"` whenever a measurement involves lesson blocks; in-page "today" during r9 was 2026-09-02 (3 lessons; the landing target changes from lesson 1 to lesson 2 once P1 ends at ~10:30 CST — measure both states), 2026-08-24 is a 3-lesson control day, 2026-08-25 a 1-late-lesson sparse day, 2026-08-22 and 2026-09-06 are empty. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying; a reaction tap is allowed only if undone in the same script or intercepted. The test account has 0 ownership keys and a connected, valid portal token — seed `HOney.experiences.keys` / `honey.portal.cred` sentinels in localStorage to reach Mine's populated/error branches and the Save-login UI; rewrite the `/api/me` body to `portalTokenValid:false` to reach the Reconnect branch; the real feed holds one post, so fulfil `/api/experiences/feed` with `{items, nextCursor, headCursor}` to reach load-more and the ≥9-post state; abort `/api/entities` and, separately, `/api/directory` and `/api/history` to reach the registry-, directory- and lesson-error branches on Compose, Entity, Explore and Mine; delay `/api/experiences/*/react` 2 s to observe the pending reaction.

Preserve from current design (MUST NOT be touched — these verified live on eede644):
- Spacing system as it renders: 214 spacing declarations, 176 on `var(--sp-*)`; rendered rhythm 97.84 % @320, 97.61 % @390/430, 92.63 % @1280 on the 4/8/12/16/20/24/32/44 ladder; `p.caption.timetable-note { margin-block: var(--sp-3) var(--sp-2) }` computing 12/8 px with 8 px note→canvas and 0 note×hour overlaps. Regression check: re-run `r8-vis-typespace.js` at 320/390/430/1280 (logs `r9-vis-typespace-*.log`); on-ladder ≥97 % mobile / ≥92 % desktop; note margin 12/8.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, one fractional (27.95 at 430); 10 off-ramp declarations (11 with admin); `h1.page-title` 28px/650 at 390 and 36px at 1280 on every page incl. the 404. Regression check: rendered-font-size census, nothing below 12 px, no new off-ramp declaration.
- Palette and accent discipline: 43 colour literals, `tokens.css` blob 1d70d963a91a5bfbeb17928b599185286ad8f836 byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; contrast floor 4.66 stone / 5.83 night on `.home-head__stale`, 0 enabled text below 4.5:1 on either boot (26 surfaces); focus rings 5.81–6.16 stone / 8.35–9.28 night against adjacent ground; disabled text 4.61–5.66 stone; placeholders 5.34–5.63; the portaled dialog on a genuine dark boot `rgb(29,33,37)` / `rgb(233,237,239)`. Regression check: rerun `r9-a11y-contrast.js` / `r9-vis-contrast.js` on stone and on a fresh `colorScheme:dark` context with no stored theme.
- Focus ring as the ONLY rest/focus distinction: `button:focus-visible, a:focus-visible, input:focus-visible, textarea:focus-visible, select:focus-visible, summary:focus-visible { outline: 3px solid var(--accent); outline-offset: 3px }` (foundations.css:66-74) with `button, a { -webkit-tap-highlight-color: transparent }` as its OWN rule above it (foundations.css:63-66); 0 of 117 controls ringed at rest on 10 routes × 3 widths × 2 boots; rest ≠ focused on 202/202 Tab stops; edge-band accent pixels 0/0/0/0; ordinary taps ring 0/10 buttons. Regression check: `r9-orch-check.js` (0 idle-with-outline on Feed/Timetable/Settings), `r9-vis-outline.js`, `r9-a11y-ring.js` after ANY stylesheet edit.
- Mobile sheet grammar: `@media (max-width:640px) { .modal { max-width:none; max-height:92dvh; border-radius:20px 20px 0 0; border-left/right/bottom:none; padding: var(--sp-5) var(--sp-4) calc(var(--sp-6) + env(safe-area-inset-bottom)); animation: sheet-up .42s var(--ease) both } }` with the compact `max-height:100dvh` as a SIBLING `@media (max-width:640px) and (max-height:620px)` (components.css:813-829); 42/42 opens are sheets; `sheet-up` consumers exactly `.modal` and `.nudge`; desktop keeps the 20 px card with `rise-in`. Regression check: `r9-vis-dialogs.js` on all 7 dialogs at 320×568 / 360×620 / 390×844 on both boots; `r9-orch-check.js` sheet lines.
- Served-CSS parse gate: `apps/web/scripts/check-css.mjs` wired as `"build": "tsc -p tsconfig.json --noEmit && vite build && node scripts/check-css.mjs dist/assets"`; 0 orphan declarations, 0 in-context duplicates, 0 bare-element+state lists in the served CSS. Keep the gate; widen it (see Discard). Regression check: `node scripts/check-css.mjs dist/assets` exit 0 and the same against a curl of the served CSS.
- Modal semantics (Modal.tsx): `createPortal(…, document.body)`; `#root` gets `inert` on open and loses it on close (7/7, both viewports; 2 mutations per life; no user path re-runs the `[onClose]` effect); `role=dialog`, `aria-modal`, name from the h2, `aria-describedby` resolving 7/7 (`lesson-dialog-body` = the `p.sr-only` sentence "09:00 to 10:30, with ChenJenny, in room 213." TimetablePage.tsx:481-490; `report-dialog-body` only on the form branch ExperiencePost.tsx:324; `school-dialog-body`; `appearance-dialog-body`; `confirm-dialog-body`); trap 0 escapes over 30 Tab + 30 Shift+Tab per dialog; nav beneath hit-tests to the overlay 7/7; overlay = viewport, `position:fixed`, `z-index:50`; opener restored 6/7 (fix the 7th, see Discard); CLS 0 and no long task on open; every dialog button ≥44. Regression check: `r9-a11y-dialogs.js`, `r9-a11y-dialogs2.js`, `r9-struct-modal.js`, `r9-wf-portal.js`.
- Pending reaction per pill: `pendingValue` state (ExperiencePost.tsx:96-115) driving `aria-disabled` on the tapped pill only (`:229,241`); `.react-btn[aria-disabled="true"] { opacity:.7; cursor:default }` (components.css:249-256) — keep the state and the dim; remove `pointer-events:none` and give the second activation a response (see Discard); one POST per tap on 4/4 boot×input cells; focus kept through the POST when nothing else is tapped.
- `useApi` (lib/useApi.ts): `if (cancelled) return;` ABOVE `cache.set` (`:72`) so a superseded request never writes the cache — verified in both race orders; the in-flight map with `.finally` eviction; `invalidate()`/`clear()`/`reload()` deleting keys; `useNames(enabled)` returning `error`/`loading`/`reload` (shared.tsx:160-186) and consumed by 3/3 callers for `error` — consume `loading` too where a retry arms on it (see Discard).
- Entity page (EntityPage.tsx): `useRetryFocus(feed.loading || namesLoading)` (`:48`) — the arming contract every sibling must copy; `registryUnknown = !entities` suppressing the Share CTA, the intro and the "no longer listed" claim while the registry is loading or failed (`:55,96,105,127`); the names error rendered ABOVE the feed (`:134-139`); the h1 carrying the directory name ("ChenJenny") through a registry failure and `KIND_TITLE` when both fail; double-fail lands on the named region with a 28 % ring and 0 px scroll.
- Compose: the error region named "Couldn’t load" (ComposePage.tsx:216-219) landing on double-fail; exactly one region named "Editor" (`:306`); the registry-pending skeleton on the `?entityKey=` branch; `useNames(!!effectiveEntityKey)` so plain /compose issues 1 data request; `.compose-actions` two 44 px rows at ≤620 heights; the `.nudge` safety sheet with `sheet-up`.
- Timetable: `?date=` written by `history.replaceState` on every change of `date` (TimetablePage.tsx:43-47) — keep the write, fix the comparison (see Discard); the real-calendar guard replacing an impossible `?date=`; `history.length` unchanged by stepping and one Back leaving `/timetable`; `formatStepperDate` and `formatDayHeading` both en-GB; the P1–P6 clause only with lessons and the note string joined without a leading space (`:178-186`); the landing gate `innerHeight > 620` returning early at normal heights (`:337-339`, 0 px scroll at 390×844 / 430×932, block 1 89 px below the daynav); the once-per-date landing on a never-seen date (`loadingSeenFor` / `trusted` / `settledDates`, `:56-71`, one instant `scrollIntoView` inside a rAF) — extend to cached dates or document the exception (see Discard); retry 0 px on every landing; `.lesson-block { scroll-margin-top: … }` keeping the heading + note above block 1 when it can be honoured (h1 y=3–24, block 1 y=94–98 at 320×568 / 360×620 / 390×620 with the first lesson as target) — keep the intent, derive the value and pad the canvas (see Discard); `.timeline { min-height: 620px }` with the 540 / 450 notches and `.daynav { position: static }` at ≤620 heights (owner's decisions); 0 overlaps on 10/10 compact rows; `.lesson-block--compact { min-height: 36px }`; every lesson block hit-tests to itself and clears the fixed nav; `.timeline__empty` `<p role="status">` "No lessons today" / "No lessons on Sun 6 Sept".
- Feed: `p.sr-only[role=status]` (FeedPage.tsx:159-167) reading "1 experience" / "N experiences" / "Nothing has been shared yet" / "" on loading and error — keep the mechanism and the silent error, fix the empty string per tab and load-more (see Discard); `#feed-panel` tabpanel renamed via `aria-labelledby` on tab change; 0 `[aria-live]`; the `role=alert` banner speaking once on error; header Share suppressed on the true-empty feed; `SHARE_PROMPT_EVERY = 8`.
- 404 inside the shell: `<Route path="*">` as a sibling of the AppLayout routes (App.tsx:62-63), `NotFoundPage` = `div.stack > h1.page-title + p.muted + div.card-actions > a.btn.btn--primary` (NotFoundPage.tsx), title "Page not found · HOney" = h1, the shell's skip link → `#main`, landmarks `{navigation:2, main:1}`, 1 API request, no Dash chunk — keep; fix `aria-current` on nested paths (see Discard).
- Skip-link ring: `.main:focus { outline:none }` + `.main:focus > .view { outline: 3px solid var(--accent); outline-offset: 3px }` (features.css:1320-1328) painting in the 10 px shell gutter with 0 content-row overlaps on 24 cells and `#main` reached 12/12 at 390 — keep the placement; change the radius and pad the Timetable bottom (see Discard).
- Reconnect/Save dialog: `submitLabel={purpose === "save" ? (stayConnected ? "Save login" : "Continue without saving") : "Reconnect"}` (ReconnectDialog.tsx:40) — keep the save-purpose binding; bind the reconnect purpose too (see Discard); the dialog checkbox named "Stay connected on this device." (30 chars) with the caveat as the dialog's description; the bodies naming the key ("with the key that unlocks it").
- Login: checkbox `aria-label="Stay connected on this device"` (29 chars) + `aria-describedby="stay-connected-note"` (LoginPage.tsx:53-62); unchecked by default; `label.stay-connected { min-height: 44px }` as the hit area; skip link "Skip to sign-in"; footnote naming both import triggers and both data types (`:70-71`).
- Settings copy: delete-data row and body "…Shared teacher, course, room and lesson entries stay." (SettingsPage.tsx:211,288); delete-account row "…your imported lessons (shared teacher, course, room and lesson entries stay). Published experiences stay — …" (`:96-98`) and confirm body (`:309`) — keep, add what the body still omits (see Discard); the "Stay connected" row caveat "the key that unlocks it is here too" (`:171`); the notes bullet (`:255-259`); the import row (`:200-204`) verbatim; the theme picker `[aria-pressed="true"]` recipe; `.btn:disabled { opacity:1; color: var(--muted); border-color: var(--line) }` — keep the text recipe, unify the fill (see Discard).
- `···` menu: `aria-haspopup="menu"`, `aria-controls`, `aria-expanded`, `role=menu`, Enter → the menuitem, Arrow keys, Escape → trigger, Tab-out close — keep the keyboard contract if the menu stays; prefer a direct "Report" (see Discard).
- Locale: every `toLocaleDateString` call `"en-GB"` (format.ts:35,44,52,61,74,83; TimetablePage.tsx:26; HomePage.tsx:43,47); identical output under en-GB / en-US / zh-CN with zero non-English tokens. Lock `format.ts:27` too (see Discard).
- Backend static headers at the origin (packages/backend/src/app.ts:100-108): `sw.js` `no-cache`, `/assets/*` immutable, everything else `must-revalidate`; the comment now states the edge rewrite. Keep.
- Zero idle motion outside a lesson in progress: `document.getAnimations().length === 0` at 2.5 s on 11 screens × 2 preferences between lessons; 6 `@keyframes` (`fade-in, ptr-rot, rise-in, settle, sheet-up, skeleton-shimmer`), the 2 infinite ones loading-only; one `.view {` block with `settle .46s`; reduced motion → 1e-06 s on 25/25, `scroll-behavior:auto`, the landing one discrete step; CLS 0 on cold /timetable and /experiences at three viewports and on dialog open. Regression check: `r9-wf-anim.js` sampled DURING a lesson (Asia/Shanghai, 09:00–10:30 or 13:30–15:00 CST) and between; `r9-wf-cls.js` / `r9-wf-cls2.js`.
- Weight: app JS 86,624 B br / 89,051 zstd / 276,692 raw; CSS 9,312 br / 40,932 raw; Dash chunk 4,405 br loaded on 0/15 student loads incl. the 404; cold /home 12 requests (3 data), warm 3 requests / 1,808 B, plain compose 8 / 1 data, /nonsense 10 / 1 data; 5 runtime dependencies; 0 duplicate API paths on 15/15. Regression check: curl br sizes under 100 KB; `r9-wf-net.js`; `r9-wf-404.js`.
- Service worker as it is (public/sw.js, 4,828 B, md5 f7513f02194664efa4e96d50773ec83d, cache `honey-v3`): 9/9 current kept, 5/5 stale evicted, `honey-v2` deleted, 0 B refetched after `stopWorker` ×3, JS-only and CSS-only mismatches evict, exact match no work, 5 entries / 348,629 B after 10 hard loads, offline shell 79–117 ms, `/api/` never cached, HTML-under-`/assets/` refused. Regression check: `r9-wf-sw1.js` (mode real), `r9-wf-sw3.js`, `r9-wf-sw4.js`, `r9-wf-sw5.js` after any sw.js change (update the signature literal per build).
- Boot script honouring prefers-color-scheme pre-FCP in 6/6 contexts, 0 later `<html>` mutations, `theme-color` rewritten (index.html:14-33).
- Touch floor: 257/259 ≥44 in a `hasTouch` context at 390×844 and 320×568 across 15 routes incl. the 404 and all 7 dialogs; the 2 sub-44 are the `aria-hidden` `input.daynav__picker` (5×4, out of the Tab order). Regression check: `r9-a11y-targets`-style census, sub-44 list unchanged.
- Clean build: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters -p tsconfig.json` = 0; 0 whole-head duplicate selectors; consumer-less classes 0 adjusted; 0 unterminated comments.
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:227-229,349-353), no author column (packages/backend/src/db/database.ts:90-92, mig. 003), `ownership_hash` sha256, 6 = 6 = 6 report categories, "no free text is collected" ↔ `free_text_not_accepted` (routes/experiences.ts:208), coarse `publishedDay` (lib/format.ts:73-79), the HOney ID line, WhyPage "design boundary, not magic", the delete-flow row-level truth (`services/accounts.ts:191-193,238-240`), the caveats ↔ `lib/portalCredentials.ts:20-21,87,115-124`, the import disclosure ↔ `routes/auth.ts:32`, `routes/data.ts:17`.

Discard (structural patterns causing the failures):
- A retry armed on a flag its button does not toggle, copied from the fix it was modelled on — for the second round running: `MinePage.tsx:59` `useRetryFocus(mine.loading)` while `:152` calls `reloadNames()` (the r8 handoff said "consume `useNames().error/loading/reload` on Mine"; `loading` was not consumed) — "Try again" that fails or succeeds leaves `activeElement=BODY`, `data-landed` absent, with and without seeded keys; `ExplorePage.tsx:31` `useRetryFocus(entities.loading)` while `:86-88` reloads `entities` AND `directory`, and `entities.reload()` on a cached key re-enters `useApi.ts:53-56` with a hit so `loading` never becomes true — a directory-only failure's retry lands on `<body>` whether it fails or succeeds; `TimetablePage.tsx:207-215` renders the `.focus-landing` region only in the success branch, so a retry that fails again has `ref.current === null` and `armed` already cleared. 3 of 10 error surfaces lose the keyboard user (7 land: Home, History, Explore-entities, Feed, Entity-feed, Entity-registry, Compose). Caused failure on principles #8 and #2.
- An opener unmounted before its dialog mounts: `ExperiencePost.tsx:267-268` calls `setMenuOpen(false)` then `setReporting(true)`, so `Modal.tsx:22` captures the menuitem that is unmounting and `:50` `opener?.focus?.()` targets a detached node — the report dialog returns focus to `<body>` on Esc, ×, Cancel and overlay-click at 390 and 1280 (6/7 dialogs restore). Caused failure on principle #8.
- A pending guard implemented in CSS: `components.css:251` `.react-btn[aria-disabled="true"] { pointer-events: none }` (the r8 handoff's parenthetical) — a second tap on the pending pill falls through to `div[role=tabpanel][tabindex=-1].feed-stream`, which takes focus and keeps it after the POST settles (2/2); the second keyboard activation is still swallowed by `if (busy) return` (`ExperiencePost.tsx:163`) with no visible or announced response. Caused failure on principles #8 and #4.
- A URL comparison against a value the write never updates: `TimetablePage.tsx:43-47` writes `history.replaceState` on `[date]` but compares against `searchParams.get("date")`, which React Router never re-reads after `replaceState`, so `q` is frozen at the mount query and any return to the mount date (Prev after Next; "Back to today" from a stepped-into page) skips the write — `?date=2026-08-25` over "Monday, 24 August 2026", `?date=2026-09-04` over 2 September; a reload of that link shows the wrong day. The same effect rewrites bare `/timetable` to `?date=<today>` within 300 ms, so every copied link is date-pinned. Caused failure on principle #4.
- A heading-in-view fix that holds only in one clock state: `.lesson-block { scroll-margin-top: 100px }` (features.css:355, an off-ladder literal) keeps `h1.schedule-header` and the note above block 1 (h1 y=3–24 on 6/6 compact rows) only while the landing target is the first lesson of the day; once the first lesson ends (target = lesson 2, measured 10:35 CST) or on a sparse day (2026-08-25, one late lesson), the requested position exceeds the scroll owner's maximum and `scrollIntoView` clamps to max on 12/12 traced loads — h1 y=−62 / −10 / −82, note off 2/3, daynav −192 / −140 / −212, block 1 at y=−10 at 390×620 (r8's −6 again). The same programmatic scroll moves Chromium's sequential-focus starting point, so at 320×568 / 360×620 / 390×620 the first Tab lands on a lesson block (y=334/386/357) past the skip link and 6–7 daynav controls (5/5 runs; the skip link is first at 844/932 and on an empty day). A step onto a cached date never lands (a cache hit never sets `loading=true`, so `loadingSeenFor.current !== date` and `trusted` is false; scroll inherited from the previous day over four cached steps) although the comment at `:56-60` says "cold load, re-entry or a date step — lands once"; a pull-to-refresh on a landed day scrolls `122→0` and does not re-land. Caused failure on principles #4, #8 and #5.
- A nav state derived from the path prefix: on `/experiences/new` and `/timetable/oops` the 404 renders with `aria-current="page"` + `is-active` on two nav items (rail + mobile) under the heading "Page not found" (`/nonsense` marks none); signed out, any wrong address is the login page (`App.tsx:29,62-64`, `*` inside `RequireAuth`). Caused failure on principle #4.
- A submit label bound to the tick on one purpose only: `ReconnectDialog.tsx:40` reads "Reconnect" in both tick states while `:53-55` runs `stayConnected ? portalCredentials.authorize(creds) : portalCredentials.clear()` — ticking saves the login and unticking deletes a stored one under an unchanged label; "Continue without saving" (save purpose) performs `POST /api/auth/login` (a live portal sign-in) it does not name. Caused failure on principles #4 and #6.
- Device state the delete copy does not mention: `api.deleteAccount()` (`api/client.ts:304-307`) clears only `HOney.session`; `AuthContext.tsx:52-58` clears `portalCredentials` on sign-out but `SettingsPage.tsx:314-319` does not on delete — after "Delete account" the encrypted school login and its key stay in `localStorage`; the account confirm body (`:309`) still omits "Published experiences stay" that the row (`:96-98`) carries. Caused failure on principle #6.
- Two truths on the feed's default tab and on load-more: `FeedPage.tsx:162-163` announces "Nothing has been shared yet" while the "Your classes" tab (selected on load) shows "Nothing from your classes yet. When someone shares an experience connected to a class you've taken, it will appear here. / Share the first one" (`:131-142`); load-more announces the cumulative total ("18 experiences", "24 experiences"), a tab change re-announces an unchanged count. Caused failure on principle #4.
- Registers still split: provenance `shared.tsx:15-19` {"Verified lesson experience", "From someone who has taken this over time", "Verified school member"} vs `ExperiencePost.tsx:18-22` {"from a class you've taken", "from someone who has taken this over time", "from a student here"} — one row of three unified, `shared.tsx` now mixing registers internally; five sentence shapes for the one storage-caveat fact (`LoginPage.tsx:60-62`, `SettingsPage.tsx:171,255-259`, `ReconnectDialog.tsx:31,32`); "Couldn’t load" region over a "Could not reach…" alert (`ComposePage.tsx:216-219`); "More options" trigger opening a "Post options" menu holding one item (`ExperiencePost.tsx:252,261`); Home "Wednesday 2 September" (`HomePage.tsx:47`) beside Timetable "Wednesday, 2 September 2026" (`format.ts:35`) — one locale, two option sets; `format.ts:27` `formatTime` the one `toLocale*` call not locked to en-GB. Caused failure on principle #4.
- Rendered inconsistencies that bar #3's anchor 3: two disabled fills on one 44 px pair (`components.css:66-72,85-90` — `.btn--primary:disabled` `color(srgb .891 .901 .907)` vs `.btn--ghost:disabled` transparent + `1px solid rgba(35,43,49,.14)`); `.react-btn:disabled` with no rule; the skip ring's `border-radius: 12px` (features.css:1320-1328) matching no other rendered radius (cards 17, blocks 10, posts 0, buttons 99) and crossing the Timetable's "20:00" hour label, which overflows `.view` by 6 px into the ring band at 390 and 320; the Reconnect sheet scrolling 1 px at 320×568; the 16 px empty-day gap under the note (`.timeline__empty { margin: var(--sp-2) 0 }` stacked on the note's 8 px); two adjacent identical `@media (max-height:620px)` blocks (`features.css:759,769`); `-webkit-tap-highlight-color` suppressed on `button, a` but not on `input, textarea` (`rgba(51,181,229,.4)`); dangling trailing comments 10 (tokens.css:58, components.css:17-22,252,527,669,828,877, features.css:1-5,80,1201). Caused failure on principles #3 and #8.
- Missing states on one compose branch: Compose with `?lessonId=` renders neither a skeleton while `/api/history` is delayed 5 s nor a banner or empty state when it is aborted (single-pass measurement with the endpoint named); Explore renders its error banner and four "Nothing here yet." sections together under a registry failure. Caused failure on principles #8 and #10.
- Two idle costs on the home screen: `features.css:80` `.nextlesson__wash { transition: width 1s }` restarted every second by `useNowTick(1000)` (`HomePage.tsx:21`) while a lesson is in progress — `document.getAnimations()` reports a running animation at 2.5 s (1/22 cells; absent between lessons); `DIV.home-foot` / `SECTION.home-voices` resolving their height at ~840–975 ms — CLS 0.09508 once contention-free at 320×568, 1/5 at 360×620 and 320×568, 4/6 under 5-way contention (r8 0/18). Caused failure on principles #9 and #5.
- A gate that catches the instance, not the class: `scripts/check-css.mjs` flags a bare `/^[a-z]+$/` token beside a `/:focus|:hover|:active/` member and an orphan declaration — it catches both r8 shapes — but passes `.card,\na:focus-visible{…}` (the same dangling-selector bug with a class), `BUTTON,`, `input[type=text],`, `:where(button),`, a stray `}` (`.a{color:red}}` becomes selector `"} .b"`), an unterminated `{` at EOF, and the same selector duplicated across two identical `@media` blocks (each context gets a fresh `Map`). Caused failure on principle #7 (durability) and left #8's regression guard partial.
- Relay, anchor #9 met by weight: one `GET /api/timetable` per date step even for a cached date (14 GETs over 13 steps; the SWR cache suppresses the skeleton, never the request); the SWR `cache` Map +2.46 MB over 20 steps (~117 KB per date), unbounded; the cancelled-write guard discards a body whose view was abandoned mid-flight (7 skeletons + a second request on return); one pull on any screen runs `apiCache.clear()` for every screen; 123,968 B of never-requested font subsets and 1,201 B of admin CSS in the student payload; the SW's eviction walk fetching the Dash chunk students never open; the public edge rewriting `sw.js` to `max-age=14400` (out of repo).
- Latent, code-only: header Share + 1 invite at 12 posts and + 2 invites at 24 (`SHARE_PROMPT_EVERY = 8`; real feed 1 post); two classless `<div>` wrappers per post (`FeedPage.tsx:169-171`); an always-mounted empty `div.ptr__disc[role=status]`; the `[onClose]` effect in `Modal.tsx` re-running 3× on a script-dispatched `honey:refresh` (no user path found); two dialogs stackable only via script; the Appearance dialog's opener rail-only (unreachable at ≤640 px; the 2×2 grid lives on Settings); Enter on a History "Select" row landing the composer on `<body>`; no keyboard equivalent to pull-to-refresh or load-more; Mine's own feed error branch unreachable with 0 keys.

Top 5 moves from the audit (verbatim):
1. #8 thorough + #2 useful — Every retry lands, every dialog returns focus, and a pending control never loses the keyboard. Enumerate the 8 `useRetryFocus` consumers and make each arm on the loading flag its "Try again" actually toggles: Mine (`MinePage.tsx:59` arms on `mine.loading`; `:152` calls `reloadNames()`) → arm on `namesLoading || mine.loading`; Explore (`ExplorePage.tsx:31` arms on `entities.loading`; `:86-88` reloads both and a cache hit keeps `loading` false) → make `reload()` set `loading=true` even on a cached key, or arm on `entities.loading || directory.loading` with a reload that clears the key first; Timetable (`TimetablePage.tsx:207-215`, the `.focus-landing` region exists only in the success branch) → render the region around both branches so a retry that fails again has somewhere to land. Report dialog (`ExperiencePost.tsx:267-268`): open the dialog before closing the menu, or pass the `···` trigger as the opener, so `Modal.tsx:22,50` focuses a mounted node. Reaction pill (`components.css:251`, `ExperiencePost.tsx:163`): drop `pointer-events:none`; keep the handler guard and give the second activation a response (`aria-live` "Saving…" or a brief note) so focus stays on the pill. Verify by keyboard on all 10 error surfaces (fail-again AND succeed) and on 7/7 dialog exits that `activeElement` is the named region / the opener, never `<body>`. Evidence: [S-R9-8][S-R9-9][A-R9-20][S-R9-33][A-R9-17][A-R9-21][S-R9-12][S-R9-29].
2. #4 understandable + #8 thorough — Make the Timetable's URL, landing and day name agree at every moment, in every clock state. `TimetablePage.tsx:43-47`: compare against `window.location.search` (or use `setSearchParams(…, { replace: true })`) so a return to the mount date writes the URL too; decide whether bare `/timetable` pins `?date=today` (a copied "today" link shows yesterday tomorrow) and record the decision in the comment. Landing: the target block can be the last of the day — pad `.timeline__canvas` (or the scroll owner) by `scroll-margin-top + viewport − block height` at ≤620 heights so `scrollIntoView` can honour the 100 px margin instead of clamping, then measure `h1` in view on 6/6 compact rows in BOTH clock states (before and after the first lesson ends) and on a sparse day; make a cached-date step land or say it does not (`loadingSeenFor` never sees a cache hit); decide whether a pull re-lands or keeps the landing; and after the programmatic scroll reset the sequential-focus start point (focus `#main` with `preventScroll`, or the skip link's container) so Tab 1 is the skip link at 320×568 as at 390×844. Replace the `scroll-margin-top:100px` literal with a value derived from the heading + note box or a token. Evidence: [S-R9-13][S-R9-14][W-R9-5][C-R9-15][A-R9-19][W-R9-8][S-R9-18][S-R9-19][S-R9-20][A-R9-18][V-R9-17][V-R9-10].
3. #4 understandable + #6 honest + #10 as little design — One truth per channel, on the surfaces the r8 pass did not reach. 404 (`AppLayout` nav): derive `aria-current`/`is-active` from the matched route, not the path prefix, so `/timetable/oops` marks nothing current; decide what a signed-out wrong address shows and say so. Reconnect dialog (`ReconnectDialog.tsx:40,53-55`): the reconnect purpose's label must change with the tick as the save purpose's does ("Reconnect and save login" / "Reconnect only"), and "Continue without saving" should say it signs in ("Sign in without saving"). Delete account (`SettingsPage.tsx:314-319`, `AuthContext.tsx:52-58`): clear `portalCredentials` on delete as on sign-out, or add "your saved school login stays on this device" to the row and body; add "Published experiences stay" to the confirm body. Feed status (`FeedPage.tsx:159-167`): the empty string per tab ("Nothing from your classes yet" on the class tab), "N more" on load-more, no re-announcement on a tab change. Dates: one formatter for Home and the Timetable heading (`HomePage.tsx:43,47` vs `format.ts:35`), and lock `format.ts:27` `formatTime` to en-GB. Registers: one provenance map for all three rows (`shared.tsx:15-19` = `ExperiencePost.tsx:18-22`), one caveat sentence for the five storage caveats, one spelling of the load failure ("Couldn’t load" region vs "Could not reach" alert), and the `···` → a direct "Report" (drop "More options"/"Post options"). Explore under an error: hide the four "Nothing here yet." while the banner shows. Evidence: [S-R9-23][S-R9-24][C-R9-3][C-R9-4][C-R9-20][C-R9-1][C-R9-10][C-R9-11][S-R9-26][C-R9-14][C-R9-5][C-R9-2][C-R9-17][C-R9-7][S-R9-25][V-R9-14].
4. #3 aesthetic + #8 thorough — Finish the rendered details the census still counts. One disabled fill for `.btn--primary:disabled` and `.btn--ghost:disabled` (`components.css:66-72,85-90`) and a rule for `.react-btn:disabled`; the skip ring's `border-radius:12px` (`features.css:1320-1328`) → a radius the system already renders (or 0) and 6 px of bottom padding on `.view`/`.timeline` so the ring never crosses the "20:00" hour label; a loading skeleton and an error banner for Compose-with-`lessonId` when `/api/history` stalls or fails (`ComposePage.tsx` lesson branch); the Reconnect sheet's 1 px scroll at 320×568; the 16 px empty-day gap under the note (`.timeline__empty { margin: var(--sp-2) 0 }` doubled with the note's 8 px); `-webkit-tap-highlight-color` on `input, textarea` as on `button, a`; the two adjacent identical `@media (max-height:620px)` blocks (`features.css:759,769`) merged; dangling comments 10 → ≤6. Evidence: [V-R9-9][S-R9-37][V-R9-4][V-R9-14][V-R9-2][V-R9-6][A-R9-1][V-R9-16][S-R9-35].
5. #9 environmentally friendly + #5 unobtrusive (relay; keep the payload) — Take the two idle costs off the home screen and widen the gate to the class of bug. `features.css:80` `.nextlesson__wash { transition: width 1s }` restarted by `useNowTick(1000)` (`HomePage.tsx:21`): step the width once per tick without a transition, or transition only on the first paint, so `getAnimations()` is 0 during a lesson; reserve `home-foot` / `home-voices` height (`HomePage.tsx`) so `/home` stops shifting at ~900 ms (CLS 0.095). `scripts/check-css.mjs`: flag any selector-list member without a pseudo-class beside members with one (class, attribute, uppercase, `:where()`), a stray `}` and an unterminated `{`, and dedupe across identical at-rule contexts — the gate catches the instance that shipped, not the class. Relay, anchor met by weight: one GET per date step even for a cached day (`useApi` always revalidates — decide and count it), the SWR map cap (+2.46 MB / 20 steps), the discarded-body re-fetch on an abandoned view, 123,968 B unrequested font subsets and 1,201 B admin CSS in `dist`, the SW eviction walk fetching the Dash chunk, the edge's `sw.js` `max-age=14400` (out of repo). Evidence: [W-R9-18][W-R9-20][W-R9-13][W-R9-14][W-R9-7][W-R9-26][W-R9-2][W-R9-25][W-R9-23][W-R9-24].

Redesign principles in priority order:
1. #8 thorough — success: on all 10 error surfaces a "Try again" that fails again AND one that succeeds leaves `activeElement` on the named region with `data-landed` and a ring, by keyboard and by mouse, with 0 px scroll; 7/7 dialogs return focus to a mounted opener on Esc, ×, Cancel and overlay-click; a second activation of a pending pill keeps focus on the pill and produces a visible or announced response; Compose-with-`lessonId` has a skeleton and a banner; one disabled fill; `.react-btn:disabled` styled; the served CSS still parses clean and the widened gate rejects the class-spelled dangling selector, a stray `}` and an unterminated `{`; `tsc` 0; dangling comments ≤6.
2. #4 understandable — success: `location.search` equals the day shown after EVERY transition including a return to the mount date and "Back to today", and the bare-`/timetable` behaviour is a recorded decision; `h1.schedule-header` and the note are in view on 6/6 compact rows in both clock states and on a sparse day, block 1 never above y=0, and Tab 1 after the landing is the skip link; a cached-date step lands or the comment says it does not; the 404 marks no nav tab current on any path; "Reconnect" changes with the tick and "Continue without saving" names the sign-in; the feed status matches the visible sentence on the selected tab and announces "N more"; one date grammar from one formatter on Home and the Timetable; one provenance map, one caveat sentence, one spelling of the load failure, a direct "Report".
3. #6 honest — success: "Delete account" either clears the saved school login or says it stays; the account confirm body says published experiences stay; the import disclosure stays exactly as it is; zero drift on the nine privacy claims.
4. #3 aesthetic + #5 unobtrusive (keep at 2, aim at 3) — success: the rest-state outline census stays 0/117 on 10 routes × 3 widths × 2 boots; 42/42 sheets; the skip ring uses a system radius and crosses nothing; the spacing census stays ≥97 % mobile / ≥92 % desktop with 43 colours and the type set unchanged; `/home` CLS 0 on 5/5 at three viewports; the daynav reachable by one upward swipe.
5. #9 environmentally friendly (return to 3) — success: `getAnimations()` 0 at 2.5 s on 22/22 cells sampled DURING a lesson in Asia/Shanghai; bundle under 100 KB wire (br and zstd); dark boot pre-FCP 6/6; reduced motion 25/25; the SW battery green; the SWR map capped.
6. #2 useful — success: the three primary tasks keep their step counts (1/0, 1 tap, 4 screens / 3 taps); a refresh fetches AND sticks; a keyboard user never lands on `<body>` after any control in the app; History "Select" lands the composer on a control.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — one `useRetryFocus` arming contract enumerated across all 8 consumers (flag = the union of every `loading` the button's handler reloads, with a landing region present in every branch); one opener contract for `Modal` (the opener must outlive the menu that opened it); one URL contract for the Timetable (compare against `location.search`; bare-route behaviour decided); one landing contract (padding so the request can be honoured; cached-date and pull behaviour decided; focus start point reset); one nav-current contract (matched route, not path prefix); everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): the Timetable at 320×568 at 10:35 CST on a 3-lesson day from cold load to tapping the 13:30 block with the day heading visible on arrival and Tab 1 on the skip link; `/timetable?date=2026-08-24` → Next → Previous → "Back to today" with the URL right at every step; Mine with the registry down → "Try again" fails → focus on the ringed region; Feed → `···` → Report → Esc → focus on `···`; Feed → tap 👍 → tap 👍 again → focus still on 👍 with a response; Settings → Reconnect dialog with the tick on and off and a label that says which; Settings → Delete account → the row says what stays on the device.
- States checklist (empty, loading, error, success, focus, disabled/pending, selected) per screen, with the focus column split into keyboard / pointer-retry / pointer-plain-tap / retry-that-fails-again / retry-that-succeeds / dialog-exit / rest, the compact-height column split into first-lesson-target / later-lesson-target / sparse-day (scroll samples, overlaps, `h1` in view, block 1 ≥ y=0, Tab 1 = skip link, daynav reachable) at 320×568 / 360×620 / 390×620, the announcement column for every `role=status` (text per tab, load-more, error), the sheet column for every dialog at 320/360/390 on both boots (radius, borders, padding, animation, scroll), and the idle column sampled during AND between lessons.
- Migration path for users currently on the old design: none for routes or data; ship a `sw.js` byte change only if the SW is edited, and re-run the seeded-eviction battery with real bodies (`r9-wf-sw1.js` mode real) and the cold-start probe (`r9-wf-sw3.js`) before calling it done.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (0/10 error surfaces landing on `<body>` in either outcome; 7/7 dialogs restoring the opener; the pending pill keeping focus; `?date=` right after every transition; `h1` in view on 6/6 compact rows in both clock states; Tab 1 = skip link at 320×568; no nav tab current on a 404; one label per tick state; delete copy naming the device state; feed status per tab; `getAnimations()` 0 during a lesson; `/home` CLS 0; 0 buttons ringed at rest; 42/42 sheets; served CSS clean under the widened gate; `tsc` 0; contrast on a genuine dark boot; SW battery green).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the wrong-flag arming, the CSS pending guard, the frozen `searchParams` comparison, the clamped landing and the path-prefix nav state must be replaced by their contracts, not patched on the surface that was measured.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the rest-state outline census, the mobile-sheet census on all seven dialogs on both boots, the touch-target census in a `hasTouch` context, the retry-landing sweep on all 10 error surfaces in BOTH outcomes, the dialog-exit sweep on 7/7, the compact-height sweep in BOTH clock states, the feed status probe per tab, the `getAnimations()` probe during a lesson, and the SW battery, after every CSS, focus, data-layer or SW change.
- Copying a fix to a sibling without copying its contract — this is the third consecutive round in which a sibling inherited the defect its model was written to cure (r7 → r8 entity, r8 → r9 Mine). Before adding or editing any `useRetryFocus`, `Modal` opener, `replaceState` or landing, enumerate every consumer of the same hook or pattern and state the contract each one must satisfy, then measure each one.
- Fixing an invariant in one state — a heading that is in view at 10:19 and off-screen at 10:35, a URL that is right going forward and wrong coming back, a focus return that works for six openers and not the seventh: test every state transition and every clock state, not the first paint and not the state the fixer happened to be in.
- Implementing a guard in CSS — `pointer-events:none` removed the second tap by removing the element from hit-testing, and the tap landed on whatever was underneath; keyboard and pointer guards belong in the handler, with a response.
- Measuring the Timetable in UTC or outside a lesson — the harness's default clock hides every lesson block and the wash animation; any measurement involving lessons must use `timezoneId:"Asia/Shanghai"`, and the idle census must be sampled during a lesson.
- Widening a gate by adding the missed case — the gate must reject the class (any list member without a pseudo beside members with one; unbalanced braces), not the next spelling.
- Claiming a change in the commit message that the deployed app does not show — verify each move on the deployed app in every state before reporting it done.
````
