# 01 — Evidence (r9, consolidated from five Opus evidence subagents plus orchestrator-verified findings; scoring withheld to 02)

Anchor IDs: `[S-R9-n]` Structural, `[V-R9-n]` Visual, `[C-R9-n]` Copy & Honesty, `[W-R9-n]` Weight & Friction, `[A-R9-n]` Accessibility, `[O-R9-n]` orchestrator (probe `r9-orch-check.js` / `.log`, screenshots `r9-orch-*.png`). All measurements on the deployed `eede644` (`index-BDmmlJxi.js` / `index-BFZUVSQz.css`), 2026-09-02, harness `/root/claude-work/design-audit/`; lesson-block measurements in `Asia/Shanghai`. Every finding cites a file:line, a probe log, a screenshot or a measured value; nothing was scored by a subagent. Probe inventory: `r9-struct-{census,branches,useapi,react,feed,modal,date,date2,404,landings,landings2,landings3,touch,css,disabled,extra,reqs}.js`; `r9-vis-{served,outline,edgeband,dialogs,ring,ring2,compact,scroll,extra,states,states2,contrast,contrast2}.js` (+ r8-vis-{css,css2,typespace,idle}.js re-run); `r9-copy-{census,main,dialogs,dialogs2,locale,errors,loadmore,nag,login,neverlisted}.js`; `r9-wf-{swr,inflight,net,perf,anim,interrupt,boot,cls,modal,tt,cachectl,ptrpoint,ring,sw1,sw3,sw4,sw5,cost2,portal,ttstep,back,404,tti,cls2,wash,idle,recon}.js`; `r9-a11y-{ring,ring2,dialogs,dialogs2,skip,skip2,landing,landing2,react,react2,feedstatus,compact,semantics,contrast,disabled,motion,reach,loose,loose2,loose3,loose4,undo}.js`. No write path exercised; every reaction undone or intercepted; every browser closed.

## 0. Verification of the r8 handoff (moves 1–4) — live

**Move 1 (repair the two CSS breakages, gate the build):** CONFIRMED by all five passes and the orchestrator. [O-R9-1] 0 buttons ringed at rest on Feed (5), Timetable (5), Settings (12) — r8 5/5, 7–8/8, 12/12. [V-R9-1] 0/117 controls ringed on 10 routes × 3 widths × 2 boots (60 loads); rest ≠ focused on 134/134 Tab stops; edge-band accent pixels 0/0/0/0 (r8 21,876 / 37,094). [A-R9-1] 0 equal rest/focused pairs over 202 Tab stops (r8 24/111). [O-R9-2][V-R9-2][A-R9-2][W-R9-4] 42/42 mobile dialog opens compute `border-radius 20px 20px 0 0`, top border only, safe-area padding, `sheet-up`, flush bottom; Reconnect at 320×568 scrolls 1 px (r8 6). [S-R9-34][V-R9-3][W-R9-12] served CSS 0 orphan declarations / 0 dead bytes (r8 209), `check-css.mjs` wired into `npm run build` and green on dist and on the served file; [W-R9-13] it catches both r8 shapes; [W-R9-14] it misses the same bug spelled with a class, an attribute or uppercase, a stray `}` and an unterminated `{`.

**Move 2 (finish the siblings by the same contract):** Entity — CONFIRMED [S-R9-5][S-R9-7][A-R9-10]: error above a loaded feed, no Share CTA / intro while `entities===null`, double-fail lands on the named region with a ring. Compose region "Couldn’t load" — CONFIRMED [S-R9-10][A-R9-11]. `useApi` cache guard — CONFIRMED [S-R9-11][W-R9-1] in both race orders; cost [W-R9-2]. Per-pill pending — CONFIRMED [S-R9-12][V-R9-9][A-R9-6]; new residue [A-R9-21]. `inert` — CONFIRMED 7/7 [A-R9-3][S-R9-31][V-R9-2]; report `describedBy` — fixed in source [A-R9-5]; lesson description — one sentence [A-R9-4][C-R9-16][V-R9-8]; `#main` band — 0/12 overlaps [A-R9-9] (Visual's one hour-label crossing [V-R9-4]). Mine / Explore — banners CONFIRMED, landings REFUTED: Mine arms on `mine.loading` while reloading names [S-R9-8][A-R9-20]; Explore's directory-only retry never lands [S-R9-9][A-R9-20] — the r8 entity pattern reproduced on two new surfaces. Report dialog now drops focus to `<body>` on close [S-R9-33][A-R9-17].

**Move 3 (Timetable URL, landing, day name):** `?date=` written on every change — PARTIAL [S-R9-13][S-R9-14][W-R9-5]: forward steps correct, but any return to the mount date (incl. "Back to today" from a stepped-into page) keeps the previous `?date=` because `searchParams` is frozen by `replaceState`; plain `/timetable` now pins `?date=today` [C-R9-15]. Landing contract — PARTIAL: a never-seen date lands once [S-R9-18][V-R9-5][W-R9-8]; a cached date never lands [S-R9-19]; PTR discards the landing [S-R9-20]. Heading in view — PARTIAL: 6/6 compact rows when the target block is the first of the day [V-R9-5][S-R9-15][W-R9-9]; clamped to max scroll with h1/note/daynav off-screen and block 1 at y=−10 (390×620) whenever the target is a later lesson [A-R9-19][W-R9-8][S-R9-18]. Daynav still parked 106–127 px above the viewport [V-R9-5]. New: first Tab at compact heights lands on a lesson block [A-R9-18][V-R9-17].

**Move 4 (one truth per channel):** feed status — CONFIRMED [S-R9-26][C-R9-9][A-R9-7] ("1 experience", "Nothing has been shared yet", silent on error); residue: cumulative total on load-more [C-R9-11], status ≠ visible sentence on the default tab [C-R9-10]. 404 in the shell — CONFIRMED [S-R9-22][W-R9-10][A-R9-8][C-R9-12]; new: nested paths mark a nav tab current [S-R9-23]; signed-out wrong address is a login page [S-R9-24]. "Save login" — CONFIRMED on the save purpose [C-R9-3][A-R9-12]; the reconnect purpose keeps "Reconnect" while saving or clearing [C-R9-3]. Locale — CONFIRMED [C-R9-14] (0 non-English tokens; `formatTime` the one unlocked call; Home vs Timetable two option sets). Delete copy — CONFIRMED true at row level [C-R9-1]; new omission: saved school login survives account deletion [C-R9-20]. Storage caveats — CONFIRMED 5/5 [C-R9-2]. Provenance — 1 of 3 rows unified [C-R9-5]. Checkbox name — CONFIRMED [C-R9-6][A-R9-12]. Menu name — CONFIRMED [C-R9-7][A-R9-13]. Note leading space — CONFIRMED [C-R9-13][V-R9-6]. Backend comment — CONFIRMED [C-R9-8][W-R9-11]. Privacy claims — zero drift [C-R9-19].

## 1. Structural [S]

- **[S-R9-1]** Interactive census 390×844 (visible/total), Δ r8: Home 9/15 (=), Timetable today 13/20 (=), Aug 24 14/21 (=), empty 11/18 (=), History 8/14 (=), History select 32/38 (=), Settings 18/25 (=), Feed 15/21 (=), Explore 26/32 (=), Mine empty 6/12 (=), Mine seeded 7/13 (=), Compose plain 7/13 (=), Compose entityKey 10/16 (=), Entity 6/12 (=), Why 6/12 (=), Login 5/5 (=), 404 `/nonsense` 6/12 (r8 2/2: skip link, Go home, 4 nav). `activeElement=body` on arrival 17/17; `aria-live` 0.
- **[S-R9-2]** DOM depth from `main#main`: first lesson block 6, first post 6, editor textarea 5; deepest node under a post 10 (r8 9/10). Max document depth feed 15 / timetable 14 / 404 9.
- **[S-R9-3]** 16 repeated families; "Try again" 10 (r8 9; new `MinePage.tsx:153`); `useRetryFocus` 8 call sites; `.focus-landing` 9; `<Skeleton>` 11 (r8 9); `role="alert"` 16 (r8 13); `<Modal>` 5; `h1.page-title` 16 (r8 14); compose entry points 13 (r8 11).
- **[S-R9-4]** `tsc --noEmit --noUnusedLocals --noUnusedParameters` → 0.
- **[S-R9-5]** Entity, `/api/entities` aborted + feed fulfilled 5 posts: DOM order header, alert (y=107 h=130), post×5 (y=253) — both visible. 0 Share CTA, 0 intro, 0 "no longer listed" while `entities===null` (`EntityPage.tsx:55,96,105,127`).
- **[S-R9-6]** Entity h1 during and after registry failure = real name "ChenJenny" (from `/api/directory`, `EntityPage.tsx:64-69`); both aborted → h1 "Teacher" (`KIND_TITLE`), region "Teacher experiences" [C-R9-18].
- **[S-R9-7]** Entity "Try again" ×2 (aborted): `activeElement = div.focus-landing[role=region][aria-label="ChenJenny experiences"]`, `data-landed=""`, 3 px ring; 2 requests per retry; a later feed settle fires no spurious landing.
- **[S-R9-8]** Mine, `/api/entities` aborted: banner renders (`MinePage.tsx:150-155`); "Try again" ×2 → `activeElement = BODY`, `[data-landed]=[]`. Cause: `MinePage.tsx:59` `useRetryFocus(mine.loading)`; `:152` `reloadNames()` never toggles `mine.loading` (`useRetryFocus.ts:11-20`).
- **[S-R9-9]** Explore, `/api/directory` only aborted: banner renders (`ExplorePage.tsx:80-93`); "Try again" fires 2 requests (`/api/entities` + `/api/directory`); after failing ×2 AND after succeeding `activeElement = BODY`. Cause: `:31` arms on `entities.loading`; `entities.reload()` on a cached key re-enters `useApi.ts:53-56` with a hit so `loading` stays false. Explore with `/api/entities` aborted lands correctly (`role=group "Everything listed"`).
- **[S-R9-10]** Compose error region `section.focus-landing[role=region][aria-label="Couldn’t load"]` (`ComposePage.tsx:216-219`); regions named "Editor" = 1 (`:306`). Double-fail lands with the ring.
- **[S-R9-11]** `useApi` guard (`useApi.ts:66-75`): A=3 s/B=3 s and A=4 s (superseded, slow)/B=0.5 s → rendered B, `markerA=0`, 2 requests. SPA re-entry: skeleton + 1 new request (again B) because `PullToRefresh` calls `apiCache.clear()` (`useApi.ts:22-25`) — one pull on Explore empties the cache for every screen.
- **[S-R9-12]** Pending: 👍 `aria-disabled="true"`, `opacity .7`, `pointer-events none`; 👎 `aria-disabled=null`, `opacity 1`, `pointer-events auto` (`ExperiencePost.tsx:97,166,185,229,241`; `components.css:251-252`). Second mouse click swallowed by `pointer-events`; keyboard Enter → focus kept, no second POST (`:163`), no feedback.
- **[S-R9-13]** Plain `/timetable` → `/timetable?date=2026-09-02` within 300 ms (`TimetablePage.tsx:43-47`); `history.length` 3→3; one Back leaves to `/home`. From `?date=2026-08-24`: Next→25 ✓, Next→26 ✓, Prev→25 ✓, Prev→ h1 "Monday, 24 August 2026" with `?date=2026-08-25` ✗.
- **[S-R9-14]** Cause: the effect compares `searchParams.get("date")`, which React Router never updates after `window.history.replaceState`, so `q` is frozen at the mount query; returning to the mount date makes `q === date` and skips the write. Invalid `?date=` still replaced. `history.length` unchanged over 5 steps.
- **[S-R9-15]** Cold landing Aug 24: 390×620 `scrollTop 120-126`, h1 y=26, note y=72, block 1 y=99 (89 px below daynav bottom); 320×568 `scrollTop 138-143`, h1 y=8, note y=54, block 1 y=99 (r8 h1 −62/−68, block 1 −6).
- **[S-R9-16]** Retry: `scrollTop 0→0` at four viewports. **[S-R9-17]** SPA re-entry lands 128 / 137; 0 at 844/932.
- **[S-R9-18]** Never-seen date step: exactly 1 `scrollIntoView` call, `0→228` (Aug 25, one late lesson) and `0→119` (Aug 26). Residue: Aug 25 clamps to max (228) → h1 y=−82, note y=−36.
- **[S-R9-19]** Step onto a cached day: 0 `scrollIntoView` calls; scroll inherited from the previous day (119 px over four cached steps). Cause: a cache hit never sets `loading=true`, so `loadingSeenFor.current !== date`, `trusted` false (`TimetablePage.tsx:64-71`). The comment's "first settle of a date — cold load, re-entry or a date step" holds only for uncached dates.
- **[S-R9-20]** PTR on a landed day: `scrollTop 122→0` (390×620), `143→0` (320×568); no re-landing.
- **[S-R9-21]** At 390×844 / 430×932 the landing is gated out (`innerHeight > 620`, `TimetablePage.tsx:339`): `scrollTop 0`, block 1 y=219/247, 89 px below the daynav; `scroll-margin-top:100px` has no effect at normal heights.
- **[S-R9-22]** `/nonsense`: title "Page not found · HOney", `h1.page-title`, skip link `#main` resolving, landmarks 4 (`aside.rail`, `nav.rail-nav`, `main#main`, `nav.mobile-nav`), 7 nav anchors, `.stack/.page-title/.card-actions` in served CSS (2/2/1 rules); 10 requests, 1 API (`/api/me`), Dash chunk not loaded.
- **[S-R9-23]** `aria-current` on `/nonsense` = 0; on `/experiences/new` and `/timetable/oops` the 404 renders with `aria-current="page"` + `is-active` on 2 nav items (rail + mobile Experiences / Timetable).
- **[S-R9-24]** Unauthenticated `/nonsense` → `/login` ("Sign in · HOney"): the `*` route is inside `RequireAuth` (`App.tsx:29,62-64`).
- **[S-R9-25]** `div.post__menu[role=menu][aria-label="Post options"]` with exactly 1 menuitem "Report" (110×44).
- **[S-R9-26]** Feed status (`FeedPage.tsx:159-167`): n=0 "Nothing has been shared yet"; n=1 "1 experience"; n=12 "12 experiences"; error ""; loading "". Load-more → "24 experiences" (cumulative, 1 mutation); tab change → 2 mutations (`""` → same count). `[aria-live]` 0; `[role=status]` 2 (`div.ptr__disc` + sr-only).
- **[S-R9-27]** 12 posts: header Share + 1 `.feed-invite`; 24 posts: header Share + 2 invites (`FeedPage.tsx:18,173-180`). **[S-R9-28]** 2 classless `<div>` wrappers per post (24 at n=12, 48 at n=24; `FeedPage.tsx:169-171`).
- **[S-R9-29]** History "Select" → `/experiences/compose?lessonId=…` with `activeElement = BODY`. **[S-R9-30]** No keyboard equivalent to PTR (`div.ptr[role=status][aria-hidden]`, 0 focusable descendants).
- **[S-R9-31]** 4 dialogs portalled to `document.body` (`Modal.tsx:52,73`), `inert` on `#root` (`:44-47`): 2 mutations per life (SET/REMOVE); `[inert]` inside the dialog 0/4; nav centre hit-tests outside `#root` 4/4; overlay click closes 4/4; only 1 `[role=dialog]` ever.
- **[S-R9-32]** Parent re-render mid-dialog: theme swatch / typing → 0 extra mutations; a script-dispatched `honey:refresh` with the lesson dialog open re-runs the `[onClose]` effect 3× (`SET,REMOVE,SET,REMOVE,SET,REMOVE,SET,REMOVE`); focus stays on `div.modal`. (Reconciled with [A-R9-16]: no user-reachable path found — a touch drag over the overlay does not reach `.main` [W-R9-3].)
- **[S-R9-33]** Focus on close: lesson → lesson block; appearance → `button.settings-trigger`; save-login → its row button; **report → `BODY`** (the opener menuitem is unmounted with the menu).
- **[S-R9-34]** `check-css.mjs` clean on `dist/assets` and on the served CSS; 0 orphan declarations; the sheet block restored (`components.css:813-829`) with the compact `max-height` as a sibling `@media (max-width:640px) and (max-height:620px)`; `sheet-up` 2 consumers (`.modal`, `.nudge`).
- **[S-R9-35]** Duplicate whole-head selectors 0; individually repeated selectors 5 (r8 4: `a` returns via `button, a { -webkit-tap-highlight-color }`); dangling trailing comments 10 (r8 9: `tokens.css:58`, `components.css:17-22,252,527,669,828,877`, `features.css:1-5,80,1201`); unterminated openers 0; consumer-less classes 14 raw / 0 adjusted; reverse sweep 12 raw / 5 real (`home`, `home-head`, `home-voices`, `login__card`, `post__dot`) unchanged. Served CSS 40,932 B / 374 rules / 222 classes; JS 276,460 B.
- **[S-R9-36]** 528 boxes; button-height families 9 at 390 / 10 at 320 (unchanged); sub-44 = 2 (login checkbox 22×22 inside its ≥44 label). **[S-R9-37]** Disabled: one text recipe, two fills (`btn--primary:disabled` `color(srgb .891 .901 .907)` vs ghost transparent + `rgba(35,43,49,.14)` border).
- **[S-R9-38]** Cold-load request table identical to r8: explore 1/1/3, entity 1/1/4, `compose?entityKey=` 1/1/3, `compose?lessonId=` 0/0/2, mine 1/1/3, plain compose 0/0/1, feed 0/0/2, timetable 0/0/2, home 0/0/3, 404 0/0/1.

## 2. Visual [V]

- **[V-R9-1]** Rest-state outline census: 10 routes × {320×568, 390×844, 1280×800} × {stone, genuine dark boot} = 60 loads, `activeElement=BODY`: ringed `button,a,input,select,textarea,summary` **0/117** (mobile) and 0/137 (1280). Rest `3px none` vs focused `3px solid rgb(51,102,124)`; rest ≠ focused **134/134** Tab stops (r8 24/111 equal). Edge-band accent pixels **0/0/0/0** on Settings/Timetable/Feed × 3 widths × 2 boots (r8 21,876 / 37,094 whole-frame). Appearance 2×2 grid `ringedInDialog=0` on 6 cells. Disabled compose buttons `outline 3px none`.
- **[V-R9-2]** Mobile sheet census, 7 dialogs × {320×568, 360×620, 390×844} × 2 boots = **42/42**: `border-radius 20px 20px 0px 0px`, borders `solid/none/none/none`, `padding 20px 16px 24px` with the CSSOM rule carrying `calc(var(--sp-6) + env(safe-area-inset-bottom))`, `animation sheet-up 0.42s`, bottom == `innerHeight`, `max-height` 568/620 (100dvh) / 776.48 (92dvh). Overflow 41/42 none; **Reconnect 320×568 568 vs 567 = 1 px** (r8 6 px). Dark boot through the portal: `.modal` bg `rgb(29,33,37)` = `--surface-solid`, colour `rgb(233,237,239)` = `--ink`. `#root[inert]` 42/42. `sheet-up` 2 consumers.
- **[V-R9-3]** Served CSS: `check-css.mjs` clean; independent parse 40,932 B, 374 blocks, 13 at-rules, **0 orphan declarations / 0 dead bytes** (r8 209), 0 in-context duplicates, 0 bare-element+state lists, 223 classes; outline rules: `:focus-visible` list, `.focus-landing[data-landed]:focus`, `.section-title[id]:focus`, `.main:focus{outline:none}`, `.main:focus>.view`. 6 `@keyframes`.
- **[V-R9-4]** Skip ring: `.main` `3px none`, `.main:focus>.view` `3px solid`, offset 3, `border-radius 12px`; gutter 10 px at 390 and 320 — fully inside the viewport 16/16. Ring × content overlap **1/8 at 390, 1/8 at 320** (r8 5/12): the Timetable's `span.timeline__hour "20:00"` bottom 844 vs `.view` bottom 838 (320: 546 vs 540) — the ring's bottom band crosses the last hour label. Painted over the ring: `nav.mobile-nav` on Timetable@390 and Feed/History@320. On 5 long routes 11–13 of 20 edge points are off-viewport (ring encloses the document-height `.view`, e.g. Settings `[10,10,370,2996]`). The ring paints on any focus of `#main` (16/16), no route change focuses `#main`. 12 px matches no other rendered radius (`.card` 17, `.lesson-block` 10, `article.post` 0, `.btn` 99). `r9-vis-skipring-*.png`.
- **[V-R9-5]** Compact landing (Asia/Shanghai, today + Aug 24; 320×568 / 360×620 / 390×620 / 390×844 / 430×932): cold `scrollTop` 139/141 · 141/143 · 122/124 · 0 · 0 (r8 156–216); **h1 in view 6/6** (y 3–24; r8 off 5/6), note in view 6/6 (r8 off 4/6), block 1 y=94…98 compact / 219/247 normal — **never above 0** (r8 −6). Overlaps 0/0/0/0 on 10/10 rows. `.daynav` static at ≤620 (sticky at 844/932), on arrival y −106…−127. One instant step 6/6 (137–146 px, +141…+200 ms after FCP), `scroll-behavior smooth`/`auto` (reduce). Note margin 12/8, note→canvas 8. Date step Aug 25→Prev→Aug 24: `0→137` @23 ms (320×568), `0→137` @25 ms (360×620), `0→119` @60 ms (390×620); h1 in view after the step 5/5; `?date=2026-08-24` 5/5. (Reconciled with [A-R9-19]: these runs had the first lesson as the landing target.)
- **[V-R9-6]** Empty-day note "Last synced 7 h ago." — no leading space; note→`.timeline__empty` gap 16 px (unchanged).
- **[V-R9-7]** 404 in the shell: `div.stack > h1.page-title + p.muted + div.card-actions > a.btn.btn--primary`; h1 28px/650 at 390, 36px at 1280 — identical to Settings/History/Explore/Why/Feed/Mine; `.stack gap 16px`; "Go home" 99×44 r99. `r9-vis-404-{stone,dark}-{390,1280}.png`.
- **[V-R9-8]** Lesson dialog `p.sr-only#lesson-dialog-body` 1×1 absolute clipped — head→`dl` gap 12 px, h2→first `dt` 34 px identical at 390 and 320 (no visible cost); description "09:00 to 10:30, with ChenJenny, in room 213."; `dl.kv` grid `43px 299px`, gap 8/16.
- **[V-R9-9]** Two disabled fills persist (`.btn--primary:disabled` `color(srgb .891 .901 .907)` transparent border vs `.btn--ghost:disabled` transparent + `1px solid rgba(35,43,49,.14)`); one text recipe; `.react-btn:disabled` no rule (latent). Pending at 100/500/1500 ms: only the tapped 👍 dims (`opacity .7`, `pointer-events none`); 👎 and `···` unaffected. `r9-vis-pending-390.png`.
- **[V-R9-10]** Spacing: rendered on-ladder **97.84 % @320, 97.61 % @390, 97.61 % @430, 92.63 % @1280** (= r8); 214 declarations, 176 on `var(--sp-*)`; off-ladder literal spacing declarations **3** (r8 2: + `features.css:355 scroll-margin-top:100px`).
- **[V-R9-11]** Type: 390 `{12,13,15,16,20,22,24,26,28}`; 320 `{12,13,15,16,20,24,26,28}`; 430 + 27.95; 1280 `{12,13,15,16,20,28,30,32,34,36}`; floor 12; off-ramp 11 incl. admin (10 excl.) — unchanged.
- **[V-R9-12]** 43 colour literals; `tokens.css` blob `1d70d963a91a5bfbeb17928b599185286ad8f836` (= r8); rendered unique colours 17 stone / 19 night; `.swatch` literals unchanged.
- **[V-R9-13]** Contrast: lowest enabled **4.66 stone / 5.83 night** (`.home-head__stale`); 0 genuine failures (the 1.00 readings are the documented ancestor-walk artefact); error banner 4.77/7.03; captions 5.34–5.66 / 7.13–8.07; disabled 4.61–5.66 / 6.64–8.07; placeholders 5.34–5.63 / 7.27–8.07; focus ring vs ground 5.81–6.16 / 8.35–9.28; 404 muted 5.34/8.07; dialog text through the portal on dark 7.27–13.75.
- **[V-R9-14]** States, 390×844: empty ✓ Timetable/Explore/Mine/Entity; loading ✓ Home, Timetable, Feed, Explore, History, Entity feed, Compose-entityKey — **0 skeleton blocks on Mine under `/api/entities` and `/api/directory` (5 s delay) and on Compose-with-`lessonId` under `/api/history`**; error ✓ Home, Timetable, Feed, History, Explore (both endpoints), Mine (banner now), Entity (3 endpoints) — **none on Compose-with-`lessonId` (`/api/history` aborted → no banner, no empty)**; success ✓; focus ✓ (a state again, 134/134); disabled ✓ (two fills); pending ✓ tapped pill only; selected ✓. Explore still renders the error banner and four "Nothing here yet." together.
- **[V-R9-15]** Idle: `getAnimations()` at 2.5 s = 0 on 11/11 × no-preference and 11/11 × reduce (sampled outside a lesson — see [W-R9-18]); 6 keyframes, 2 infinite loading-only; one `.view {` block with `settle .46s`; gradients 1 (skeleton); blur rules 3 (`.modal-overlay` 8px, `.rail` 12px, `.mobile-nav` 12px); shadows 9 declarations / 4 recipes; radius families 13 + 3 asymmetric; `color-mix()` 18; largest mobile type 28.
- **[V-R9-16]** Dangling comments 6 / 7 by the two detectors (= r8); source line-parser duplicate groups 0→1 (`a {` at `foundations.css:48` and `:64`, artefact of the multi-line list; served 0); `features.css:759` and `:769` open two adjacent `@media (max-height:620px)` blocks with identical conditions; 7 dynamically composed classes never literal.
- **[V-R9-17]** At 320×568 on the Timetable after the landing, the first Tab lands on `button.lesson-block`, not the skip link (390×844: skip link first) — the programmatic `scrollIntoView` moves Chrome's sequential-focus start point.

## 3. Copy & Honesty [C]

- **[C-R9-1]** Delete copy ↔ rows: account row `SettingsPage.tsx:96-98` "Removes your HOney account and your imported lessons (shared teacher, course, room and lesson entries stay). Published experiences stay — …"; data row `:211` "…Shared teacher, course, room and lesson entries stay."; confirm bodies `:288`, `:309`. `deleteImportedData` = `DELETE FROM user_lesson_exposures` (`accounts.ts:238-240`); `deleteAccount` = `DELETE FROM honey_users` + cascades (`database.ts:26,35,44,81`); `lesson_instances`, `teachers/courses/rooms`, organic `entity_registry` rows survive both. Both clauses true at row level. Residue: "lesson" on both sides of one sentence; `entity_registry` unnamed; the account confirm body lacks "Published experiences stay".
- **[C-R9-2]** Storage caveats 5/5 name the key (`LoginPage.tsx:60-62`, `SettingsPage.tsx:171`, `ReconnectDialog.tsx:31,32`, notes bullet `SettingsPage.tsx:255-259`) ↔ `portalCredentials.ts:20-21,87,115-124` same store — accurate; five sentence shapes for one fact.
- **[C-R9-3]** Save purpose: ticked "Save login" / unticked "Continue without saving" / re-ticked "Save login" (live). Reconnect purpose: submit "Reconnect" in both tick states while `:53-55` runs `stayConnected ? authorize(creds) : clear()`.
- **[C-R9-4]** "Continue without saving": both fields `required` (`SchoolLoginForm.tsx:43-65`), submit → `POST /api/auth/login` (live portal login) then `clear()`; on the save purpose the row only offers the dialog when `isAuthorized()` is false so `clear()` is a no-op — label true, but it does not say a sign-in happens.
- **[C-R9-5]** Provenance: `shared.tsx:15-19` {"Verified lesson experience", "From someone who has taken this over time", "Verified school member"} vs `ExperiencePost.tsx:18-22` {"from a class you've taken", "from someone who has taken this over time", "from a student here"} — retrospective unified (case aside); lesson/member still two registers; `shared.tsx` now mixes registers internally.
- **[C-R9-6]** Login checkbox accessible name 29 chars (r8 199), description the 199-char note; dialog checkbox name "Stay connected on this device." (30), caveat via the dialog's `describedBy`.
- **[C-R9-7]** Trigger `aria-label="More options"`, menu `aria-label="Post options"`, 1 item "Report" — two names for one one-item affordance; "Post" a fourth noun beside experience/experiences/Experience.
- **[C-R9-8]** `app.ts:103-106` now states origin `no-cache` and the edge rewrite; wire: origin `no-cache`, edge `max-age=14400` `cf-cache-status: REVALIDATED` — true.
- **[C-R9-9]** Feed status live: "1 experience" / "12 experiences" / "Nothing has been shared yet" / "" loading / "" error with exactly one `role=alert`.
- **[C-R9-10]** On the default tab "Your classes" (n=0) the status says "Nothing has been shared yet" while the visible block says "Nothing from your classes yet. When someone shares an experience connected to a class you've taken, it will appear here. / Share the first one" (`FeedPage.tsx:131-142` vs `:162-163`).
- **[C-R9-11]** Load-more: "12 experiences" → "18 experiences" (cumulative).
- **[C-R9-12]** 404: title = h1 "Page not found", body "That page doesn't exist.", button "Go home"; landmarks 3, 7 nav links, `aria-current` null on `/nonsense`; skip link the shell's "Skip to content" → `#main`. Never-listed entity: title "Not found · HOney" ≠ h1 "Nothing is listed at this address." (unchanged).
- **[C-R9-13]** Note: lesson day "P1–P6 are the school's six lesson periods. Last synced 7 h ago."; empty day "Last synced 7 h ago." (20 chars, no leading space).
- **[C-R9-14]** Locale: 11 `toLocale*` calls; `format.ts:35,44,52,61,74,83`, `TimetablePage.tsx:26`, `HomePage.tsx:43,47` all `"en-GB"`; `HomePage.tsx:95` `"en-CA"` (URL only); **`format.ts:27` `toLocaleTimeString(undefined, …hour12:false)` the one unlocked call**. Under en-GB / en-US / zh-CN every string identical: h1 "Wednesday, 2 September 2026", stepper "Wed 2 Sept", empty "No lessons on Sun 6 Sept", hours "09:00…", post day "1 Sept 2026", Home "Wednesday 2 September". Zero non-English date tokens. Home vs Timetable same day: "Wednesday 2 September" vs "Wednesday, 2 September 2026" — one locale, two option sets.
- **[C-R9-15]** Plain `/timetable` → `?date=2026-09-02`; a copied link names a fixed day.
- **[C-R9-16]** `#lesson-dialog-body` textContent "09:00 to 10:30, with 陈拯侃, in room 416." — no space before the period; the visible `<dl>` repeats Time/Teacher/Room.
- **[C-R9-17]** Compose (`?entityKey=`, registry down): region "Couldn’t load" containing alert "Could not reach the HOney server…" — two spellings of one failure; one identical banner string on Explore/Mine/Entity/Compose. Compose without an `entityKey` renders the healthy picker with `/api/entities` aborted (error branch gated on `effectiveEntityKey`, `ComposePage.tsx:210`; the plain chooser issues no registry request).
- **[C-R9-18]** Entity under registry failure: h1 "ChenJenny", overline "Teacher", 0 intro, no Share CTA, one alert, "No experiences here."; both aborted: "Teacher" ×3 (title, h1, region).
- **[C-R9-19]** Privacy map zero drift: `git diff --stat 13cbcee..eede644` over `api/`, `ownershipKeys.ts`, `portalCredentials.ts`, backend `routes/db/services`, `packages/shared` = empty; identity-free publish `client.ts:227-229,349-353`; no author column `database.ts:90-92`; `ownership_hash` sha256; 6=6=6 categories; `free_text_not_accepted` `routes/experiences.ts:208`; coarse day `format.ts:73-79`; HOney ID line live; WhyPage "design boundary, not magic" live; `syncTimetable` exactly two callers (`auth.ts:32`, `data.ts:17`); import disclosure verbatim at `SettingsPage.tsx:200-204`, `LoginPage.tsx:70-71`.
- **[C-R9-20]** `api.deleteAccount()` (`client.ts:304-307`) clears only `HOney.session`; `AuthContext.tsx:52-58` clears `portalCredentials` on sign-out but the delete path (`SettingsPage.tsx:314-319`) does not — after "Delete account" the encrypted school login and its key stay in `localStorage`, unmentioned.
- **[C-R9-21]** 937 user-facing strings / 40 files; delta since 13cbcee 10 changed, 4 added, 4 removed (all quoted in the logs). Inflations **1** (`WhyPage.tsx:73` "strongly protected"). Dark patterns **0/7** (login checkbox unchecked; save dialog opens ticked as the affordance just pressed; nagging bounded: 1 aside at 12 posts, 2 at 18 beside a persistent header Share). Jargon / unclear **5** (r8 6): two provenance registers; "clearing site data"; "press Sync now" on the login page; "Post options"/"More options" over one item; "Couldn’t load" beside "Could not reach…". Label→behaviour mismatches **4** (r8 4, all four new): "Reconnect" ↔ save/clear; status "Nothing has been shared yet" ↔ visible "Nothing from your classes yet."; status announces a total not the event; "Delete account" ↔ saved login kept.

## 4. Weight & Friction [W]

- **[W-R9-1]** `useApi.ts:72` guard: A 4,000 ms / refresh 500 ms (replaced finishes last) → screen GEN2, re-entry first frame GEN2 (0 skeletons); B 500 / 4,000 → GEN2 / GEN2. r8 last-to-arrive-wins gone.
- **[W-R9-2]** Guard cost: leaving Explore 700 ms into a 2,500 ms request and returning → 7 skeletons + a second request; Timetable 09-02→03→04 cancels 09-03; back to 09-03 → `{skel:5, blocks:0}` + new GET, while settled 09-04 renders from cache. One wasted round trip + skeleton per view abandoned mid-flight.
- **[W-R9-3]** Portal + inert, 8 opens: overlay parent `BODY`, `#root[inert]` set/removed, `aria-hidden` never set, opener refocused; overlay = viewport, `position fixed`, `z-index 50`; CLS on open 0.00000 8/8; 0 long tasks 7/8 (one 52 ms); click→two frames 133–348 ms (median ≈215); background `.main.scrollTop` unchanged under wheel/touch on the overlay 4/4; `body{overflow:hidden}` is the shell's permanent state.
- **[W-R9-4]** Sheet skin live (`components.css:810-828`), served orphan sites 0 / bytes 0 (r8 1 / 209).
- **[W-R9-5]** `?date=`: 5 forward steps correct; `q` frozen at the mount value → `prev 5` shows 2 September with `?date=2026-09-03`; mount `?date=2026-08-24`, +1, −1 → 24 August under `?date=2026-08-25`; "Back to today" leaves `?date=2026-09-04` over 2 September; reload of that URL shows the wrong day. Bare `/timetable` rewrites to `?date=<today>`.
- **[W-R9-6]** `history.length` 2→2 over 5 next + 5 prev + Back to today; after `/home`→`/timetable`→5 steps, one Back returns to `/home`.
- **[W-R9-7]** 1 GET per date step including cached dates: 13 steps over 6 dates = 14 GETs; 20 forward steps → 21 requests / 21 keys; 5 back-steps → 5 more requests, 0 skeleton frames. The SWR cache suppresses the skeleton, never the request.
- **[W-R9-8]** Date step at compact heights: 360×620 `144→0` (+92 ms) then `0→156` (+128); 320×568 `140→0` (+80) then `0→208` (+150); CLS 0; heading after the step **y=−10 (360×620) / −62 (320×568)** vs cold +2 / +6 — the step scrolls to max.
- **[W-R9-9]** Cold: 390×844 `scrollTop 0` (max 90, no landing); 360×620 144 (block 1 93, h1 +2, note +48, daynav −128); 320×568 140 (block 1 97, h1 +6, note +52, daynav −124). 12/12 cells two transitions (`null→0`, one step 119–146), CLS 0/12.
- **[W-R9-10]** `/nonsense` cold: 10 requests, 1 data, 154,641 B, Dash chunk not fetched, 4 nav links, skip link `#main`; SPA `/home`→`/nonsense` 0 app requests; `/dash` fetches `DashPage-C5s-hczQ.js` (5,132 B); Dash on student views 0/15.
- **[W-R9-11]** `app.ts:100-106` matches origin `no-cache` and edge `max-age=14400`.
- **[W-R9-12]** `package.json` build = `tsc … && vite build && node scripts/check-css.mjs dist/assets`; clean on dist and served (md5 `140ba727…` identical).
- **[W-R9-13]** Gate catches: dangling `button,` before a `:focus-visible` list; brace-nested `@media` orphan declaration; reversed `a:focus-visible,button{}`; duplicate selector inside one `@media`; `.x{}` twice at root across a `@media`.
- **[W-R9-14]** Gate misses: `.a{color:red}}` stray brace (becomes `"} .b"`); **`.card,\na:focus-visible{…}` (class instead of element)**; `BUTTON,`; `input[type=text],`; unterminated `{` at EOF; the same selector across two identical `@media` blocks. Passes correctly: `.btn,.btn:hover{}`, `url(data:…;…)`, `content:";"`, nested `@media`/`@supports`, `@font-face`. Bare test `/^[a-z]+$/`, state test `/:focus|:hover|:active/`.
- **[W-R9-15]** Bytes: `index-BDmmlJxi.js` **br 86,624** (r8 86,192, +432) / zstd 89,051 / raw 276,692; CSS br 9,312 / raw 40,932; Dash br 4,405; `dist/` 666,818 B / 18 files; dependencies 5 runtime / 9 dev unchanged.
- **[W-R9-16]** Requests (total/same-origin/data/wire): cold /home 12/11/3/156,126 B; /timetable 14/12/2; /experiences 11/10/2; plain compose 8/7/1; /nonsense 10/9/1; explore 12/11/3; entity 13/12/4; mine 12/11/3; history 10/9/3; settings 10/9/1; why 10/9/1. Warm /home→/experiences 3 / 1 data / 1,808 B. Duplicate API paths 0/15; `/api/entities` ≤1 anywhere; one font subset 15/15.
- **[W-R9-17]** TTI (method: max(FCP, first primary content) → end of last long task ≥50 ms with a 5 s quiet window), 6 cold loads each at 390×844: /home median 339 ms (248–472), /timetable 347 (319–411); boot long tasks 10 across 12 loads (49–79 ms, all <400 ms); host load 2.4–2.7 on 4 vCPU.
- **[W-R9-18]** Idle animations 22 cells: no-preference 2 non-zero — `/home` a **running** `width` transition on `.nextlesson__wash` (during a lesson; `HomePage.tsx:21` `useNowTick(1000)` + `features.css:80` `transition: width 1s` restarts every second), `/history?select=1` a finished `rise-in`; reduce 1 finished. Running idle animations **1/22** (r8 0/22; code unchanged, state-dependent — absent between lessons in `r9-wf-wash.log`). Idle cost over 20 s: 0 mutations, 0 requests, 0 long tasks.
- **[W-R9-19]** Interrupts on load 24 cells: dialogs/toasts/badges/alerts/`aria-live` 0/24; `role=status` `div.ptr__disc` 24/24 (empty), feed sibling 2/24, history opt-in banner 2/24.
- **[W-R9-20]** CLS: 17/18 contention-free cells 0; **`/home` 320×568 Asia/Shanghai 0.09508** (t=670 ms, `SECTION.home-voices` + `DIV.home-foot`); repeats: /home 360×620 1/5 (0.01566), 320×568 1/5 (0.01843); under 5-way contention /home 4/6 non-zero; every shift `DIV.home-foot` at ~840–975 ms; /timetable 0/5 ×3.
- **[W-R9-21]** Boot theme 6/6 pre-FCP (surface at 50.7–97.5 ms, FCP 228–388), 0 `<html>` mutations, `theme-color` rewritten 6/6.
- **[W-R9-22]** Reduced motion: `settle` 1e-06 s 11/11; 25/25 elements 1e-06 s; scroll owner `auto` 6/6.
- **[W-R9-23]** SW md5 `f7513f02194664efa4e96d50773ec83d` (= r8) on disk and wire, 4,828 B, `honey-v3`; seeded battery: 9/9 current kept, 5/5 stale evicted, `honey-v2` deleted; mode fake 8/10 wrongly evicted (`sw.js:33-34` trusts bodies, unchanged); cold-start refetch 0 B 3/3; JS-only / CSS-only mismatch evict; exact match no work; 10 hard loads → 5 entries / 348,629 B; `/api/` 0; HTML-under-`/assets/` refused; offline shell /timetable 117 ms, /experiences 79 ms; eviction walk fetches the Dash chunk.
- **[W-R9-24]** Headers: origin `/sw.js` `no-cache`, `/assets/*` immutable, others `must-revalidate`, weak ETags; edge `/sw.js` `max-age=14400`, icons `max-age=14400, must-revalidate`, `/` DYNAMIC with ETag stripped; `/` `If-None-Match` → 304 (r8 200).
- **[W-R9-25]** Dead bytes: orphan CSS 0 (r8 209); 7 `@font-face` / 123,968 B across 6 never-requested subsets; admin CSS 11 rules / 1,201 B in the student payload; Dash 0/15.
- **[W-R9-26]** SWR map: 20 steps → 21 keys, heap 2,948,956 → 5,408,465 B (+2.46 MB, ~117 KB/date), no eviction.
- **[W-R9-27]** PTR: 5 pulls in 892 ms → one round, 2 requests, never POST; a pull during a 3 s in-flight request issues new requests; `.focus-landing` 0 listeners after blur.
- **[W-R9-28]** Ordinary tap inside a landing region: `outline 3px none`.

## 5. Accessibility [A]

- **[A-R9-1]** 0 buttons/links/inputs ringed at rest on 10 routes at 390 + 3 at 320/1280; 0/202 Tab stops with rest == focused; touch on 12 controls rings 2/12 (both text inputs, browser `:focus-visible`), 0/10 buttons/links/tabs/pills; `-webkit-tap-highlight-color` suppressed on `button, a` only — `input`/`textarea` compute `rgba(51,181,229,.4)`.
- **[A-R9-2]** `.modal` at 390×844 `sheet-up 0.42s`, `20px 20px 0 0`, `border-bottom 0`, `padding 20px 16px 24px`, `[0,583,390,261]`.
- **[A-R9-3]** `#root[inert]` true 7/7 (390 hasTouch + 1280 keyboard); `dialog.closest('[inert]')` false 7/7; only `aria-hidden` descendants the 4 decorative swatches; `role=dialog` + `aria-modal` + name==h2 + description resolving 7/7; Tab×30 escapes 0/7, Shift+Tab×30 0/7; nav hit-tests to overlay/dialog 7/7; dialog buttons 44–410×44, 0 sub-44.
- **[A-R9-4]** Lesson description "09:00 to 10:30, with ChenJenny, in room 213." (44 chars); the `<dl>` is still read afterwards (`"Time09:00–10:30Topic…Room213"`). Other descriptions: Appearance 56, Save 206, Reconnect 239, delete-data 161, delete-account 141, Report 122 chars.
- **[A-R9-5]** Report `describedBy={done ? undefined : "report-dialog-body"}`; form branch resolves (measured); done branch source-only.
- **[A-R9-6]** Pending per pill 4/4 (stone/dark × touch/keyboard); 1 POST per pass; second Enter + Space + tap during pending → still 1.
- **[A-R9-7]** Status (40 ms poll): n=1 "1 experience" (+596 ms); n=0 "Nothing has been shared yet"; n=12 "12 experiences"; load-more one announcement "24 experiences"; tab change one announcement + tabpanel renamed via `aria-labelledby`; error silent, alert speaks once. Residue: 2 unnamed `role=status` nodes, no `aria-atomic`, unnamed `main`; status ≠ visible sentence.
- **[A-R9-8]** 404: landmarks `{navigation:2, main:1}`, title == h1, skip link → `MAIN#main`, `.view` ring painted, 6 controls, 0 sub-44.
- **[A-R9-9]** Skip ring 12 surfaces × 2 widths: present 12/12, target 12/12, `activeElement==#main` 12/12 @390, 11/12 @320; ring band vs content border boxes **0 overlaps 24/24** (r8 5/12); band centres hit `MAIN.main` on four sides 10/12, `H1.page-title` at the top band on Compose/Why @390 and Why/404 @320, `NAV.mobile-nav` bottom band on Feed @320; focused skip link `[12,12,134,48]` uncovered 11/11 @390, 10/11 @320.
- **[A-R9-10]** Entity registry double-fail lands: region "ChenJenny experiences", `data-landed`, 28.0 % ring px, scroll 0→0.
- **[A-R9-11]** Compose "Couldn’t load" lands, 26.5 % ring, 0 scroll.
- **[A-R9-12]** Login checkbox name 29 chars, description 199, Space toggles, 22×22 in a 342×132 label, 5 Tab stops; dialog checkbox name "Stay connected on this device." (30); submit flips "Save login" ↔ "Continue without saving" on each Space.
- **[A-R9-13]** Menu "Post options", trigger "More options", `aria-haspopup/controls/expanded`; Enter → "Report" menuitem focused (110×44, ringed); Arrow keys hold; Escape → trigger; Tab closes.
- **[A-R9-14]** Contrast 26 surfaces × 2 boots: enabled text below 4.5:1 = 0; floors 4.66 / 5.83; 404 muted 5.34 / 8.07; portaled dialog on dark boot bg `rgb(29,33,37)`, floor 6.11 night / 5.14 stone; rings vs ground `.focus-landing` 5.81–6.13 / 8.36–9.28, `#main > .view` 5.81 / 9.28, `.daynav__date` 6.13 / 8.36; disabled 5.34–5.66 / 7.26–8.07; placeholders 5.34–5.63 / 7.27–8.07.
- **[A-R9-15]** Reduced motion: `sheet-up` / overlay `fade-in` / `settle` / skip-link transition → 1e-06 s; scroll owner `auto`; landing one discrete step under both.
- **[A-R9-16]** Effect-rerun hunt negative on user paths: swatch by keyboard and tap → 0 inert mutations, focus stays on the swatch; Save dialog typing + checkbox ×2 → 0; delete-data 12 Tabs → 0.
- **[A-R9-17]** **Report dialog does not restore focus**: Esc → `activeElement = BODY` at 390 and 1280; on all four exits. Cause `ExperiencePost.tsx:267-268` `setMenuOpen(false)` before `setReporting(true)` → `Modal`'s `opener` (`Modal.tsx:22`) is the unmounting menuitem; `opener?.focus?.()` (`:50`) targets a detached node. 6/7 dialogs restore.
- **[A-R9-18]** At 320×568 / 360×620 / 390×620 cold, **Tab 1 lands on a lesson block** (y=334/386/357), Shift+Tab goes to the previous block; the skip link and 6–7 daynav controls are behind it; at 844/932 Tab 1 is the skip link; on the empty day Tab 1 is the skip link. Cause `TimetablePage.tsx:340-346` `scrollIntoView` moves the sequential-focus start point. 5/5 runs.
- **[A-R9-19]** Compact landing with the target = a later lesson (today at 10:35 CST, target P3 13:30): request exceeds the owner's max → clamps **12/12 traced loads** — `scrollTop 208/156/228 == max`, **h1 y=−62 / −10 / −82 (off 3/3)**, note off 2/3, daynav −192/−140/−212, **block 1 at y=−10 at 390×620**. With the target = lesson 1 (Aug 24; today at 10:19 CST): `scrollTop 144/126`, h1 +2/+20, block 1 +93. Clock-dependent.
- **[A-R9-20]** Retry that fails again, 390×844: **Timetable** `ae=(body)` (the `.focus-landing` region renders only in the success branch, `TimetablePage.tsx:207-215`, so `ref.current` is null and `armed` is cleared; a succeeding retry lands on "Day timeline"); **Explore, `/api/directory` only** → body on fail AND on success (`ExplorePage.tsx:31` vs `:86-88`); **Mine, `/api/entities`** → body on fail and success, with and without seeded keys (`MinePage.tsx:59` vs `:152`). Working landings (ring, `data-landed`, 0 px scroll): Home 27.6 %, History 26.5 %, Explore-entities 26.1 %, Feed 26.5 %, Entity-feed 28.1 %, Entity-registry 28.0 %, Compose 26.5 %; plain touch rings 0/7; Timetable retry at 320×568 scroll samples `[0]`; History "Select" → composer `activeElement=(body)`.
- **[A-R9-21]** `pointer-events:none` on the pending pill: activate and wait → focus stays 2/2; one extra tap on the pending pill falls through to `DIV[role=tabpanel][tabindex=-1].feed-stream` and **focus stays there after the POST settles** 2/2; no announcement, `role=status` unmoved; `aria-pressed` flips optimistically at press time.
- **[A-R9-22]** Two dialogs only via script (`.click()` on an inert background opener); no user path.
- **[A-R9-23]** Appearance dialog opener is rail-only (`display:none` at ≤ desktop breakpoint) — unreachable at 390/320; at 1280 the grid is one row of 4 (96.5 px), swatch 97×81.
- **Required fields**: focus order Feed 15 / Timetable 13 / Settings 17 (7 of 17 off-screen when reached) / Compose editor 10 / Login 5 — every stop ringed. Keyboard reachability: all primary actions reachable except Settings "Export" (disabled by design), "Load more" (no control; IntersectionObserver fires incidentally) and pull-to-refresh (no control on any route). Landmarks (390): `navigation 2` + unnamed `main` + hidden unnamed `complementary` on every authenticated route; named regions per page as listed; `banner`/`contentinfo` 0; Explore search in no `search` landmark; title starts with h1 on 13/15; `/login` empty h1, `{main:1}`, skip link "Skip to sign-in". Touch (`hasTouch`, 15 routes incl. 404): **257/259 ≥44 at 390×844 and at 320×568**; the 2 sub-44 are the `aria-hidden` `input.daynav__picker` 5×4 (out of the Tab order); login checkbox 22×22 inside a 342×132 label.

## 6. Cross-pass reconciliations

- **Rest-state ring gone** — orchestrator ([O-R9-1] 0/22 buttons on three routes), Visual (0/117 on 60 loads, 134/134 stops differ), Accessibility (0/202 stops equal), Weight & Friction ([W-R9-28]) and Structural (no bare+state selector survives the gate) agree. Consensus: [O-R8-1] repaired.
- **Sheet skin restored** — orchestrator ([O-R9-2]), Visual (42/42), Accessibility, Weight & Friction, Structural agree; Reconnect at 320×568 scrolls 1 px (Visual) — recorded as residue.
- **Retry landings on Mine / Explore-directory** — Structural ([S-R9-8][S-R9-9]) and Accessibility ([A-R9-20]) independently measured `activeElement=BODY` and diagnosed the same causes (arming flag ≠ the flag the retry toggles; a cache hit keeps `loading` false). Accessibility adds the Timetable fail-again case (region only in the success branch). Consensus.
- **Report dialog focus drop** — Structural ([S-R9-33]) and Accessibility ([A-R9-17]) agree on the measurement and the cause (opener unmounted with the menu). Consensus.
- **Compact landing heading** — Visual ([V-R9-5], 6/6 in view) and Structural ([S-R9-15]) measured the first-lesson target; Accessibility ([A-R9-19], 12/12 clamped, h1 off 3/3, block 1 y=−10) measured the later-lesson target at 10:35 CST; Weight & Friction ([W-R9-8]) and Structural ([S-R9-18]) measured the same clamp on a date step to a sparse day. Reconciled: the fix holds when the target block is the first of the day; whenever the target is a later lesson the request exceeds the document's maximum scroll and the heading, note and daynav leave the viewport (block 1 at y=−10 at 390×620, the r8 −6 again). Clock-dependent — the "today" state a student sees after the first lesson ends.
- **First Tab at compact heights** — Visual ([V-R9-17]) and Accessibility ([A-R9-18]) agree. Consensus.
- **`?date=` at the mount date** — Structural ([S-R9-13][S-R9-14]) and Weight & Friction ([W-R9-5]) agree on measurement and cause (`searchParams` frozen by `replaceState`); Copy ([C-R9-15]) and Visual ([V-R9-5]) saw only the forward path, where it is correct. Consensus.
- **Modal effect re-run** — Structural's 3× un-inert ([S-R9-32]) was produced by a script-dispatched `honey:refresh`; Accessibility ([A-R9-16]) found no user path (swatch, typing, checkbox, Tabs), and Weight & Friction ([W-R9-3]) found a touch drag over the overlay does not reach `.main`. Recorded as a latent code fact, not scored.
- **Idle animation** — Visual 0/22 ([V-R9-15]) vs Weight & Friction 1/22 ([W-R9-18]): time-of-day dependent — the `/home` wash `transition: width 1s` restarts on every 1 Hz tick while a lesson is in progress; absent between lessons. Both measurements stand; the running state is the one a student sees during school hours.
- **Skip ring vs content** — Accessibility 0/24 on content border boxes ([A-R9-9]); Visual 1/8 per width ([V-R9-4]) where the Timetable's "20:00" hour label overflows `.view` by 6 px into the ring band. Recorded as one instance (the label, not a content row).
- **Loading state on Mine / Compose-lessonId** — Visual only ([V-R9-14]), this time with the endpoints named (`/api/entities`, `/api/directory`, `/api/history`); Structural's census shows Mine's empty branch rendering immediately with 0 keys (the mine feed is never requested). Compose-with-`lessonId` (`/api/history` delayed / aborted → no skeleton, no banner) stands as a single-pass finding with an identified endpoint; Mine's is explained by the empty-branch precedence and is not counted as missing.
- **Feed status** — Structural, Copy, Accessibility and Weight & Friction agree on the strings, the silent error, the cumulative load-more and the default-tab mismatch. Consensus.
- **Provenance / caveats / delete copy** — Copy only (source + rendered); no contradiction elsewhere.
- **Bundle / SW / headers** — Weight & Friction only; consistent with Structural's served-byte counts.
- **Touch targets** — Structural (528 boxes, sub-44 = 2 nested checkboxes) and Accessibility (257/259, sub-44 = the hidden picker) agree that no visible control is below 44.
- **Contrast** — Visual and Accessibility agree: 0 genuine failures, floors 4.66 / 5.83, portal palette intact on a dark boot.
