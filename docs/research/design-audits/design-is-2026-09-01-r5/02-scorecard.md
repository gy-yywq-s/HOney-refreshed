# 02 — Scorecard (r5; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `487c1c4`)

1. Good design is innovative — Score: 2/3 (r4 2, Δ0)
   Evidence: [C8] identity-free publish, no-author-column storage, browser-held ownership keys and category-only reports re-verified with zero drift (`api/client.ts`, `packages/backend/**`, `WhyPage.tsx` byte-identical since `5d810d8`); every UI pattern this round touched (focus landing hook, two-purpose dialog, row-of-three actions, per-route titles, tabpanel) is conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing shipped this round is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1.

2. Good design makes a product useful — Score: 2/3 (r4 2, Δ0)
   Evidence: [W7] feed → typing is still 4 screens / 3 taps with `activeElement` BODY on landing; the r4 blockers are gone — [V7][S-N1][A6] every compose action is hit-testable on landing at 320×568, [A3] the date picker has a named, operable fallback, [S-N5][A-N5] every "Try again" moves focus onto the replacing content 7/7; all three primary tasks complete on-screen ([S17][A3]). New detours on adjacent surfaces: [R1] on the primary screen at 320×568 the "No lessons today" answer is 219 px below the viewport on landing; [R6] the composer fails open on a registry error and offers an enabled Share for a never-listed key; [A-R5-A1] the History/Explore retry landings scroll the filters off-screen.
   Justification: Every primary task completes and no path requires a wrong-screen detour (rules out 1); the chooser → History → editor path still adds steps and the smallest supported viewport hides the empty-day answer until the student scrolls, so not "fewest possible steps; no decoy actions" (rules out 3).

3. Good design is aesthetic — Score: 1/3 (r4 1, Δ0)
   Evidence: The system verifies harder than ever — [V2] 97.83 % on-ladder at 320/390/430 with 0 author-level off-ladder values on mobile, desktop 92.8 %, [V4] 43 colours byte-identical, [V3] type unchanged, [V5] floor 4.66 / 5.83, [S4] 0 duplicate selectors, [V10] 0 element-pair overlaps, [V-N2][V-N3] dividers back and ring unclipped. Rendered inconsistencies now: [R1] the Timetable empty state drawn under the fixed nav at 390 and wholly off-screen at 320 on the primary screen's default view; [R2] two action labels breaking to two lines inside 47 px pills at 320; [R11] the never-listed page's CTA rendered as a bare 22 px inline link where the composer renders the same CTA as a 44 px primary button. Orphans: [R3] three status-chip classes emitted by the JS with no rule in the CSS, [R15] 10–11 consumer-less classes (4 dangling descendants of deleted base rules), 4 dangling comments, one uncommented literal.
   Justification: Anchor 1 is "3–5 inconsistencies OR one jarring violation" — three rendered inconsistencies, of which the empty state hidden behind chrome on the primary screen is not minor, so anchor 2's "≤2 minor inconsistencies" is exceeded on both count and severity, and anchor 3's "no orphan styles" is exceeded by the chip rules alone; 1 by count, not by tie.

4. Good design makes a product understandable — Score: 2/3 (r4 1, Δ+1)
   Evidence: Fixed and verified: [C7] "verified exposure" 0, `aria-label="Filter by name"`, "P1–P6 are the school's six lesson periods." rendered once and correct against the six period bands, "Nothing is listed at this address." with no kind-as-name on the entity page, the survivor link "Open the current entry for 213" no longer repeats the heading, both reconnect bodies match the real `portalTokenValid` state, 10 routes carry their own title, the feed tablist is complete. Remaining unclear control: [R4] the "Save school login" dialog's only submit reads "Reconnect". Jargon and copy residue: [C7] six connection verbs on one Settings card; [R9] the P1–P6 sentence sits under the nav / below the fold on landing; [R7] the composer says "Nothing is listed" for an id the entity page links to a survivor; [R13] two sentences for one idea twice (not-found; Sync clause) and entity/404/login titles generic.
   Justification: Every primary action is identifiable without help (not 0); exactly one control carries a label that names the other purpose, which is anchor 2's "1 control needs a tooltip", while anchor 1's "2–3 controls unclear" does not hold — r4 had two misleading labels plus the kind-as-name heading, all three of which verified fixed — so 2; the residual jargon and duplicated wording keep it from anchor 3's "names every primary control correctly" only by the one control, and the two-sentence residue bars a generous reading upward.

5. Good design is unobtrusive — Score: 2/3 (r4 2, Δ0)
   Evidence: [W4][V9] 0 idle animations on every screen under both motion preferences; [W5] 0 interrupts on load (the one cold `[role=status]` is the in-flow empty-day text); [V8][A7] overlays own the viewport; [S5] header Share hidden on error and empty; [W10] focus landings never steal focus or scroll on a normal load; but [W4] the 460 ms `settle` still replays on every route and a primary-styled Share still sits in the stream header in the normal state; [W6f] the SW silently re-downloads ~99 KB with `no-store` on the first navigation after every cold start.
   Justification: Chrome is quiet and functional (anchor 2); per-route entrance motion, a primary button in the stream chrome and uninvited network work at the moment the user is waiting keep the ground from fully receding (bars 3); nothing decorative competes with content (bars 1).

6. Good design is honest — Score: 2/3 (r4 2, Δ0)
   Evidence: [C8] every privacy claim re-verified 1:1 against unchanged code; [C4] 0 dark patterns by the rubric list; [C3] one degree-word inflation ("strongly protected", `WhyPage.tsx:73`). Fixed: the false "session ended" premise ([C-N6], both branches observed), the caveat at 4/4 opt-in sites, the Timetable re-auth clause 24 px from its button and present on error ([C-N7]), the Delete dialog naming Sync now, `contract.ts` true. Mismatches that remain or were introduced: [R8] the import disclosure — the one privacy-critical statement under the owner's decision — names "pull to refresh" as an import trigger and it is not one (0 `POST /api/sync` measured); [R4] "Save school login" submits "Reconnect"; [R7] composer and entity page contradict each other on the same delisted id; [R13] two wordings of one Sync clause; [C6-M5] "Open timetable" vs the "Tomorrow" chip (weak); [C9] `ReconnectDialog.tsx:16-19` and `BE/routes/auth.ts:30-31` comments false.
   Justification: Anchor 1 requires "2+ inflations OR one dark pattern" and neither holds — one inflation, and no forced-continuity, hidden-cost, scarcity or confirmshaming mechanic in any flow (the Save dialog's forced stay-connected is disclosed in its body); anchor 2's "≤1 minor inflation" is met; the disclosure that over-names a trigger and the four displaced or contradictory statements bar anchor 3's "every claim, badge, and label maps 1:1", so 2.

7. Good design is long-lasting — Score: 2/3 (r4 2, Δ0)
   Evidence: [V3][V4] single humanist face, one accent per surface, no gradients, no fad type, 43 colours unchanged for three rounds; [W4] the staggered per-route entrance choreography and frosted translucent surfaces remain the one plausible trend marker.
   Justification: One dated marker matches anchor 2 exactly; no skeuomorph residue or trend typography (bars 1); the entrance signature bars 3.

8. Good design is thorough down to the last detail — Score: 1/3 (r4 1, Δ0)
   Evidence: Fixed and verified: retry focus 7/7 with keyboard ring [A-N5], date ring unclipped + fallback [V-N3][A-N3], compose at 320 [V7], survivor link 349×72 and login checkbox 22×22 with the submit above the fold [V11][A6], composer skeleton [S-N18], menu Tab-out [S11], stepper dividers [V-N2], hero overlap [V10], `tsc` 0, 0 duplicate selectors, 100 % of Tab stops ringed, 0 contrast failures of 726 nodes ×2 [A1]. Rough states on the worst instance: **empty** — [R1] the primary screen's empty state is under the fixed nav at 390 and off-screen at 320 on landing; **error** — [R6] the composer has no error state for a registry failure (full editor, "Loading…" target, enabled Share, no alert), and [R5] the headline retry landing paints no ring for the pointer/touch user on all six surfaces, scrolls the filters away on two, is unnamed on five and a focus dead-end on two; **focus** — [R10] the ··· outside-click now parks focus on the feed container (two r4 fixes cancelling), [S-R13] `/settings#privacy` lands ringless, the skip link still no-ops on four routes. Detail cluster: [R4] Save dialog submit "Reconnect" with no checkbox; [R3] chips without rules; [R11] never-listed CTA 22 px; [R2] wrapped labels; [R12] SW activate eviction throws and the Dash chunk is still over-evicted; [R9] P1–P6 foot absent on error.
   Justification: Three states are rough on their worst instance (an empty state hidden behind chrome on the primary screen, an error branch that fails open with a live decoy, a focus system that loses the trigger and the ring for the primary input modality), which is past anchor 2's "1 state missing or rough"; the state framework exists on every surface and every r4 blocker verified fixed, so not 0.

9. Good design is environmentally friendly — Score: 3/3 (r4 3, Δ0)
   Evidence: [W1] app JS 87,863 B wire (85,069 br), under 100 KB; CSS −6.9 % raw; Dash chunk fetched on 0 of 12 student loads; [W4][A8] 0 idle animations on 11 screens, `infinite` bound only to the pull spinner and loading skeleton; [W9] prefers-color-scheme honoured pre-paint in six boot contexts with 0 later mutations; [W4] prefers-reduced-motion → 0 on every route; [W2] 12 requests cold, 3 warm, 0 duplicate API calls; [W6c] navigation-shell cache 374,893 B → 2,217 B. Relayed but outside the anchor: [R12] the SW's activate-time eviction throws, the current Dash chunk is deleted on every first navigation, ~99 KB is re-downloaded `no-store` per SW cold start, the deploy trigger keys on one filename, `sw.js` `max-age=14400`; 123,968 B of unused font subsets ship in `dist`.
   Justification: All four of anchor 3's conditions (<100 KB, no idle animation, dark mode honoured, reduced-motion respected) verify live for the third round running; the SW regressions are resource defects relayed to the plan, but the anchor is met as written, so 3.

10. Good design is as little design as possible — Score: 2/3 (r4 2, Δ0) — tie 2↔3, settled lower
    Evidence: [S1] census unchanged on every surface — the round added no control; r4's one same-screen removable (two buttons into one identical dialog) is resolved into two purposes with distinct titles and bodies [C-N6]; [S4] 0 duplicate selectors, one hidden-text utility, one `settle` rule. Candidates for a removable element on one screen: "Reconnect" rendered on a connected account whose portal token is valid, opening a dialog that offers to "refresh" a working connection (`SettingsPage.tsx:150`, `ReconnectDialog.tsx:36`); the ··· menu wrapping a single item "Report" (`ExperiencePost.tsx`); the fallback state showing the date button and the date input side by side [A-R5-A9]. Latent: header Share + interleaved invite at ≥9 posts (`FeedPage.tsx:33,160-166`). Repeated-affordance families 14 [S3], the additions being one hook rendered per screen and a "Delete…" label pair under two different section headings.
    Justification: No page is dominated by decoration or duplicated affordances (not 0); at most two same-screen elements could be removed without breaking a task, which is anchor 2's "≤2 removable elements"; whether the healthy-account "Reconnect" and the one-item menu "earn their place" is a genuine judgment call against anchor 3's "removing any one breaks the task", and the tie-breaker settles it at 2.

**Total: 19/30** (r4 18/30, Δ+1; r3 16/30; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #10. Principles scored with no tie: #1, #2, #3, #4, #5, #6, #7, #8, #9.
