# 04 — /make-plan handoff (r5)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 487c1c4). Current design failed audit at 19/30 with critical gaps in principles #3 aesthetic and #8 thorough (both scored 1/3; no principle scored 0).

Verdict paragraph (quoted from 03-verdict.md):
> The r4 handoff verified as built almost everywhere it could be measured — every "Try again" now lands focus on the content that replaced the banner, the date button's ring is a full ring with a named native fallback, the stepper's dividers are back, every compose action is hit-testable on landing at 320×568, the login submit sits above the fold with a 22 px checkbox, the survivor link is 72 px tall and names something the heading does not, the composer shows a skeleton and no editor while the registry loads, never-listed addresses are told apart from delisted ones on the entity page, "Save school login" and "Reconnect" open dialogs with their own titles and bodies whose premise matches the real token state, the re-auth clause sits 24 px under Sync now and survives the error branch, P1–P6 is explained once and correctly, "verified exposure" and "entities" are gone, ten routes carry their own title, the feed has a tabpanel, the stylesheets have zero duplicate selectors and 16 fewer orphans, and the navigation-shell cache shrank from 375 KB to one 2 KB entry — so HOney moves again, from 18 to 19/30, with #4 gaining a point as the two misleading labels and the kind-as-name heading verified fixed; but the score still says REDESIGN because the same wave again shipped a rendered defect on the primary screen (the Timetable empty state relocated from over the "Free" rows to under the fixed nav at 390 and wholly off-screen at 320), left the one remaining label unparameterized so the "Save school login" dialog submits "Reconnect" with no stay-connected control, gave the headline retry landing a ring that only paints for keyboard activation, deleted three status-chip rules the JS still emits, copied a false import trigger ("pull to refresh") from a backend comment into the app's single privacy-critical disclosure — a wording this audit's own r4 handoff dictated — and broke the service worker's activate-time eviction while trying to widen it; #3 and #8 therefore stay at 1 on new defects of the fix wave's own making, and the audit reports the number it measured.

Why redesign and not refine: total 19/30 is below the REFINE threshold of 20; one principle (#10) sat on a tie that was settled downward per the audit's convergence rule, so this is the first round where a single tie separates the verdicts — the rule commits to the lower score, and #3 and #8 are each at 1 on measured counts regardless of that tie, so this prompt is the REDESIGN template even though every move is a finish-the-detail move on a layer whose tokens, type, colour, spacing, motion budget, weight and trust architecture all verify for the third round running.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 320×600, 390×620, 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decision, not up for re-litigation: timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), and the plan may only make that disclosure more accurate. Verify everything in a real browser against the deployed app, not from screenshots alone. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop})`; probe scripts `r5-{struct,vis,copy,wf,a11y}-*.js` with `.log` files; `matrix.js` = 9-device regression) can be re-run to confirm each fix. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying. The test account has 0 ownership keys and a connected, valid portal token — seed `HOney.experiences.keys` and `honey.portal.cred` sentinels in localStorage (as the r4/r5 probes did) to reach Mine's populated/error branches and the saved-login UI.

Preserve from current design (MUST NOT be touched — these verified live on 487c1c4):
- Spacing system as it renders: 181 spacing lines on `var(--sp-*)` (212 usages) against 4 literal-px lines (apps/web/src/styles); rendered rhythm at 320/390/430 = 857 of 876 values on the 4/8/12/16/20/24/32/44 ladder (97.83 %), desktop 92.8 %; the only mobile off-ladder values being the commented 12+1 hit-area exception (features.css:1233-1240), the 96/90 px nav clearance (components.css:669, features.css:1192) and 1 px of UA padding on the aria-hidden date input. `.main` top = `var(--sp-4)`; modal sheet `var(--sp-5) var(--sp-4) calc(var(--sp-6) + safe-area)`; `.settings-trigger { padding: 0 }` (components.css:487); desktop `.main` `var(--sp-7) clamp(22px,5vw,76px) 110px` with its inline comment (components.css:527); nudge sheet tokenized (features.css:806). Regression check: re-run the rendered spacing census at 390/320/430; on-ladder share must not drop below 97 % and no 2/9/10/14/18/22/26/30 px value may appear; desktop must not drop below 92 %.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, no fractional at 390/320; 11 off-ramp `font-size` declarations. Regression check: rendered-font-size census, nothing below 12 px, no new off-ramp declaration.
- Palette and accent discipline: 43 colour literals, byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; `.nextlesson__state` 11.52:1 stone / 9.80:1 night; date button 13.98/13.75; the new `.timetable-foot` and `.daynav__note` 5.34/7.26; the "Nothing is listed at this address." h1 13.26/15.27; the Save-login dialog body 5.63/7.27; the focus-landing ring 5.81–6.13 against inside/parent/page on stone, 8.36–9.28 on night. Regression check: rerun the live contrast probe on stone and on a fresh `colorScheme:dark` context with no stored theme; 0 enabled text below 4.5:1 on either (current floor 4.66 stone / 5.83 night, 726 nodes each); every focus ring ≥3:1 against adjacent colours.
- Zero idle motion: `document.getAnimations().length === 0` at 2.5 s after settle on every screen under both motion preferences; `infinite` keyframes only on the pull spinner (components.css:582) and loading skeleton (:861); 6 `@keyframes` total; `.view` is ONE rule (components.css:529) carrying `animation: settle .46s var(--ease) backwards` plus layout — do not split it again; prefers-reduced-motion collapses durations. Regression check: getAnimations() on all 11 screens with and without reduced-motion; `grep -n '\.view' apps/web/src/styles/*.css` returns one `.view {` block.
- Weight: app JS 87,863 B wire / 85,069 br / 270,886 raw (index-*.js), CSS 10,479 wire / 41,641 raw, Dash chunk 4,403 br loaded only on /dash (0 of 12 student loads); 12 requests cold /home (3 app-origin data), 3 warm to /experiences, `GET /api/entities` exactly once per entity page; 0 long tasks in 25 loads; no new dependency. Regression check: curl br sizes under 100 KB; DashPage never fetched on student routes; request counts unchanged.
- Boot script honouring prefers-color-scheme pre-paint (index.html) with stored-choice-wins in both directions, garbage fallback to stone, and 0 later `<html>` mutations in six contexts; SW `honey-v3` content-type guard (public/sw.js:81-82) refusing to cache 200-HTML under /assets/; `/api/` never cached; navigations network-first; navigation shells capped to one `/` entry (sw.js:94-98); offline shell served in 68 ms.
- Route-settle containment: lesson modal and report dialog overlays equal the viewport at 390 and 1280, nav not hit-testable beneath, focus trap + Esc + opener restore on all five dialogs (lesson, report, reconnect, save, delete-confirm).
- Focus system: 3 px accent outline offset 3 on 100 % of ~150 Tab stops across 15 route states; Timetable Tab order Previous day → date button (one stop, 268×44, `aria-label="Pick a date (…)"`, input `tabIndex -1 aria-hidden` while `showPicker` exists) → Next day → Back to today → History → Sync now → lesson block → nav; `.daynav__stepper` with NO `overflow:hidden` and the end radii on `.daynav__arrow:first-child/:last-child` (features.css:162-167); the `showPicker`-unavailable fallback that makes the input `.daynav__picker--visible` 162×44, `tabIndex 0`, named, focused (TimetablePage.tsx:84-111, features.css:202-220); one `.daynav__date` rule with `border-left/right: 1px solid var(--line)` (features.css:183-202); scope tablist with arrow keys, exactly two tabs fully visible; feed tabs `#tab-my-classes`/`#tab-school` with `aria-controls="feed-panel"` and one `role=tabpanel` whose `aria-labelledby` follows selection (FeedPage.tsx:79-80,90-91,108-116); `/settings#privacy` focuses `h2#privacy`; `useRetryFocus` (lib/useRetryFocus.ts) arming only from the seven "Try again" handlers — landings never focus on a normal load, never in the Tab order, never scroll on a normal load; ··· menu `focusout` close on Tab-out with fresh `aria-expanded` (ExperiencePost.tsx:123-131) and Shift+Tab onto the trigger keeping it open.
- Login: `.login { height:100dvh; flex-direction:column; overflow-y:auto }` + `.login__doorway { margin:auto }`; wordmark top +32 at 320×568, centred at 390/430/1280; checkbox 22×22 (components.css:881-885) inside the `<form>` before the submit via `SchoolLoginForm`'s `beforeSubmit` slot; submit `[24,488,272,48]` bottom 536 < 568 at 320×568; the shortened opt-in copy "Stay connected on this device. Portal time-outs reconnect on their own. Your login is encrypted and kept only here (a browser is less protected than a phone's secure storage). Turn it off in Settings." (LoginPage.tsx:53-55); skip link "Skip to sign-in" → `#school-username`; footnote "…created on first sign-in. Your timetable comes along with it." (LoginPage.tsx:62-63).
- Settings: the static "Timetable import" disclosure row (SettingsPage.tsx:195-203) in "Imported data" above "Delete imported data" — keep it static, keep the date claim "since 1 September 2026" (consistent with migration 009 and `/api/me grantedAt`), correct only the trigger list (see Discard); Delete dialog "You can import again with Sync now." (:286); "Sync now" (:147) → `api.syncSeamless()` with its re-auth clause in the same row; `ReconnectDialog` `purpose`/`sessionExpired` props and `SettingsPage.tsx:321-323` passing `sessionExpired={!connection.portalTokenValid}`; the three bodies (ReconnectDialog.tsx:33-36) — save "Enter your school login once; it stays encrypted on this device (a browser is less protected than a phone's secure storage) so routine portal time-outs reconnect on their own.", reconnect-valid "Sign in again to refresh the school connection — your HOney data stays as it is.", reconnect-expired "The portal session ended. Sign in again to restore it — your HOney data stays as it is."; the reconnect purpose's checkbox inside the form before the submit with the caveat (:44-55); the caveat at all four opt-in sites (LoginPage.tsx:54-55, SettingsPage.tsx:169, ReconnectDialog.tsx:33,52-53); one key name "ownership key"; "scrambled at rest (unreadable without this device's key)" glossed; "Turn off" rendering with a saved login.
- Timetable: `.daynav__note` "Sync now signs in again with your saved school login if the portal session expired." 24 px under "Sync now", gated only on `portalCredentials.isAuthorized()`, present in the error branch (TimetablePage.tsx:138-142, features.css:214-217) — keep the placement, unify the wording with Settings (see Discard); `.timetable-foot` "P1–P6 are the school's six lesson periods." (TimetablePage.tsx:181-183), correct against the six period bands — keep the sentence, move it (see Discard); `.timeline__empty` `role="status"` (TimetablePage.tsx:375) — keep the role, fix the placement (see Discard); backend placeholder rooms serialized as no room.
- Error states: all seven surfaces render `role=alert .banner--danger` with the same `btn--ghost btn--small` "Try again" 77×44 that fires exactly one refetch and then focuses a `.focus-landing[tabindex=-1]` (HomePage.tsx:66,80; TimetablePage.tsx:168,174; HistoryPage.tsx:107,113; ExplorePage.tsx:83,88; MinePage.tsx:130,170; FeedPage.tsx:109,125; EntityPage.tsx:81,132) — keep the hook and the landing, fix the ring/scope/name (see Discard); feed header Share hidden on error and empty (FeedPage.tsx:33).
- Compose: the registry-pending skeleton with no editor (ComposePage.tsx:196-204: h1 + `Skeleton lines={4}` `role=status aria-label="Loading"`, ~19 ms on a warm registry) — keep it, add the error branch (see Discard); course targets captioned "Course"; footer link "How privacy works" 102×44 → /settings#privacy; the `@media (max-height:620px) and (max-width:960px)` row layout that keeps every action above the nav at 320×568/320×600/390×620 (features.css:1275-1287) — keep the clearance, fix the wrap (see Discard).
- Explore: complete-set rule (every section rendered with counts, search only filters, 19 rows at 390 and 320, `Food 0` rendered); the `markable` rule and the `<span class="sr-only">, </span>` separator (ExplorePage.tsx:170) with `.sr-only` (foundations.css:89-99) as the ONE hidden-text utility; `aria-label="Filter by name"` (ExplorePage.tsx:75); "Find someone or something" at all sites.
- Entity: never-listed ids (absent from the registry AND unnamed by `/api/directory`, EntityPage.tsx:53-60) render h1 "Nothing is listed at this address." with no overline, no kind-as-name and no Share CTA (EntityPage.tsx:69-77) — keep the state, fix the CTA size (see Discard); delisted ids with a name render the overline, the name, "This entry is no longer listed." and, for a same-name duplicate, "Open the current entry for {name}" 349×72 via `.entity-survivor` in the hit-area rule (EntityPage.tsx:105-112, features.css:1236-1240); `GET /api/entities` once.
- Mine: header Share hidden when empty — one CTA "Share your first experience".
- Post ··· menu: `aria-haspopup="menu"`, `aria-controls`, `aria-expanded`, `role=menu`/`menuitem`, focus on "Report" on open, Escape closes and returns focus, Tab-out closes; Report item `min-height:44px`; the `not_eligible` note "Reactions are open to students who have had the same class or place." (ExperiencePost.tsx:163).
- Titles: `AppLayout.tsx:36-47` per-route `document.title` "<Name> · HOney" on Home/Timetable/History/Settings/Dash/Experiences/Find someone or something/Share an experience/Your notes & posts/Why this space exists, mutating on SPA navigation — keep the map, add the missing branches (see Discard). PTR disc `aria-label` "Refreshing" while refreshing (PullToRefresh.tsx:103).
- Clean build: `tsc --noEmit --noUnusedLocals` = 0 errors; 0 same-context duplicate selectors across the four stylesheets; `packages/shared/src/api/contract.ts:12-16` "Login carries ONLY identity. Import is part of the account (2026-09-01)…" (true).
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:229,351-353; contract.ts:311-316), no author column (packages/backend/src/db/database.ts:103-121), 6 = 6 report categories, "no free text is collected" ↔ `{category}` only, the HOney ID line (SettingsPage.tsx:72-75), WhyPage "design boundary, not magic".

Discard (structural patterns causing the failures):
- Empty state pinned where the nav is: `.timeline__empty { position:absolute; left:0; right:0; bottom:var(--sp-3) }` (features.css:440-448) — on the default Timetable (today, six free bands) at 390×844 the text `[170,765,106,23]` sits under `nav.mobile-nav` `[10,770,370,64]` (`elementFromPoint` = nav; only the glyph and ~5 px of the label visible), at 320×568 the whole box is 219 px below the viewport. Caused failure on principles #3, #8 and #2.
- Compact compose row that wraps its labels: `@media (max-height:620px) and (max-width:960px){ .compose-actions{flex-direction:row} .compose-actions .btn{flex:1 1 0; padding-inline:var(--sp-2)} }` (features.css:1275-1287) — "Share anonymously" → "Share / anonymously" and "Keep private" → "Keep / private" in 103/85 px pills 47 px tall at 320×568 and 320×600; "Share anonymously" wraps at 390×620. Caused failure on principle #3.
- CSS rules deleted while the JS still emits the class: `.chip--ok`, `.chip--danger`, `.chip--muted` removed from components.css:220-228 by 1f87fae; `MinePage.tsx:244` renders `chip chip--${meta.tone}` with tones from `MinePage.tsx:21-38` — Published/Hidden/Removed fall back to the base `.chip`. Caused failure on principles #3 and #8.
- Orphan descendants of deleted base rules and dangling comments: `.segmented button[aria-pressed="true"]` (components.css:780), `.sort-toggle__btn` inside its `@media` (:840), `.primary .card-index` / `.primary .card-copy` (foundations.css:229-230), `.stat` / `.stat span` (foundations.css:203,220), plus `.action-card .lift .btn--secondary .section-head__copy .reading .report-option .is-in`; comments features.css:1266, components.css:268,811, foundations.css:234 describing deleted rules; the uncommented 90 px literal (features.css:1192). Caused failure on principle #3.
- Retry landing ring bound to `:focus-visible` only: `.focus-landing:focus-visible { outline: 3px solid var(--accent); outline-offset: 3px }` (features.css:1291-1296) with `useRetryFocus` focusing programmatically after a click (lib/useRetryFocus.ts:11) — after a pointer/touch "Try again" all six reachable surfaces show `outline: 3px none`; only keyboard Enter paints the ring. Landings on History (HistoryPage.tsx:107) and Explore (ExplorePage.tsx:88) are the whole page (1780/1254 px) so focusing them scrolls the search and filters off-screen (+174/+278 px); Timetable/History/Explore/Mine/Entity landings are bare `div[tabindex=-1]` with no role and no name; Timetable and History landings contain nothing focusable (next Tab → nav); the feed landing is one node carrying `role=tabpanel` + `aria-live=polite` + `tabindex=-1` with the `role=alert` banner nested inside (FeedPage.tsx:108-116). Caused failure on principle #8.
- Composer that fails open on a registry error: `ComposePage.tsx:197` guards only `!entities.data && !entities.error`; with `/api/entities` failing the full editor mounts under a target that reads "Loading… / Teacher", Share and Keep private enable after typing, no `role=alert`, and the unlisted guard is bypassed for `teacher:t_doesnotexist`. Caused failure on principles #8 and #2.
- Outside-click focus return that the pointerdown default overrides: `ExperiencePost.tsx:117-122` calls `moreBtnRef.current?.focus()` in the outside-pointerdown handler, then the browser focuses the click target's nearest focusable ancestor — the feed's new `tabindex=-1` container — so 0/3 outside clicks return to `···`; no ArrowDown/Up. Caused failure on principle #8.
- Dialog with one submit verb for two purposes: `ReconnectDialog.tsx:41` passes `mode="reconnect"` unconditionally and `SchoolLoginForm.tsx:66` hard-codes "Reconnect", so the dialog titled "Save school login" submits "Reconnect"; `:43` renders the stay-connected control only for `purpose === "reconnect"` while `:28` forces `stayConnected = true` for save; the header comment `:16-19` claims both the submit and the choice are parameterized; `Modal.tsx:54` names dialogs by `aria-label` with no `aria-describedby`, so the sentence that distinguishes the two purposes is never announced. Caused failure on principles #4, #6 and #8.
- Import disclosure naming a trigger that does not import: SettingsPage.tsx:199-201 "…and again whenever you press Sync now or pull to refresh." — pull-to-refresh calls `emitRefresh()` (PullToRefresh.tsx → lib/refresh.ts) which only re-reads via `useApi`; measured on /timetable, /home, /history: 0 `POST /api/sync`; the wording came from the false comment packages/backend/src/routes/auth.ts:30-31 (and from the r4 audit handoff, which was wrong). The real triggers are account creation (auth.ts:29-33) and Sync now (SettingsPage.tsx:128, TimetablePage.tsx:134 → `api.syncSeamless()` → `POST /api/sync`). Caused failure on principle #6.
- Composer that collapses delisted into never-listed: `ComposePage.tsx:191-203` computes `unlisted` from `/api/entities` alone and never consults `useNames()`/`/api/directory` the way `EntityPage.tsx:53-60` does, so `room:r_d48ff4b2f68a` reads "Nothing is listed at this address, so nothing can be shared about it." with no survivor link while `/experiences/room/r_d48ff4b2f68a` reads "This entry is no longer listed. Open the current entry for 213". Caused failure on principles #4 and #6.
- Two sentences for one fact, twice: "Nothing is listed at this address." (EntityPage.tsx:72) vs "Nothing lives at this address." (NotFoundPage); " · Sync signs in again with your saved school login if the portal session expired." (SettingsPage.tsx:118) vs "Sync now signs in again with your saved school login if the portal session expired." (TimetablePage.tsx:140). Caused failure on principle #4.
- Explanation placed after what it explains: `.timetable-foot` "P1–P6 are the school's six lesson periods. Last synced …" (TimetablePage.tsx:181-183, features.css:206-208) renders below the timeline at `[16,800,358,18]` — under the nav at 390×844 on landing (`elementFromPoint` = nav item), below the fold at 320×568 — and only in the success branch (absent on error). Caused failure on principle #4.
- Title map without an entity branch: `AppLayout.tsx:45` `p.startsWith("/experiences")` catches every `/experiences/<kind>/<id>` → "Experiences · HOney" (h1 "ChenJenny", "213", "Nothing is listed at this address."); `/login` and the 404 render outside `AppLayout` → "HOney". Caused failure on principle #4.
- Never-listed CTA outside the button system: `EntityPage.tsx:74` renders "Find someone or something" as a bare inline `<a>` in `<p class="muted">` — 189×22 at 390 and 320, outside the 12+1 hit-area rule — while `ComposePage.tsx:212-214` renders the same CTA as `btn btn--primary` 220×44. Caused failure on principles #3 and #8.
- AT names that cannot be heard and landings without rings: `.ptr__disc[role=status]` gets `aria-label="Pull down to refresh"` (PullToRefresh.tsx:103) inside a wrapper that is `aria-hidden="true"` in exactly that state (:98); the `not_eligible` note (ExperiencePost.tsx:163) has no `role`; the hidden date input carries `aria-label="Pick a date"` while `aria-hidden="true"` (TimetablePage.tsx:103-104) and the fallback shows the button and the input side by side with near-identical names; `h2#privacy` lands with `outline-style: none` (SettingsPage.tsx:19-27); `#main` has no `tabindex`, so "Skip to content" lands on BODY on Home/Feed/Compose/Mine (the four routes whose content does not overflow) and the 404 has no skip link; modal background carries neither `inert` nor `aria-hidden` under any of the five dialogs; `banner`/`contentinfo` 0 on every route. Caused failure on principle #8 (and #4 for the unheard names).
- Service-worker eviction that throws, over-evicts and re-downloads: `sw.js:56-63` `.then(evictStaleAssets)` hands `Promise.all`'s resolved array to the new `html` parameter (`sw.js:36`) — truthy, so `if (!html)` (:38) skips the index fetch and `html.match` (:25) throws a `TypeError` swallowed at :50; seeded stale hashes all survive install+activate (r4's parameterless version evicted them). The chunk regex `sw.js:31` requires an `assets/` segment; Vite emits `import("./DashPage-PRZwOVZX.js")`, 0 matches, so the current Dash chunk is deleted on the first controlled navigation (fonts now survive). `lastIndexSig` (`sw.js:54`) is a module global that resets whenever the idle worker is terminated, so the next navigation re-fetches `index-c6M0Emr3.js` + `index-D-vVlHpE.css` with `cache:"no-store"` — 98,712–100,606 B on the navigation critical path per SW cold start. The deploy trigger `sw.js:102` compares only `seed.split("|")[0]` (today the CSS), so a JS-only deploy would not evict. `sw.js` is served `max-age=14400`. Caused a resource defect relayed under principle #9 (anchor still met) and an unobtrusiveness defect under #5.
- Same-label pair and single-item menu (tie-register items under #10, optional): "Delete…" ×2 on Settings (account, SettingsPage.tsx:101; imported data, :213) with the same label and style under different headings; "Reconnect" rendered on a connected account whose token is valid (SettingsPage.tsx:150) offering to "refresh" a working connection; the ··· menu wrapping the single item "Report". Kept #10 from 3.
- Same-screen duplicate at scale (code only, 1 post live): header Share + interleaved invite co-render at ≥9 posts (FeedPage.tsx:33,160-166, `SHARE_PROMPT_EVERY = 8`). Latent failure on principle #10.

Top 4 moves from the audit (verbatim):
1. #3 aesthetic + #8 thorough — Put the primary screen's empty state and the compact compose row on the ladder, and restore the rules the JS still needs. Render `.timeline__empty` in flow (or pinned to the top of the timeline under the daynav) instead of `position:absolute; bottom:var(--sp-3)` (`features.css:440-448`) so "No lessons today" is visible on landing at 390×844 and 320×568 without being drawn under `nav.mobile-nav` (today: text `[170,765,106,23]` vs nav `[10,770,370,64]`; 219 px below the viewport at 320); keep the `role="status"` (`TimetablePage.tsx:375`). Give the row-of-three compose actions room so no label wraps at 320 (`features.css:1275-1287` — `padding-inline:var(--sp-2)` in 103/85 px pills breaks "Share anonymously" and "Keep private" to two lines; use two rows of 44 px — Share full-width above Keep private + Cancel — or a smaller `--fs` step for the compact query, not a wrap). Restore `.chip--ok`, `.chip--danger`, `.chip--muted` (deleted from `components.css:220-228` by `1f87fae` while `MinePage.tsx:244` emits `chip chip--${tone}`) or drop the tone from the JS — one or the other, verified against a seeded Mine. Delete the four dangling descendants (`components.css:780,840`, `foundations.css:229-230`), the remaining orphans (`.action-card .lift .btn--secondary .section-head__copy .reading .report-option .is-in .stat .stat span`), the four dangling comments (`features.css:1266`, `components.css:268,811`, `foundations.css:234`), and comment the 90 px literal (`features.css:1192`). Evidence: [R1][R2][R3][R15].
2. #8 thorough + #2 useful — Finish the retry landing for the student who taps, and close the composer's open error branch. Paint the landing ring on programmatic focus regardless of `:focus-visible` (add `.focus-landing:focus { outline: … }` alongside the `:focus-visible` rule at `features.css:1291-1296`, or set a `data-landed` attribute from `useRetryFocus` and style that) — today after a pointer "Try again" all six reachable surfaces show `outline: 3px none`; make the History and Explore landings the result list rather than the whole page (`HistoryPage.tsx:107`, `ExplorePage.tsx:88` — focusing them scrolls the search and filters off-screen by 174/278 px and shows only two ring edges); give every landing a role and name (`role="region" aria-label="…"` or focus the first result) — Timetable/History/Explore/Mine/Entity are bare `div[tabindex=-1]`; give the Timetable and History landings something focusable or focus the first lesson/row so Tab does not leave the content; split the feed's one node (`FeedPage.tsx:108-116` is tabpanel + `aria-live=polite` + landing with the `role=alert` banner inside it). In the composer, treat `entities.error` as its own state (`ComposePage.tsx:197` guards `!data && !error` only): a `role=alert` banner with "Try again" and no editor, never an enabled Share behind a "Loading…" target. Return focus to `···` on outside close in a way the pointerdown default cannot override (`ExperiencePost.tsx:118-120` — `preventDefault` on the outside pointerdown, or focus in a microtask after the default) and add ArrowDown/Up. Give `h2#privacy` a visible ring on the hash landing (`SettingsPage.tsx:19-27`; `:focus-visible` false today) and `#main` a `tabindex="-1"` so the skip link lands on Home/Feed/Compose/Mine (BODY today). Evidence: [R5][R6][R10][S-R13][A3][A-R5-A1..A4].
3. #4 understandable + #6 honest — One verb per dialog, one sentence per fact, and a disclosure that names only real triggers. Parameterize the submit label with the purpose (`ReconnectDialog.tsx:41` passes `mode="reconnect"` for both; `SchoolLoginForm.tsx:66` hard-codes "Reconnect") so "Save school login" submits "Save login"; render the stay-connected control inside the form for the save purpose too, checked by default, with the caveat (`:43` gates it on `reconnect`; `:28` forces `true`) — or, if the owner wants no choice there, say so in the body and fix the header comment (`:16-19`); add `aria-describedby` to the dialog body on both purposes (`Modal.tsx:54` names by `aria-label` only). Correct the Settings import row (`SettingsPage.tsx:199-201`) to "…imported from the school portal when your account is created, and again whenever you press Sync now." — pull-to-refresh issues no `POST /api/sync` on any surface (measured; the r4 handoff's wording was wrong, and so is `BE/routes/auth.ts:30-31`, which should be corrected in the same change). Make the composer consult the directory the way the entity page does (`ComposePage.tsx:191-203` vs `EntityPage.tsx:53-60`) so a delisted id says "This entry is no longer listed" with the survivor link, and only an unknown id says "Nothing is listed at this address"; use one sentence for not-found across `EntityPage.tsx:72` and `NotFoundPage` ("Nothing is listed at this address." / "Nothing lives at this address."); use one Sync clause across `SettingsPage.tsx:118` and `TimetablePage.tsx:140`; move the P1–P6 sentence above the timeline (`TimetablePage.tsx:181-183` renders it after, under the nav at 390 and below the fold at 320) and render it in the error branch; add an entity branch to the title map (`AppLayout.tsx:45` — "213 · HOney", "ChenJenny · HOney", "Not found · HOney") and set a title on `/login` and the 404 outside the layout; make the never-listed page's CTA the same 44 px primary button the composer uses (`EntityPage.tsx:74`, 189×22 today); expose the PTR idle name (`PullToRefresh.tsx:98` `aria-hidden` while `:103` names it) or drop it; give the `not_eligible` note a `role=status` (`ExperiencePost.tsx:163`). Evidence: [R4][R7][R8][R9][R11][R13][R14].
4. #9 environmentally friendly (relay; anchor met) — Make the service worker's eviction run when it should, keep what is live, and stop the cold-start re-download. In `activate` call `evictStaleAssets()` with no argument (`sw.js:56-63` currently hands it `Promise.all`'s array → `html.match` throws → nothing is evicted); make the chunk regex match Vite's relative `import("./DashPage-….js")` (`sw.js:31` requires an `assets/` segment; resolve matches against the importing file's URL) so `DashPage-PRZwOVZX.js` survives; persist the index signature in the cache (e.g. a `/__index-sig` entry) instead of the module-global `lastIndexSig` (`sw.js:54`) so a SW cold start does not re-fetch ~99 KB of JS+CSS with `cache:"no-store"` on the navigation critical path; compare the whole sorted signature, not `seed.split("|")[0]` (`sw.js:102`), so a JS-only deploy evicts; when building the live set read the JS/CSS bodies from the cache before falling back to the network; serve `sw.js` with `cache-control: no-cache` (hostd manifest). Evidence: [R12][W6].

Redesign principles in priority order:
1. #3 aesthetic — success: "No lessons today" visible on landing at 390×844 and 320×568 with nothing drawn under the nav; no compose label wraps at any of 320×568 / 320×600 / 390×620; every class the served JS emits has a rule (`chip--ok/--danger/--muted` restored or removed from the JS), zero consumer-less class names, zero dangling comments, every spacing literal commented; the rendered census stays ≥97 % mobile / ≥92 % desktop with 43 colours and the type set unchanged; 0 element-pair overlaps.
2. #8 thorough — success: after a pointer "Try again" the landing shows the 3 px accent ring on all seven surfaces; no landing scrolls the page's controls away; every landing has a role and an accessible name and contains (or hands focus to) something focusable; the composer shows a `role=alert` with "Try again" and no editor when the registry fails; the ··· menu returns focus to its trigger on every close and supports ArrowDown/Up; `h2#privacy` and `#main` land with a visible ring; the Save dialog carries the form-order stay-connected control (or an explicit statement that there is none) and an `aria-describedby` body; every tappable element ≥44 px including the never-listed CTA; `tsc` 0; no `overflow:hidden` pill around a focus stop.
3. #4 understandable — success: the "Save school login" dialog's submit says what it does; the composer and the entity page say the same thing about the same id; one not-found sentence, one Sync clause; the P1–P6 explanation is visible before the labels it explains at both viewports and on error; every route including entity pages, `/login` and the 404 has a title that names it; PTR and `not_eligible` names are exposed.
4. #6 honest — success: the import disclosure names exactly the real triggers (account creation, Sync now) and nothing else, with the date claim intact; the two comments that carried the false trigger (`BE/routes/auth.ts:30-31`, `ReconnectDialog.tsx:16-19`) are corrected; no dialog label names the other purpose; "Open timetable" opens the day the chip names or the chip stops naming a day.
5. #9 environmentally friendly (keep at 3) — success: the SW evicts stale hashes at activate (verified with seeded fakes), never deletes the current font subsets or the Dash chunk, evicts on a JS-only deploy without an `sw.js` edit, performs no `no-store` re-download of current assets after a cold start, keeps one navigation shell, and the bundle stays under 100 KB wire with 0 idle animations.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — the Timetable empty state's placement relative to the band rows and the daynav; the compose action layout under 620 px height (two rows vs a smaller type step); the composer's third state (registry error) beside pending and resolved; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): Timetable landing on an empty day at 320×568 and 390×844 from first paint; compose at 320×568 from landing to Share/Keep private/Cancel with unwrapped labels; error → Try again → landing with ring, by tap and by keyboard, on each of the seven surfaces; Settings "Save school login…" → dialog → submit label → "Turn off" row; composer at a delisted key vs a never-listed key vs a registry failure.
- States checklist (empty, loading, error, success, focus, disabled) per screen, with the empty state drawn in its viewport at both sizes, the retry landing drawn per surface with its ring under pointer activation, the composer's three registry states drawn separately, and the Save and Reconnect dialogs drawn with their submit labels.
- Migration path for users currently on the old design: none for routes or data; ship an `sw.js` byte change with this deploy and verify with seeded stale entries that activate-time eviction runs and the current font subsets and Dash chunk survive.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (empty state visible on landing at 320 and 390 with `elementFromPoint` on its text ≠ nav, no wrapped compose label at 320×568, chip rules present for every emitted tone, retry ring after a pointer tap on all seven surfaces, landings named and non-scrolling, composer registry-error state with no editor, ··· focus return on outside click, Save dialog submit label ≠ "Reconnect", import row without "pull to refresh", composer/entity agreement on a delisted id, entity titles, P1–P6 above the timeline, rendered spacing census ≥97 %, getAnimations() 0, contrast on a genuine dark boot, SW activate eviction with seeded fakes, Dash chunk surviving, no `no-store` re-fetch after `ServiceWorker.stopWorker`).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the absolutely-positioned empty state, the `:focus-visible`-only landing ring, the `!data && !error` composer guard and the one-verb `SchoolLoginForm` mode must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients, or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the spacing census, the dark-boot contrast probe, the 320×568 timetable/compose/login sweeps and the SW seeded-eviction probe, after every CSS or SW change.
- Deleting a rule without grepping the JS for its class name — that is how this wave removed `.chip--ok/--danger/--muted` while `MinePage.tsx:244` still emits them; run the reverse sweep (classes applied in TSX with no rule) before and after.
- Moving a defect instead of removing it — the r4 fix moved `.timeline__empty` from over the band rows to under the nav; measure the fixed element's box against the nav and the viewport at 320×568 and 390×844 before calling it done.
- Fixing a surface without fixing its siblings — the r4 wave gave the entity page the never-listed/delisted distinction but not the composer, and parameterized the dialog's title and body but not its submit; the plan must enumerate every site of a repeated affordance before changing one.
- Writing copy from a comment instead of from the code — the "pull to refresh" trigger came from `BE/routes/auth.ts:30-31` and from the r4 audit handoff; every trigger named in a disclosure must be traced to the request it sends (`POST /api/sync`) and measured in the browser.
- Adding a parameter without updating every caller — `evictStaleAssets(html)` gained a parameter and the `activate` chain silently handed it the wrong value; test the SW's activate path with seeded stale entries, not by reading the code.
- Claiming a change in the commit message that the diff does not contain — verify each move on the deployed app before reporting it done.
````
