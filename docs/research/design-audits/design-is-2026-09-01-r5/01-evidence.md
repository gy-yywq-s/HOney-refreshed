# 01 — Evidence (r5, consolidated from five verification subagent passes, all run on opus)

Audited live at https://honey.gaelisus.com (integration/product-v2, commit `487c1c4` on `1f87fae` on `5d810d8`; served `index-c6M0Emr3.js` / `index-D-vVlHpE.css`, matching `apps/web/dist` built from HEAD; live `sw.js` byte-identical to `public/sw.js`, cache name still `honey-v3`). All dynamic evidence was measured by operating the app in Playwright/Chromium: 390×844 (primary), 320×568, 320×600, 390×620, 430×932, desktop 1280×800; "genuine dark boot" = fresh context, `colorScheme: dark`, no stored theme. Source paths relative to `/home/honey/HOney-refreshed/apps/web/src` unless noted; `BE` = `packages/backend/src`. Probe scripts `r5-{struct,vis,copy,wf,a11y}-*.js` (+ `.log`) and screenshots `r5-*.png` retained at `/root/claude-work/design-audit/` (the three 20:33 `r5-*.png` are the implementer's and were not used). Constraints honored by every pass: nothing published, no report submitted, one reaction set during the keyboard sweep and undone (verified `aria-pressed=false`, count 0), no Sync/Reconnect/Save/Disconnect/Delete/Turn off/Sign out pressed (dialogs opened and cancelled only), no secrets read (`.session.json` expired mid-run in three passes and was re-minted through `lib.js`; never printed). `tsc --noEmit --noUnusedLocals -p tsconfig.json` → 0 errors. `git diff --stat 5d810d8..487c1c4`: 20 web source files, `public/sw.js`, `packages/shared/src/api/contract.ts`; `packages/backend/**` and `api/client.ts` and `WhyPage.tsx` byte-identical; no dependency change.

Anchors `[S…]` Structural, `[V…]` Visual, `[C…]` Copy & Honesty, `[W…]` Weight & Friction, `[A…]` Accessibility; `[R…]` = defects first seen this round. Owner's standing product decision (00-scope): timetable import is part of the account — no consent gate, no switch; only the disclosure's accuracy is scored.

## r4 handoff verification (all passes)

| r4 move | Status | Evidence |
|---|---|---|
| M1 retry focus lands on the replacing content, 7 surfaces | **VERIFIED-FIXED (focus) / PARTIAL (ring)** | `lib/useRetryFocus.ts:1-15`; `.focus-landing[tabindex=-1]` at `HomePage.tsx:66`, `TimetablePage.tsx:174`, `HistoryPage.tsx:107`, `ExplorePage.tsx:88`, `MinePage.tsx:130`, `FeedPage.tsx:109`, `EntityPage.tsx:81`; 7/7 + Entity-empty-refetch land on the landing (0 BODY) [S-N5][A-N5]. Ring `3px solid rgb(51,102,124)` offset 3 only on keyboard activation; after a **pointer/touch** "Try again" `:focus-visible=false`, computed `outline: 3px none`, on all 6 reachable surfaces [V-R5][S-R3]. Landings never focus on a normal load, never in the Tab order [S][A][W10] |
| M1 feed landing ringless | **VERIFIED-FIXED (keyboard)** | `features.css:1291-1296` `.focus-landing:focus-visible`; ring vs adjacent colour 5.81–6.13 stone / 8.36–9.28 night [A1] |
| M1 date-button ring unclipped + reachable picker fallback | **VERIFIED-FIXED** | `.daynav__stepper` no `overflow` (live `visible`), ring y19–75 across a stepper y24–70; arrows `border-radius 99px 0 0 99px` / `0 99px 99px 0` (`features.css:162-167`); with `showPicker` undefined the input becomes `162×44`, `tabIndex 0`, `aria-hidden` removed, `aria-label="Pick a date"`, focused, typing `09/05/2026` moves the day [V-N3][A-N3][S8] |
| M1 ··· menu Tab-out / outside-close focus / arrows | **PARTIAL (1 of 3)** | `ExperiencePost.tsx:123-131` `focusout`: Tab from Report closes, `aria-expanded=false`, focus → nav Home [S-N4][A]; outside click closes but focus → BODY ×2 / `div.feed-stream.focus-landing` ×1, never `···` — the `moreBtnRef.focus()` at `:118-120` is overridden by the pointerdown default, which focuses the new `tabindex=-1` container [S-R4][A-R5-A7]; ArrowDown/Up no-ops |
| M1 compose actions above the nav at 320×568 | **VERIFIED-FIXED (different mechanism; new wrap)** | order swap deleted; `@media (max-height:620px) and (max-width:960px){.compose-actions{flex-direction:row}…}` (`features.css:1275-1287`); Share `[16,364,103,47]`, Keep private `[127,364,85,47]`, Cancel `[220,364,85,47]`, nav `[10,494,300,64]`, `elementFromPoint` = each button, at 320×568/320×600/390×620; column at 390×844/430 [V7][S-N1][A6]. "Share anonymously" and "Keep private" wrap to two lines in 103/85 px pills (47 px tall) [V-R2] |
| M1 survivor link ≥44 / login checkbox ≥22 / submit above fold | **VERIFIED-FIXED** | "Open the current entry for 213" `[16,111,349,72]` (`features.css:1233-1240` `.entity-survivor` in the hit-area rule) [V6][S-N10]; checkbox 22×22 (`components.css:881-885`) inside the form before submit; submit `[24,488,272,48]` bottom **536 < 568** at 320×568, wordmark top +32 [V11][A6] |
| M1 no editor under "Loading…" | **VERIFIED-FIXED (pending) / fails open on error** | `ComposePage.tsx:196-204`: with `/api/entities` delayed 4.5 s → h1 + `div.skeleton[role=status][aria-label=Loading]`, no textarea/Share; warm registry 0/40 samples show a skeleton (~19 ms) [S-N18][V5][W10]. With `/api/entities` **aborted** the full editor mounts, target "Loading… / Teacher", Share and Keep private enable after typing, no `role=alert`, unlisted guard bypassed for `teacher:t_doesnotexist` (`ComposePage.tsx:197` guards `!data && !error` only) [S-R6] |
| M1 `ReconnectDialog` carries form-order + caveat | **PARTIAL** | reconnect purpose: checkbox inside `<form>` before submit, 22×22, caveat present (`ReconnectDialog.tsx:44-55`); save purpose: **no checkbox** (`:43` `purpose === "reconnect" ? … : null`), `stayConnected` forced `true` (`:28`) [C-D3][A-R5-A6] |
| M2 merge `.daynav__date` (dividers back) | **VERIFIED-FIXED** | one block `features.css:183-202`; live `border-left/right 1px solid rgba(35,43,49,0.14)` [V-N2] |
| M2 merge `.post__menu button`, one hidden-text utility | **VERIFIED-FIXED** | `.post__menu button` ×1 (`features.css:1008`); `.visually-hidden` deleted, `.sr-only` (`foundations.css:89-99`) consumed at `ExplorePage.tsx:170` [S4][V12] |
| M2 delete the 27 orphans, `.login__consent`, `.action-cta`, dangling comment | **PARTIAL** | consumer-less class names **27 → 10/11** (Structural 10: `.action-card .lift .btn--secondary .segmented .sort-toggle__btn .card-index .card-copy .section-head__copy .reading .report-option`; Visual adds `.is-in`, `.stat`, `.stat span`); 4 are dangling descendants of deleted base rules (`components.css:780,840`, `foundations.css:229-230`) [S-R10][V-R7]; **3 classes deleted from CSS while still emitted by the served JS**: `.chip--ok/--danger/--muted` (`MinePage.tsx:244` `chip chip--${tone}`; 0 hits in `index-D-vVlHpE.css`) [S-R5][V-R3]; dangling comment `features.css:1266` survives and three new ones (`components.css:268,811`, `foundations.css:234`) [V-R6] |
| M2 `.settings-trigger` padding reset, `.main` literal, nudge sheet | **VERIFIED-FIXED** | `components.css:487 padding:0` (desktop UA values 41 → 0); `.main` desktop `var(--sp-7) clamp(22px,5vw,76px) 110px` with inline comment (`:527`); nudge sheet tokenized (`features.css:806`) [V1] |
| M2 `.timeline__empty` not over the band rows | **FIXED, DEFECT RELOCATED** | `features.css:440-448` `position:absolute; left:0; right:0; bottom:var(--sp-3)`; 0 overlap with P1–P6/Lunch/Dinner labels; but on the default Timetable (today, six free bands) at 390×844 the text `[170,765,106,23]` sits under the nav `[10,770,370,64]` (`elementFromPoint` = `nav.mobile-nav`, only the glyph and ~5 px visible); at 320×568 the whole box is **219 px below the viewport**; clear at 430×932 [V-R1][S-R2] |
| M2 hero caption overlap | **VERIFIED-FIXED** | `.nextlesson .caption { display:inline-flex; min-height:44px; margin-top:0 }` (`features.css:111-116`); 0 element-pair overlaps at 4 viewports [V10] |
| M3 one dialog per purpose | **PARTIAL** | `purpose` + `sessionExpired` props (`ReconnectDialog.tsx:9-13,20-33`; `SettingsPage.tsx:150,186,321-323`); titles "Reconnect school account" / "Save school login"; bodies three-way, both reconnect bodies observed (valid token → "Sign in again to refresh the school connection…"; `/api/me` rewritten `portalTokenValid:false` → "The portal session ended…") [C-N6]; **submit reads "Reconnect" in both** (`ReconnectDialog.tsx:41` `mode="reconnect"`, `SchoolLoginForm.tsx:66`) [C-D2][V-R4][S-R8][A-R5-A5]; no state leak between purposes [S] |
| M3 re-auth clause beside Sync now | **VERIFIED-FIXED** | `TimetablePage.tsx:138-142` `.daynav__note`, gated only on `portalCredentials.isAuthorized()`; with the sentinel `[16,146,358,36]`, **24 px** below "Sync now", at 390 and 320, present in the error branch [C-N7][S-N7][V] |
| M3 import row + Delete dialog name the real triggers | **REFUTED (new inaccuracy)** | live "…imported from the school portal when your account is created, and again whenever you press Sync now **or pull to refresh**." (`SettingsPage.tsx:199-201`); pull-to-refresh → `emitRefresh()` → `useApi` re-read only; measured on `/timetable`, `/home`, `/history`: **0 `POST /api/sync`** (`r5-copy-6.log`); the r4 handoff's own wording, copied from the false comment `BE/routes/auth.ts:30-31` [C-D1]. Delete dialog "You can import again with Sync now." true (`:286`) |
| M3 never-listed ≠ delisted | **VERIFIED-FIXED (entity) / REFUTED (composer)** | `EntityPage.tsx:57,69-77`: `t_doesnotexist`, `r_df12aeba9bb9`, `c_bogus` → h1 "Nothing is listed at this address.", no kind-as-name, one link; `r_d48ff4b2f68a` keeps "213" + "This entry is no longer listed." + survivor link. Composer (`ComposePage.tsx:191-203`) consults `/api/entities` only: `room:r_d48ff4b2f68a` and `room:r_48a1706eca5e` (directory names "213"/"308", survivors exist) render "Nothing is listed at this address, so nothing can be shared about it." — contradicting the entity page on the same id [C-D4][S-R11] |
| M3 relabel survivor link; P1–P6 once; "verified exposure"/"entities" gone; per-route title; contract comment | **VERIFIED-FIXED (with placement/coverage gaps)** | "Open the current entry for 213" [C]; `TimetablePage.tsx:181-183` "P1–P6 are the school's six lesson periods. Last synced 2 h ago." correct against `BANDS` (6 periods) but rendered after the timeline at `[16,800,358,18]` under the nav at 390 (`elementFromPoint` = nav item) and below the fold at 320, and absent in the error branch [C-D10][S-R1][V-R8]; `ExperiencePost.tsx:163` "Reactions are open to students who have had the same class or place."; `ExplorePage.tsx:75` `aria-label="Filter by name"`; `AppLayout.tsx:36-47` titles on 10 routes, entity pages → "Experiences · HOney", `/login` and 404 → "HOney" [C-M6][A7]; `contract.ts:12-16` rewritten and true [C6] |
| M3 feed tabpanel; PTR name | **VERIFIED-FIXED / VERIFIED-FIXED (not exposed idle)** | `FeedPage.tsx:79-80,90-91,108-116` ids + `aria-controls="feed-panel"` + one `role=tabpanel` with `aria-labelledby` following selection, 0 duplicate ids [A4]; `PullToRefresh.tsx:103` `aria-label="Pull down to refresh"` on `.ptr__disc[role=status]`, but the wrapper `.ptr` is `aria-hidden="true"` while idle (`:98`) [C-D6][W5] |
| M4 SW live-asset set (font + Dash survive) | **PARTIAL** | `sw.js:21-35` `liveAssetSet()`: CSS `url()` yields all 7 font subsets — fonts survive; JS regex requires an `assets/` segment, Vite emits `import("./DashPage-PRZwOVZX.js")` → 0 matches → **Dash chunk still deleted** on the first SW-controlled navigation [W6a] |
| M4 eviction at activate | **REFUTED (regression)** | `sw.js:56-63` `.then(evictStaleAssets)` passes `Promise.all`'s resolved array as `html`; truthy → skips `fetch("/")` → `html.match` throws `TypeError`, swallowed at `:50`; seeded stale hashes `index-BQhfgddr.js`, `index-D7RZ4AWU.css`, `DashPage-CGVLT9aO.js`, `…OLDHASH1.woff2` all survive install+activate (r4's parameterless version evicted them) [W6a][W-new1] |
| M4 eviction on a changed index from navigation | **PARTIAL** | `sw.js:94-104` runs live on a byte-identical `sw.js` (stale fakes evicted on the first controlled navigation); trigger `!lastIndexSig.includes(seed.split("|")[0])` compares only the first sorted ref (today the CSS) → a JS-only deploy would not fire; fires anyway because `lastIndexSig` is a module global that resets on every SW cold start [W6b][W-new4] |
| M4 shells capped to "/" | **VERIFIED-FIXED** | `cache.put("/", copy)` gated on `mode==="navigate"`; after 10 distinct hard loads 1 non-asset entry (`/`), cache 2,217 B (r4 14 / 374,893 B); offline `/timetable` served in 68 ms [W6c] |
| M4 `sw.js` `no-cache` | **NOT DONE** | live `cache-control: public, max-age=14400`; `updateViaCache:"imports"` default mitigates [W6e] |
| Preserve list (spacing ≥97 %, type, 43 colours, contrast floor, 0 idle motion, weight, boot theme, containment, focus ring, login, Explore complete set, honesty mapping) | **HELD** | see [V1]–[V4], [V9], [W1]–[W4], [W7], [A1]–[A2], [C6], [S6], [S17] |

## Structural pass

- **[S1] Census @390 (visible/total)**: Home 9/15; Timetable 10/17; Feed 15/21; Explore 26/32; Compose default 7/13, `?entityKey=` 10/16, `?lessonId=` 10/16, unlisted/never-listed 6/12, editing 6/16; Entity 6/12 (normal, delisted-duplicate, never-listed); Mine empty 6/12; Settings 19/26; History 8/14; Login 5/5. **Net change from r4: zero** — no control added or removed. Nesting 10 in `main` / 14 in document (Feed), unchanged.
- **[S3] Repeated-affordance families: 14** (r4 13). Resolved: `.daynav__date` ×2, `.post__menu button` ×2, `.visually-hidden`+`.sr-only`, one identical dialog for two buttons. Added: `.focus-landing` ×7 (one hook, one rule; granularity inconsistent — Home = hero card, Timetable/History/Explore = content div, Mine/Entity = whole page `.stack`, Feed = stream); "Delete…" ×2 on Settings with two destructive purposes (`SettingsPage.tsx:101,213`, same label, same style); three wordings of "not listed" for two conditions (`EntityPage.tsx:72,100`, `ComposePage.tsx:210`). Surviving: dual nav DOM (2 `nav` on 7 routes); 11 compose entry definitions, max co-visible 1 except feed at ≥9 posts (`FeedPage.tsx:33,160-166`, `SHARE_PROMPT_EVERY = 8`, latent); six wordings of "share"; "Stay connected" ×3 sites, three bodies (all now with the caveat); "Reconnect"/"Save school login…" → one component, two purposes, **identical submit "Reconnect"**.
- **[S4] Hygiene**: `tsc --noUnusedLocals` 0; same-context duplicate selectors **0** (368 blocks; the 33 naive hits are `@media` overrides); consumer-less class names **10** (Structural; Visual counts 11 with `.is-in`/`.stat`); **reverse sweep: 3 classes applied in TSX with no rule** — `.chip--ok/--danger/--muted` (`MinePage.tsx:21-38,236,244`; rules existed at `5d810d8` `components.css:220-228`, removed by `1f87fae`); pre-existing rule-less names `.home .home-head .home-voices .post__dot .login__card` unchanged.
- **[S5] Feed**: header Share shown loading + normal, hidden on error and empty; empty carries one CTA "Share the first one"; tabs `#tab-my-classes`/`#tab-school` → `#feed-panel`; ArrowLeft/Right move and select.
- **[S6] Explore**: 19 rows at 390 and 320, "Food 0" rendered, `zzzz` → four sections at 0 with "Nothing by that name.", error branch keeps four sections + Try again 77×44; marks 0/19 (Teachers 6/6, Courses 8/8 mine, Places 0/5); `aria-label="Filter by name"`.
- **[S7] Composer**: skeleton state has no textarea/Share; 320×568 row-of-three all hit-testable; `elementFromPoint` = button at every centre; **registry error → fails open** (`r5-struct-hunt2.log`).
- **[S8] Timetable**: Tab order Previous day → date button (one stop, 268×44, `aria-label="Pick a date (Wed 2 Sept)"`) → Next day → Back to today → History → Sync now → lesson block → nav; hidden input `tabindex -1 aria-hidden` 5×4 carrying `aria-label="Pick a date"` while hidden; fallback grows the stepper 358→370 in a 358 content box (no document overflow); `.daynav__note` 24 px under Sync now with the sentinel, present in the error branch; `.timetable-foot` `[16,800,358,18]` under the nav at 390 on landing (`footCoveredByNav:true`), below the fold at 320, absent in the error branch.
- **[S9] Entity**: never-listed → h1 "Nothing is listed at this address." + bare `<a>` "Find someone or something" `[16,112,189,22]` in `<p class="muted">` (`EntityPage.tsx:74`, no `.btn`, outside the hit-area rule) while the composer's same CTA is `btn btn--primary` 220×44; delisted-duplicate → "213" + survivor link 349×72; `r_df12aeba9bb9` (r3/r4 delisted) now renders never-listed [V-R9].
- **[S10] Settings**: verbs live `["Sync now","Reconnect","Disconnect","Save school login…"]` on a connected, `portalTokenValid:true` account; "Timetable import" row static `DIV.setting-row`, 0 controls, no role; `/settings#privacy` → `activeElement = h2#privacy` with `:focus-visible false`, `outline-style none` (r4 had a UA ring) [S-R13]; reopening Reconnect after Save restores the reconnect copy with the checkbox unchecked.
- **[S11] ··· menu**: trigger 44×44, `aria-haspopup="menu"`, `aria-controls`, `aria-expanded` fresh after Escape/Tab-out/outside click; Report 110×44; Escape → `···`; Tab-out → closes, focus → nav Home; Shift+Tab → trigger, menu stays open; outside click → focus BODY/container; no arrow keys.
- **[S17] Still-good**: scope tabs 2, `scrollWidth 239 == clientWidth 239`; `.mobile-nav` `[10,770,370,64]` identical on 7 routes; cold `/timetable` 2 API calls, no duplicates; `/experiences` leave-and-return 0 refetch / 0 skeletons (`/timetable` re-issues one SWR revalidation, 0 skeletons).

## Visual pass

- **[V1] CSS spacing**: 220 spacing declaration lines, 181 with `var(--sp-*)` (212 occurrences); literal-px spacing lines **4** (+ `.sr-only` `margin:-1px`) — `components.css:527` (110 px, commented), `features.css:1192` (90 px, **uncommented**), `features.css:1238-1239` (12+1, commented); 96 px nav clearance commented at `components.css:669`; the r4 desktop `.main` 34 px and nudge 18 px literals are gone.
- **[V2] Rendered census** (10 screens): @320 `1(1) 4(188) 8(191) 12(289) 13(8) 16(120) 20(58) 24(11) 90(10)` = **857/876 = 97.83 %**; @390 `…13(8)…96(10)` = **97.83 %**; @430 **97.83 %**; desktop 891/960 = **92.81 %** (r4 87.9 %). Off-ladder attribution: 13×8 = the commented 12+1 rule; 96/90/110 = nav/rail clearance; 1×1 = UA padding on the aria-hidden picker; desktop 64/216/249 = `.main` clamp, `--rail`, `.rail-nav{margin:auto}`. **Author-level off-ladder on mobile: 0.** No 2/9/10/14/18/22/26/30 px value at any viewport. `.settings-trigger` computed `padding 0`, 46×46.
- **[V3] Type**: @320 `{12,13,15,16,20,24,26,28}`; @390 `{12,13,15,16,20,22,24,26,28}`; @430 + one fractional 27.95 (`clamp(26px,6.5vw,34px)`, `features.css:101`); desktop `{12,13,15,16,20,28,30,32,34,36}`; login `{13,16,17}`; floor 12; off-ramp `font-size` declarations 11 (r4 12; `.card-title` deleted).
- **[V4] Colour**: **43** unique literals, byte-identical to `5d810d8` and `fd522c3`; one accent; warm only on danger/ok.
- **[V5] Contrast (live, alpha-composited; stone / dark boot)**: body 14.05 / 15.27; `--ink-2` 5.34 / 8.07; `--ink-3` `.home-head__stale` **4.66 / 5.83** (floor); captions 5.34–5.66 / 7.26–8.07; accent links 5.34–14.05 / 7.27–15.27; scope tab on/off 6.30 & 5.34 / 9.27 & 8.07; nav active (vs pill) 13.26 / 15.27; `.nextlesson__state` 11.52 / 9.80; error banner 4.77 (home 5.02) / 7.03; Try again 11.80 / 13.48; daynav arrows 5.63 / 7.27; date button 13.98 / 13.75; **new** `.timetable-foot` 5.34 / 7.26; **new** `.daynav__note` 5.34 / 7.26; **new** Save-login dialog title/body 13.98 / 5.63 — 13.75 / 7.27; **new** "Nothing is listed at this address." h1 / link 13.26 / 5.81 — 15.27 / 9.28; survivor link 5.81 / 9.28; Report menuitem 13.98 / 13.75; login label / caveat / skip / footnote / submit 11.78 / **4.87** / 13.26 / 5.34 / 13.26 — 14.09 / 6.92 / 15.27 / 8.07 / 15.27; Reconnect dialog checkbox label **5.14 / 6.11**; focus-landing ring vs ground 3.75 (non-text) and 5.81–6.13 vs inside/parent. **0 enabled text below 4.5:1 on either surface** (A11y: 0 of 726 nodes ×2). Disabled (exempt): ghost 4.49 / 7.01, primary 4.61 / 6.64.
- **[V6] States**: error — 7 banners `role=alert` 316–358×130, Try again 77×44, +1 refetch; after **pointer** retry landing `:focus-visible=false` `outline 3px none` on home/timetable/history/explore/feed/entity, after Enter `3px solid rgb(51,102,124)` offset 3 (`r5-vis-retry-*[-kbd].png`); Mine unreachable (0 keys). Loading — skeletons `role=status aria-label=Loading` on Home/Timetable/History/Explore/Feed/Entity/Compose (Mine issues no fetch with 0 keys — not a missing state; r4 measured a 3-line Mine skeleton with a seeded key). Empty — Timetable "No lessons today" now `role=status`, box `[73,735,300,53]` at 390 (text under the nav), 219 px below the viewport at 320 [R1]; History "No lessons match."; Explore "Nothing by that name."; Feed "Nothing from your classes yet."; Entity "No experiences here yet…"; Mine "Nothing here yet" + one CTA. Success — not exercised (hard limits). Focus — date ring full; 100 % of stops ringed. Disabled — Share op 1 / Keep private op .65 empty, both enable on typing.
- **[V7] Compose (six viewports)**: 320×568 row `[16,364,288,47]` gap 8: Share `[16,364,103,47]`, Keep private `[127,364,85,47]`, Cancel `[220,364,85,47]`, footnote `[16,429,280,62]` (2 lines), nav `[10,494,300,64]` — all hit-testable, `.main` maxScroll 0; **"Share anonymously" → "Share / anonymously", "Keep private" → "Keep / private"** at 320×568 and 320×600; "Share anonymously" wraps at 390×620 (114 px pill); column at 390×844 (44 px rows) and 430; row at 1280 with no wrap.
- **[V8] Modal**: overlay `[0,0,390,844]` / `[0,0,1280,800]`; sheet padding `20px 16px 24px`; nav centres → `DIV.modal-overlay`, `insideNav:false`; 2 finite animations open, 0 after Esc, focus → `button.lesson-block`.
- **[V9] Idle motion**: 0 at 2.5 s on 11 screens × 2 preferences (twice, at 390 and 320); `.view` ONE rule (`components.css:529`; `foundations.css` block deleted in `487c1c4`), settle 460 ms `backwards` per route, 1 µs under reduce; `@keyframes` 6; `infinite` 2 (`ptr-rot`, `skeleton-shimmer`).
- **[V10] Sweep 4 viewports × 10 screens**: `scrollWidth == innerWidth` everywhere; 0 per-element overflow; **0 element-pair overlaps** (hero caption fixed); 1 ellipsis (`.post__provenance`).
- **[V11] Login**: wordmark top +32 / 152 / 215 / 149; doorway 559/539/502/502; checkbox 22×22 at all; submit bottom 536 / 637 / 680 / 614; caveat verbatim "Stay connected on this device. Portal time-outs reconnect on their own. Your login is encrypted and kept only here (a browser is less protected than a phone's secure storage). Turn it off in Settings."; landmarks `main` + `form`; fonts {13,16,17}.
- **[V12] Deleted-vs-consumed**: every class deleted in `git diff 5d810d8..487c1c4 -- apps/web/src/styles` scores 0 hits in `index-c6M0Emr3.js` **except `chip--ok`, `chip--danger`, `chip--muted`** [R3]; `.sr-only` consumers 1; no other `overflow:hidden` pill contains a focus stop; the new states render correctly on a night boot (`r5-vis-savedialog-390-night.png`, `r5-vis-entity-delisted-390-night.png`, `r5-vis-timetable-390-night.png`).

## Copy & Honesty pass

- **[C1] Strings changed since `5d810d8`** (23 items, diff-driven; all LIVE-SEEN except `/dash` title and the `sr-only` separator which renders 0 times on this account): route titles (`AppLayout.tsx:37-46`); PTR idle name (`PullToRefresh.tsx:103`); dialog title "Save school login" (`ReconnectDialog.tsx:30`); save body "Enter your school login once; it stays encrypted on this device (a browser is less protected than a phone's secure storage) so routine portal time-outs reconnect on their own." (`:33`); reconnect bodies (`:35-36`) and caveat on the checkbox (`:51-53`); `not_eligible` note (`ExperiencePost.tsx:163`); shortened login opt-in (`LoginPage.tsx:53-55`); Settings import row (`SettingsPage.tsx:199-201`) and Delete dialog (`:286`); hidden-input `aria-label` (`TimetablePage.tsx:104`); `.daynav__note` "Sync now signs in again with your saved school login if the portal session expired." (`:140`); `.timetable-foot` (`:182-183`); "No lessons today" in `role=status`; composer "Nothing is listed at this address, so nothing can be shared about it." (`ComposePage.tsx:203`) and its "Loading…" text **removed** (skeleton only); entity h1 "Nothing is listed at this address." and "Open the current entry for {name}" (`EntityPage.tsx:69-77,110`); `aria-label="Filter by name"` (`ExplorePage.tsx:75`); feed tab/panel ids; `contract.ts:12-16`.
- **[C3] Inflations**: `WhyPage.tsx:73` "can be **strongly** protected" (live, unchanged); `:63` "where possible" (hedge); `SettingsPage.tsx:225` "The detail, honestly:" (framing). New dialog/timetable strings: 0 superlatives. Marketing superlatives across 19 routes: **0**.
- **[C4] Dark patterns (rubric)**: forced continuity 0 (login `checked:false`; reconnect default mirrors `isAuthorized()`); hidden cost 0 (Delete imported data / Disconnect / Delete account bodies state the cost; the Save dialog forces `stayConnected=true` with no checkbox but discloses the storage in its body — disclosed-but-unrefusable, not hidden); fake scarcity 0; confirmshaming 0.
- **[C5] Import disclosure**: (A) Settings row verbatim "Timetable import — On for every HOney account since 1 September 2026 — your timetable and history are imported from the school portal when your account is created, and again whenever you press Sync now or pull to refresh. Delete them below at any time." — static, no toggle, above "Delete imported data"; (B) row top 1384 of 3082 at 390, 1504 of 3616 at 320 (not visible without scrolling); (C) date claim consistent with `BE/db/database.ts:244-253`, `/api/me` `grantedAt 2026-09-01T16:51Z`; (D) trigger accuracy: account creation TRUE (`BE/routes/auth.ts:29-33`), Sync now TRUE (`SettingsPage.tsx:128`, `TimetablePage.tsx:134` → `api.syncSeamless()` → `POST /api/sync` → `importer.syncTimetable`), **pull to refresh FALSE** — `PullToRefresh.tsx` → `emitRefresh()` → `useApi.ts:78-80` re-read; measured 0 `POST /api/sync` on `/timetable` (`GET /api/timetable`), `/home` (`GET /api/next-lesson`, `…/from-my-classes`), `/history` (`GET /api/directory`, `GET /api/history`) — the row over-names, does not under-name; the false trigger is copied from `BE/routes/auth.ts:30-31` and was dictated verbatim by the r4 handoff; (E) login footnote "…created on first sign-in. Your timetable comes along with it." (`LoginPage.tsx:62-63`) visible at 390, `footTop 536` of 568 at 320 (below the fold); "Turn it off in Settings." TRUE (sentinel: `turnOffPresent:true`).
- **[C6] Label→behaviour**: (M1) "Save school login" dialog submit **"Reconnect"** (`ReconnectDialog.tsx:30` vs `:41`, `SettingsPage.tsx:185-187`), live at 390 and 320; (M2) import row names a non-trigger (above); (M3) PTR idle name inside `aria-hidden` (`PullToRefresh.tsx:98` vs `:103`); (M4) composer "Nothing is listed at this address" for `room:r_d48ff4b2f68a` / `room:r_48a1706eca5e` while the entity page says "This entry is no longer listed. Open the current entry for 213" — same id, two statements, no survivor link on the composer; (M5) Home "Open timetable" → today while the chip says "Tomorrow · 01:00" (unchanged, weak); (M6) `document.title`: 9 truthful, entity pages "Experiences · HOney" (h1 "ChenJenny"/"213"/"Nothing is listed…"), 404 and `/login` "HOney"; (M7) `not_eligible` note plain text, no `role`; (M8) Explore aria = placeholder.
- **[C7] Jargon**: "P1–P6" explained once, correct against `BANDS` (`TimetablePage.tsx:221-229`), 678/714 px below Sync now, off-screen on landing at 390 and 320; "verified exposure" 0 user-facing (survives only as the error key `no_verified_exposure`, `shared.tsx:73`); "entities" 0; "ownership key" single name; "scrambled at rest" glossed; "registry" 0 UI; "Not selected" 0; six connection verbs on one Settings card (Connected / Sync now / Reconnect / Disconnect / Stay connected / Turn off / Save school login…); "from your classes" 2 live referents; **two sentences for one idea ×2**: "Nothing is listed at this address." (entity) vs "Nothing lives at this address." (404); " · Sync signs in again…" (`SettingsPage.tsx:118`) vs "Sync now signs in again…" (`TimetablePage.tsx:140`).
- **[C8] Honesty-claims drift**: `git log 5d810d8..487c1c4 -- api/client.ts packages/backend packages/shared WhyPage.tsx` → only `contract.ts` (6 lines). Re-confirmed 1:1: publish `auth:false` (`client.ts:229`) + `credentials:"omit"` (`:351-353`); `ownership_hash` only, no author column (`BE/db/database.ts:103-121`); 6 = 6 report categories (`BE/routes/experiences.ts:10-17` = `contract.ts:366` = the live dialog); "no free text is collected" ↔ `client.ts:245-247` `{category}`; HOney ID line (`SettingsPage.tsx:72-75`); "design boundary, not magic" (`WhyPage.tsx:75`). `contract.ts:12-16` now "Login carries ONLY identity. Import is part of the account (2026-09-01): the first sync runs on creation; `POST /api/consent` remains only for wire compatibility with older iOS builds and no longer gates anything." — true (`BE/routes/auth.ts:135-147` still writes `revoked_at`; `importer.ts:42-43` ignores it; `SyncStatus "no_consent"` dead member remains).
- **[C9] Stale comments**: `lib/portalCredentials.ts:16` now TRUE (caveat at 4/4 opt-in sites: `LoginPage.tsx:54-55`, `SettingsPage.tsx:169`, `ReconnectDialog.tsx:33,52-53`); `SchoolLoginForm.tsx:8,11` TRUE; **`ReconnectDialog.tsx:16-19` FALSE on two counts** ("title, body and submit say what THIS opening does" — submit does not; "the stay-connected choice sits inside the form" — not for save); **`BE/routes/auth.ts:30-31` FALSE** ("Sync now / pull-to-refresh are the manual paths").

## Weight & Friction pass

- **[W1] Bundle**: `index-c6M0Emr3.js` **87,863 B wire (CDP) / 85,069 br (curl) / 270,886 raw** (r4 87,022 / 84,469 / 268,076; +0.97 % wire); CSS **10,479 / 9,458 br / 41,641 raw** (r4 10,997 / 44,717; −6.9 % raw); latin font 29,238; Dash `DashPage-PRZwOVZX.js` 4,403 br, fetched on 0 of 12 student loads, 245 ms on `/dash`; cold `/home` **154,574 B / 12 requests** (11,130 B Cloudflare telemetry); 123,968 B unused font subsets in `dist/assets`; 1,202 B admin CSS in the student bundle (pre-existing); `/assets/*` `max-age=14400` (no `immutable`), `/` `max-age=0`, `sw.js` `max-age=14400`; no preload/preconnect; no dependency change.
- **[W2] Requests**: cold `/home` 12 (3 data); warm SPA → `/experiences` 3 (1); cold `/experiences` 11, `/timetable` 12, `/settings` 10, explore 12, mine 12, entity 13 with **`/api/entities` ×1** on 2 cold + 2 SPA loads; **no duplicate in-flight API calls** on 14 blocks.
- **[W3] Timing** (medians of 5, quiet, 6 s window): cold `/home` TTFB 40 / FCP 192 / **TTI 267**; cold `/experiences` 46 / 228 / 445; warm SW reload FCP 108 / TTI 130; `/login` light TTI 228–253, dark 191–294; **0 long tasks in 25 loads**; transitions 103–306 ms; not load-controlled vs r4.
- **[W4] Idle**: 0 at settle and 2.5 s on 11 screens cold + SPA; one `settle` per route commit (460 ms, `backwards`); `/history?select=1` 1 finished `rise-in`; modal 2 finished; menu/picker 0; reduce → 0 on 11 routes + login; `infinite` 2; `@keyframes` 6; `.view` one rule.
- **[W5] Interrupts on load**: 0 dialogs/banners/toasts/badges on cold `/home`, `/experiences`, `/settings`, explore, mine, `/login`; cold `/timetable` 1 `[role=status]` = the in-flow "No lessons today" (new role, content not interrupt); PTR disc opacity 0, wrapper `aria-hidden` idle; Settings disclosure row no role, transparent, 0 focusables.
- **[W6] Service worker** (read in full; live = repo): (a) `liveAssetSet` — CSS regex 7/7 fonts, JS regex 0 (Vite `import("./DashPage-…js")` has no `assets/` segment); live: fonts survive, **Dash chunk deleted** on the first controlled navigation; (a′) **activate-time eviction throws** (`sw.js:56-63` → array as `html` → `:38` skips fetch → `:25` `TypeError`, swallowed `:50`); seeded stale hashes all survive activate (r4 evicted them); (b) navigation eviction runs on a byte-identical `sw.js`, but the predicate `sw.js:102` compares the first sorted ref only (today the CSS) — JS-only deploy would not fire; `lastIndexSig` module global resets on SW termination; (c) shells capped: 1 entry `/`, 2,217 B after 10 URLs; offline `/timetable` 68 ms; (d) content-type guard refuses `200 text/html` under `/assets/` (`sw.js:81-82`); `/api/` 0 entries; navigations network-first; (e) `sw.js` `max-age=14400`; (f) **cost**: after a forced `ServiceWorker.stopWorker` the next navigation re-fetches `index-c6M0Emr3.js` + `index-D-vVlHpE.css` with `cache:"no-store"` — **98,712–100,606 B** on the navigation critical path per SW cold start (`r5-wf-sw5.log`); correlates with the slowest warm reload in every series (FCP 320 / TTI 357 vs 68–124 / 93–162); 0 fetches at activate (throws first).
- **[W7] Compose path**: feed → Share → chooser → History (24 Select) → editor = **4 screens / 3 taps**, `activeElement` BODY; Explore → row → entity → Share = 3 / 2, BODY. Unchanged. Warm-registry skeleton ~19 ms (one frame); cold 98 ms.
- **[W8] PTR**: feed 604 ms, timetable 599 ms, exactly 1 API call per pull (`GET`, never `POST /api/sync`), sub-threshold 0, no native reload.
- **[W9] Boot theme**: `data-surface` at 54–82 ms, before FCP 272–368 in six contexts (system dark/light, stored white-on-dark, stored night-on-light, garbage, `/home` dark); 0 later `<html>` mutations; head order boot script → module → stylesheet; static `theme-color #edf0f1` outside the boot palette (unchanged).
- **[W10] New-cost hunt**: focus landings never steal focus or scroll on a normal load (`activeElement BODY`, `scrollTop 0`, 1 landing per route); per-route title 1 write, 0 further mutations in 4 s; no new fetch in `AppLayout`.

## Accessibility pass

- **[A1] Contrast**: per-surface lowest enabled (stone / night) — home 4.66/5.83, feed 5.34/8.07, explore 5.34/8.07, entity 5.34/8.07, compose 5.34/8.07, compose never-listed 5.66/7.26, entity never-listed 5.81/9.28, settings 5.66/7.26, timetable 5.34/7.27, lesson modal 5.34/7.13, history 5.34/8.07, mine 5.66/7.26, Save dialog 5.63/7.26, Reconnect dialog **5.14**/6.11, report dialog 5.34/7.27, seven error surfaces 5.34–5.36/7.17–7.96, login **4.87**/6.92. **0 failures of 726 enabled nodes on each surface.** Landing ring vs inside/parent/page 5.81–6.13 stone / 8.36–9.28 night; date button 6.13/8.36 — 1.4.11 met.
- **[A2] Focus order** (all stops `3px solid rgb(51,102,124)` offset 3, `:focus-visible`; ~150 stops across 15 route states): Home 7; Feed 15 (skip → Share → Find… → Your notes → Why → tab → tab → context link → ± → ··· → nav); Explore 26; Compose editor skip → textarea → (Share → Keep private after typing) → Cancel → How privacy works → nav at all four viewports; chooser 7; never-listed 6; Timetable 8 (date button one stop); Settings skip + 13 controls + nav; Login skip → username → password → checkbox 22×22 → submit; Entity listed 6, delisted-duplicate (synthesised) survivor link 349×72, never-listed link 189×22. **`.focus-landing`**: `tabindex -1` on all 7, in the Tab order on none, focused on a normal load on none; History landing focus scrolls the owner **+174 px**, Explore **+278 px** (search and filters leave the viewport; the 1780/1254 px landings show only two vertical ring edges); role/name: Home `region` "Now and next", Feed `tabpanel` labelled by the active tab, **Timetable/History/Explore/Mine/Entity none/none**; Tab from the Timetable and History landings → nav Home (no focusable content inside).
- **[A3] Keyboard reachability**: YES — lesson modal open/trap/Esc→opener; scope tabs + arrows; ··· open → Report → Escape → trigger; ··· Tab-out closes (focus forward to nav); react ± (undone); context link; Share → chooser → Explore → row → entity → composer (BODY after every route change); Keep private/Share after typing at 320×568/320×600/390×620/390×844; Try again ×7 with landing; entity empty refetch; date button + `showPicker` (called, no error); native fallback (focused, named, operable); Settings 13/13; privacy link → `h2#privacy`; both dialog purposes trap + Esc + opener. NO — ··· outside click → trigger (0/3); ··· arrows. Skip link → `#main` has no `tabindex`: lands on `MAIN` on the 6 overflowing routes, on **BODY** on Home/Feed/Compose/Mine; `/login` → input; 404 has no skip link.
- **[A4] Landmarks**: `banner` 0, `contentinfo` 0 on every route; nav 2 + main 1 on AppLayout routes; regions Home 2, Explore 4 (incl. `Food` with 0 rows), Compose 1/1/**0** (never-listed), Settings 5; Feed tablist + 2 tabs + **1 tabpanel** (`aria-labelledby` follows selection, `aria-controls` both → `feed-panel`); `/login` `main` + `form`, h1 text empty (`<img alt>`).
- **[A5] Skip link**: 10/10 authed + login; absent on 404.
- **[A6] Touch @390 and @320×568**: 131 visible interactive elements per viewport; genuine sub-44: **never-listed "Find someone or something" 189×22** (new) and Why "Back to Experiences" 133×22 (pre-existing, first measured); aria-hidden picker 5×4 exempt. Login submit at 320×308 keyboard viewport bottom 536 → off-screen, reachable by scroll. Compose at four viewports: no action resolves to `nav.mobile-nav`; names complete at 320.
- **[A7] Semantics**: 7/7 `role=alert`; feed banner and skeleton are children of `div.feed-stream[role=tabpanel][aria-live=polite][tabindex=-1].focus-landing` (one node, four jobs — alert may announce twice); skeletons `role=status` "Loading" (composer announces "Loading" via `aria-label`); PTR "Pull down to refresh"/"Refreshing" (idle inside `aria-hidden`); "Nothing is listed at this address." plain text, no live region, both surfaces; `document.title` 13 states verbatim (entity "Experiences · HOney", 404 and `/login` "HOney"), 4 mutations on 4 SPA navs; dialogs `role=dialog aria-modal tabindex=-1` named by `aria-label`, **`aria-labelledby`/`aria-describedby` null on all five** (the sentence distinguishing Save from Reconnect is not announced); body children `["DIV","SCRIPT"]`, no `inert`/`aria-hidden` under any dialog; `aria-expanded` never stale; date button "Pick a date (Wed 2 Sept)"; fallback shows button and input simultaneously with near-identical names.
- **[A8] Reduced motion**: 0 on 12 routes × 2 preferences.

## New-defect register (first seen r5)

- **[R1] Timetable empty state under the fixed nav / off-screen**: `.timeline__empty` `position:absolute; bottom:var(--sp-3)` (`features.css:440-448`) → on the default view (today, six free bands) at 390×844 the text `[170,765,106,23]` is hit-tested as `nav.mobile-nav`, only the glyph and ~5 px of the label visible on landing; at 320×568 the whole box is 219 px below the viewport; clear at 430. Replaces r4 [N12]'s collision with a different one on the primary screen [V-R1][S-R2][V6].
- **[R2] Compose labels wrap in the row-of-three**: "Share anonymously" / "Keep private" break to two lines in 103/85 px pills (47 px, off the 44 ladder) at 320×568 and 320×600; "Share anonymously" at 390×620 (`features.css:1275-1287`); hit-testable, names complete [V7][V-R2][S-R9][A6].
- **[R3] Status chips without rules**: `.chip--ok/--danger/--muted` deleted from `components.css` by `1f87fae` while `MinePage.tsx:244` still emits `chip chip--${tone}` — Published/Hidden/Removed fall back to the base `.chip`; code-verified against the served JS and CSS, unobservable on this account (Mine empty) [V-R3][S-R5].
- **[R4] Save-login dialog submit "Reconnect", no checkbox**: `ReconnectDialog.tsx:41` `mode="reconnect"` unconditional → `SchoolLoginForm.tsx:66` label "Reconnect" under the title "Save school login"; `:43` renders the stay-connected control only for `reconnect`; `:28` forces `stayConnected=true`; header comment `:16-19` false on both counts [C-D2][C-D3][V-R4][S-R8][A-R5-A5,A6].
- **[R5] Retry landing ring invisible on pointer activation**: `.focus-landing:focus-visible` is the only ring rule; programmatic focus after a click/tap does not match `:focus-visible` in Chromium → `outline 3px none` on all 6 reachable surfaces; keyboard Enter paints the ring [V-R5][S-R3]. Plus: History/Explore landings scroll the filters off-screen (+174/+278 px) [A-R5-A1]; 5/7 landings have no role or name [A-R5-A2]; Timetable/History landings are focus dead-ends (next Tab → nav) [A-R5-A3]; the feed landing is simultaneously tabpanel + polite live region containing the alert [A-R5-A4].
- **[R6] Composer fails open on registry error**: `ComposePage.tsx:197` guards only `!entities.data && !entities.error`; with `/api/entities` aborted at `?entityKey=teacher:t_doesnotexist` the full editor mounts, target "Loading… / Teacher", Share and Keep private enable after typing, no `role=alert` (pre-existing condition, first measured; the skeleton branch makes the "Loading…" label persist) [S-R6].
- **[R7] Composer collapses never-listed and delisted**: `ComposePage.tsx:191-203` consults `/api/entities` only; `room:r_d48ff4b2f68a` / `r_48a1706eca5e` get "Nothing is listed at this address…" with no survivor link while the entity page says "This entry is no longer listed. Open the current entry for 213" [C-D4][C-M4][S-R11].
- **[R8] Import disclosure names a non-trigger**: "…and again whenever you press Sync now or pull to refresh" — pull-to-refresh issues no `POST /api/sync` on any surface (measured); wording dictated by the r4 handoff from the false comment `BE/routes/auth.ts:30-31` [C-D1][C5-D].
- **[R9] P1–P6 explanation off-screen and absent on error**: `.timetable-foot` `[16,800,358,18]` under the nav at 390 on landing, below the fold at 320, only in the success branch [C-D10][S-R1][V-R8].
- **[R10] ··· outside-click loses the trigger**: the added `moreBtnRef.focus()` (`ExperiencePost.tsx:118-120`) is overridden by the pointerdown default, which now focuses the feed's `tabindex=-1` container — two r4 fixes cancelling; 0/3 clicks return to `···` [S-R4][A-R5-A7].
- **[R11] Never-listed entity page CTA 189×22**: bare inline `<a>` in `<p class="muted">` (`EntityPage.tsx:74`), outside the hit-area rule, while the composer's same CTA is a 220×44 primary button [S-R7][A-R5-A8].
- **[R12] SW eviction regressions**: activate-time eviction throws (array as `html`); Dash chunk still deleted (JS regex never matches Vite's relative import); ~99 KB `no-store` re-download per SW cold start on the navigation critical path; deploy trigger keys on one sorted filename; `sw.js` `max-age=14400` [W6].
- **[R13] Titles and sentences**: entity pages "Experiences · HOney", 404 and `/login` "HOney" (`AppLayout.tsx:45`; `LoginPage`/`NotFoundPage` outside the layout); "Nothing is listed at this address." vs "Nothing lives at this address."; " · Sync signs in again…" vs "Sync now signs in again…" [C-D5][C-D7][C-D8][A-R5-A10].
- **[R14] AT residue new this round**: PTR idle name inside `aria-hidden` [C-D6]; `not_eligible` note no `role` [C-M7]; dialogs without `aria-describedby` [A7]; hidden date input carries `aria-label` while `aria-hidden` [S8]; fallback shows two date affordances [A-R5-A9]; `/settings#privacy` lands ringless [S-R13].
- **[R15] CSS hygiene residue**: 10–11 consumer-less classes, 4 of them dangling descendants of deleted base rules; 4 dangling comments (1 surviving, 3 new); 90 px compact-height clearance uncommented (`features.css:1192`); `.stat`/`.stat span` orphaned [S4][V1][V-R6][V-R7][S-R10].
- **[R16] Repeated-affordance additions**: "Delete…" ×2 on Settings (account vs imported data, same label and style, `SettingsPage.tsx:101,213`); families 13 → 14 [S3].

## Standing residue (unchanged, no r4 move targeted it)
`banner`/`contentinfo` 0; `#main` no `tabindex` (skip link BODY on 4 routes); modal background not `inert`; Home "Open timetable" vs "Tomorrow" chip; six connection verbs on one card; dual nav DOM; compose path 4/3 with BODY on landing; Why "Back to Experiences" 133×22; admin CSS in the student bundle; 123,968 B unused font subsets; static `theme-color` outside the palette; Explore mark 0/19 (separator ships unobserved); `/api/consent` answers `{ok:true}` while ignored.

## Gaps (all passes)
Mine error/loading/populated branches need a seeded key (0 real keys) — [R3] code-verified only; saved-login states rendered with the `honey.portal.cred` sentinel; `sessionExpired=true` body produced by rewriting `/api/me`, not a real expiry; no dialog submitted, no Sync/Reconnect/Disconnect/Delete/Turn off/Sign out/publish/report/Keep private; delisted-duplicate survivor state now unreachable on live data (measured against a synthesised `/api/entities`); Explore mark unobservable; `showPicker` real picker unobservable headless; screen-reader runtime untestable; SW TSX-only deploy not staged (Playwright cannot intercept SW-originated navigations — predicate evaluated offline against the real index); SW termination forced via CDP; timings not load-controlled vs r4; harness TZ UTC (`.nextlesson__wash`/now-line never render); no room-bearing lesson block in the probed window; 430/desktop censuses by the Visual pass only.
