# 03 — Verdict (r2)

**REDESIGN.**

The r1 handoff verified largely as built — containment, focus, enabled-text contrast, primary touch targets, the type ramp, the humanized countdown, code-splitting and dark boot all measure fixed on the live HOney app — yet at 16/30 no principle crossed an anchor boundary: spacing is a system on paper only (39 token uses against 159 raw-px rules) and the night focal chip is unreadable, "revoke" and a leaked raw entity key still sit on student paths, two states remain rough beside a fresh detail cluster, the timetable still pulses at idle, and three removable duplications survive — so the presentation layer still has to be finished from the tokens down rather than patched.

## Why the verdict is mechanical — and what it is sensitive to

Phase 3 rule: total < 20 → REDESIGN. The total is 16. No principle scored 0.

Robustness check, stated honestly: six principles (#1, #3, #5, #7, #8, #10) were settled downward by the lower-tie-breaker rule. Granting every one of those upward would reach 22 — above the REFINE threshold. In r1 the same exercise reached only 19. So the verdict is REDESIGN by the rule the skill commits to, but the shipped design is now within tie-break distance of REFINE, and the audit says so plainly rather than re-scoring to reach either answer. The practical consequence: the redesign scope is narrow and enumerable — every move below is a finish-the-migration or remove-the-pattern move, none is a start-from-purpose teardown.

Why r2 equals r1 numerically despite verified fixes: the anchors score threshold conditions, not effort. Each principle's blocking condition survived — #3 (spacing adherence + one jarring violation), #4 (jargon present), #8 (two rough states), #9 (idle animation), #10 (≥3 removable) — while the fixes cleared sub-findings inside those conditions.

Anti-pattern check: not sunk-cost (codebase size played no role); not single-screen (defects measured across Home, Compose, Explore, Mine, Feed, Timetable, Settings); not hedged (one verdict).

## Highest-leverage moves

1. **#3 aesthetic — Finish the spacing migration and remove the last hard-coded ink.** Convert the 159 raw-px margin/padding/gap declarations (foundations 12, components 48, features 99; 116 off-ladder literals) to the `--sp-*` ladder so the rendered rhythm stops being dominated by 2/10/6/14/9/18 px; collapse the Explore 50 px head-to-filter gap (foundations.css:116,120, 30+18 px). Replace `color: #15181a` in `.nextlesson__state` (features.css:96-104) with a token that resolves on night — the chip measures 1.54:1 under a genuine dark boot. Evidence: [V2][V4][V6].
2. **#8 thorough — Clear the loading/error roughness and the residual detail cluster.** Compose entity lookup must never show the raw key (`ComposePage.tsx:84` `entity?.name ?? effectiveEntityKey`) — use a skeleton or "Looking up…" label; feed error banner needs a Retry action (only pull-to-refresh recovers today); at 320×568 the privacy footnote sits 9 px below the fold and behind the fixed nav (y498–558); delisted rooms (`/experiences/room/r_df12aeba9bb9` "Not selected", `r_d48ff4b2f68a`) still render entity pages with a Share CTA (`EntityPage.tsx:66,80-85` vs `entities.ts:130-135`) — redirect to the surviving id or 404; bring history chips (40 px, features.css:1257), the post context link (65×18, features.css:934) and the Settings checkbox (18×18) to 44 px; drop the two unused imports in `shared.tsx:5,7`. Evidence: [V7][V11][V8][S2.3][A3][S1].
3. **#4 understandable — Finish the vocabulary and land the privacy link.** Replace the four surviving "revoke" strings — the Mine action button "Revoke…" (`MinePage.tsx:258`) opens a dialog titled "Remove this post?" (`:199-201`); "finds and revokes" (`MinePage.tsx:152`); "find or revoke" (`SettingsPage.tsx:215`); "manage or revoke" (`ComposePage.tsx:140`). Give the Explore surface one vocabulary: "Browse teachers, places & food" (`ComposePage.tsx:206`) and "Filter by name…" (`ExplorePage.tsx:79`) vs "Find someone or something". Make "How privacy works" (`ComposePage.tsx:304`) land on its section (`SettingsPage.tsx:199`, currently y≈1434 off-screen). Make history chips read as navigation, not filters. Evidence: [C1][C4][V7][S1].
4. **#10 as little design as possible — Remove the duplications the commit claimed and did not ship.** The composer banner still renders a Keep-private button (`ComposePage.tsx:249-257` byte-identical to `3d91cb3^`; 4 buttons defined, 2 co-visible); make the feed header Share conditional so an empty feed does not show two compose CTAs (`FeedPage.tsx:55` vs `:130,:140`) and does not co-render with the interleaved invite (`:148-154`); decide whether a quick-jump strip earns its place above a 19-row directory when 9 of 10 chip names repeat as rows (`ExplorePage.tsx:85-96` vs `:101-158`). Evidence: [S1][S2.4][S5].
5. **#9 environmentally friendly + #6 honest — Stop the idle pulse and fix the registry name split.** Replace the infinite `pulse-soft` on `.timeline__now::before` (features.css:349, 2.4 s) with a static mark or a one-shot; with 84 KB br, dark boot and reduced-motion already verified, this alone clears anchor 3. Sanitize directory course names at read time, not only on future imports (`ExplorePage.tsx:54-58` reads `dir.courses[].name`; `entities.ts:60-61` sanitizes only the registry) so one course does not carry two names on one screen, and narrow `sanitizeCourseName` (`entities.ts:23-28`) so a 5-character teacher name like "活动课老师" is not stripped. Evidence: [W4][S2.1][S2.2].

Also relay to the owner: the `honey-v1` service-worker cache never evicts superseded hashed assets across deploys (`sw.js:22-27`) — a slow, unbounded growth until a version bump ([W7]).
