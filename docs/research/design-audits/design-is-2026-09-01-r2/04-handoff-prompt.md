# 04 — /make-plan handoff (r2)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 3d91cb3). Current design failed audit at 16/30 with critical gaps in principles #3 aesthetic, #4 understandable, #8 thorough, #10 as little design as possible (all scored 1/3; no principle scored 0).

Verdict paragraph (quoted from 03-verdict.md):
> The r1 handoff verified largely as built — containment, focus, enabled-text contrast, primary touch targets, the type ramp, the humanized countdown, code-splitting and dark boot all measure fixed on the live HOney app — yet at 16/30 no principle crossed an anchor boundary: spacing is a system on paper only (39 token uses against 159 raw-px rules) and the night focal chip is unreadable, "revoke" and a leaked raw entity key still sit on student paths, two states remain rough beside a fresh detail cluster, the timetable still pulses at idle, and three removable duplications survive — so the presentation layer still has to be finished from the tokens down rather than patched.

Why redesign and not refine: total 16/30 is below the REFINE threshold of 20 under the audit's lower-tie-breaker rule; six principles sat on a tie and were scored down, so the scope is a narrow, enumerable finish-the-migration pass, not a start-from-purpose teardown — but the verdict follows the rule, and this prompt is the REDESIGN template.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 430×932). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Verify everything in a real browser against the deployed app, not from screenshots alone.

Preserve from current design (MUST NOT be touched — these verified live):
- Type ramp tokens `--fs-micro…--fs-display` (apps/web/src/styles/tokens.css:57-66) and every rendered size on it: live sizes {12,13,15,16,20,22,24,26,28} px, floor 12 px, no fractional at 390/320. Regression check: rerun the rendered-font-size census at 390/320/430; nothing below 12 px, no new fractional sizes.
- Spacing ladder tokens `--sp-1…--sp-8` = 4/8/12/16/20/24/32/44 (tokens.css:71-79) — the ladder is right; only adherence is missing.
- Palette and accent discipline: 44 color literals, one accent per surface, warm hues only on --danger/--ok (tokens.css:13-40, 86-120); `--ink-3: #667079` (4.66:1).
- Route-settle fix: `.view { animation: settle .46s … backwards }` (foundations.css:342-347) — after settle, transform is `none`, overlays own the viewport (lesson modal 0,0,390×844; nav not hit-testable). Regression check: open the lesson modal at 390×844, assert overlay rect equals viewport and elementFromPoint at nav centres never resolves inside `.mobile-nav`.
- Focus system: 3 px accent outline, offset 3, on every control incl. new chips and header Share (foundations.css:68-77); report-dialog focus returns to the "···" trigger (ExperiencePost.tsx `moreBtnRef`); Modal.tsx trap/Esc/opener-restore.
- Scope tablist with arrow-key navigation (FeedPage.tsx:75-86); exactly two tabs, both fully visible.
- Enabled-text contrast: disabled primary 4.61:1 (components.css:97-103), placeholders/hints ≥5.34:1 (foundations.css:169), all r1 passes intact. Regression check: rerun the live contrast probe on stone and night.
- Humanized countdown "In 3 h 12 min" / "Tomorrow · 01:00" / "{Weekday} · HH:MM" and empty state "No upcoming lessons in your imported timetable." (HomePage.tsx:32-42,92).
- Dash code-split (App.tsx:8,57-62): student routes load only index-*.js (84 KB br); DashPage chunk only on /dash.
- Boot script honoring prefers-color-scheme pre-paint (index.html:14-32) and the stored-choice-wins order.
- Service worker precaching "/" and never caching /api/ (public/sw.js:13-19,32).
- Pull-to-refresh single MIN_SPIN (PullToRefresh.tsx:78-85): ~0.6 s spin, exactly one refetch.
- Feed header Share entry (FeedPage.tsx:55-57) — keep it, but make it conditional (see Discard).
- Explore complete-set rule: every section rendered with counts; search only filters (ExplorePage.tsx:101-158).
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:233,356; packages/shared/src/api/contract.ts:311-316), no author column (packages/backend/src/db/database.ts:90-121), 1:1 report categories, "scrambled at rest", WhyPage "design boundary, not magic", the new "HOney ID: … never shown on published experiences." line (SettingsPage.tsx:61-63).
- Registry hygiene that verified: 5 unique places, no "Not selected" in listings, no roster surnames in course names (packages/backend/src/experiences/entities.ts:20-30,63-95).

Discard (structural patterns causing the failures):
- Raw-px rhythm declarations beside a token ladder: 159 margin/padding/gap rules use literal px (foundations 12, components 48, features 99), 116 of them off-ladder (2,3,5,6,7,9,10,11,13,14,18,22,26,28,30,34,76,90,96,110 px), so rendered spacing is still a near-continuous 1–14 px series and Explore shows a 50 px head-to-filter gap (foundations.css:116,120). Caused failure on principle #3.
- Hard-coded ink outside tokens: `color: #15181a` on `.nextlesson__state` (features.css:96-104) — 1.54:1 on a genuine dark boot, the focal "Tomorrow · 01:00" chip nearly invisible. Caused failure on principle #3 (and #8).
- Raw identifiers as fallback labels: `entity?.name ?? effectiveEntityKey` (ComposePage.tsx:84) renders `teacher:t_23348879d1b4` as the compose target title during lookup. Caused failure on principles #4 and #8.
- The "revoke" vocabulary: button "Revoke…" (MinePage.tsx:258) opening "Remove this post?" (MinePage.tsx:199-201); "finds and revokes" (MinePage.tsx:152); "find or revoke" (SettingsPage.tsx:215); "manage or revoke" (ComposePage.tsx:140). Caused failure on principle #4.
- Explore naming split: "Browse teachers, places & food" (ComposePage.tsx:206) and "Filter by name…" (ExplorePage.tsx:79) beside "Find someone or something" (ExplorePage.tsx:66, FeedPage.tsx:59); chips styled as filters that navigate away (ExplorePage.tsx:85-96). Caused failure on principle #4.
- Error states without recovery: feed error banner "…try again." with no Retry control (FeedPage error branch; only pull-to-refresh recovers). Caused failure on principle #8.
- Compact-compose that hides the privacy footnote: at 320×568 the footnote is 9 px below the fold and behind the fixed nav (y498–558) (ComposePage.tsx:300-310; components.css:724 nav clearance). Caused failure on principle #8.
- List-level-only delisting: deactivated rooms (`r_df12aeba9bb9` "Not selected", `r_d48ff4b2f68a`) still render entity pages with "Share your experience" (EntityPage.tsx:66,80-85) while the registry hides them (entities.ts:130-135). Caused failure on principles #8 and #2.
- Sub-44 targets that survived: history chips 40 px (features.css:1257), post context link 65×18 (features.css:934), Settings checkbox 18×18. Caused failure on principle #8.
- Duplicated compose affordances: composer banner Keep-private button (ComposePage.tsx:249-257, unchanged since 3d91cb3^; 4 defined, 2 co-visible); unconditional header Share co-rendering with empty-state CTAs (FeedPage.tsx:55 vs :130,:140) and the interleaved invite (:148-154); 9 of 10 history chip names repeating as rows on the same 19-row screen. Caused failure on principle #10.
- Always-on idle motion: `pulse-soft 2.4s infinite` on `.timeline__now::before` (features.css:349). Caused failure on principle #9.
- Two names for one course on one screen: chips read unsanitized `dir.courses[].name` (ExplorePage.tsx:54-58) while rows read the sanitized registry (entities.ts:60-61); `sanitizeCourseName` (entities.ts:23-28) strips a 5-character teacher name ("活动课老师"). Caused failure on principle #6.
- Dead code shipped: unused imports shared.tsx:5,7 and the empty "The post card" section comment (shared.tsx:185-187). Caused failure on principle #8.

Top 5 moves from the audit (verbatim):
1. #3 aesthetic — Finish the spacing migration and remove the last hard-coded ink. Convert the 159 raw-px margin/padding/gap declarations (foundations 12, components 48, features 99; 116 off-ladder literals) to the `--sp-*` ladder so the rendered rhythm stops being dominated by 2/10/6/14/9/18 px; collapse the Explore 50 px head-to-filter gap (foundations.css:116,120, 30+18 px). Replace `color: #15181a` in `.nextlesson__state` (features.css:96-104) with a token that resolves on night — the chip measures 1.54:1 under a genuine dark boot. Evidence: [V2][V4][V6].
2. #8 thorough — Clear the loading/error roughness and the residual detail cluster. Compose entity lookup must never show the raw key (`ComposePage.tsx:84` `entity?.name ?? effectiveEntityKey`) — use a skeleton or "Looking up…" label; feed error banner needs a Retry action (only pull-to-refresh recovers today); at 320×568 the privacy footnote sits 9 px below the fold and behind the fixed nav (y498–558); delisted rooms (`/experiences/room/r_df12aeba9bb9` "Not selected", `r_d48ff4b2f68a`) still render entity pages with a Share CTA (`EntityPage.tsx:66,80-85` vs `entities.ts:130-135`) — redirect to the surviving id or 404; bring history chips (40 px, features.css:1257), the post context link (65×18, features.css:934) and the Settings checkbox (18×18) to 44 px; drop the two unused imports in `shared.tsx:5,7`. Evidence: [V7][V11][V8][S2.3][A3][S1].
3. #4 understandable — Finish the vocabulary and land the privacy link. Replace the four surviving "revoke" strings — the Mine action button "Revoke…" (`MinePage.tsx:258`) opens a dialog titled "Remove this post?" (`:199-201`); "finds and revokes" (`MinePage.tsx:152`); "find or revoke" (`SettingsPage.tsx:215`); "manage or revoke" (`ComposePage.tsx:140`). Give the Explore surface one vocabulary: "Browse teachers, places & food" (`ComposePage.tsx:206`) and "Filter by name…" (`ExplorePage.tsx:79`) vs "Find someone or something". Make "How privacy works" (`ComposePage.tsx:304`) land on its section (`SettingsPage.tsx:199`, currently y≈1434 off-screen). Make history chips read as navigation, not filters. Evidence: [C1][C4][V7][S1].
4. #10 as little design as possible — Remove the duplications the commit claimed and did not ship. The composer banner still renders a Keep-private button (`ComposePage.tsx:249-257` byte-identical to `3d91cb3^`; 4 buttons defined, 2 co-visible); make the feed header Share conditional so an empty feed does not show two compose CTAs (`FeedPage.tsx:55` vs `:130,:140`) and does not co-render with the interleaved invite (`:148-154`); decide whether a quick-jump strip earns its place above a 19-row directory when 9 of 10 chip names repeat as rows (`ExplorePage.tsx:85-96` vs `:101-158`). Evidence: [S1][S2.4][S5].
5. #9 environmentally friendly + #6 honest — Stop the idle pulse and fix the registry name split. Replace the infinite `pulse-soft` on `.timeline__now::before` (features.css:349, 2.4 s) with a static mark or a one-shot; with 84 KB br, dark boot and reduced-motion already verified, this alone clears anchor 3. Sanitize directory course names at read time, not only on future imports (`ExplorePage.tsx:54-58` reads `dir.courses[].name`; `entities.ts:60-61` sanitizes only the registry) so one course does not carry two names on one screen, and narrow `sanitizeCourseName` (`entities.ts:23-28`) so a 5-character teacher name like "活动课老师" is not stripped. Evidence: [W4][S2.1][S2.2].

Redesign principles in priority order:
1. #3 aesthetic — success: zero raw-px margin/padding/gap declarations outside canvas-geometry math; a rendered-spacing census at 390 returns only ladder values (4/8/12/16/20/24/32/44) plus documented exceptions; zero ink literals outside tokens.css; every text element ≥4.5:1 on stone AND on a genuine dark boot, including the Home focal chip.
2. #8 thorough — success: compose lookup shows a skeleton or plain label and never an internal key; every error state offers a recovery control; the privacy footnote is readable without scrolling at 320×568; delisted entity URLs never show a Share CTA; every tappable element ≥44 px; `tsc --noUnusedLocals` clean.
3. #4 understandable — success: `grep -ri revoke apps/web/src` returns nothing user-facing; one name for the Explore surface everywhere it is referenced; "How privacy works" scrolls to its section; chips are visibly links.
4. #10 as little design as possible — success: at most one Keep-private control visible in any composer state; never two compose CTAs on one feed screen; either the chip strip or the duplicate row names, not both.
5. #9 environmentally friendly — success: `document.getAnimations()` is empty on every idle screen after settle.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — Explore's history strip and the compose entry model (header Share vs empty-state CTAs vs interleaved invite) need one decided structure; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): feed → share → typing, showing where the target chooser, the keep-private control and the privacy link live at 390×844 and 320×568.
- States checklist (empty, loading, error, success, focus, disabled) per screen, with the compose loading and feed error states drawn explicitly.
- Migration path for users currently on the old design: none needed for students (same routes, same data); note the SW `honey-v1` cache name must bump so superseded hashed assets are evicted (public/sw.js:22-27).
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (spacing census, contrast on dark boot, raw-key absence, revoke grep, animation count, target sizes).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the raw-px rules must be converted, not wrapped in new class names.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients, or type faces; the palette and font are fixed.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them.
- Claiming a change in the commit message that the diff does not contain (the r1 Keep-private banner change never landed) — verify each move on the deployed app before reporting it done.
````
