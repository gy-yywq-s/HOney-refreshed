# 01 — Evidence (r7, consolidated from five Opus evidence subagents; scoring withheld to 02)

Target: https://honey.gaelisus.com at `213707d`, served `index-CkJ_sIHn.js` (274,090 B raw) / `index-CukErUfj.css` (40,914 B raw), verified byte-identical to `apps/web/dist` by all five passes. Probes: `/root/claude-work/design-audit/r7-{struct,vis,copy,wf,a11y}-*.js` + `.log` + `r7-*.png`. Harness in UTC; every lesson-block measurement used a hand-built `timezoneId:"Asia/Shanghai"` context (in-page "today" = 2026-09-02, a 3-lesson day; 3-lesson control day 2026-08-24; empty days 2026-08-22 Sat, 2026-09-06 Sun). Hard limits held: no publish, no report submitted, no Sync/Reconnect/Save/Disconnect/Delete/Sign-out pressed (dialogs opened and cancelled); every reaction tap undone in-script; no secrets read. Anchor prefixes: [S-R7-n] Structural, [V-R7-n] Visual, [C-R7-n] Copy & Honesty, [W-R7-n] Weight & Friction, [A-R7-n] Accessibility.

## 0. Verification of the r6 handoff (all four moves) — live

| r6 move / claim | Status | Evidence |
|---|---|---|
| Coarse-pointer 44 px floor for `.btn--small`/`.react-btn`/`.usermenu__button` | **CONFIRMED** — served `@media (pointer:coarse){.btn--small,.react-btn,.usermenu__button{min-height:44px}}` (components.css:828-835). Touch census in `hasTouch` at 390×844 and 320×568: 518 boxes (S) / 161 per viewport (A) / 136 per viewport (V); every r6 offender now 44 px — "Try again" 77×44 on all 8 error surfaces, reactions 49×44, `···` 44×44, feed header 59/183/131×44, Timetable History/Sync now/Back to today 67/79/104×44, Mine "Forget it" 75×44, 24 History "Select" 62×44, menu item "Report" 110×44. Residual sub-44: 2 controls, both outside the rule and pre-existing — bare `a` "Back to Experiences" 133×22 on `/experiences/why` (`WhyPage.tsx:100`, no class; the same label on Explore is `a.btn.btn--ghost` 170×44) and the login `input[type=checkbox]` 22×22. Desktop fine pointer keeps 36/38. | [S-R7-6][V-R7-1][V-R7-F1][V-R7-F3][A-R7-1][A-R7-F] |
| Pressed theme card `background: var(--ink)` | **CONFIRMED** — pressed card bg `rgb(35,43,49)`/color `rgb(244,246,247)` on stone, `rgb(233,237,239)`/`rgb(20,23,26)` on a genuine dark boot; other three translucent white; label contrast 13.26/15.27. | [V-R7-2][S-R7-3c] |
| Chip modifiers after `.chip` | **CONFIRMED** — three synthesised Mine rows give 3/3 distinct (background, colour) pairs on both boots; contrast 5.57/5.68/5.16 stone, 8.39/7.16/6.10 night. | [V-R7-3][V-R7-D3] |
| Dead `.timeline__empty svg` rule + six dangling comments removed; note renamed `.timetable-note`, spaced | **PARTIAL** — the 20 px duplicate is gone (one `svg` rule survives, 0 whole-head duplicate selectors); the six named comments are gone (S detector: 14 → 8, six of the eight remaining are trailing documented-exception comments; one true dangling comment at features.css:344-345; V detector: 1). The rename shipped, but **`.timetable-note{margin-top:var(--sp-3)}` (features.css:226-228, specificity 0,1,0) is overridden by `p.caption,p.text-4{margin-block:0}` (foundations.css:160-163, 0,1,1) — computed margin 0 px at all six viewports.** The 8 px gap between the note and the empty line comes from `.timeline__empty{margin:var(--sp-2) 0}`; on a lessons day note-bottom → canvas-top is **0 px** and the note's box overlaps the `09:00` hour label by 27×6 px at 390 and 1280 (crowding, ~2 device px between glyphs). | [S-R7-3][V-R7-4a][V-R7-4b][V-R7-4c][V-R7-G1][V-R7-J2] |
| Empty day states emptiness once | **CONFIRMED** — `.timeline__canvas--empty .timeline__ghost:not(.timeline__ghost--break){display:none}`; six period ghosts remain in the DOM with `display:none` and no boxes; two break ghosts (Lunch Break, Dinner Break) stay; one `role=status` line "No lessons on Sat 22 Aug" / "No lessons on Sun 6 Sept"; 0 overlaps on the empty day at 320 and 390. Residue: the note "P1–P6 are the school's six lesson periods…" still renders on a canvas where zero P labels render. | [S-R7-11][V-R7-5][V-R7-10e][A-R7-7] |
| Landing ring bound to `data-landed` | **CONFIRMED, no stale ring** — `useRetryFocus.ts:16-18` sets `data-landed`, clears on `blur` `{once:true}`, focuses `{preventScroll:true}`; CSS `.focus-landing[data-landed]:focus` (features.css:1292). Ordinary mouse AND touch tap on plain text in 8/8 landings: focus moves to the container, `data-landed` absent, `outline-style: none`, 0 accent pixels on the edge bands (16/16). Retry by mouse/touch/keyboard on 8/8 error surfaces: `data-landed=""`, ring `3px solid rgb(51,102,124)` offset 3, scroll 0→0, 13,679–22,494 accent pixels (24/24). Hunt: click-away clears attribute and ring; retry twice → ring both times; retry then Tab → focus to next real control, attribute cleared; navigate away/back → 0 stale; exactly one `blur` listener per landing, 0 residual; landings out of the Tab order (0/91 stops). | [V-R7-6][A-R7-2][W-R7-25][S-R7-13] |
| `···` outside-click guard ignores `tabindex=-1` containers | **CONFIRMED** — `ExperiencePost.tsx:124` `[tabindex]:not([tabindex="-1"])`; 5/5 outside-click variants (post text mouse, real control, `#main` background, landing edge, post text touch) × Feed and Entity: menu closes, focus returns to `···` (without painting a ring; Escape returns with the control ring). A link inside the post clicked while the menu is open keeps its own focus and navigates. Comment at :121-123 now accurate. Mine has no `···` (its cards are `MineExperienceCard`/`PrivateNoteCard`). | [A-R7-3][S-R7-15][C-R7-9] |
| Composer registry-error retry has a landing; reactions keep focus | **CONFIRMED on success / NEW DEFECT on failure** — retry that succeeds lands on `section.compose-editor.focus-landing[role=region][aria-label="Editor"]` with `data-landed`, ring 22,494 px, scroll 0, by mouse/touch/keyboard (the textarea itself is not focused). **A retry that fails again drops focus to `(body)`**: the editor landing is rendered only when `target` exists (`ComposePage.tsx:298`), so on the error branch `ref.current` is null and `useRetryFocus.ts:15` returns early — the only one of eight landings absent from its own error branch. Reactions: `aria-disabled={busy||undefined}` (`ExperiencePost.tsx:226,238`), `if (busy) return` guard (:162); focus stays on the button during and after the round trip by Enter, Space and pointer; a second real tap at 40/80/150 ms while pending → 1 POST; two `.click()` in the same JS task → 2 POSTs (a case native `disabled` blocked). | [A-R7-4][A-R7-5][S-R7-13][S-R7-14][W-R7-10][W-R7-26] |
| `#main:focus` inset ring; `/history` landing | **CONFIRMED** — `.main:focus{outline:3px solid var(--accent);outline-offset:-3px}` on `main.main#main[tabindex=-1]`; skip link "Skip to content" → 27,005 ring px light / 22,584 dark on 12/12 routes, contrast 5.81/9.28, scroll 0. The inset band sits over content at the sampled mid-edges on 5/12 routes (Settings, History, Explore, Why, Dash). `/history`: the region "Lessons" still has 0 focusables, but Tab after `#main` goes to "Search lessons" inside `#main` — focus stays in content. The 404 renders `<main class="login">` with no id, no skip link, no nav. Login's skip link targets `#school-username` and works. | [A-R7-6][A-R7-E][V-R7-7][S-R7-17] |
| Save-login dialog: one-line label, submit in the first fold at 320×568 | **CONFIRMED with residue** — label "Stay connected on this device." 1 line (h 20); caveat in `#school-dialog-body` (120 px); submit `[16,517,288,44]` bottom **561** ≤ 568; **sheet still overflows 17 px (scrollHeight 539 vs clientHeight 522, `overflow-y:auto`)**; at 390×844 no scroll. **Reconnect purpose kept the long label**: 4 lines / 78 px at 320×568 ("Stay connected on this device — encrypted, kept only here (a browser is less protected than a phone's secure storage)."), sheet 525/522 scrolls, submit bottom 547; 3 lines at 390. One component, two label grammars; the `ReconnectDialog.tsx:14-18` doc comment ("with the same caveat the login shows") is now stale. | [V-R7-9a][V-R7-9b][S-R7-22][C-R7-7][C-R7-E14] |
| Timetable compact canvas 540 (≤620 h) / 450 (≤360 w); landing scroll; every block hit-tests to itself at 320×568 | **CONFIRMED with new defects** — `.timeline` min-height 620 @390×844/430×932, 540 @390×620, 450 @320×568/320×600/360×620 (features.css:759-777); band 84/73/61 px; hour labels 12 px evenly spaced; `.lesson-block{scroll-margin-top:140px}` (:355). On the 3-lesson day at all six viewports 12/12 blocks: centre in viewport, `elementFromPoint` = the block's own descendant, not under nav, not below fold — r6's P3-under-nav at 320×568 refuted (P3 `[79,320,216,61]`). Lesson modal contains without scrolling at all six (320×568: actions bottom 544). Text: no vertical clipping on real lessons (61/61); subject ellipsis on "CIE Chinese Language & Literature" at 216/252 px widths (by design, `white-space:nowrap`); room label hidden ≤359 px (pre-existing). **Defects:** (a) the landing scroll is `scrollIntoView({block:"start"})` (`TimetablePage.tsx:304-312`) with no `behavior`, so the owner's `scroll-behavior:smooth` (components.css:520) animates the canvas ~120 ms, ~200 ms after FCP (168 → 363–480 ms at 320×568) — a visible unrequested jump; instant under `reduce`; (b) it fires on every date change (Next day ×2: 0→188 each) and on retry (0→77, simultaneously with the retry ring — two landings on one screen; retries no longer move the page 0 px on compact heights); (c) it is lost on SPA re-entry — `AppLayout.tsx:33-35` `scrollTo({top:0})` on pathname change wins when cached data lets the child effect run in the same commit (scrollTop 0 after /home → back); (d) after landing the sticky daynav and the History/Sync-now row sit on `h1.schedule-header` and `.timetable-note` — 8 genuine overlaps at 320×568 and 360×620 (up to 79×26 px), 5 at 390×620; the day heading and the P1–P6 note are invisible on arrival; (e) the stated goal ("last lesson clears the nav") is missed by 6 px on today's day at 320×568 (last block bottom 500 vs nav top 494) and by 30 px at 390×620; (f) **latent:** a synthesised <45 min lesson on the 450 canvas renders 27 px tall with a 7 px meta box for a 12.6 px line → `clipY:true`, text cut mid-glyph (no such lesson in this account's data). Empty day at 320×568: scrollTop 0, CLS 0, 0 overlaps. | [S-R7-9][S-R7-9b][S-R7-9c][S-R7-10][V-R7-10a..10f][V-R7-G1][W-R7-20..24] |
| Empty sentence date-aware; `?date=` rejects impossible dates; 404 own sentence | **CONFIRMED with residue** — "No lessons today" only when `isToday` (forced-empty today), "No lessons on Sat 22 Aug" / "Sun 6 Sept" / "Thu 3 Sept" / "Thu 1 Jan" (2099) otherwise, `role=status`. **Two date formats from two locale sources on one screen**: heading `formatDayHeading` → `toLocaleDateString(undefined,…)` "Saturday, August 22, 2026" (browser locale) vs sentence/stepper `formatStepperDate` → hard-coded `"en-GB"` "Sat 22 Aug"; `en-GB` short month gives "Sept" (4 letters), contradicting the "tabular" comment at `TimetablePage.tsx:22`. `?date=2026-13-45`, `2026-02-30`, `2026-00-10`, `0000-00-00`, `abcd`, `banana`, unpadded `2026-2-3` → all fall back to today **silently**: URL unchanged, no redirect, no notice, "Back to today" absent. 404: title "Not found · HOney", h1 "Page not found", body "That page doesn't exist." — distinct from the never-listed entity's "Nothing is listed at this address." (F2 count now 0), but the **title** "Not found · HOney" is still shared by the 404 and the never-listed entity page, and the 404's title ≠ its h1. | [C-R7-1][C-R7-2][C-R7-3][C-R7-D4][C-R7-E11][C-R7-E12][S-R7-16][S-R7-17][A-R7-7] |
| Mine grammar and region; reconnect body; hidden date input; Explore `group`; `describedBy` | **CONFIRMED** — "1 stored key no longer matches…" / "{n} stored keys no longer match…" with "Forget it/them" (3/3 states); region = h1 = title "Your notes & posts"; entity region "ChenJenny experiences" in OK and error branches (kind fallback gone); the third reconnect body and `sessionExpired` prop removed, both surviving bodies reachable; hidden date input: `aria-label` absent while `aria-hidden`, `hidden={pickerVisible}` on the button → fallback shows 1 visible affordance, 0 unnamed (showPicker undefined and throwing); Explore `role=group` "Everything listed" with four `section` regions "Teachers 6 / Courses 8 / Places 5 / Food 0", search only filters (all four headings survive "zzzzz"); `aria-describedby` resolves on 5/7 dialogs (delete-account, delete-data, report, Reconnect, Save) — lesson modal and desktop theme dialog have none; **no dialog sets `inert` or `aria-hidden` on `.app-frame`**; all seven name themselves with `aria-label` duplicating the visible h2; focus trap + Esc + opener restore 7/7. | [C-R7-4][C-R7-5][C-R7-6][A-R7-8][A-R7-9][A-R7-10][A-R7-11][S-R7-20] |
| Feed `aria-live` always mounted (items only) | **CONFIRMED — NEW AT DEFECT** — `<div aria-live="polite">` (no `aria-atomic`/`aria-relevant`/`aria-busy`/name) mounted in skeleton, error and items branches; 0 children and 0 px tall when empty (no paint, CLS 0, request count unchanged); `role=alert` banner and skeleton outside it. Because it mounts empty at ~475 ms and is filled at ~578 ms (one insertion for the real post; 8 insertions in one tick for a fulfilled 8-item page, 1,120 chars), **a polite region announces the entire feed on first load**; tab change = 1 removal + 1 addition (wholesale replacement, whole list announced again). Load-more unmeasurable (1 real post). Depth unchanged (the wrapper existed in the items branch before); the empty/error feeds carry one zero-content node. The feed landing `div.feed-stream[role=tabpanel]` has no accessible name. | [A-R7-12][W-R7-18][S-R7-2][S-R7-4] |
| `useApi` in-flight dedupe; compose shares "entities" | **CONFIRMED — with two demonstrated hazards** — `useApi.ts:14` `inflight` map, `:55-64` `run()`; exactly **1** `GET /api/entities` on compose?entityKey= (r6: 2), plain compose, explore, entity, Mine; 0 on feed/home/history; 0 duplicate API paths on 14 cold views. (iii) a rejected promise is evicted (`.finally`), "Try again" issues a new request and recovers. **(ii) a refresh while a keyed request is in flight is silently swallowed**: `/api/entities` held 4–5 s, the pull-to-refresh bus (`honey:refresh`, `PullToRefresh.tsx:77-78` clears `cache` but not `inflight`) fired at +1.5/+2.7 s → 0 new `/api/entities` requests while `/api/directory` (settled) refetched twice; the 600 ms spinner runs and the pre-refresh body is written back into the cleared cache. **(i) latent**: same key, different fetcher → first caller's `fn` wins and its payload is cached under the shared key with no error (proven by executing `run()` standalone; no live pair today). Related: `useNames()` (`shared.tsx:160-168`) drops `directory.error`/`entities.error` — with `/api/entities` aborted the entity page renders fully and silently (title, h1, CTA, `banners: []`); plain `/experiences/compose` (two links) still fires `GET /api/entities` and `GET /api/directory` it does not use (`ComposePage.tsx:101`). SWR cache is an unbounded per-session `Map` with per-date keys. | [S-R7-18][S-R7-19][W-R7-6][W-R7-8..12] |

## 1. Structural [S]
- [S-R7-1] Interactive census at 390×844: Home 9/15, Timetable today (CST) 13/20 (UTC-comparable 10 — r6 10/17), lessons day 14/21, empty day 11/18, History 8/14, History?select=1 32/38 (24 Select), Settings 18/25, Feed 15/21, Explore 26/32, Compose 7/13, Compose?entityKey 10/16, Entity 6/12, Mine 6/12 (seeded 7/13), Why 6/12, Login 5/5, 404 1/1. Delta vs r6 on every comparable surface: **0**. `activeElement` on landing = BODY on all 18 rows.
- [S-R7-2] DOM depth #main → deepest interactive: Feed 9 (unchanged), Timetable 6 (lessons) / 5 (empty).
- [S-R7-5] Repeated-pattern families 16 (counting-boundary change from r6's 15, no code change): "Try again" ×8 (pixel-identical), `useRetryFocus` landing ×8, Skeleton ×9, `role=alert` banner ×12, status banners ×4, `.card` ×17, `.card-actions` ×13, Modal shell ×5, ConfirmDialog ×5, ReconnectDialog ×2, `h1.page-title` ×14, compose entry point ×12, pill grammar, hairline row ×4, empty state ×9, filter/search ×3.
- [S-R7-3] `npx tsc --noEmit --noUnusedLocals --noUnusedParameters -p tsconfig.json` → 0 (TS 5.9.3; re-run with `--incremental false`). Served CSS 40,914 B, 374 rules, 225 class names. Whole-head duplicate selectors 0; 5 individual selectors repeated in-context (`a`, `button`, `.caption`, `.feed-identity a`, `.post__context a`) — deliberate layering, unchanged. Dangling comments 8 by the S detector (6 trailing exception notes; 1 true: features.css:344-345); **unterminated comment at components.css:45 swallows the "Buttons" section banner at :46** (pre-existing). Consumer-less classes 2: `.reveal`/`.is-in` (foundations.css:220,228,244) — their only writer `export function Reveal` (`lib/motion.tsx:38-81`) is imported nowhere and tree-shaken (unchanged from r6). Orphaned descendant 1: `.option-grid span` (components.css:775-778; the picker renders `<i>`+`<strong>`, 0 spans). Classes without rules 5 (`home`, `home-head`, `home-voices`, `login__card`, `post__dot`) unchanged. Deletion scars: 4 double/triple blank-line runs [V-R7-J3].
- [S-R7-12] Task steps: next lesson 1 screen / 0 taps (hero `[16,95,358,217]`); stream 1 tap (focus stays on the nav tab, no ring); share 4 screens / 3 taps to the editor via History chooser (5 / 4 via Explore → entity), +1 tap for a cursor (textarea never auto-focused). Unchanged from r6.
- [S-R7-20] Same-screen duplicate labels/hrefs: none on any surface except `Select` ×24 (one per row); two `<nav>` in DOM (rail `display:none` ≤960). [S-R7-21] `SHARE_PROMPT_EVERY = 8` still in `FeedPage.tsx:18`; a fulfilled 12-post feed renders header "Share" (`btn--primary btn--small`) and the interleaved "Share an experience" (`btn--ghost btn--small`) together; the true-empty feed shows one action. `···` menu still wraps the single item "Report" (no accessible name on the menu).

## 2. Visual [V]
- [V-R7-A1] Rendered spacing census: 97.83 % (320), 97.61 % (390, 430), 92.62 % (1280) on the 4/8/12/16/20/24/32/44 ladder — r6 97.82/92.78. Off-ladder: `.main` nav clearance 96/90/110 px (documented), `calc(--sp-3 + 1px)` 13 px on two link rules, the 1 px hidden input, `auto`/`clamp` resolutions. Source: 214 spacing lines, 176 on `var(--sp-*)`, 2 literal px (both commented). New `scroll-margin-top:140px` is a scroll offset, not a spacing declaration; the `.timetable-note` 12 px margin is on-ladder but never applies [V-R7-4b].
- [V-R7-B1..B3] Type at 390: {12,13,15,16,20,22,24,26,28}; floor 12; one fractional 27.95 at 430; 10 off-ramp `font-size` declarations (unchanged). Inside compact blocks subject 15/22.5, meta 12/18.
- [V-R7-C1..C3] 43 colour literals; `tokens.css` blob `1d70d963…` byte-identical; rendered union 17 stone / 19 night across Feed+Timetable+Settings; non-token intrusions: `.swatch` border `rgba(0,0,0,0.18)` and four `.swatch--*` hex literals duplicating surface tokens (components.css:787-801).
- [V-R7-D1..D5] Contrast floor 4.66 stone / 5.83 night (`.home-head__stale`), 0 failures on 304 nodes × 9 routes + error (V) and 24 surfaces (A); every ring 5.81–6.30 stone / 8.36–9.28 night vs its adjacent ground (an accent ring on an ink fill would be 2.28/1.65 but the +3 offset never touches it); chips 5.16–8.39; pressed card 13.26/15.27; compact lesson text 7.61–13.75. Active nav label measured 1.00 by the ancestor-walk (sibling-pill artefact; true 13.26/15.27).
- [V-R7-E] States at 390: empty ✓ (Timetable/Explore/Mine/Entity), loading ✓ skeletons on Home/Timetable/Feed/Explore/History (Mine 0 with 0 keys; Entity/Compose skeletons not captured), error ✓ 44 px "Try again" on 8 surfaces (Explore shows the banner AND four "Nothing here yet." at once; with `/api/entities` aborted the Entity page renders the OK copy silently — see [S-R7-18]), success ✓ feed error→retry recovery, focus ✓ one ring grammar on every control, selected ✓ scope tab / theme card / chips, **disabled/pending rough**: [V-R7-E1] the pending reaction (`aria-disabled`) computes `opacity 1, cursor pointer` — byte-identical to idle (r6's `disabled` gave `opacity .7, cursor default`; `.react-btn:disabled` at components.css:249-252 and `:hover:not(:disabled)` at :245 are now dead for these buttons; 0 `[aria-disabled]` rules in any stylesheet); the optimistic `react-btn--on` class and `aria-pressed` still flip; the count text lags until settle; [V-R7-E2] two disabled treatments in one compose row (Share `opacity 1` grey fill vs Keep private `opacity .65` transparent). App-shell auth gate: bare centred "Loading…" text, no skeleton.
- [V-R7-F1..F3] Button-height families on mobile (coarse): **44** (15 classes, incl. every "Try again", reactions, `···`, daynav, login submit) and **81** (theme cards); r6's 36/38 gone; other heights are rows/canvas (45/46/48/49/50/61/73/84) and the two residual 22 px targets. Desktop fine pointer: 36/38/44/46/48-50/81.
- [V-R7-G1] Overlap census (fixed-nav pass-under excluded): 390×844 **1** (`.timetable-note` × `09:00`, 27×6), 1280 **1** (same), **320×568 8 / 360×620 8 / 390×620 5** — all Timetable, all caused by the compact landing scroll parking the sticky daynav/action row over `h1.schedule-header` and the note; empty day 0 at both widths.
- [V-R7-H1..H4] Modal containment 15/15 opens (5 dialogs × 320/390/1280): overlay = viewport, 60/60 nav hit-tests blocked, sheet padding `20/16/24` on ladder; every dialog button 44 except the Report dialog's 4th option wrapping to 47 at 320. Mobile has no theme dialog (inline ThemeControls); ThemeDialog is rail-only.
- [V-R7-I1..I3] `getAnimations()` 0 at 2.5 s on 11 screens × 2 preferences; 6 `@keyframes`; `skeleton-shimmer` only while mounted; `.view` one rule, `settle` byte-identical.
- [V-R7-J1..J8] In-context duplicate selector 1 (`a` at foundations.css:48/64); dangling 1 (the unterminated components.css:45); deletion scars 4; rules with no consumer 1 (`.option-grid span`); TSX classes with no rule 4–5; orphaned descendant selectors 0 by V's definition; the minifier strips comments so J2/J3 are source-only.
- [V-R7-K #7] Trend markers listed: `border-radius:99px` pill grammar; 17–20 px card radii with `--shadow: 0 14px 40px rgba(22,30,34,.1)`; floating detached translucent bottom pill-nav (inset 10, radius 17, `0.95` bg) with a 300 ms sliding ink pill; `color-mix()` tints; plus the per-route `settle` entrance. Counter-markers: no gradients, no glass blur, no icon-only nav, largest mobile type 28 px.
- [V-R7-L] Gaps: Explore retry ring not exercised (banner rendered, button not found in 2.8 s); Entity/Compose/Mine skeletons unmeasured; `.usermenu__button` coarse only at ≥960; short-lesson clip synthesised; write paths not exercised.

## 3. Copy & Honesty [C]
- [C-R7-A] Full string inventory with file:line (A1–A17 in the C report; delta since `03875ed`: 404 body changed; "No lessons on …" new; Mine grammar; Mine region; unreachable reconnect body + `sessionExpired` removed; label split by purpose; date input label conditional; date button `hidden`; reaction `aria-disabled`; `describedBy` ids; "Editor" region; Explore `group`; no change to WhyPage/Login/SchoolLoginForm/shared/client/contract/backend; no CSS `content:` text).
- [C-R7-B1] Inflations: **1** — "strongly protected" (`WhyPage.tsx:73`), unchanged; the next sentence ("a design boundary, not magic") supplies the honest bound. Backed absolutes checked (never shown / never published / no free text / nothing publishes unchecked). Watch: "Share anonymously" (`ComposePage.tsx:365`) is an absolute label qualified in three nearby places.
- [C-R7-C1] Dark patterns by the rubric: **0** across 7 categories (no billing; every irreversible action two-step with the cost in the body; the only countdown is the server-driven moderation cooldown; no confirmshaming; Save-login default-on reached only via "Save school login…" and disclosed twice; Reconnect default = stored state; login checkbox off; nagging = one passive aside per 8 posts).
- [C-R7-D1..D4] Jargon/unclear: provenance in two registers ("Verified — has taken this over time" `shared.tsx:17` vs "from someone who has taken this over time" `ExperiencePost.tsx:20`); "clearing site data" (`SettingsPage.tsx:374`, `MinePage.tsx:144`); a fourth "delisted" wording (`shared.tsx:69` "This entry isn't listed any more — it may have been removed." vs "This entry is no longer listed."); "Sept" 4-letter short month. Jargon defined at point of use otherwise: 0.
- [C-R7-E1] Seven privacy claims re-mapped 1:1, **0 drift** (no author column `db/database.ts:103-121`; `auth:false` + `credentials:"omit"` `api/client.ts:229,351-353`; `ownership_hash` sha256 `:116`; coarse `publishedDay` `format.ts:69-79`; `free_text_not_accepted` `routes/experiences.ts:208`; 6=6=6 categories; HOney ID line; "design boundary, not magic"; `contract.ts:12-16`).
- [C-R7-E1/E3] Import disclosure: `syncTimetable` callers still exactly two (`routes/auth.ts:32` guarded by `result.created`; `routes/data.ts:17`); Settings row names exactly those; nine-route sweep + real pull-to-refresh → 0 `POST /api/sync`; the Sync clause is truthful and shown only when a credential exists. **[C-R7-E2]** the login footnote ("Your timetable comes along with it.") names one of the two triggers and one of the two data types the Settings row names — narrower, not false.
- **[C-R7-E4]** "Delete imported data" overstates: row "Removes all timetable and history data imported from the school portal." (`SettingsPage.tsx:211`), body "All imported timetable and history data will be removed from HOney." (`:288`) ↔ `deleteImportedData` = one `DELETE FROM user_lesson_exposures WHERE honey_id = ?` (`services/accounts.ts:238-240`); the importer's school-wide `teachers`/`courses`/`rooms`/`lesson_instances` rows (`services/importer.ts:88-102`, no `honey_id` column, `db/database.ts:52-77`) and organic registry entries persist. Every user-linked row is deleted (all reads go through the exposure join, `services/timetable.ts:45-48`); the shared schedule facts are not. Same shape on "everything imported with it" (`SettingsPage.tsx:96` ↔ `deleteAccount` cascade). [C-R7-E5/E6/E8] Disconnect, "import again with Sync now" and Mine remove-post bodies truthful. [C-R7-E7] the delete-account confirm omits the row's "Published experiences stay" fact.
- [C-R7-E9] Save-login caveat truthful and stated at every point of decision; AES-GCM ciphertext and its key both in `localStorage` (`lib/portalCredentials.ts:20-21,78-88,116-124`) — exactly the caveat; no server-side password column; a sealed portal *token* is server-held, which "kept only here" (about the login) does not deny.
- [C-R7-E10] PTR label truthful. [C-R7-E11/E12/E13] titles: 12/13 match their h1; "Not found · HOney" serves both the 404 and never-listed entity; 404 title "Not found" vs h1 "Page not found"; Mine fully unified.
- [C-R7-8] Reactions while pending: `aria-disabled=true` announced, DOM `disabled=false`, opacity 1, cursor pointer, class `react-btn--on`, `aria-pressed` optimistic → the state channels contradict each other; a second click in the window is swallowed with no feedback.
- [C-R7-E14..E16] Stale code comments: `ReconnectDialog.tsx:14-18` ("same caveat the login shows"), `TimetablePage.tsx:92-94` (input "stays out of the tab order" — false on the fallback path), `TimetablePage.tsx:22` ("Sep" tabular). r6's [C9] fixed. `routes/auth.ts:30-31`, `db/database.ts:91-93`, `MinePage.tsx:25-27`, `portalCredentials.ts:8` accurate.
- [C-R7-F1/F2] Two-sentences-one-idea on one surface: 1 clear (Save dialog body + checkbox), 2 borderline; one-sentence-three-surfaces: **0** (r6's finding resolved). Cross-surface: "stay connected" worded four ways, "no author ID" five places, "scrambled not secret" three.
- Gaps: forced-empty today via route-fulfil; en-US locale only; write paths and Dash not exercised; [C-R7-E4] is a schema/service reading.

## 4. Weight & Friction [W]
- [W-R7-1..4] JS 85,987 B br / **88,310–88,461 B zstd (what Chromium actually receives — Cloudflare prefers zstd)** / 84,646 gzip / 274,090 raw (+296 br, +970 raw vs r6); CSS 9,281 br / 10,010 zstd / 40,914 raw; Dash chunk 4,404 br, **0/14** student loads; no new dependency.
- [W-R7-5..7] Requests: cold /home 12 (3 data), /timetable 12 (2), /experiences 11 (2), compose 12 (3), compose?entityKey 12 (3, **1** entities), explore 12, entity 13, Mine 12, /history 12, /settings 10, /why 10; warm SPA /home→/experiences **3** (1 data, 1,800 B); warm reload 9 (JS/CSS SW-served). PTR: 1 GET, 0 POST, no `/api/sync`.
- [W-R7-13..15] **Correction to r6**: `r6-wf-perf.js` read `getEntriesByType("longtask")`, which Chromium never buffers → its "0 long tasks in 25 loads" was an artefact. Observer-callback method: **21 long tasks in 25 cold loads**, 50–114 ms, all between t=155 and 468 ms (boot/parse/first render), none after settle (unthrottled x86; a phone is worse). FCP 196–248 ms, LCP 440–828 ms (timetable 828), TTI ≈317–362 ms; synthetic input latency 0.4–2.3 ms.
- [W-R7-16..17] 0 running animations 22/22 screen×pref (one `finished` `rise-in` on the opted-into `/history?select=1` banner); 6 `@keyframes`; `.view` one rule; reduce → 1e-06 s on 25/25 sampled elements, `scroll-behavior:auto`. Interrupts on load: 0 dialogs/toasts/badges/alerts on 11 × 2 (the select-mode instruction banner is the only status).
- [W-R7-18] Feed live wrapper 0 px / no paint when empty; CLS 0. [W-R7-19] CLS 0 in 18 cold combinations (3 routes × 2 tz × 390/360×620/320×568), tab change, two synthetic load-more appends.
- [W-R7-20..24] compact canvas facts and the landing-scroll defects (animated after FCP; fires on date change and retry; lost on SPA re-entry; 6 px / 30 px misses) — see §0.
- [W-R7-25] `data-landed`: one `{once:true}` blur listener, 0 residual, no timers; latent stacking if two landings occur without a blur (not producible live). [W-R7-26..27] reactions: 1 POST for real double taps at 40/80/150 ms; 2 POSTs only for two `.click()` in one task; pending has no visual state.
- [W-R7-28] Dark boot 6/6 pre-FCP (surface set 43–152 ms, FCP 260–452 ms), 0 later `<html>` mutations; **theme-color is rewritten by the boot script to the surface colour in 6/6 contexts** — the `#edf0f1` literal is a pre-script placeholder (corrects r6's relay).
- [W-R7-29..33] SW byte-identical (md5 `f7513f02…`, `honey-v3`): mode real 10/10 current kept, 5/5 stale evicted, `honey-v2` deleted; mode fake (1-byte bodies) still evicts 8/10 current assets (`sw.js:33-34` trusts cached bodies — unchanged); cold-start refetch 0 B ×3; JS-only / CSS-only signature mismatch evicts, exact match no work; cache after 10 hard loads = 5 entries / 346,009 B, 0 `/api/`; content-type guard holds; offline shell 84–92 ms. **Headers unchanged: `/sw.js` `public, max-age=14400` (relay's `no-cache` not applied), `/assets/*` `max-age=14400` without `immutable`.**
- [W-R7-34..35] 7 font subsets declared, 1 fetched on 14/14 views; 123,968 B never requested (0 wire cost at runtime via `unicode-range`; 1,793 B of `@font-face` text in every CSS payload; CJK names fall to a system font); 1,201 B admin CSS (11 rules) in the student payload — consumed by the separate Dash chunk [S-R7-3].
- [W-R7-12] SWR `cache` is an unbounded per-session `Map` (per-date, per-limit keys), heap flat over 3 legs.
- Gaps: load-more with synthetic data; hazard (i) proven standalone; listener stacking not producible; `/dash` reachable with the student session (as r6).

## 5. Accessibility [A]
- [A-R7-A] 0 contrast failures on 24 surfaces × 2 boots; floors 4.66/5.83; ring contrast landing 5.81–6.13 / 8.36–9.28, `#main` 5.81/9.28, control 5.81/9.28, `.daynav__date` 6.13/8.36.
- [A-R7-B] 201/201 Tab stops ringed (`solid/3px/rgb(51,102,124)/offset 3px`, `:focus-visible` true) across 16 route states; orders listed per route; desktop rail 0 tabbable at 390.
- [A-R7-C] Keyboard reachability 22/23 primary actions unconditional; compose Share/Keep private `disabled` until text exists (Tab #3/#4 once typed); every dialog opens by Enter and closes by Escape.
- [A-R7-D] Landmarks: `main` 1 (unnamed) on every route; `nav` 2 in DOM (rail hidden); named regions Home 2, Explore 4 (inside `group` "Everything listed"), Compose 1–2 ("Pick a target"; "What this is about", "Editor"), Timetable "Day timeline" + `group` "Choose a day", Settings 5 + `group` "Background surface", History "Lessons", Mine "Your notes & posts", Entity "ChenJenny experiences"; 404 0 nav; Login unnamed `form`; feed `role=tabpanel` unnamed; Explore search input outside any `search` landmark; banner/contentinfo 0.
- [A-R7-E] Skip link 12/13 present ("Skip to content"), 12/12 land on `#main` with a visible ring (27,005 px light / 22,584 dark), scroll 0; ring band over content on 5/12 routes; 404 none; login skip → `#school-username`.
- [A-R7-F] Touch census (coarse, CST): 161 elements per viewport, **1 sub-44 per viewport** (`/why` link 133×22; login checkbox 22×22 on the unauthed surface); desktop hasTouch: `a.home-voices__body` 936×22.5 at 1280.
- [A-R7-2] ring hunt clean (see §0); note: on Home/History/Mine/Feed/Entity the "Try again" is inside the landing, so a failed second retry moves a keyboard user outward from the button to its ancestor (not a regression).
- [A-R7-10] Dialogs 7/7 `aria-modal`, trap, Esc, opener restore; 5/7 described; 0/7 `inert`; report "done" branch's `describedBy` id would dangle (static; not exercised).
- [A-R7-12] live region announces whole feed on first load and whole list on tab change (see §0). [A-R7-13] login: submit 342×44, inputs 49.8, Tab order 5/5 ringed, wordmark img alt "HOney", form unnamed.
- Gaps: second-post `···` untestable (1 post); load-more unmeasured; report "done" unmeasured; attributes not a platform AT tree; session rotated twice mid-run (rows re-run); landmark counts include the hidden rail; reduced-motion not re-verified here (W did: 0).

## 6. Cross-pass reconciliations
- "Mine error state missing" (V) is a 0-key artefact: with a seeded key and an aborted POST, Mine renders `role=alert` + "Try again" 77×44 and lands with a ring (A, S).
- "Entity error missing" (V) and "entity page silent when registry down" (S, W): the feed-abort branch shows the alert + retry with the region named from the directory (A); the `/api/entities`-abort branch renders the OK copy silently because `useNames` drops errors — an error state genuinely missing on that branch.
- Dangling-comment counts differ by detector (S 8 incl. trailing exception notes, V 1): the substantive residue is one true dangling comment (features.css:344-345) and one unterminated comment (components.css:45-46).
- Long tasks: r6's 0/25 was an artefact of the measurement; corrected 21/25, boot-only, unthrottled.
- The r6 "static theme-color" relay was wrong; the boot script rewrites it pre-FCP (W).
- Both S and V confirm the landing scroll happens; W measured its timing/animation; V measured the overlaps it causes.
