# 02 — Scorecard (r7; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `213707d`)

1. Good design is innovative — Score: 2/3 (r6 2, Δ0)
   Evidence: [C-R7-E1] identity-free publish, no-author-column storage, browser-held ownership keys and category-only reports re-verified with zero drift for the sixth round (`api/client.ts:229,351-353`, `db/database.ts:103-121`, `routes/experiences.ts:208`); every pattern this round touched (`data-landed` state on a landing, in-flight request map, compact canvas notches, `scroll-margin-top` landing, `aria-disabled` toggle, polite live wrapper) is conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing shipped is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1.

2. Good design makes a product useful — Score: 2/3 (r6 2, Δ0)
   Evidence: [S-R7-12] next lesson 1 screen / 0 taps; stream 1 tap; share 4 screens / 3 taps to the editor (+1 to a cursor, textarea never auto-focused). Every r6 blocker on this principle verified gone: [S-R7-6][A-R7-F] 44 px floor holds for 160/161 mobile targets at 390 and 320 (r6: 34 sub-44 instances); [S-R7-10] all 12 lesson-block/viewport pairs hit-test to themselves at 320×568 → 430×932 in Asia/Shanghai (r6: P3 under the nav); [A-R7-4][A-R7-5] composer retry lands on the editor region and reactions keep focus. New detours on adjacent surfaces: [W-R7-9][S-R7-18] a pull-to-refresh or "Try again" issued while a keyed request is in flight fetches nothing and rewrites the stale body into the cleared cache while the spinner runs; [A-R7-4] a compose retry that fails a second time drops focus to `<body>`; [W-R7-24] at 320×568 the compact landing leaves the last block 6 px under the nav on today's day; [A-R7-F] the `/why` back link is 133×22 and the login checkbox 22×22.
   Justification: All three primary tasks complete on-screen with no wrong-screen detour (rules out 1); the chooser → History → editor path still adds steps and a cursor costs an extra tap, and the refresh gesture can silently do nothing, so not "fewest possible steps; no decoy actions" (rules out 3). No tie.

3. Good design is aesthetic — Score: 1/3 (r6 1, Δ0)
   Evidence: The system holds and improved: [V-R7-A1] 97.6–97.8 % on-ladder mobile / 92.6 % desktop; [V-R7-C1] 43 colours, `tokens.css` byte-identical; [V-R7-B1] type unchanged; [V-R7-D1] floor 4.66/5.83, 0 failures; [V-R7-F1] mobile button families collapsed to 44 and 81 (r6: 36/38/44/48/81); [V-R7-2][V-R7-3] pressed card and three chip tones distinct; [V-R7-E3] one ring grammar on every control. Rendered inconsistencies on `213707d`: [V-R7-9b] one `ReconnectDialog` component renders two label grammars — Save: one line + caveat in the body; Reconnect: a 4-line caveat inside the checkbox label (78 px at 320×568, sheet scrolls); [V-R7-4b][V-R7-G1] the renamed `.timetable-note` margin is dead (overridden by `p.caption{margin-block:0}`), so on a lessons day the note touches the canvas at 0 px and its box overlaps the `09:00` hour label 27×6 at every width; [V-R7-10d][V-R7-G1] at ≤620 px heights the compact landing scroll parks the sticky daynav and action row over `h1.schedule-header` and the note — 8 genuine overlaps at 320×568 and 360×620 (up to 79×26 px), 5 at 390×620; [A-R7-F] the same label "Back to Experiences" is a bare 133×22 `<a>` on `/why` and a 170×44 `.btn` on Explore; [V-R7-E2] two disabled treatments in one compose row (`opacity 1` grey fill vs `opacity .65` transparent); [V-R7-E1] the pending reaction is byte-identical to idle. Orphans: [V-R7-J4][S-R7-3c] `.option-grid span` ships with 0 consumers; [V-R7-4b] `.timetable-note{margin-top}` ships and never applies; [V-R7-J2][S-R7-3b] one unterminated comment swallows the "Buttons" banner; [S-R7-3] `.reveal`/`.is-in` dead; [V-R7-C3] `.swatch` literals duplicate surface tokens.
   Justification: Anchor 1 is "3–5 inconsistencies OR one jarring violation" — five rendered inconsistencies on the audited surfaces (two-grammar dialog, note crowding and dead margin, compact-height chrome over the heading, bare-link vs pill for one label, two disabled treatments), so anchor 2's "≤2 minor inconsistencies" is exceeded on count, and anchor 3's "no orphan styles" is exceeded by the inert `.timetable-note` rule and the `.option-grid span` orphan alone; 1 by count, not by tie. A visible system exists (not 0).

4. Good design makes a product understandable — Score: 2/3 (r6 2, Δ0) — tie 2↔3, settled lower
   Evidence: Fixed and verified: [V-R7-2] the theme picker shows its choice (r6's one unreadable control); [C-R7-1][A-R7-7] the empty sentence names its day; [C-R7-3][S-R7-17] the 404 has its own sentence; [C-R7-4][A-R7-8] Mine grammar and region name unified; [A-R7-9] one named date affordance in the fallback; [A-R7-11] Explore's complete set with counts; [C-R7-D] jargon defined at point of use, count 0. Residue: [C-R7-2][S-R7-16] an impossible `?date=` shows today under an address that still says `date=2026-13-45`, with no notice and no redirect; [C-R7-1] the heading and the empty sentence print the same date in two formats from two locale sources ("Saturday, August 22, 2026" / "Sat 22 Aug"); [V-R7-5] the "P1–P6 are the school's six lesson periods" note renders on an empty day where no P label exists; [C-R7-E11][C-R7-E12] the title "Not found · HOney" serves the 404 and the never-listed entity, and the 404's title ≠ its h1; [C-R7-8][V-R7-E1] a pending reaction is announced as disabled to AT and looks fully live to sight, and a second tap in that window is swallowed with no feedback; [A-R7-D] the feed landing is an unnamed `tabpanel`; [C-R7-D1][C-R7-D3] provenance in two registers and a fourth "delisted" wording.
   Justification: Anchor 3's literal condition — a first-time user names every primary control correctly — now holds (zero controls need a tooltip, jargon 0), which also means anchor 2's "1 control needs a tooltip" no longer literally matches; but the principle asks whether the structure clarifies function, and a page whose URL and heading disagree in silence, one date in two grammars, an explanatory note for labels that are not there and a control whose state channels contradict each other are structure that does not clarify. Genuinely uncertain between 3 and 2; the tie-breaker settles it at 2. Not 1: no control is unclear and no jargon is unexplained.

5. Good design is unobtrusive — Score: 2/3 (r6 1, Δ+1)
   Evidence: [V-R7-6][A-R7-2] the r6 blocker is gone — 0 accent pixels on 16/16 ordinary mouse and touch taps across 8 landings, ring only on retry (24/24), no stale ring in 5 hunt cases; [V-R7-I1][W-R7-16] 0 idle animations on 11 screens × 2 preferences; [W-R7-17] 0 dialogs/toasts/badges on load; [W-R7-19] CLS 0 in 18 combinations plus tab change and load-more; [W-R7-18] the always-mounted live wrapper is 0 px and paints nothing when empty; [V-R7-5] the empty day says "no lessons" once; [W-R7-6] one `GET /api/entities` per view. Against: [W-R7-21] on ≤620 px-tall screens the timetable animates its canvas ~120 ms, ~200 ms after first paint, and again on every date change — movement the student did not initiate (instant under `reduce`); [W-R7-23] the same scroll fires on retry alongside the retry ring, so retries no longer move the page 0 px on compact heights; [A-R7-12] the polite region reads the whole feed to a screen-reader user on arrival and again on every tab change; [W-R7-16] the 460 ms `settle` replays on every route; [S-R7-21] a primary-styled Share sits in the stream header and co-renders with the interleaved invite at ≥9 posts.
   Justification: Chrome is visible but quiet — no decoration competes with content on any surface (rules out 1), and the intrusions that remain are one route entrance, a compact-height landing motion in the deliberate-degradation zone and an AT announcement, none of which make chrome the figure; a whole-feed announcement and an unrequested post-paint scroll bar anchor 3's "chrome recedes". No tie.

6. Good design is honest — Score: 2/3 (r6 2, Δ0)
   Evidence: [C-R7-E1] seven privacy claims re-mapped 1:1 with zero drift; [C-R7-E1][C-R7-E3] the import disclosure names exactly the two `syncTimetable` callers that exist (`routes/auth.ts:32`, `routes/data.ts:17`) and a nine-route sweep plus a real pull issue 0 `POST /api/sync`; [C-R7-C1] 0 dark patterns across seven rubric categories; [C-R7-5] the unreachable reconnect body is gone (0 unreachable copy); [C-R7-F2] one-sentence-three-surfaces count 0; [C-R7-1] "today" is asserted only on today; [C-R7-E9] the credential caveat matches the store exactly. Against: [C-R7-B1] one degree-word inflation ("strongly protected", `WhyPage.tsx:73`); [C-R7-E4] "All imported timetable and history data will be removed from HOney" ↔ `deleteImportedData` deletes only `user_lesson_exposures` (`services/accounts.ts:238-240`) — every user-linked row goes, but the importer's shared teacher/course/room/lesson rows and registry entries stay; [C-R7-E2] the login footnote names one of the two triggers and one of the two data types the Settings row names; [C-R7-E11] one title for two situations; [C-R7-E14..E16] three stale code comments (not user-facing).
   Justification: Anchor 1 requires "2+ inflations OR one dark pattern" — one inflation and zero dark patterns; the deletion body is an imprecision about shared rows that carry no user link, not an inflated value or a deceptive flow, so it bars anchor 3's "every claim maps 1:1" rather than adding to the inflation count; 2, no tie.

7. Good design is long-lasting — Score: 2/3 (r6 2, Δ0)
   Evidence: [V-R7-B1][V-R7-C1] single humanist face, one accent per surface, no gradients, no glass blur, 43 colours held for four commits, spacing 97.6 % tokenised; [V-R7-K] trend markers listed: the per-route `settle` entrance and the floating, translucent, detached pill-nav with a sliding ink indicator (one soft-app-shell signature), plus pill radii, 17–20 px cards with a soft drop shadow and `color-mix()` tints. Durability facts: [V-R7-4b] a renamed rule shipped inert because its specificity was never checked against `p.caption`; [S-R7-3b] an unterminated comment has survived seven rounds; [W-R7-8] the shared cache key's first-fetcher-wins rule is a latent corruption path for any future consumer.
   Justification: The entrance choreography and the translucent floating shell are one dated signature (as in r1–r6), which matches anchor 2 exactly; pill radii and soft shadows are not date-specific and `color-mix` is not a visual marker; no skeuomorph residue or trend typography (bars 1); the signature bars 3. No tie.

8. Good design is thorough down to the last detail — Score: 1/3 (r6 1, Δ0)
   Evidence: Fixed and verified: [A-R7-1][S-R7-6] 44 px floor 160/161; [V-R7-2][V-R7-3] selected states for picker and chips; [A-R7-2][V-R7-6] ring on retry only, 24/24 and 16/16, no stale attribute; [A-R7-3] `···` focus return 5/5 × 2; [A-R7-5] reactions keep focus; [A-R7-6][V-R7-7] skip landing visible 12/12; [S-R7-10] blocks hit-testable at 320×568; [S-R7-22] Save submit bottom 561; [A-R7-10] `aria-describedby` 5/7; [A-R7-9] hidden input unnamed; [A-R7-A] 0 contrast failures × 2 boots; [A-R7-B] 201/201 stops ringed; [S-R7-3] `tsc` 0, duplicate selector 0, six comments removed. Rough states on the worst instance: **disabled/pending** — [V-R7-E1][W-R7-27] the pending reaction lost its only visual (`.react-btn:disabled` is dead, 0 `[aria-disabled]` rules; opacity 1, cursor pointer, count lagging) while AT is told "disabled"; **error** — [S-R7-18] `useNames` drops registry errors so the entity page renders complete and silent with `/api/entities` down; [V-R7-E] Explore shows the banner and four "Nothing here yet." together; **loading/refresh** — [W-R7-9] pull-to-refresh during an in-flight keyed request spins 600 ms and fetches nothing, re-caching the stale body; **focus** — [A-R7-4] compose retry failing twice → `<body>` (the one landing absent from its error branch), [A-R7-E] the `#main` inset ring is drawn over content on 5/12 routes, [A-R7-10] 0/7 dialogs set `inert`, 2/7 undescribed, [S-R7-17] the 404 has no skip link or `#main`, [A-R7-D] unnamed feed tabpanel; **edge/spacing** — [V-R7-4b] the fix for the 0 px caption gap shipped inert, [V-R7-G1] note × `09:00` crowding at every width, [V-R7-10d] compact landing hides the day heading and note under the sticky bar, [W-R7-23] compact landing lost on SPA re-entry and fired on retry, [W-R7-24] 6 px / 30 px misses, [V-R7-10c] a <45 min lesson clips on the 450 canvas (latent), [V-R7-9a][V-R7-9b] Save sheet still scrolls 17 px and Reconnect keeps the 4-line label; **hygiene** — [S-R7-3b] unterminated comment, one dangling comment, `.option-grid span`, `Reveal` dead code, four blank-line scars.
   Justification: Four state families are rough on their worst instance (a pending state with no visual, an error state that renders as success, a refresh that spins without fetching, a focus system with a `<body>` drop and a ring over content), which is past anchor 2's "1 state missing or rough"; the state framework exists on every surface, every r6 blocking condition verified fixed and nothing is default-browser, so not 0. No tie.

9. Good design is environmentally friendly — Score: 3/3 (r6 3, Δ0)
   Evidence: [W-R7-1] app JS 85,987 B br / 88,461 B zstd as actually delivered to Chromium, under 100 KB either way (+296 B br); [W-R7-16][V-R7-I1] 0 idle animations on 11 screens × 2 preferences; [W-R7-28] prefers-color-scheme honoured pre-FCP in 6/6 boot contexts with 0 later `<html>` mutations and the theme-color rewritten to the surface (correcting r6's relay); [W-R7-16][W-R7-22] prefers-reduced-motion → 1e-06 s on 25/25 sampled elements, `scroll-behavior:auto`, the compact landing instant; [W-R7-29..32] SW battery green (10/10 kept, 5/5 evicted, 0 B cold-start refetch, exact-match no work, offline shell 84–92 ms); [W-R7-19] CLS 0; [W-R7-5] warm navigation 3 requests / 1,800 B; [W-R7-6] one `GET /api/entities` per view (r6's duplicate gone). Relayed, outside the anchor: [W-R7-13] r6's "0 long tasks" was a measurement artefact — 21/25 cold loads carry a 50–114 ms boot task; [W-R7-33] `sw.js` still `max-age=14400`, `/assets/*` without `immutable`; [W-R7-29] `liveAssetSet` still trusts cached bodies; [W-R7-34][W-R7-35] 123,968 B of unrequested font subsets in `dist` (1,793 B of `@font-face` text per CSS payload) and 1,201 B admin CSS; [W-R7-12] unbounded per-session SWR map; [S-R7-19] plain `/compose` fetches two unused payloads.
   Justification: All four of anchor 3's conditions verify live for the fifth round running and the one resource defect relayed in r6 (the duplicate request) is repaired; the remaining relay items do not touch the anchor, so 3.

10. Good design is as little design as possible — Score: 2/3 (r6 2, Δ0)
    Evidence: [S-R7-1] census delta 0 on every comparable surface; [S-R7-5] families 16 (counting boundary, no code change); [S-R7-3] `tsc` 0; [S-R7-11][V-R7-5] the empty day's six ghost "Free" labels are gone, so r6's same-screen duplicate grammar is resolved. Candidates for a removable element on one screen: [S-R7-15] the `···` menu wrapping the single item "Report" (1 menu, 1 item, no menu name); [V-R7-5] on an empty day the P1–P6 clause of the note explains labels that do not render (the "Last synced" clause still earns its place); [C-R7-F1] the Save dialog's body and checkbox label state the same idea. Latent: [S-R7-21] header Share + interleaved invite co-render at ≥9 posts (demonstrated with a fulfilled 12-post feed); [S-R7-4] the empty/error feed carries a zero-content `aria-live` node; [S-R7-19] `useNames()` on the two-link chooser screen.
    Justification: No page is dominated by decoration or duplicated affordances (not 0); at most two same-screen elements could be removed without breaking a task (the one-item menu, the P-clause on an empty day), which is anchor 2's "≤2 removable elements"; the one-item menu alone bars anchor 3's "removing any one breaks the task". No tie.

**Total: 19/30** (r6 18/30, Δ+1; r5 19/30; r4 18/30; r3 16/30; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #4 (2↔3). Principles scored with no tie: #1, #2, #3, #5, #6, #7, #8, #9, #10. Sensitivity, stated honestly: granting #4 upward gives 20/30, which crosses the REFINE threshold — as in r5, the verdict below turns on one tie, and the rule settles it downward. Sensitivity the other way: reading the two trend markers under #7 as two rather than one signature would give 18/30.
