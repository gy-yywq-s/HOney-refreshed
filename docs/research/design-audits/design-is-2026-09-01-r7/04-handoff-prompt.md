# 04 — /make-plan handoff (r7)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 213707d). Current design failed audit at 19/30 with critical gaps in principles #3 aesthetic and #8 thorough (each scored 1/3; no principle scored 0; #4 understandable sits at 2/3 on a tie settled downward).

Verdict paragraph (quoted from 03-verdict.md):
> The r6 handoff verified as built almost everywhere it could be measured — the coarse-pointer 44 px floor is back and holds for 160 of 161 interactive targets at 390×844 and 320×568 (every "Try again" 77×44, reactions 49×44, `···` 44×44, all 24 History "Select" rows 62×44; r6 had 34 sub-44 instances); the pressed theme card computes `--ink` on both boots and three synthesised Mine chips render three distinct tones; the landing ring is bound to `data-landed` and paints on 0 of 16 ordinary mouse and touch taps across eight landings while painting on 24 of 24 retries with 0 px scroll and no stale attribute in five hunt cases; the `···` guard returns focus to its trigger on 5 of 5 outside-click variants on Feed and Entity; reactions keep focus through the round trip by Enter, Space and pointer with one POST; the skip link lands on a visible inset ring on 12 of 12 routes (27,005 pixels light, 22,584 dark); every lesson block on a three-lesson day hit-tests to itself at all six viewports down to 320×568 in the student's timezone and the lesson modal contains without scrolling; the Save-login submit sits at bottom 561 inside the 568 px fold; the empty day states its emptiness once and names the day it describes; impossible `?date=` values no longer render impossible days; the 404 has its own sentence; Mine's grammar and region name agree; the unreachable reconnect body is gone; the hidden date input is unnamed and the fallback shows one affordance; `aria-describedby` resolves on five of seven dialogs; the entity region is named from the directory in both branches; `compose?entityKey=` issues exactly one `GET /api/entities`; the seven privacy claims, the two-trigger import disclosure and the service worker battery all re-verify with zero drift — but the same wave shipped a `.timetable-note` margin that is overridden by `p.caption { margin-block: 0 }` and never paints, so the caption still touches the canvas and crowds the `09:00` label; a compact-height landing scroll that animates the canvas ~120 ms after first paint, fires on every date step and on retry alongside the retry ring, is lost on SPA re-entry, parks the sticky day nav over the day heading and the note (eight overlaps at 320×568) and still leaves the last block 6 px under the nav; a `ReconnectDialog` that now renders two label grammars, one line for Save and four lines for Reconnect; reaction pills that tell assistive technology "disabled" while looking exactly like idle because the only pending style was bound to `:disabled`; a polite live region that reads the entire feed to a screen-reader user on arrival and again on every tab change; a composer landing that exists only after the registry resolves, so a retry that fails twice drops focus to `<body>`; and an in-flight request map that a pull-to-refresh cannot bypass, so a refresh issued while a keyed request is pending spins for 600 ms, fetches nothing and rewrites the stale body into the cache it just cleared — so HOney moves from 18 to 19/30: #5 regains its point because the ring no longer fires on the content, #3 and #8 stay at 1 on defects of the fix wave's own making plus the pre-existing ones the wave did not reach, #4 holds at 2 on a tie that the rule settles downward, and the audit reports the number it measured.

Why redesign and not refine: total 19/30 is below the REFINE threshold of 20; #3 and #8 are each at 1 on measured counts, and #4 is at 2 on a tie settled downward — granting the tie upward would give 20, so the verdict turns on that one tie and the rule takes it lower; this prompt is therefore the REDESIGN template even though every move is a finish-the-detail move on a layer whose tokens, type, colour, spacing, motion budget, weight, trust architecture and service worker all verify live for the fifth round running.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 320×600, 360×620, 390×620, 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decisions, not up for re-litigation: (1) timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), the disclosure is accurate (exactly the two `syncTimetable` callers that exist), and the plan must keep it so; (2) the 620 px timetable canvas density on normal-height screens stays; the compact-height notches (540 px at ≤620 px height, 450 px at ≤360 px width) are the deliberate degradation — fix how the degradation behaves (landing motion, sticky-bar occlusion, block clipping), not whether it exists. Verify everything in a real browser against the deployed app, not from screenshots alone. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop, reducedMotion})` returning `{browser, context, page}`; probe scripts `r7-{struct,vis,copy,wf,a11y}-*.js` with `.log` files and `r7-*.png` screenshots; `matrix.js` = 9-device regression) can be re-run to confirm each fix. IMPORTANT: the harness runs in UTC, where this account's lessons fall outside the 09:00–20:00 timeline window and the Timetable always looks empty — build contexts by hand with `timezoneId: "Asia/Shanghai"` (see `r7-struct-timetable.js`, `r7-vis-compact.js`, `r7-a11y-tz.js`) whenever a measurement involves lesson blocks; in-page "today" during r7 was 2026-09-02 (3 lessons), 2026-08-24 is a 3-lesson control day, 2026-08-22 and 2026-09-06 are empty days. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying; a reaction tap is allowed only if undone in the same script. The test account has 0 ownership keys and a connected, valid portal token — seed `HOney.experiences.keys` / `honey.portal.cred` sentinels in localStorage (as the r7 probes do) to reach Mine's populated/error branches and the Save-login UI; fulfil `/api/experiences/mine` with `published`/`blocked`/`revoked` rows (`r7-vis-chips.js`) to render the three status chips; rewrite the `/api/me` body to `portalTokenValid:false` to reach the Reconnect branch; the real feed holds one post, so fulfil `/api/experiences/feed` with `{items, nextCursor, headCursor}` (`r7-wf-live2.js`, `r7-struct-menu.js`) to reach load-more and the ≥9-post state; delay `/api/experiences/*/react` to observe the pending reaction.

Preserve from current design (MUST NOT be touched — these verified live on 213707d):
- Spacing system as it renders: 214 spacing lines, 176 on `var(--sp-*)`, 2 literal-px lines and both commented (foundations.css:94, features.css:1191); rendered rhythm at 320/390/430 = 97.6–97.8 % on the 4/8/12/16/20/24/32/44 ladder, desktop 92.6 %; the only author-level off-ladder family is the documented `.main` nav clearance (96/90/110 px). `.timeline__empty { margin: var(--sp-2) 0 }` in flow (features.css:443-450) — keep it; the note gap is the Discard item. Regression check: re-run `r7-vis-typespace.js` at 320/390/430/1280; on-ladder share must not drop below 97 % mobile / 92 % desktop and no 2/9/10/14/18/22/26/30 px value may appear.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, one fractional (27.95 at 430 only); 10 off-ramp `font-size` declarations; inside compact lesson blocks subject 15/22.5 and meta 12/18. Regression check: rendered-font-size census, nothing below 12 px, no new off-ramp declaration.
- Palette and accent discipline: 43 colour literals, `tokens.css` blob 1d70d963a91a5bfbeb17928b599185286ad8f836 byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; contrast floor 4.66 stone / 5.83 night on `.home-head__stale`, 0 enabled text below 4.5:1 on either boot (24 surfaces); every focus ring 5.81–6.30 stone / 8.36–9.28 night against its adjacent ground; chips 5.16–8.39; pressed card 13.26/15.27. Regression check: rerun `r7-a11y-contrast.js` / `r7-vis-contrast.js` on stone and on a fresh `colorScheme:dark` context with no stored theme; 0 enabled text below 4.5:1; every focus ring ≥3:1.
- Zero idle motion: `document.getAnimations().length === 0` at 2.5 s on 11 screens × both preferences; `infinite` keyframes only on the pull spinner and skeleton; 6 `@keyframes`; `.view` ONE rule (components.css:524-537) carrying `settle .46s var(--ease) backwards` — do not split it; prefers-reduced-motion collapses durations to 1e-06 s and `scroll-behavior` to `auto`; CLS 0 on cold /timetable, /home, /experiences in both timezones at 390×844, 360×620 and 320×568, on tab change and on load-more. Regression check: `r7-wf-anim.js`, `r7-wf-cls.js`; `grep -n '\.view' apps/web/src/styles/*.css` returns one `.view {` block.
- Weight: app JS 85,987 B br / 88,461 B zstd / 274,090 raw, CSS 9,281 br / 40,914 raw, Dash chunk 4,404 br loaded only on /dash (0 of 14 student loads); 12 requests cold /home (3 data), 3 warm to /experiences; no new dependency; exactly one `GET /api/entities` per view that needs it and 0 duplicate API paths on 14 cold views. Regression check: curl br sizes under 100 KB; DashPage never fetched on student routes; request counts unchanged; `GET /api/entities` exactly once on compose?entityKey=, explore, entity, Mine.
- Service worker as it is (public/sw.js, 4,828 B, md5 f7513f02194664efa4e96d50773ec83d, cache `honey-v3`): `liveAssetSet` resolving Vite's relative chunk imports (sw.js:21-40); `activate` → `evictStaleAssets()` (sw.js:66-74); `/__index-sig` persisted and compared whole (sw.js:57-65,110-118); content-type guard refusing 200-HTML under /assets/; `/api/` never cached; navigations network-first; one `/` shell. Verified r7: 10/10 current kept, 5/5 stale evicted, `honey-v2` deleted, 0 B refetched after `ServiceWorker.stopWorker` ×3, JS-only and CSS-only mismatches evict, exact match does no work, cache 5 entries / 346,009 B after 10 hard loads, offline shell 84–92 ms. Regression check: re-run `r7-wf-sw1.js` (mode real), `r7-wf-sw3.js`, `r7-wf-sw4.js` after any sw.js change; keep the cache name unless a deliberate flush is intended.
- Boot script honouring prefers-color-scheme pre-FCP in six contexts with stored-choice-wins, 0 later `<html>` mutations, and theme-color rewritten to the surface colour before first paint (index.html:14-33).
- Route-settle containment: lesson modal, report dialog, Save, Reconnect and delete-confirm overlays equal the viewport at 320/390/1280 (15/15 opens), nav not hit-testable beneath (60/60), sheet padding `20/16/24` on the ladder, every dialog button 44 px, focus trap + Esc + opener restore 7/7.
- Focus system: 3 px accent outline offset 3 on 201/201 Tab stops across 16 route states; `useRetryFocus` (lib/useRetryFocus.ts:9-21) arming only from the eight "Try again" handlers, setting `data-landed` on the element it focuses (:16), clearing it on `blur` `{once:true}` (:17), focusing with `{preventScroll:true}` (:18) — landings never focus on a normal load, never in the Tab order, scroll 0 px on retry (24/24), ring 0/16 on ordinary mouse and touch taps; `.focus-landing[data-landed]:focus` (features.css:1292-1299) — keep the trigger, the colour, the width, the offset and the radius; named landing regions "Now and next" (HomePage.tsx:66), "Day timeline" (TimetablePage.tsx:189), "Lessons" (HistoryPage.tsx:107), `group` "Everything listed" (ExplorePage.tsx:88), "Your notes & posts" (MinePage.tsx:130), "<name> experiences" (EntityPage.tsx:87), "Editor" (ComposePage.tsx:298 — keep the landing, add it to the error branch, see Discard); `#main[tabIndex=-1].main` (AppLayout.tsx:120) with `.main:focus { outline: 3px solid var(--accent); outline-offset: -3px }` (features.css:1310) — keep the ring, stop it drawing over content (see Discard); the `···` guard `closest('button, a, input, textarea, select, [tabindex]:not([tabindex="-1"])')` (ExperiencePost.tsx:124) with `aria-haspopup="menu"`, `aria-controls` resolving, `aria-expanded`, Escape → trigger, Tab-out close, Shift+Tab keep-open, ArrowUp/Down; reactions `aria-disabled={busy || undefined}` (ExperiencePost.tsx:226,238) with the `if (busy) return` guard (:162) — keep the attribute and the guard, restyle the pending state (see Discard).
- Timetable: `.timeline__empty` `<p role="status">` "No lessons today" / "No lessons on <day>" as flow child 0 of the "Day timeline" landing (TimetablePage.tsx:317-322), present only on genuinely empty days; `.timeline__canvas--empty .timeline__ghost:not(.timeline__ghost--break) { display: none }` (features.css:1305-1307) with `timeline__ghost--break` on the two break ghosts (TimetablePage.tsx:365-374); the `?date=` real-calendar guard (TimetablePage.tsx:32-40) — keep the rejection, add the URL/notice (see Discard); `.timeline { min-height: 620px }` with `@media (max-height: 620px) { 540px }` and `@media (max-height: 620px) and (max-width: 360px) { 450px }` (features.css:759-777) — owner's decision, keep; `button.lesson-block[data-lesson]` (TimetablePage.tsx:384) — keep the hook, fix the scroll (see Discard); the lesson modal containing without scrolling at all six viewports; `hidden={pickerVisible}` on `.daynav__date` and the conditional `aria-label` on the hidden input (TimetablePage.tsx:87,115); the "Sync now" clause shown only when a credential exists.
- Compose: the registry-pending skeleton; the registry-error branch (ComposePage.tsx:206-225: h1 + `role=alert` + "Try again" arming `landing.arm()` before `entities.reload()`) — keep it, give it a landing element (see Discard); `useApi(…, effectiveEntityKey ? "entities" : undefined)` (ComposePage.tsx:55-59) sharing the key with `useNames()` (shared.tsx:162) and `ExplorePage.tsx:29`; the delisted branch offering "Open the current entry for {name}" as a 44 px primary button; the never-listed branch "Nothing is listed at this address, so nothing can be shared about it." + "Find someone or something" 44 px; `.compose-actions` two 44 px rows at ≤620 px heights (features.css:1260-1276).
- `useApi` in-flight map (lib/useApi.ts:14,55-64): `.finally` eviction so a rejected request is retried — keep; add the refresh bypass (see Discard).
- Feed: `#feed-panel` / `div.feed-stream[role=tabpanel][tabindex=-1]` with the `role=alert` banner and the skeleton OUTSIDE the `aria-live` wrapper, the wrapper 0 px and unpainted when empty, header Share suppressed on the true-empty feed (FeedPage.tsx:34), "Share the first one" as the single empty-feed action — keep all; change what the polite region contains (see Discard).
- Entity: never-listed ids → h1 "Nothing is listed at this address." + "Find someone or something" 44 px, title "Not found · HOney"; delisted ids → "This entry is no longer listed." + `.entity-survivor` "Open the current entry for {name}"; region named `${name} experiences` in both branches (EntityPage.tsx:87).
- Settings: "Reconnect" rendered only when `connection.connected && !connection.portalTokenValid` (SettingsPage.tsx:150-154); the import row "…imported from the school portal when your account is created, and again whenever you press Sync now. Delete them below at any time." (:199-203) — the two named triggers are the only two `syncTimetable` callers (packages/backend/src/routes/auth.ts:32, routes/data.ts:17) and a real pull issues 0 `POST /api/sync`; `ReconnectDialog` `describedBy="school-dialog-body"`, `submitLabel={purpose === "save" ? "Save login" : "Reconnect"}`, checkbox inside the form before the submit, checked by default when saving (ReconnectDialog.tsx:25-27,34-60) — keep; unify the label grammar (see Discard); `ConfirmDialog` `describedBy="confirm-dialog-body"` (Modal.tsx:94-96) and the report dialog's `report-dialog-body` (ExperiencePost.tsx:321,336); the theme picker `.option-grid button[aria-pressed="true"] { background: var(--ink); color: var(--surface); border-color: var(--ink) }` (components.css:779-783); `.chip` before `.chip--ok/--danger/--muted` (components.css:194-216).
- Login: `.login { height:100dvh; flex-direction:column; overflow-y:auto }`; `.login .btn` and `.login .input` `min-height: 44px` (features.css:46-52); submit 342×44 / 272×44 with bottom 532 < 568 at 320×568; skip link "Skip to sign-in" → `#school-username`; title "Sign in · HOney"; footnote "…created on first sign-in. Your timetable comes along with it." — keep as the minimum; extend only per Discard.
- Titles: per-route `document.title` "<Name> · HOney" on all 10 AppLayout routes, entity titles, "Sign in · HOney"; the 404 sentence "That page doesn't exist." (NotFoundPage.tsx:12).
- PTR: idle disc unnamed inside `aria-hidden="true"`, `aria-label="Refreshing"` only while refreshing (PullToRefresh.tsx:98-103); 1 `GET` per pull, never `POST /api/sync`.
- Explore: complete-set rule (four sections with counts at 390 and 320, "Food 0" rendered, search only filters — all four headings survive a no-match query); `aria-label="Filter by name"`; `role=group` "Everything listed".
- Clean build: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters -p tsconfig.json` = 0; 0 whole-head duplicate selectors in the served CSS; the coarse floor `@media (pointer: coarse) { .btn--small, .react-btn, .usermenu__button { min-height: 44px } }` (components.css:828-835) — never delete a compound rule for a dead sibling again.
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:229,351-353), no author column (packages/backend/src/db/database.ts:103-121), 6 = 6 = 6 report categories (ExperiencePost.tsx:290-295 ↔ routes/experiences.ts:11-16), "no free text is collected" ↔ `free_text_not_accepted` (routes/experiences.ts:208), coarse `publishedDay` (lib/format.ts:69-79), the HOney ID line (SettingsPage.tsx:73-74), WhyPage "design boundary, not magic", contract.ts:12-16, the Save/Reconnect caveat matching `lib/portalCredentials.ts:20-21,78-88,116-124`, the corrected comments routes/auth.ts:30-31 and ExperiencePost.tsx:121-123.

Discard (structural patterns causing the failures):
- A spacing fix that never checked the cascade: `.timetable-note { margin-top: var(--sp-3) }` (features.css:226-228, specificity 0,1,0) is beaten by `p.caption, p.text-4 { margin-block: 0 }` (foundations.css:160-163, 0,1,1); computed margin `0px` at all six viewports; on lesson days the note's bottom equals the canvas top (0 px) and the note box overlaps the centred `09:00` hour label 27×6 px at 390 and 1280. The 8 px gap on empty days comes from `.timeline__empty`, not from the renamed rule. Caused failure on principles #3 and #8.
- A landing scroll bound to the owner's smooth behaviour, fired on the wrong events, and aimed at the wrong offset: `TimetablePage.tsx:304-312` `useEffect` on `[date, visible.length]` calling `el?.scrollIntoView({ block: "start" })` with no `behavior`, so `[data-scroll-owner] { scroll-behavior: smooth }` (components.css:520) animates the canvas ~120 ms starting 363–510 ms after a 168–264 ms FCP on ≤620 px heights; it runs on every date step (0 → 188 twice) and on retry (0 → 77, simultaneously with the retry ring — two landings on one screen, and the "retries move 0 px" invariant broken on compact heights); it is undone on SPA re-entry because `AppLayout.tsx:33-35` resets the owner after the child effect when data is cached; `.lesson-block { scroll-margin-top: 140px }` (features.css:355) parks the sticky daynav and action row over `h1.schedule-header` and `.timetable-note` (eight genuine overlaps at 320×568 and 360×620, up to 79×26 px; five at 390×620) so the day heading and the note are invisible on arrival; the last block still ends 6 px under the nav at 320×568 on today's day and 30 px under it at 390×620; a synthesised <45 min lesson on the 450 canvas is 27 px tall with a 7 px meta box for a 12.6 px line (`clipY: true`). Caused failure on principles #8, #5 and #2.
- One component, two label grammars: `ReconnectDialog.tsx:47-52` shortens the checkbox label only for `purpose === "save"`; the `reconnect` purpose keeps the caveat inside the label (4 lines / 78 px at 320×568, sheet 525/522 scrolls, 3 lines at 390) and carries no caveat in its body, while the Save sheet still overflows 17 px (539/522) even with its submit at 561. The doc comment at `ReconnectDialog.tsx:14-18` describes the pre-split arrangement. Caused failure on principles #3 and #8.
- A pending state whose only style was bound to the attribute that was removed: `.react-btn:disabled { opacity: .7; cursor: default }` (components.css:249-252) and `.react-btn:hover:not(:disabled)` (:245) are dead for the reaction buttons since `disabled={busy}` became `aria-disabled={busy || undefined}` (ExperiencePost.tsx:226,238); 0 `[aria-disabled]` rules exist; while a POST is in flight the pill computes `opacity 1, cursor pointer, pointer-events auto`, the count text lags the optimistic `aria-pressed`, a second tap is swallowed by `if (busy) return` (:162) with no feedback, and assistive technology is told "disabled" while sight is told nothing. Caused failure on principles #8 and #4 (and #3: a state with no rendering).
- A polite live region that wraps the initial list: `FeedPage.tsx:157-178` mounts `<div aria-live="polite">` (no `aria-atomic`, `aria-relevant`, `aria-busy` or name) empty at ~475 ms and fills it with every post at ~578 ms (one insertion for the real post; eight insertions in one tick, 1,120 characters, for a fulfilled 8-item page), so a screen-reader user arriving at /experiences is read the entire feed, and a tab change (one removal + one addition in the same node) reads the whole new list; the feed landing `div.feed-stream[role=tabpanel]` has no accessible name. Caused failure on principles #8 and #5.
- A landing that exists only in the success branch: `ComposePage.tsx:298` renders `section.compose-editor.focus-landing[role=region][aria-label="Editor"]` only when `target` resolves; on the registry-error branch (:206-225) `ref.current` is null, `useRetryFocus.ts:15` returns early, and a "Try again" that fails a second time leaves `activeElement = (body)` with the banner still on screen — the only one of eight landings absent from its own error branch. Caused failure on principle #8 (and #2 for a keyboard user).
- An in-flight map that a refresh cannot bypass: `lib/useApi.ts:14` `inflight` and `:55-64` `run()` return the pending promise for a key regardless of why the effect re-ran; `PullToRefresh.tsx:77-78` calls `apiCache.clear()` (which clears `cache`, `useApi.ts:10`, but not `inflight`) and emits `honey:refresh`, so a pull or a `reload()` issued while `/api/entities` is in flight fetches nothing (0 new requests while `/api/directory` refetched twice), the 600 ms spinner runs, and the pre-refresh body is written into the cleared cache. Latent in the same code: two hooks with the same key and different fetchers — the first caller's `fn` wins and its payload is cached under the shared key with no error (proven standalone; no live pair today). Also: `useNames()` (shared.tsx:160-168) returns `names`/`directory`/`entities` and drops both `.error` values, so with `/api/entities` aborted the entity page renders its full success copy silently (title, h1, "Share your experience", `banners: []`); `useNames()` is called unconditionally on the two-link chooser (ComposePage.tsx:101), issuing `GET /api/entities` and `GET /api/directory` the rendered screen does not use. Caused failure on principle #8 (error and refresh states) and #2; resource relay under #9.
- Silence where the URL and the page disagree: `TimetablePage.tsx:32-40` rejects `?date=2026-13-45` / `2026-02-30` / `abcd` correctly but leaves the address bar unchanged, issues no redirect and renders no notice, and "Back to today" is correctly absent — a copied link means a different day for the next reader; `searchParams` is read only in the `useState` initialiser (:31-40). Two formats and two locale sources for one date on one screen: `formatDayHeading` (lib/format.ts:34-41, `toLocaleDateString(undefined, …)` → "Saturday, August 22, 2026") 160 px above `formatStepperDate` (TimetablePage.tsx:23-27, hard-coded `"en-GB"` → "Sat 22 Aug", "Sept" in September; the comment at :22 promises "Sep", tabular). The note "P1–P6 are the school's six lesson periods…" renders on an empty day where zero P labels render. Caused failure on principle #4.
- Copy that is one word wider than the code: "Removes all timetable and history data imported from the school portal." (SettingsPage.tsx:211) and "All imported timetable and history data will be removed from HOney." (:288) ↔ `deleteImportedData` = one `DELETE FROM user_lesson_exposures WHERE honey_id = ?` (packages/backend/src/services/accounts.ts:238-240); every user-linked row goes, but the importer's shared `teachers`/`courses`/`rooms`/`lesson_instances` rows (services/importer.ts:88-102; no `honey_id` column, db/database.ts:52-77) and organic registry entries stay; same shape on "everything imported with it" (:96 ↔ `deleteAccount` cascade). The login footnote "Your timetable comes along with it." (LoginPage.tsx:66) names one of the two triggers and one of the two data types the Settings row names. "strongly protected" (WhyPage.tsx:73) remains the one degree-word inflation. "Not found · HOney" titles both the 404 (NotFoundPage.tsx:6) and the never-listed entity (EntityPage.tsx:70); the 404's title "Not found" ≠ its h1 "Page not found". Two provenance registers ("Verified — has taken this over time" shared.tsx:17 vs "from someone who has taken this over time" ExperiencePost.tsx:20); a fourth "delisted" wording (shared.tsx:69). Three stale comments: ReconnectDialog.tsx:14-18, TimetablePage.tsx:22, TimetablePage.tsx:92-94. Caused failure on principles #6 and #4.
- Two residual targets outside the restored rule and two dialogs outside the described set: `<a>` "Back to Experiences" on /experiences/why (WhyPage.tsx:100) is a bare 133×22 inline link while the same label on Explore is `a.btn.btn--ghost` 170×44; the login `input[type=checkbox]` is 22×22; the lesson modal (TimetablePage.tsx:445) and the desktop theme dialog (ThemeControls.tsx:46) have no `aria-describedby`; no dialog sets `inert` or `aria-hidden` on `.app-frame` (Modal.tsx — modality rests on `aria-modal` + the JS trap at :24-42); the 404 (NotFoundPage.tsx:11) renders `<main class="login">` with no id, no skip link and no nav; the `#main` inset ring band sits over content rows at the sampled edges on Settings, History, Explore, Why and Dash (5/12 routes); two disabled treatments in one compose row (Share `opacity 1` with a grey fill vs Keep private `opacity .65` transparent); the report dialog's `describedBy` id exists only on the form branch (ExperiencePost.tsx:321 vs :336) and would dangle after submit. Caused failure on principle #8 (and #3 for the bare link and the two disabled treatments).
- Hygiene residue: an unterminated comment at components.css:45 that swallows the "── Buttons ──" banner at :46 (pre-existing, seventh round); one true dangling comment at features.css:344-345; `.option-grid span { color; font-size }` (components.css:775-778) with 0 consumers (the picker renders `<i>` + `<strong>`); `export function Reveal` (lib/motion.tsx:38-81) imported nowhere and tree-shaken, leaving `.reveal`/`.is-in` (foundations.css:220,228,244) dead; four double/triple blank-line scars where comments were removed (foundations.css:141-142, 187-189; components.css:43-44, 802-803); `.swatch` border `rgba(0,0,0,0.18)` and four `.swatch--*` hex literals duplicating surface tokens (components.css:787-801); `a` declared twice in the same context (foundations.css:48/64). Caused failure on principle #3 (orphan styles) and #10.
- Relay, anchor #9 met: `sw.js` served `cache-control: public, max-age=14400` (the r5/r6 relay asked for `no-cache` via the hostd manifest; not done) and `/assets/*` `max-age=14400` without `immutable`; `liveAssetSet` still trusts cached bodies (seeding 1-byte bodies evicts 8/10 current assets, sw.js:33-34); 123,968 B of never-requested font subsets in `dist` (1,793 B of `@font-face` text in every CSS payload; CJK names fall to a system font) and 1,201 B of admin CSS in the student payload; Cloudflare delivers zstd (88,461 B) rather than the 85,987 B br the build produces; 21 of 25 cold loads carry a 50–114 ms boot long task (r6's "0" was a measurement artefact; unthrottled x86); the SWR `cache` is an unbounded per-session `Map` with per-date keys; `/dash` reachable with the student session.
- Latent same-screen duplicate (code only, 1 post live): header Share (`btn--primary btn--small`) + the interleaved "Share an experience" invite (`btn--ghost btn--small`) co-render at ≥9 posts (FeedPage.tsx:18,34,42-46,164-171, `SHARE_PROMPT_EVERY = 8`; demonstrated with a fulfilled 12-post feed); the `···` menu wrapping the single item "Report" with no menu name (tie-register item under #10).

Top 4 moves from the audit (verbatim):
1. #8 thorough + #3 aesthetic — Finish the states the fixes left half-done, and verify each by computed style, not by reading. (a) Give the pending reaction a visual again: replace `.react-btn:disabled { opacity: .7; cursor: default }` (`components.css:249-252`) and `.react-btn:hover:not(:disabled)` (`:245`) with `[aria-disabled="true"]` equivalents, so a pending pill reads pending to sight as it does to assistive technology (today `opacity 1, cursor pointer`, byte-identical to idle, 0 `[aria-disabled]` rules in any stylesheet). (b) Give `ReconnectDialog` one label grammar for both purposes — the one-line "Stay connected on this device." with the caveat in the body (`ReconnectDialog.tsx:47-52`; body `:29-32`) — and verify at 320×568 that the Reconnect sheet no longer scrolls (525/522 today, 4-line label) and the Save sheet fits (539/522 today with the submit already at 561). (c) Make the note spacing actually apply — `p.caption.timetable-note { margin-top }` or put the gap on `.timeline`/`.timeline__canvas` — so note-bottom → canvas-top is ≥ 8 px on lesson days and the `09:00` hour label no longer overlaps the note (27×6 px at every width today; computed margin `0px` at all six viewports because `p.caption, p.text-4 { margin-block: 0 }` at `foundations.css:160-163` outranks `features.css:226-228`). (d) Give `/why`'s "Back to Experiences" (`WhyPage.tsx:100`, bare `<a>` 133×22) the `.btn.btn--ghost` 44 px shape the same label has on Explore, and give the login checkbox a 44 px hit area through its label. (e) One disabled treatment in the compose action row (Share `opacity 1` grey fill vs Keep private `opacity .65` transparent). Evidence: [V-R7-E1][W-R7-27][C-R7-8][V-R7-9a][V-R7-9b][V-R7-4b][V-R7-G1][A-R7-F][V-R7-E2].
2. #8 thorough + #5 unobtrusive + #2 useful — Make the compact-height landing quiet, once, and correct. In `TimetablePage.tsx:304-312` call `scrollIntoView({ block: "start", behavior: "instant" })` (or set the scroll owner's `scrollTop` directly) so nothing animates after first paint — today the owner's `scroll-behavior: smooth` (`components.css:520`) animates the canvas ~120 ms starting ~200 ms after FCP at 320×568; run it only for the cold landing of a date, never on retry (the retry ring is the landing there and today both fire together, so retries move the page 77 px on compact heights) and never twice for one date step; make it survive SPA re-entry or drop it consistently (`AppLayout.tsx:33-35` resets the owner to 0 after the child effect when data is cached, so the landing is cold-load-only today); set `.lesson-block { scroll-margin-top }` (`features.css:355`, 140 px) to the measured sticky stack so the day heading and the note are not parked under the daynav and action row (eight genuine overlaps at 320×568 and 360×620, up to 79×26 px; five at 390×620) and verify the last block clears the nav (6 px short at 320×568, 30 px at 390×620 today); give `.lesson-block--compact` a minimum height on the 450 px canvas so a <45 min lesson cannot clip its meta line (27 px block, 7 px box for a 12.6 px line, synthesised). Keep the 620 px canvas on normal heights and the 540/450 notches — the owner's decision — and measure the degradation, not its existence. Evidence: [W-R7-21][W-R7-22][W-R7-23][W-R7-24][V-R7-10c][V-R7-10d][V-R7-G1][S-R7-9].
3. #8 thorough + #4 understandable — Make the assistive layer say only what changed, and never drop focus. Feed (`FeedPage.tsx:157-178`): keep the wrapper mounted but do not fill a polite region with the initial list — announce a short status from a sibling `role=status` ("12 experiences" / "Feed updated") and reserve `aria-live` for appended items, or set `aria-live` only after the first render — so a screen-reader user is not read 1,120 characters on arrival and the whole list again on every tab change; name the `div.feed-stream[role=tabpanel]` from its tab. Compose (`ComposePage.tsx:206-225` vs `:298`): render the landing (or arm the alert region) in the registry-error branch so a retry that fails a second time lands with a ring instead of `<body>` — the only one of eight landings absent from its own error branch. Dialogs (`Modal.tsx`): set `inert` on `.app-frame` while any modal is open (0/7 today) and pass `describedBy` to the lesson and theme dialogs (5/7 today). 404 (`NotFoundPage.tsx:11`): a skip link and `#main`. `#main` ring: keep the inset ring but pad the scroll owner (or move the ring to a wrapper) so the 3 px band does not draw over content rows on Settings, History, Explore, Why and Dash (5/12 routes today). Evidence: [A-R7-12][A-R7-D][A-R7-4][A-R7-10][A-R7-E][S-R7-17].
4. #4 understandable + #6 honest + #9 relay — One truth per surface, and no silent work. Timetable: on an invalid `?date=` replace the URL with today's date (`history.replaceState`) or render a one-line status ("That date doesn't exist — showing today"), so the address bar and the page agree (today `?date=2026-13-45` stays in the URL over a page showing today, with nothing said; `TimetablePage.tsx:32-40`); use one date formatter for the heading and the status line — both `formatDayHeading` (`lib/format.ts:34-41`, browser locale) or both locale-aware — so "Saturday, August 22, 2026" and "Sat 22 Aug" (`TimetablePage.tsx:23-27`, hard-coded `en-GB`, "Sept" in September) stop coexisting 160 px apart; hide the "P1–P6 are the school's six lesson periods" clause on an empty day, where no P label renders. Copy: give the 404 a title that matches its h1 and differs from the never-listed entity's ("Not found · HOney" serves both today, `NotFoundPage.tsx:6`, `EntityPage.tsx:70`); make the delete-imported-data row and body precise about what runs (`SettingsPage.tsx:211,288` ↔ `services/accounts.ts:238-240` deletes `user_lesson_exposures` only; every user-linked row goes, the importer's shared teacher/course/room/lesson rows stay) and, if the point-of-decision disclosure is to equal the Settings row, add "and history … and whenever you press Sync now" to the login footnote (`LoginPage.tsx:66`); unify the provenance register (`shared.tsx:17` vs `ExperiencePost.tsx:20`) and the fourth "delisted" wording (`shared.tsx:69`); refresh the three stale comments (`ReconnectDialog.tsx:14-18`, `TimetablePage.tsx:22`, `:92-94`). Data layer (`lib/useApi.ts:14,55-64`): make `reload()` and `apiCache.clear()` bypass or clear `inflight` so a refresh issued during an in-flight request re-fetches (today 0 new requests, spinner runs, stale body re-cached); surface `entities.error`/`directory.error` from `useNames()` (`shared.tsx:160-168`) so the entity page shows its error branch when the registry is down instead of rendering complete and silent; do not call `useNames()` on the two-link chooser (`ComposePage.tsx:101`); delete `Reveal` (`lib/motion.tsx:38-81`, never imported) with `.reveal`/`.is-in`, delete `.option-grid span` (`components.css:775-778`), close the comment opened at `components.css:45`; serve `sw.js` with `cache-control: no-cache` (still `max-age=14400`). Evidence: [C-R7-2][S-R7-16][C-R7-1][C-R7-D4][V-R7-5][C-R7-E11][C-R7-E12][C-R7-E4][C-R7-E2][C-R7-D1][C-R7-D3][C-R7-E14][C-R7-E15][C-R7-E16][W-R7-9][S-R7-18][S-R7-19][S-R7-3][S-R7-3b][W-R7-33].

Redesign principles in priority order:
1. #8 thorough — success: the pending reaction computes a visibly different style from idle under `[aria-disabled="true"]` on stone and night while the POST is delayed 2 s; the Reconnect and Save sheets both render a one-line checkbox label with the caveat in the body and neither sheet scrolls at 320×568 (scrollHeight ≤ clientHeight); `p.caption.timetable-note` computes a non-zero top or bottom margin and the overlap census reports 0 pairs between the note and any hour label at 320/390/1280; the compact landing produces 0 overlaps between the sticky daynav/action row and `h1.schedule-header`/`.timetable-note` at 320×568, 360×620 and 390×620, lands instantly (no scroll samples between FCP and rest under `no-preference`), does not fire on retry (retry scroll 0 px, 24/24) and either survives SPA re-entry or is removed; a synthesised 40-min lesson on the 450 canvas has `clipY:false`; the compose retry that fails twice leaves `activeElement` on a landing with a ring; `inert` is true on `.app-frame` while any of the seven dialogs is open and `aria-describedby` resolves on 7/7; the 404 has a skip link landing on `#main`; the `#main` ring band overlaps no content at the sampled edges on 12/12 routes; `/why`'s back link and the login checkbox measure ≥44 px in a `hasTouch` context; `tsc` 0; 0 dangling or unterminated comments; 0 consumer-less rules.
2. #3 aesthetic — success: one label grammar per component (`ReconnectDialog` identical shape for both purposes); one disabled treatment per action row; the same label renders in the same shape on every surface ("Back to Experiences" 44 px pill on /why and Explore); the rendered spacing census stays ≥97 % mobile / ≥92 % desktop with 43 colours and the type set unchanged; mobile button families stay at 44 and 81; 0 element-pair overlaps outside the fixed-nav pass-under on every route at 320×568, 360×620, 390×620, 390×844 and 1280×800; every class the served JS emits has a rule that applies and every rule the served CSS carries has a consumer.
3. #5 unobtrusive — success: no motion after first paint that the student did not initiate (compact landing instant, `settle` the only route entrance); no accent ring on any tap that is not a retry landing; the feed's polite region announces only appended items or a short status, never the initial list; 0 interrupts on load; `compose?entityKey=` issues one `GET /api/entities`; the SW adds 0 requests to a warm navigation.
4. #4 understandable + #6 honest — success: the URL and the heading agree on every `?date=` (valid, impossible, malformed); one date grammar per screen; the P1–P6 clause appears only where a P label renders; the 404's title matches its h1 and differs from the never-listed entity's; the delete-imported-data copy names exactly what the service deletes; the login footnote and the Settings row name the same triggers and data types (or the footnote is deliberately the minimum and says so); one provenance register and one "delisted" sentence; no stale comment describes behaviour that does not occur; the import disclosure stays exactly as it is.
5. #2 useful — success: a pull-to-refresh or "Try again" issued during an in-flight request always issues a new request; the entity page shows an error branch when `/api/entities` fails; the two-link chooser issues 0 registry requests; the three primary tasks keep their current step counts (1/0, 1 tap, 4 screens / 3 taps); every lesson block hit-tests to itself and clears the nav at 320×568 on a 3-lesson day in Asia/Shanghai.
6. #9 environmentally friendly (keep at 3) — success: bundle under 100 KB wire (br and zstd), 0 idle animations, dark boot honoured pre-FCP, reduced-motion 0; the SW battery (`r7-wf-sw1/3/4.js`) still passes after any sw.js change; `sw.js` served `no-cache`.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — the compact-height landing's contract (instant, cold-load-of-a-date only, sticky-stack-aware offset, survives or is removed on re-entry); the feed announcement model (status sibling + append-only live region); the compose error branch as a landing; the refresh/in-flight contract in `useApi` (a refresh always bypasses `inflight`); the `ReconnectDialog` label/body split as one shape; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): a lessons day at 320×568 from cold load to tapping the 13:30 block with the day heading visible on arrival; Feed → tap 👍 (pending pill visibly pending) → tap 👍 again (no double POST, visible feedback); Feed arrival for a screen-reader user (short status, not the whole feed); Compose registry error → "Try again" fails → focus on a ringed landing; Settings → "Save school login…" and Reconnect at 320×568 with identical one-line labels and no sheet scroll; `?date=2026-13-45` → URL and heading agree.
- States checklist (empty, loading, error, success, focus, disabled/pending, selected) per screen, with the focus column split into keyboard / pointer-retry / pointer-plain-tap / retry-that-fails-again, the touch-target column listing every control's height in a `hasTouch` context including bare links and checkboxes, the pending column for reactions, the announcement column for every `aria-live`/`role=status` region (what text, when), and the compact-height column (scroll samples, overlaps, last-block-vs-nav) for the Timetable at 320×568 / 360×620 / 390×620.
- Migration path for users currently on the old design: none for routes or data; ship a `sw.js` byte change only if the SW is edited, and re-run the seeded-eviction battery with real bodies (`r7-wf-sw1.js` mode real) and the cold-start probe (`r7-wf-sw3.js`) before calling it done.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (pending reaction visibly distinct; both dialogs one-line label + no sheet scroll at 320×568; note margin non-zero and 0 note/hour overlaps; compact landing instant, once, 0 sticky-bar overlaps, last block clear of the nav, 0 px on retry; compose double-fail lands with a ring; `inert` 7/7, `describedBy` 7/7; 404 skip link; `#main` ring over 0 content; /why link and login checkbox ≥44; feed announces status not list; refresh during in-flight re-fetches; entity error branch on registry failure; `?date=` URL/heading agreement; one date grammar; delete copy precise; rendered spacing census ≥97 %; `getAnimations()` 0; contrast on a genuine dark boot; SW battery green; `tsc` 0; 0 consumer-less rules; 0 unterminated comments).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the `:disabled`-bound pending style, the smooth-scroll landing, the list-wrapping live region, the success-only landing and the refresh-blind in-flight map must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the touch-target census in a `hasTouch` context (now including bare links and checkboxes), the plain-tap ring test on all eight landings, the spacing census, the dark-boot contrast probe, the 320×568/360×620/390×620 timetable sweep in Asia/Shanghai with scroll sampling and the overlap census, the feed MutationObserver probe, and the SW seeded-eviction battery, after every CSS, focus, data-layer or SW change.
- Adding a rule without checking the cascade — the note margin shipped inert because `p.caption`'s specificity was never checked; verify every new spacing rule by computed style at every viewport, not by reading.
- Changing an attribute without restyling its state — `disabled` → `aria-disabled` silently killed the pending style; grep every selector that references the old attribute before switching.
- Mounting a live region before its content — a polite region filled after mount announces the fill; decide what should be announced first, then place the attribute.
- Adding a landing that only exists on success — every `useRetryFocus` consumer must render its landing element in the error branch too.
- Adding a scroll without a behaviour and an owner check — `scrollIntoView` inherits the owner's `scroll-behavior: smooth`; pass `behavior` explicitly, pick the events it runs on, and account for `AppLayout`'s pathname reset.
- Sharing a cache key without sharing a fetcher — the in-flight map serves the first caller's promise to every later caller under the same key; keep one fetcher per key, and let a refresh bypass the map.
- Measuring the Timetable in UTC — the harness's default clock hides every lesson block; any measurement involving lessons must use `timezoneId:"Asia/Shanghai"`.
- Fixing a surface without fixing its siblings — enumerate both `ReconnectDialog` purposes, all seven `Modal` call sites, every `useApi` consumer of `"entities"`/`"directory"`, every bare `<a>` used as an action, and both date formatters before changing one.
- Claiming a change in the commit message that the diff does not contain — verify each move on the deployed app before reporting it done.
````
