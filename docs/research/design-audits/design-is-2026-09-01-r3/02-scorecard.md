# 02 — Scorecard (r3; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `fd522c3`)

1. Good design is innovative — Score: 2/3 (r2 2, Δ0)
   Evidence: [C9] identity-free publish, no-author-column storage, browser-held ownership keys, exposure-verified provenance and no-free-text reports re-verified with zero drift since `3d91cb3`; every UI pattern (tabs, feed, chooser, cards) remains conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing this round produced a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1.

2. Good design makes a product useful — Score: 2/3 (r2 2, Δ0)
   Evidence: [W7] feed → typing is still 4 screens / 3 taps with `activeElement` BODY on landing; [N8] recovery control on 2 of 7 error surfaces; [N2] a composer reachable by URL offers "Share anonymously" for a key the server refuses; [S8] Mine shows two compose CTAs; [C4-F] "Open timetable" opens today while the chip says Tomorrow; all three primary tasks complete on-screen without a wrong-screen detour ([S2][S10][A6]).
   Justification: Every primary task completes and no path requires an unnecessary detour (rules out 1), but adjacent surfaces still add steps and one decoy action survives by URL, so not "fewest possible steps; no decoy actions" (rules out 3).

3. Good design is aesthetic — Score: 1/3 (r2 1, Δ0) — tie 1↔2, settled lower
   Evidence: [V1][V2] 214 `--sp-*` usages, 9 literal lines, rendered rhythm 95 % on-ladder @390 (r2 roughly half); [V3][V4] type {12…28} and 44 colours unchanged; [V5][A1] night chip 9.80:1, no enabled text below 4.5:1 on either surface. Remaining system breaks: [N11] `.main` 14 px top on every screen and modal 18 px (author literals, rendered), UA `<p>` margins un-reset on `.caption`/`.text-4` (13 px) and UA button padding on daynav (1/6 px); [N4] four orphan/duplicate rules (`.checkbox`, `.primary .action-cta` failing night as written, `pulse-soft`, `.entity-row .caption` ×2); [F1] the login clips its own wordmark at 320×568.
   Justification: The r2 blocker is verifiably gone — spacing, type and colour now obey one visible system in what renders — but anchor 3 says "no orphan styles" and four exist, and anchor 2 allows "≤2 minor inconsistencies" while the rendered residue counts three author-level breaks (14 px, 18 px, un-reset UA margins) plus a clipped login at the smallest supported viewport; uncertain between 1 and 2, the lower is taken.

4. Good design makes a product understandable — Score: 1/3 (r2 1, Δ0)
   Evidence: Fixed: [C2] zero "revoke", [C1] privacy link lands, [C5] one Explore name at all three sites, [V7] "Loading…" during lookup, [C7] one name per course. Remaining unclear controls: [A5] the date input is an invisible affordance consuming 4 Tab stops (unchanged since r1); [F3] Settings "Turn on" and "Reconnect" open the same dialog and "Sync"/"Sync now" are two labels for one action; [N6] a course target is captioned "Teacher". Jargon present: [C8] one key under three names ("ownership key", "private control key", "device-held key"), "verified exposure", "registry", "scrambled at rest" unglossed on Mine, "Not selected" rendered as a room in three places.
   Justification: Every primary action is identifiable without help (not 0), but 2–3 controls remain unclear and jargon is present on Settings, Mine and the feed — anchor 1 by the letter; anchor 2 ("1 control needs a tooltip") is exceeded by the Settings pair alone.

5. Good design is unobtrusive — Score: 2/3 (r2 2, Δ0)
   Evidence: [W4][V6] 0 idle animations on every screen (r2: one infinite pulse); [W5] 0 interrupts on load; [V12][A4] overlays own the viewport; [S1] composer notice is text; [S2] the chip strip is gone; but [W4] the 460 ms `settle` still replays on every route, [S3][N7] a primary-styled Share sits in the stream header including over the error state, and [S8] Mine shows two CTAs.
   Justification: Chrome is quiet and functional (anchor 2); per-route entrance motion and a primary button in the stream chrome keep the ground from fully receding (bars 3); nothing decorative competes with content (bars 1).

6. Good design is honest — Score: 1/3 (r2 2, Δ−1) — tie 1↔2, settled lower
   Evidence: [C9] every privacy claim re-verified 1:1 against unchanged code; [C2][C3] "revoke" and the dish "actually" gone; [C3] one degree-word inflation survives ("strongly protected", `WhyPage.tsx:73`); [C4] label→behaviour mismatches now: "Sync" silent re-auth undisclosed on the button and duplicated as "Sync now" [F3], raw-key composer with an enabled Share the server refuses [N2], "This entry is no longer listed." for a place listed under another id [N5], header Share over the error banner [N7], "…try again." with nothing to press ×5 [N8]. [F2] `fd522c3` removed the import choice, deleted the Settings toggle, and migration 009 flips every prior opt-out to granted with no notice; the login copy that promised "Nothing is imported unless you turn it on" is gone and the stay-connected caveat the code assumes is shown was dropped while `portalCredentials.ts:16` still claims it is.
   Justification: On-screen flows contain no forced-continuity, hidden-cost or scarcity mechanics and the on-screen inflation count is one (which alone would be anchor 2), but silently overriding a recorded user opt-out and removing the switch is a consent-override pattern by any deceptive-design taxonomy — anchor 1's "one dark pattern" — and the mismatch set grew from 3 to 6; between 1 and 2 the lower is taken. Nothing on screen deceives (not 0).

7. Good design is long-lasting — Score: 2/3 (r2 2, Δ0)
   Evidence: [V3][V4] single humanist face, one accent per surface, no gradients, no fad type, 44 colours; [W4] the staggered per-route entrance choreography and frosted translucent surfaces remain the one plausible trend marker.
   Justification: One dated marker matches anchor 2 exactly; no skeuomorph residue or trend typography (bars 1); the entrance signature bars 3.

8. Good design is thorough down to the last detail — Score: 1/3 (r2 1, Δ0)
   Evidence: Fixed and verified: containment, focus return, Try again on feed/entity, delisted CTA, night chip, chips removed, tsc clean, `useLoadMoreSentinel`. Rough states: error is text-only on 5 of 7 surfaces [N8] (worst instance scored); focus is ringless on the date input's 4th stop [A5]. Detail cluster: [F1] login wordmark clipped at 320×568; [V7] compose privacy link behind the fixed nav at 320×568 (r2 item not cleared); [N2] raw key on unknown keys; [N9] three links padded to 42 px (2 px short), privacy link 18 px, Report item 35.5 px; [N10] ··· menu with no Esc/outside dismissal, Try again and privacy landing drop focus; [F4] login checkbox after the submit button and outside the form; [A7] no header on three pages, no contentinfo; [N1] double registry fetch; [N4] orphan CSS; [S5] stale comments; [F5] SW not bumped for the wave.
   Justification: Two states are rough on their worst instance (error without recovery on five screens; a ringless focus stop) and the measured detail cluster grew rather than closed, which is past anchor 2's "1 state missing or rough"; the state framework exists and most r2 defects verified fixed, so not 0.

9. Good design is environmentally friendly — Score: 3/3 (r2 2, Δ+1)
   Evidence: [W1] app JS 86,394 B wire (96,116 B with the Cloudflare beacon), both under 100 KB; [W4] 0 idle animations on all 11 screens after settle, the only `infinite` keyframes bound to the pull gesture and the loading skeleton; [W9][V5] prefers-color-scheme honoured pre-paint with the night surface fully readable; [W4][A11] prefers-reduced-motion → 0 animations on every route tested. Noted but outside the anchor: [F5] SW cache not bumped this wave so superseded hashes linger; [N1] one duplicated fetch on entity pages.
   Justification: All four of anchor 3's conditions (<100 KB, no idle animation, dark mode honoured, reduced-motion respected) verify live for the first time; the SW and double-fetch findings are relayed as resource defects but the anchor is met as written, so 3.

10. Good design is as little design as possible — Score: 1/3 (r2 1, Δ0)
    Evidence: The three r2 duplications are gone ([S1] banner button, [S3] empty-feed double CTA, [S2] chip strip). Same-screen removables now: [S8] Mine renders "Share an experience" and "Share your first experience" together; [F3] Settings "Turn on" and "Reconnect" open one dialog, and its card carries four connection verbs; [N7] header Share over the error banner beside Try again; [N3] the "from your classes" mark on 14 of 19 rows distinguishes nothing within Teachers or Courses for this account and never marks Places; [S3] header Share + interleaved invite at ≥9 posts (code).
    Justification: Four same-screen elements could be removed without breaking any task (anchor 1's "3–5 removable elements"); no page is dominated by decoration or duplicated affordances (not 0); the Mine pair and the Settings pair alone exceed anchor 2's "≤2".

**Total: 16/30** (r2 16/30, Δ0; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #3, #6. Principles scored with no tie: #1, #2, #4, #5, #7, #8, #9, #10.
