# 01 — Evidence (r3, consolidated from five verification subagent passes)

Audited live at https://honey.gaelisus.com (integration/product-v2). The deploy moved from `26812cb` to `fd522c3` while the passes ran; `git diff 26812cb..fd522c3` touches only `LoginPage.tsx`, `SettingsPage.tsx`, 8 lines of `TimetablePage.tsx` (the `no_consent` banner branch) and backend auth/import; the stylesheet hash `index-CHlTuHHx.css` is unchanged and the JS moved `index-OOE7rlZj.js` → `index-DdKuPcuL.js`. Every /login, /settings and Timetable-banner measurement below was re-taken on `fd522c3`; all other surfaces are byte-identical between the two commits and their measurements stand. All dynamic evidence was measured by operating the app in Playwright/Chromium: 390×844 (primary), 320×568, 430×932, desktop 1280×800; "genuine dark boot" = fresh context, `colorScheme: dark`, no stored theme. Source paths relative to `/home/honey/HOney-refreshed/apps/web/src` unless noted; `BE` = `packages/backend/src`. Probe scripts `r3-*.js` (+ `.log`) and screenshots `r3-*.png` retained at `/root/claude-work/design-audit/`. Constraints honored: nothing published, no reports submitted, no reactions tapped, no persisted settings changed, no secrets read or reproduced. `tsc --noEmit --noUnusedLocals -p tsconfig.json` → 0 errors.

Anchors `[S…]` Structural, `[V…]` Visual, `[C…]` Copy & Honesty, `[W…]` Weight & Friction, `[A…]` Accessibility. Each r2 handoff move is marked VERIFIED-FIXED / PARTIAL / REFUTED; `[N…]` are defects first seen this round (`[F…]` = introduced by `fd522c3`).

## r2 handoff verification (all passes)

| r2 move | Status | Evidence |
|---|---|---|
| Spacing: 150 rules → `--sp-*`, 9 literals left | **VERIFIED-FIXED** | [V1][V2] 214 `var(--sp-*)` usages (r2 39); 9 literal lines; rendered census @390 95 % on-ladder |
| `.nextlesson__state` / `.primary .action-cta` → `var(--ink)` | **VERIFIED-FIXED** (chip) | [V5][A1] chip 11.52 stone / **9.80 night** (r2 1.54); `.action-cta` has no consumer (dead rule; `foundations.css:295-299`) |
| Zero idle animations | **VERIFIED-FIXED** | [W4] `getAnimations()` 0 after settle on 11 screens incl. Timetable now-line; `infinite` only on pull spinner + skeleton |
| Login doorway `fill-mode backwards` | **VERIFIED-FIXED** | [V10] `features.css:22`; 0 fixed descendants |
| Compose target label never a raw key | **PARTIAL** | [V7][C4] "Loading…" during lookup; raw key rendered once the registry loads and the key is unknown/delisted (`ComposePage.tsx:86`) |
| Composer notice text-only | **VERIFIED-FIXED** | [S1] `ComposePage.tsx:250-254` `<p>`; Keep-private buttons 4→3 defined, max 1 co-visible |
| Compose footer → `/settings#privacy`, Settings scrolls | **VERIFIED-FIXED** | [C1][A6] heading on screen at top 2–16 px on 390 and 320, cold and from compose; focus not moved |
| "Remove" replaces every "revoke" | **VERIFIED-FIXED** | [C2] 16 grep hits, 0 user-facing; live sweep of 9 screens 0 "revok" |
| Dish intro without "actually" | **VERIFIED-FIXED** | [C3] `EntityPage.tsx:30` (source; Food 0 live) |
| Explore one vocabulary, strip removed, inline mark | **VERIFIED-FIXED** / placeholder unchanged | [S2][C5] 0 chips; "Find someone or something" ×3 sites; placeholder "Filter by name…" remains; mark on 14/19 rows |
| Feed/entity error → Try again | **VERIFIED-FIXED** (2 of 7 surfaces) | [V8][C6] refetch proven (+1 request per click); 5 other surfaces still text-only |
| Feed header Share hides on empty feed | **VERIFIED (code)** | [S3] `FeedPage.tsx:32`; live feed has 1 post; shown during error and loading |
| `useLoadMoreSentinel` shared | **VERIFIED-FIXED** | [S4] `useLoadMoreSentinel.ts:11-26`, consumed by Feed and Entity |
| shared.tsx dead imports/comment | **VERIFIED-FIXED** | [S5] tsc 0; file ends at `shared.tsx:183` |
| Delisted entries: no Share CTA + note | **VERIFIED-FIXED** (entity page) | [S6][V9] `EntityPage.tsx:48-51,69-78`; composer URL bypass remains ([N2]) |
| Touch: context links padded, checkbox 22 px, disabled .65 | **PARTIAL** | [A3] links 65×**42** (2 px short); Settings checkbox removed in `fd522c3`; `.btn:disabled` .65 (4.49 stone), `.react-btn:disabled` still .7 (2.92) |
| SW cache `honey-v2` | **PARTIAL** | [W6] name bumped, old *names* evicted; `fd522c3` shipped without a bump, old hashes linger cache-first |
| Backend: names sanitized once, teacher protected | **VERIFIED-FIXED** | [S7][C7] one name for `c_55456` across Explore/Entity/compose/Timetable modal; `活动课老师` survives |

## Structural pass

- **[S1] Composer**: `ComposePage.tsx:250-254` notice is `<p className="text-4">You can keep it as a private note instead.</p>`; Keep-private buttons defined at `:290,:345,:382` (r2 4), max co-visible 1 in every state; live default composer 1 button. Live editor actions: "Share anonymously" / "Keep private" / "Cancel".
- **[S2] Explore**: 0 `.history-chip` / `section[aria-label="From your history"]` at 390 and 320 (`history-chip` CSS absent from HEAD; present at `3d91cb3:features.css:1249-1266`). 19 `a.entity-row`; **14 carry** `<span class="caption">from your classes</span>` (`ExplorePage.tsx:154`; Teachers 6/6, Courses 8/8, Places 0/5). Mark 95×18, `white-space:nowrap; flex:0 0 auto` (`features.css:1273-1277`), one line at every width; course *names* wrap to 2 lines @390 (row 73 px) and 3 @320 (97 px). Complete-set rule holds: "Teachers 6 / Courses 8 / Places 5 / Food 0"; `q="zzzz"` → four "Nothing by that name." sections. Explore visible interactives 36 → **26** (r2-r3).
- **[S3] Feed header Share**: `FeedPage.tsx:32` `showHeaderShare = feed.loading || feed.error !== null || feed.items.length > 0`, rendered `:40-44`; empty-state CTAs `:122,:132` never co-render with it; interleaved invite `:140-146` (i % 8) still co-renders at ≥9 posts (code; 1 post live). Header Share is visible during the **error** state beside the banner + "Try again" (`r3-struct-feed-error.png`).
- **[S4] Load-more**: `features/experiences/useLoadMoreSentinel.ts:11-26`; `FeedPage.tsx:12,30,153`; `EntityPage.tsx:13,46,102`; no `IntersectionObserver` left in either page.
- **[S5] Dead code**: `tsc --noUnusedLocals` **0** (r2 2). Residual: `ExplorePage.tsx:49-52` builds `refs:{key}[]` then `new Set(refs.map(r=>r.key))` (leftover of the chip shape); `[F]` `api/client.ts:121-123 setConsent()` now has zero callers; stale comments `SchoolLoginForm.tsx:7,23-24`, `BE/routes/auth.ts:26-29` still describe a consent step that no longer exists.
- **[S6] Delisted entities**: `EntityPage.tsx:48-51` `listed = !registry.data || registry.data.entities.some(...)`, `:69-76` CTA gated, `:78` "This entry is no longer listed." Live: `/experiences/room/r_df12aeba9bb9` h1 "Not selected", `shareCta []`, note present; `/experiences/room/r_d48ff4b2f68a` h1 "213", same; survivor `/experiences/room/r_360` h1 "213", CTA "Share your experience", no note. The delisted page still renders "No experiences here yet — yours could be the first." (`:94`) under the note; a nonexistent id shows the same copy as a delisted one.
- **[S7] Course names**: `BE/experiences/entities.ts:23-34` (`isTeacher` guard), `:64` registry mirrors `c.name`; `BE/services/importer.ts:130` passes `l.teacherDisplayName`, `:93-94` `ON CONFLICT DO UPDATE name`. Live `/api/directory` courses == `/api/entities` courses (8/8). `c_55456` reads "Activity 2026年秋活动课 活动课老师" on Explore row, Entity h1, compose `?entityKey=course:c_55456`, Timetable lesson-modal "Course". Lesson surfaces show `lesson.subjectName` "Activity" (History row, Timetable block, compose `?lessonId=`) — a different field, consistently. Registry hygiene holds: Places `213,308,309,416,A4`, no "Not selected" in listings, no duplicate names, every course tail is a listed teacher. **But** the room placeholder "Not selected" renders as a room name on the Timetable block, the lesson modal "Room" row, the History caption ("活动课老师 · Not selected") and the compose lesson-target detail (`TimetablePage.tsx:324-326,400-403`; `HistoryPage.tsx:126`).
- **[S8] Census @390 (visible/total; r2 in brackets)**: Home 9/15 [9/15]; Timetable 9/16 [9/16]; Feed 15/21 [15/21]; Explore **26/32** [36/42]; Compose 7/13 & 10/16 [same]; Compose editing 6/16 (mobile nav `display:none` while `.compose-textarea:focus`, `features.css:1231-1235`, pre-existing); Entity 6/12 [6/12]; **Mine 7/13** — "Share an experience" (`MinePage.tsx:134`) and "Share your first experience" (`:157`) co-visible on the empty Mine screen; Settings (`fd522c3`) **19/26** [19/26] with the "Import my timetable" checkbox gone and "Sync now" 102×44 added (`SettingsPage.tsx:116-143`); Login (`fd522c3`, unauthenticated) 4/4. Nesting 10 / 13 / 14 unchanged.
- **[S9] Repeated affordances**: r2's 13 families → 11 surviving (dual nav DOM; Sign out ×2; Settings ×2; Dash ×2; Appearance ×2; Reconnect ×3; Keep-private ×3 defined; "Your notes & posts" ×3; "Back to Experiences" ×4; Home→feed ×2; compose entries **11** sites, none in primary nav `navTabs.tsx:17-21,73-78`); **gone**: Explore chip-vs-row, duplicated IO block. **New**: [F] "Sync" (`TimetablePage.tsx:99-105` → `api.sync`) vs "Sync now" (`SettingsPage.tsx:117-142` → `api.syncSeamless`) — one purpose, two labels, two API calls; [F] Settings "Turn on" (`:179`) and "Reconnect" (`:144`) both open the same `ReconnectDialog`; [F] "Stay connected" control at 3 sites (`LoginPage.tsx:44-55`, `ReconnectDialog.tsx:22-33`, `SettingsPage.tsx:158-184`); "Try again" ×2 (`FeedPage.tsx:108-110`, `EntityPage.tsx:89-91`); identical Explore-link label ×3; duplicate CSS rule `.entity-row .caption` (`features.css:574-576` vs `:1273-1277`); **`EntityPage.tsx:50` adds a second `useApi("entities")` beside `useNames()` (`shared.tsx:160-168`) → live `GET /api/entities` ×2 on every entity-page load** (`r3-struct-network.js`).
- **[S10] Still-good re-checked**: scope tabs exactly 2, `scrollWidth 239 == clientWidth 239`, 44 px tall; leave-and-return 0 refetch / 0 skeleton; `.mobile-nav` rect `[10,770,370,64]` identical across routes (height 60→64 from the `--sp` migration of `components.css:733`); `.view` settle 460 ms `fill: backwards` per route, 0 animations and transform `none` after 600 ms.
- **[S11] Login (`fd522c3`)**: one `<form>` (username, password, submit "Continue with school account" 342×48); the "Stay connected on this device" `<input type=checkbox>` (`LoginPage.tsx:45-49`) sits **outside the form** after `.login__fields`, no id/name, `checked=false` default, state read from React at `onSuccess` (`:35-38`); wrapped by `label.stay-connected` 342×132. Footnote `:57-58` "…created on first sign-in. Your timetable comes along with it." No consent step remains (diff removed `Phase`, `ImportConsentStep`, `credsRef`). `TimetablePage.tsx:110-127` now only `error` / `ok` / `portal_reconnect_required`; `grep no_consent src` (excl. tests) = 0.
- **[S12] Gaps**: empty feed, ≥9-post interleave, composer nudge/cooldown states, publish with a delisted key, non-admin `/dash`, PTR gesture, Settings "Turn off"/"Sync now"/login submit — not exercised (forbidden or not reproducible).

## Visual pass

- **[V1] CSS spacing**: `var(--sp-*)` in margin/padding/gap: foundations 16, components 63, features 135 = **214** (r2 39). Literal-px spacing lines in the 4 files: **9** (foundations 1, components 3, features 5). Of the 9: `foundations.css:94` `.sr-only margin:-1px` (hairline ✔); `components.css:724` `.main padding: calc(14px + safe-area) 16px calc(96px + safe-area)` (96 clearance ✔, **14 top is neither exception**); `features.css:1199` `.main padding-bottom:90px` @max-height 700 (✔); `components.css:587` desktop `.main padding: 34px clamp(22px,5vw,76px) 110px` (110 ✔, 34/22–76 not); `components.css:919` `.modal padding: 20px 18px calc(24px + safe-area)` (**18 rendered in the lesson modal**); `features.css:72,75` `.home-greeting` 14/6 px (class not rendered on Home); `features.css:583` `.exp-card` clamp(22–34) (never rendered); `features.css:814` nudge sheet 18 px (publish-flow only). Net 3 of 9 match the documented exception classes. `admin.css` 0 `--sp` / 7 literals (out of scope). Inline: `ComposePage.tsx:251` `style={{margin:"6px 0 0"}}`.
- **[V2] Rendered spacing (margins/paddings/gaps > 0, 9 screens)**: @390 `1(5) 4(184) 6(4) 8(173) 12(304) 13(4) 14(10) 16(105) 20(74) 24(11) 96(10) 130(1)` → **851/895 = 95 % on ladder** (r2: 1…30,96,136 dominated by 2/12/10/16/8/6/14/9/18; 2 px ×152 → 0, 10 px ×83 → 0, 9/18/22/26/30 → 0). @320 `1 4 6 8 12 13 16 20 24 90`; @430 adds `3(3)` (UA checkbox margin of the toggle since removed) and `44(10)`; desktop adds `32 34 64 110 216 249`. Off-ladder attribution: 1/6 px = UA `<button>`/`<input>` padding on `.daynav__arrow`/`.daynav__picker` (no author rule); 13 px = UA `<p>` margin (1 em @13 px) on `p.caption` (Timetable "Last synced…", Settings "Saved on this device…") and `p.text-4` (Compose footnote) — `authorRules: []`; 14 = `.main` top (`components.css:724`); 96/90 = nav clearance; 130/170/702 = `.lesson-block__room{margin-left:auto}`. Explore head→filter gap **40 px** (r2 50) = `.section-head` mb 20 (`foundations.css:116`) + `.stack` gap 16 + `.search-box` mt 4 (`components.css:174`).
- **[V3] Type**: rendered @390 and @320 **{12,13,15,16,20,22,24,26,28}** (= r2); @430 one fractional 27.95 (`.nextlesson__subject` `clamp(26px,6.5vw,34px)`, `features.css:111`); desktop {12,13,15,16,20,28,30,32,34,36}; login {13,17} + 44 wordmark. Nothing < 12 px. 13 `font-size` declarations off `var(--fs-*)` (11 display `clamp()` + `24px` ×2 at `components.css:345,471`) — unchanged.
- **[V4] Colour**: **44** unique literals (= r2). Ink/surface literals outside tokens.css: `components.css:117,121` `.btn--danger` on-colour (commented AA rationale), `:854-863` theme swatches, `features.css:896` `var(--on-accent,#fff)` fallback, `index.html:6,26`, `theme.ts:22-25`. `.nextlesson__state` `color: var(--ink)` (`features.css:100`).
- **[V5] Contrast (live, alpha-composited)**: stone lowest enabled text `.stay-connected span` **4.87:1** (login, `--muted` on `--soft`, `components.css:976-980`), then 5.34 (`.text-4`/provenance/placeholder/nav off); chip 11.52. Genuine night boot: chip **9.80:1** fg (233,237,239) on (47,59,65) — `r3-vis-home-night-390.png`; lowest enabled 6.11 (daynav arrow) / 6.92 (login label). `.primary .action-cta` (`foundations.css:295-299`) rendered nowhere (`grep action-cta pages components` → 0); as written its night pair measures 1.08:1 via injection ([A1]). Disabled: `.btn:disabled` opacity .65 (`components.css:82-85`) → 4.49 stone / 7.01 night; disabled primary 4.61 / 6.64; `.react-btn:disabled` still .7 (`:273-276`) → **2.92** stone / 4.57 night (WCAG-exempt, reported).
- **[V6] Idle motion**: `getAnimations()` 0 at 2.5 s after settle on Home, Timetable, Feed, Explore, Compose, Settings, Mine, History; lesson modal open → 2 `finished`; 0 after close. `.timeline__now::before` static (`features.css:341-350`); `.nextlesson__wash` `transition: width 1s` only (`:83-91`). `@keyframes pulse-soft` still defined, no consumer (`foundations.css:331-339`).
- **[V7] Compose**: loading (API delayed 4 s): lesson → "A lesson from your history" → "Activity"; entity → **"Loading…" · Teacher** (`r3-vis-compose-loading-entity.png`) → "ChenJenny". After the directory loads, an absent key renders **the raw key** (`room:r_df12aeba9bb9`, `teacher:t_doesnotexist`; `r3-vis-compose-delisted-settled.png`), detail "Place"/"Teacher", full editor with enabled "Share anonymously" — `ComposePage.tsx:86` `entity?.name ?? (entities.data ? effectiveEntityKey : "Loading…")`. Course-entity target detail reads "Teacher" (`:87` maps only room/dish). Default composer renders no banner at 320/390/430. 320×568: overflow 87 px (r2 99); actions stack in a column; footnote y511–565 with the "How privacy works" link y529–565 vs fixed nav y494–558 → **the link sits behind the nav** (`r3-vis-compose-320x568.png`); 390 and 430 clear.
- **[V8] States**: feed error `role=alert` + "Try again" `btn--ghost btn--small` `[33,359,77,44]` (`FeedPage.tsx:107-110`) → +1 request, alert clears; entity identical (`EntityPage.tsx:87-91`). Loading: feed skeleton `role=status` (6 shimmer rows); compose "Loading…" label. Empty: Explore "Nothing by that name." with 4 sections visible; Mine "Nothing here yet"; Entity "No experiences here yet — yours could be the first." Success: Keep private → full "Kept private" page. Focus: 3 px accent outline, offset 3. Disabled: Share/Keep private disabled on empty body.
- **[V9] Delisted pages**: no Share CTA, note present, generic intro + "yours could be the first" beneath (`r3-vis-entity-delisted-r_df12aeba9bb9.png`).
- **[V10] Login (`fd522c3`)**: doorway `animation: settle .5s var(--ease) backwards` (`features.css:22`), 0 fixed descendants, 0 animations after settle; `scrollWidth == innerWidth` at 320/390/430/1280. **[F1] At 320×568 `.login` is `height:100dvh; display:flex; align-items:center; overflow-y:auto` (`features.css:8-17`); doorway 595 px > 568; scrollTop range 0…46; `.login__wordmark` top **−14 px at scrollTop 0** — the top of the wordmark is unreachable** (`r3-vis-login-320-viewport.png`). Cause: the 171 px `.stay-connected` label (`LoginPage.tsx:44-55`, `components.css:957-968`); at 390+ the 556 px doorway fits. Login checkbox `.stay-connected input` **18×18** (`components.css:969-975`), label 342×132.
- **[V11] Settings (`fd522c3`)**: no checkbox exists (`.checkbox input` 22 px rule `components.css:165-169` now orphaned); School-connection `.card-actions` wraps at 320 and 390 (Sync now `[37,1044,102,44]` + Reconnect `[147,1044,110,44]`, Disconnect drops to `[37,1096,112,44]`), one row at 430. Post context link `[16,290,65,42]`; nav 64 px.
- **[V12] Modal containment**: 390 overlay `[0,0,390,844]`, sheet `[0,427,390,417]` bottom gap 0, `.view` transform `none`; `elementFromPoint` at 4 nav centres → modal's own `.btn`; a real tap at (195,804) activated "Share experience" → `/experiences/compose?lessonId=…`. Desktop overlay `[0,0,1280,800]`, dialog `[410,189,460,422]`. Post ··· menu `[254,452,120,46]`, item "Report" 110×36.
- **[V13] Sweep 320/390/430/1280 (10 screens)**: `scrollWidth == innerWidth` everywhere; 0 per-element overflow; 0 wrapped button labels; only ellipsis `.post__provenance` (as r2); `.daynav` two rows and `.page-head` wrap identical to r2. **No new clipping from the spacing sweep** other than [F1].
- **[V14] Gaps**: 430/desktop censuses on `26812cb` JS (CSS identical); `.action-cta`, `.explore-letter__mark`, `.home-head__stale` not rendered; nudge sheet/publish success/cooling-off/report layout not exercised; 1 post, 1 lesson/day.

## Copy & Honesty pass

- **[C1] Privacy anchor**: `ComposePage.tsx:298-301` footer "A safety check runs first. Published posts carry no author ID; private notes never leave this device. How privacy works" → `/settings#privacy` (ownership-key sentence removed). `SettingsPage.tsx:17-23` rAF `scrollIntoView({block:"start"})`, `id="privacy"` (`:203/206`), `features.css:1279-1281` `scroll-margin-top: var(--sp-4)`. Live 390×844 from compose: hash `#privacy`, `main.scrollTop` 1498–1539 (main is the scroll owner), h2 top **2 px** (`26812cb`) / **16 px** (`fd522c3`, Settings shorter), on screen, `elementFromPoint` = `H2#privacy`; cold `/settings#privacy` at 390 and 320 identical; `activeElement` stays BODY.
- **[C2] Revoke**: `grep -rni revoke src` 16 hits, all identifiers/API paths/`URL.revokeObjectURL`/status key `revoked` rendered as "Removed"; live innerText sweep of /home, /experiences, /explore, /compose, /mine, /settings, /timetable, /history, /why → 0 "revok". "Remove…" (`MinePage.tsx:258`) ↔ "Remove this post?" / "Remove post" (`:199-201`); Mine empty "finds and removes" (`:150-153`); Settings "find or remove" (`:216-219`); Shared card "manage or remove" (`ComposePage.tsx:141-143`).
- **[C3] Inflations**: r2's "actually" gone (`EntityPage.tsx:30` "What students thought of it."). Flagged now: `WhyPage.tsx:73` "ordinary peer speech can be **strongly** protected" (degree word, no metric); `SettingsPage.tsx:207` "The detail, **honestly**:" (self-certifying); [F] `LoginPage.tsx:51-53` / `SettingsPage.tsx:163` "Your login is encrypted and kept only on this device." — true, but the caveat "(a browser is less protected than a phone's secure storage)" was dropped in `fd522c3` while `lib/portalCredentials.ts:16` still reads "weaker store than the iOS Keychain; the opt-in copy says so". Marketing superlatives: 0. Dark patterns by the rubric list: 0 in on-screen flows (Remove dialog states cost, Cancel unshamed; stay-connected unchecked by default; report dialog no free text).
- **[C4] Label→behaviour**: r2's privacy link and Revoke/Remove **fixed**; "Sync" silent re-auth **still present** (`TimetablePage.tsx:104` → `client.ts:137-147`, disclosed only on the Settings card `:163` and Reconnect dialog) and [F] now duplicated as "Sync now" (`SettingsPage.tsx:141` → `:83 api.syncSeamless`). New: (A) composer renders a raw key + enabled "Share anonymously" for unknown/delisted keys (`ComposePage.tsx:86`; server answers `entity_unknown`, `shared.tsx:69`); (B) "This entry is no longer listed." on `/experiences/room/r_d48ff4b2f68a` ("213") while "213" is listed at `r_360`, no link to it (`EntityPage.tsx:78`; `entities.ts:81-98`); (C) row name "ChenJennyfrom your classes" — no separator, no `title`/`aria-label` (`ExplorePage.tsx:152-155`); the mark is truthful (14/14 in the History filter set) though 7/8 marked course names never appear verbatim in a History row (rows show `subjectName`); (D) "…try again." banners with **no control** on Explore (`ExplorePage.tsx:78`, sections render "Teachers 0 … Food 0" beneath), Home (`HomePage.tsx:76`), Timetable (`:131`), History (`HistoryPage.tsx:108`), Mine (`MinePage.tsx:164`); (E) header Share visible over the feed error banner (`FeedPage.tsx:32`); (F, weak) Home "Open timetable" opens today while the chip says "Tomorrow · 01:00".
- **[C5] Explore vocabulary**: h1 `ExplorePage.tsx:59`, feed link `FeedPage.tsx:46`, compose picker `ComposePage.tsx:207` all "Find someone or something" (r2: 3 different labels); subline `:61` "Teachers, courses, places and food are all listed below — typing only narrows the list."; placeholder `:72` "Filter by name…" + aria "Filter entities by name" unchanged; History uses "Search lessons…" (`HistoryPage.tsx:72`); Home has no Explore link.
- **[C6] Error recovery**: "Try again" refetches (feed: requests 1→2→3 per click; after unblocking the alert clears and 1 post renders; entity → empty state). Shell error has "Retry" (`AppLayout.tsx:45`).
- **[C7] Course names / placeholders**: one name per course live (Explore row, History `<option>`, Timetable modal "Course"); "活动课老师" retained as teacher `t_9e75caffc183` and as the course tail; portal class codes remain inside names ("CIE Physics-A2 2026秋A2PHY备考5班 陈拯侃"); "Not selected" rendered as a room in three places ([S7]).
- **[C8] Jargon (facts)**: one key under three names — "ownership key" (`MinePage.tsx:140,151`; `SettingsPage.tsx:216-217,349-353`), "private control key" (`ComposePage.tsx:141`), "device-held key" (`SettingsPage.tsx:220`); "verified exposure" (`ExperiencePost.tsx:128`); "registry" (`shared.tsx:69`), "entities" (aria `ExplorePage.tsx:73`); "scrambled at rest" unglossed on Mine (`MinePage.tsx:152`); two provenance phrasings (`shared.tsx:17` vs `ExperiencePost.tsx:20`); "from your classes" carries three referents (Explore mark, Home section `HomePage.tsx:102`, feed scope `FeedPage.tsx:82`); [F] four connection verbs on one Settings card ("Sync now", "Reconnect", "Disconnect", "Turn on"); "P1…P6" unexplained; "Dash — The operational console for admins." (admin only).
- **[C9] Honesty claims drift**: `git log 3d91cb3..fd522c3 -- api/client.ts BE/routes/experiences.ts WhyPage.tsx packages/shared/src/api/contract.ts` **empty**; `BE/db/database.ts` changed only by appending migration 009 (`experiences` table `:103-121` untouched). Mapping re-confirmed: publish `auth:false` (`client.ts:233`) + `credentials:"omit"` (`:354-356`); body `{eligibilityToken, pass, body, rating?}` (`contract.ts:311-316`); no author column, `ownership_hash` only; report categories FE 6 = BE 6; "scrambled at rest" ×3 with the same-device-key caveat; HOney ID line `SettingsPage.tsx:69-70`; WhyPage `:75` "design boundary, not magic"; report dialog "no free text is collected" ↔ `client.ts:250-252` sends `{category}` only.
- **[C10] Consent model (`fd522c3`)**: login is one step; the "Stay connected on this device." checkbox controls only `portalCredentials.authorize(creds)` client-side (`LoginPage.tsx:38`; `portalCredentials.ts:78-88`, AES-GCM blob + key both in localStorage), unchecked by default (`:19`; live `checked:false`); backend receives username/password only (`BE/routes/auth.ts:16-24`). Import: first sign-in triggers `syncTimetable` in the background on `result.created` (`auth.ts:31-34`); consent row inserted **granted** (`BE/services/accounts.ts:66-69`); **migration 009 flips every existing `import_consents` row to granted with no user-facing notice** (`BE/db/database.ts:244-253`); `importer.ts:42-43` consent gate removed; `/api/consent` still exists (`auth.ts:138-146`) with no web caller; contract keeps `"no_consent"` (`contract.ts:96`). Removed strings: "One more choice.", "HOney can copy your timetable… Nothing is imported unless you turn it on, and you can switch it off any time in Settings.", "Import my timetable"/"Not now", Settings "Import my timetable" / "Consent granted {ago}" / "Import is switched off", Timetable "Timetable import is switched off…" + "Open Settings". The only remaining user-facing disclosure is the login footnote `:58` "Your timetable comes along with it."; the only remaining control is Settings "Delete imported data" (`:191-197`). Not observed live for an opted-out account (test account was opted in).
- **[C11] Gaps**: "Shared."/"Kept private" cards, nudge/cooldown, Mine populated states, dish surfaces, feed empty states, report send, key export/import, Sync/Reconnect/login submits — source-only; harness TZ is UTC (Home "Tomorrow · 01:00" = 09:00 CST; not a product defect).

## Weight & Friction pass

- **[W1] Bundle (`fd522c3`)**: `/assets/index-DdKuPcuL.js` **86,394 B wire / 264,874 raw** (r2 86,711 / 294,956); with the Cloudflare beacon (9,722 wire) **96,116 B wire** total; CSS 11,009 / 44,714; one font `source-sans-3-latin…woff2` 29,247; `DashPage-VAAIvKF2.js` 4,403 br, fetched only on `/dash` (never on cold or SPA `/home`, `/experiences`, `/timetable`, `/settings`). Static assets `cache-control: public, max-age=14400`, br.
- **[W2] Requests**: cold `/home` **12** (3 app-origin data: `/api/me`, `/api/next-lesson`, `/api/experiences/from-my-classes?limit=10`; 3 `/cdn-cgi/rum` pings); warm SPA → `/experiences` **3** (1 app request); cold `/experiences` **11** — identical to r2. No duplicate API calls on primary views (the entity-page double `GET /api/entities` is [S9]).
- **[W3] Timing (method: init-script polling every 16 ms; TTI = max(main-bundle responseEnd, first nav item with React props, first data element))**: cold `/home` TTFB 96, FCP **388 ms**, TTI ≈ **639 ms**, 0 long tasks; warm (SW-controlled) FCP **100 ms**, TTI 127; `/experiences` warm reload FCP 140 / TTI 296. Route transitions in-page 28–99 ms (title paint), 94–367 ms Playwright round-trip on a quiet run. `/login` fresh FCP 248 ms light / 456 dark.
- **[W4] Idle animation**: **0** after settle (≥600 ms and 2.5 s) on Home, Timetable (now-line present), Feed, Explore, Compose, Entity, Mine, Settings, Why, History, Login (fresh, unauthenticated). Sole non-zero: `/history?select=1` retains 1 **finished** `rise-in` object (`.banner--success`, `components.css:201`, fill both) — not running. Entrance-only: `.view` `settle 0.46s backwards` (`foundations.css:346-348`) replays on every route, cold and SPA; login doorway 500 ms; History stagger `rise-in` 0.5 s + ≤320 ms. `infinite` exactly twice: `ptr-rot` (`components.css:638`, pull only) and `skeleton-shimmer` (`:949`, loading only). prefers-reduced-motion → 0 on `/home`, `/timetable`, `/experiences`, `/compose`, `/settings`, `/login` (`foundations.css:370-379`).
- **[W5] Interrupts on load**: 0 visible dialogs/banners/toasts/badges on cold `/home`, `/experiences`, `/login`; `.ptr__disc[role=status]` present at opacity 0.
- **[W6] Service worker**: `public/sw.js:10` `CACHE = "honey-v2"`, `:21-27` activate deletes every other key — live: seeded `honey-v1`/`honey-v0` → after first load `caches.keys()` = `["honey-v2"]`; second load adds `/`, `/home`, CSS, JS, font, wordmark; third load serves them `fromServiceWorker`; `/api/` entries **0** (`:32`). **But** `fd522c3` deployed without a name bump (`git diff 26812cb..fd522c3 -- sw.js` empty): seeded previous-hash entries `/assets/index-OOE7rlZj.js`, `/assets/DashPage-C6vl-eAk.js` **linger** in `honey-v2` beside the new hashes and are served cache-first (`:34-47`; in-page fetch of the old URL → 200 from cache). The origin answers a missing hashed asset with **200 `text/html`** (SPA shell, `max-age=14400`), and `sw.js:42` caches any `res.ok` `/assets/` response — a stale shell would cache HTML under a `.js` URL.
- **[W7] Compose path**: feed (1 post) → header Share → chooser → `/history?select=1` (14 "Select") → `/experiences/compose?lessonId=…` — **4 screens / 3 taps**, `document.activeElement` BODY on landing (both commits).
- **[W8] Pull-to-refresh** (`26812cb`, code path unchanged): feed spin ≈558 ms, timetable 541–609 ms, exactly 1 API call per pull, sub-threshold 0, no native reload (`PullToRefresh.tsx:12-14,66-84`, `MIN_SPIN_MS = 650`).
- **[W9] Boot theme** (`26812cb`, `index.html` unchanged): `data-surface` set at 59–118 ms, before FCP; 0 later mutations in a 2.5 s window; theme-color `#14171a`/`#f4f6f7`; head order boot script → module → stylesheet.
- **[W10] Gaps**: droplet-local timings, concurrent subagent load inflated one run (`r3-wf-perf.log`, discarded in favour of the `fd522c3` run); LCP null headless; `.nextlesson__wash` never rendered live; old-hash lingering demonstrated with seeded entries.

## Accessibility pass

- **[A1] Contrast table (stone / genuine night boot)**: body 13.26 / 15.27; `--ink-2` 5.34 / 8.07; `--ink-3` 4.66 / 5.83 (injected); `--muted` & `.text-4` 5.34 / 8.07; captions 5.34–5.66 / 7.26–8.07; provenance & context link 5.34 / 8.07; placeholders 5.63 & 5.34 / 7.27 & 8.07; accent links 5.81 / 9.28; scope tab on/off 6.30 & 5.34 / 9.27 & 8.07; nav on/off 13.26 & 5.34 / 15.27 & 8.07; **`.nextlesson__state` 11.52 / 9.80**; "from your classes" 5.34 / 8.07; "This entry is no longer listed." 5.34 / 8.07; "Try again" 11.80 / 13.48; error banner text **4.77** / 7.03 (lowest rendered enabled text on stone); daynav arrow 5.14 / 6.11; login label 12.10 / 13.08, stay-connected span 4.87 / 6.92; "Sync now" 13.26 / 15.27. Disabled (exempt): primary 4.61 / 6.64; plain/ghost 4.49 / 7.01; react-btn 2.92 / 4.57. **No rendered enabled text below 4.5:1 on either surface.** `.primary .action-cta` as written: 1.08:1 on night (dead rule).
- **[A2] Focus return**: report dialog Tab ×10 cycles Close → 6 categories → Close; Esc → `button.react-btn--more "More options"`. Lesson modal: Enter opens, Tab cycles, Esc/overlay-click → `button.lesson-block`. VERIFIED.
- **[A3] Touch targets @390 (<44 px)**: Home "Open timetable" 83×**42.2**; feed identity link 111×**42**; post context link 65×**42** (r2 18; `features.css:1239-1244` padding-block 12 / margin-block −12 → 12+18+12 = 42); post menu "Report" 110×**35.5** (`features.css:1016-1027`, no min-height); compose "How privacy works" 102×**18** (`ComposePage.tsx:300`, not in the hit-area rule list); login checkbox input 18×18 (label 342×132 effective); `.lesson-block` min-height 20 (`features.css:365`, one 56 px block live). All others ≥44 (scope tabs, react ±, ···, header Share 59×44, nav 85×46, skip 134×48, daynav 44×44, date input 266×44, Explore rows 358×48–73, search 358×59, modal Close 44×44, Settings 15 controls incl. Sync now 102×44). History chips 0. Settings has **no checkbox** (`fd522c3`).
- **[A4] Nav under modal**: not hit-testable at 390 and desktop (`hitIsNav:false` ×4). VERIFIED. Modal has no `inert`/`aria-hidden` on the background (trap only).
- **[A5] Focus order**: Home skip → Open timetable → post preview → Share something → School Portal → nav; Feed skip → Share → Find… → Your notes → Why → tab/tab → context link → ± → ··· → nav; Explore skip → Back → search → rows → nav; Compose editor skip → textarea → (Share/Keep private when enabled) → Cancel → How privacy works → nav; Timetable skip → Previous day → **date input ×4 stops, 4th with `outline: none`** (`features.css:225-232` opacity-0 picker; unchanged r1/r2) → Next day → Back to today → History → Sync → lesson block → nav; Settings (`fd522c3`) skip → 4 theme → Sign out → Open Dash → Delete… → Sync now → Reconnect → Disconnect → Turn on → Delete… → Import… → nav; Login (`fd522c3`) username → password → **submit → checkbox** (the opt-in comes after the button that submits it; `LoginPage.tsx:44` after `SchoolLoginForm` `:33-42`). No traps.
- **[A6] Keyboard reachability**: YES for every primary action (lesson modal, scope tabs + arrows, ···→Report→Esc, react ±, context link, Share→chooser→Explore→row→entity→composer, Keep private/Share after typing, Try again, Explore search, Settings controls, skip link, privacy link). Defects: `.post__menu` (`role=menu`) opens with focus staying on ··· and has **no Escape / outside-click / Tab-out dismissal, no `aria-haspopup`/`aria-controls`, no arrow keys** (`ExperiencePost.tsx:194-217`); "Try again" activation **drops focus to BODY** (button unmounts); privacy landing scrolls but `activeElement` stays BODY; skip link → `#main` has no tabindex (activeElement BODY, next Tab lands on first content link — works).
- **[A7] Landmarks**: Home header + nav×2 + main + aside(hidden) + region×2; Feed + tablist (no tabpanel, no aria-controls); Explore region×4; **Compose, Timetable, Settings: no header**; Login: **main + form only, no skip link**, h1 = `<img alt="HOney">`; **contentinfo: none anywhere** (unchanged r1/r2).
- **[A8] Skip link**: YES on every AppLayout page (`AppLayout.tsx:57-59`; visible 12,12 134×48), absent on /login.
- **[A9] Error/loading semantics**: feed/entity/home errors `role=alert`; "Try again" accessible name = text; skeleton `role=status aria-label=Loading`. "This entry is no longer listed." is a plain `<p>` with no live region; delisted page has 0 focusables in main; `document.title` "HOney" on every route.
- **[A10] Explore mark for AT**: plain span, no aria/title, appended to the link name ("ChenJenny from your classes" in ariaSnapshot; DOM text has no separator) on 14/19 rows. Lesson block name lacks separators between subject/room ("ActivityNot selectedP1 · …", `TimetablePage.tsx:313-328`).
- **[A11] Reduced motion**: 0 animations on /home, /timetable, /experiences, /login; also 0 under no-preference after settle.
- **[A12] Gaps**: screen-reader runtime untestable (ariaSnapshot proxy); destructive actions reach-only; shortest-lesson case not reproducible; Mine populated states absent.

## New-defect register (first seen r3)

- **[N1]** Duplicate registry fetch: `EntityPage.tsx:50` `useApi("entities")` beside `useNames()` → `GET /api/entities` ×2 per entity page ([S9]).
- **[N2]** Composer at an unknown/delisted key renders the raw key with a full editor and enabled "Share anonymously" that the server refuses (`ComposePage.tsx:86`; [V7][C4-A]).
- **[N3]** "from your classes" is the majority state on Explore (14/19; every Teacher and Course row; Places never marked) and is appended to row names without a separator ([S2][A10]).
- **[N4]** Duplicate `.entity-row .caption` rule; orphan `.checkbox` 22 px rule; orphan `.primary .action-cta` (fails night as written); orphan `@keyframes pulse-soft` ([S9][V1][V6][A1]).
- **[N5]** Delisted page stacks "This entry is no longer listed." over "yours could be the first" with no action; same copy for nonexistent ids; "213" delisted under one id while listed under another with no link ([S6][C4-B]).
- **[N6]** Compose course-entity detail reads "Teacher" (`ComposePage.tsx:87`).
- **[N7]** Header Share rendered over the feed error state ([S3][C4-E]).
- **[N8]** "…try again." with no control on 5 of 7 error surfaces ([C4-D]).
- **[N9]** Three padded links at 42 px (2 px short), privacy link 18 px, Report menuitem 35.5 px ([A3]).
- **[N10]** Post ··· menu has no dismissal or menu semantics; "Try again" and the privacy landing do not move focus ([A6]).
- **[N11]** Rendered off-ladder residue: `.main` 14 px top, modal 18 px, UA `<p>` margins on `.caption`/`.text-4`, UA button padding on daynav ([V1][V2]).
- **[F1]** Login 320×568: doorway 595 px centred in a 568 px `100dvh` box → wordmark top clipped 14 px, unreachable by scroll ([V10]).
- **[F2]** Consent: migration 009 grants import for every existing opt-out silently; the Settings toggle is gone; the only disclosure is the login footnote; the only remaining control is "Delete imported data" ([C10]).
- **[F3]** "Sync" (Timetable) and "Sync now" (Settings): two labels, two API calls, one silent re-auth; Settings "Turn on" and "Reconnect" open the same dialog ([S9][C4]).
- **[F4]** Login: opt-in checkbox after the submit button in Tab order and outside the `<form>`; 18×18 input; no skip link/nav landmarks on /login; stay-connected caveat dropped while `portalCredentials.ts:16` assumes it is shown ([S11][A5][C3]).
- **[F5]** SW `honey-v2` not bumped for the `fd522c3` wave; previous hashes linger cache-first; origin 200-HTML fallback for missing assets is cacheable under `/assets/` ([W6]).
