# 04 — /make-plan handoff (r3)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit fd522c3). Current design failed audit at 16/30 with critical gaps in principles #3 aesthetic, #4 understandable, #6 honest, #8 thorough, #10 as little design as possible (all scored 1/3; no principle scored 0).

Verdict paragraph (quoted from 03-verdict.md):
> The r2 handoff verified as built — spacing is now a system in what renders (95 % on the ladder), the night chip reads at 9.80:1, "revoke" and the loading-time raw key are gone, the privacy link lands, the chip strip and the banner button are removed, idle motion is zero and #9 reaches 3 for the first time — yet HOney still totals 16/30 because the deploy audited (`fd522c3`) also shipped a one-step login that clips its own wordmark at 320×568 and silently converts every recorded import opt-out to granted while deleting the switch, which costs #6 the point #9 gained, and the residue on #3, #4, #8 and #10 (three author-level off-ladder values and four orphan rules; two unclear Settings controls plus key-name jargon; five error surfaces with no recovery and a 42-px target cluster; four same-screen removables) sits exactly at each anchor's count threshold — so the remaining work is a short, enumerable finishing pass, but the score says REDESIGN and the audit reports it.

Why redesign and not refine: total 16/30 is below the REFINE threshold of 20; only two principles (#3, #6) sat on a tie and granting both upward reaches 18, still below the threshold — so the verdict holds under any reading of the tie-break, and this prompt is the REDESIGN template even though every move is a finish-the-detail or remove-the-pattern move on a layer whose tokens, type, colour, spacing and trust architecture all verify.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes (backend consent handling may change behaviour behind existing endpoints, not their shape). Verify everything in a real browser against the deployed app, not from screenshots alone. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage()`; probe scripts `r3-*.js`) can be re-run to confirm each fix.

Preserve from current design (MUST NOT be touched — these verified live on fd522c3):
- Spacing system as it now renders: 214 `var(--sp-*)` margin/padding/gap usages against 9 literal lines (apps/web/src/styles), rendered rhythm at 390 = 851 of 895 values on the 4/8/12/16/20/24/32/44 ladder (`--sp-1…--sp-8`, tokens.css:71-79). Regression check: re-run the rendered spacing census at 390/320/430; on-ladder share must not drop below 95 % and no new 2/9/10/18/22/26/30 px values may appear.
- Type ramp `--fs-micro…--fs-display` (tokens.css:57-66); live sizes {12,13,15,16,20,22,24,26,28} px, floor 12, no fractional at 390/320. Regression check: rendered-font-size census, nothing below 12 px.
- Palette and accent discipline: 44 colour literals, one accent per surface, warm hues only on --danger/--ok; `.nextlesson__state` on `var(--ink)` (features.css:100) measuring 11.52:1 stone / 9.80:1 on a genuine dark boot. Regression check: rerun the live contrast probe on stone and on a fresh `colorScheme:dark` context with no stored theme; no enabled text below 4.5:1 on either.
- Zero idle motion: `document.getAnimations().length === 0` after settle on every screen; `.timeline__now::before` static (features.css:341-350); `.nextlesson__wash` is a width transition only (features.css:83-91); `infinite` keyframes only on the pull spinner (components.css:638) and loading skeleton (:949); prefers-reduced-motion → 0 (foundations.css:370-379). Regression check: getAnimations() on all 11 screens at 2.5 s after settle, with and without reduced-motion.
- Weight: app JS 86,394 B wire / 264,874 raw (index-*.js), Dash chunk 4.4 KB br loaded only on /dash (App.tsx:8,57-59); 12 requests cold /home, 3 warm to /experiences. Regression check: curl br sizes of the two chunks; DashPage never fetched on student routes.
- Boot script honouring prefers-color-scheme pre-paint (index.html:14-33) and the stored-choice-wins order; theme-color set before FCP, no later mutation.
- Route-settle containment: `.view { animation: settle .46s … backwards }` (foundations.css:346-348); lesson modal overlay equals the viewport at 390 and 1280, nav not hit-testable beneath it (Modal.tsx trap/Esc/opener-restore). Regression check: elementFromPoint at the four nav centres under the open modal never resolves inside `.mobile-nav`.
- Focus system: 3 px accent outline offset 3 on every control; report-dialog and lesson-modal focus return to the opener (ExperiencePost.tsx `moreBtnRef`, Modal.tsx:19-21,44); scope tablist with arrow keys (FeedPage.tsx:62-73), exactly two tabs fully visible.
- Vocabulary that landed: zero user-facing "revoke" — the Remove family end-to-end (MinePage.tsx:199-201,258; SettingsPage.tsx:216-219; ComposePage.tsx:141-143); "Find someone or something" at all three Explore entry sites (ExplorePage.tsx:59, FeedPage.tsx:46, ComposePage.tsx:207) with the subline "…typing only narrows the list." (ExplorePage.tsx:61); dish intro without "actually" (EntityPage.tsx:30).
- Compose: text-only notice `<p>You can keep it as a private note instead.</p>` (ComposePage.tsx:250-254) with exactly one Keep-private control per state (:290,:345,:382); footer "A safety check runs first. Published posts carry no author ID; private notes never leave this device. How privacy works" → /settings#privacy (:298-301) landing with `id="privacy"` + `scroll-margin-top` (SettingsPage.tsx:17-23,203; features.css:1279-1281); "Loading…" as the lookup label (ComposePage.tsx:86 first branch).
- Feed: header Share conditional on `feed.loading || feed.error !== null || feed.items.length > 0` (FeedPage.tsx:32) — keep the empty-feed hiding, change only the error clause (see Discard); "Try again" on feed and entity errors that provably refetches (FeedPage.tsx:106-111, EntityPage.tsx:87-92 → useFeedController.ts:227); shared `useLoadMoreSentinel` (features/experiences/useLoadMoreSentinel.ts:11-26); pull-to-refresh single MIN_SPIN, one refetch (PullToRefresh.tsx:12-14,66-84).
- Explore: complete-set rule — every section rendered with counts, search only filters (ExplorePage.tsx:121-126); no chip strip; 19 rows; the inline mark's non-wrapping rule (features.css:1273-1277) may stay if the mark stays.
- Entity: delisted ids render without a Share CTA and say "This entry is no longer listed." (EntityPage.tsx:48-51,69-78).
- Backend naming: course names sanitized once at import with the lesson's teacher name protected (packages/backend/src/experiences/entities.ts:23-34,64; services/importer.ts:130) — one name per course across Explore, Entity, compose and the Timetable modal; teacher "活动课老师" preserved. Registry hygiene: 5 places, no "Not selected" in listings, no duplicate names.
- Clean build: `tsc --noEmit --noUnusedLocals` = 0 errors; keep it so.
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:233,354-356; packages/shared/src/api/contract.ts:311-316), no author column (packages/backend/src/db/database.ts:103-121), 6 = 6 report categories, "scrambled at rest" with its same-device-key caveat, WhyPage "design boundary, not magic", the HOney ID line (SettingsPage.tsx:69-70), the report dialog's "no free text is collected".

Discard (structural patterns causing the failures):
- Silent consent override: migration 009 flips every existing `import_consents` row to granted with no notice (packages/backend/src/db/database.ts:244-253) while fd522c3 deleted the Settings "Import my timetable" toggle and the login promise "Nothing is imported unless you turn it on"; the only disclosure left is "Your timetable comes along with it." (LoginPage.tsx:58) and the only control "Delete imported data" (SettingsPage.tsx:191-197). Caused failure on principle #6.
- Dropped caveat the code still assumes: LoginPage.tsx:51-53 / SettingsPage.tsx:163 "Your login is encrypted and kept only on this device." without the browser-storage caveat, while lib/portalCredentials.ts:16 reads "weaker store than the iOS Keychain; the opt-in copy says so". Caused failure on principle #6.
- Centred `100dvh` login box: `.login { height:100dvh; display:flex; align-items:center; overflow-y:auto }` (features.css:8-17) with a 595 px doorway at 320×568 → wordmark top at −14 px, unreachable (scrollTop range 0…46). Caused failure on principles #3 and #8.
- Login form order: the "Stay connected on this device" checkbox sits outside the `<form>` and after the submit button in Tab order (LoginPage.tsx:44-55 after SchoolLoginForm.tsx:33-42); 18×18 input (components.css:969-975); /login has no skip link or nav landmarks. Caused failure on principle #8.
- Error banners without recovery: "…try again." with no control on Explore (ExplorePage.tsx:78), Home (HomePage.tsx:76), Timetable (TimetablePage.tsx:131), History (HistoryPage.tsx:108), Mine (MinePage.tsx:164); Try again on feed/entity drops focus to BODY after activation (FeedPage.tsx:108, EntityPage.tsx:89). Caused failure on principles #8 and #2.
- Raw identifiers as fallback labels after load: `entity?.name ?? (entities.data ? effectiveEntityKey : "Loading…")` (ComposePage.tsx:86) renders `room:r_df12aeba9bb9` / `teacher:t_doesnotexist` with a full editor and an enabled "Share anonymously" the server refuses; course targets captioned "Teacher" (:87). Caused failure on principles #8, #6 and #4.
- Two-pixel-short hit areas: `padding-block: 12px; margin-block: -12px` (features.css:1239-1244) yields 42 px on "Open timetable", the feed identity link and the post context link; "How privacy works" 102×18 (ComposePage.tsx:300) is not in the rule; Report menuitem 110×35.5 (features.css:1016-1027). Caused failure on principle #8.
- Menu without menu behaviour: `.post__menu[role=menu]` opens with focus left on "···", no Escape / outside-click / Tab-out dismissal, no `aria-haspopup`/`aria-controls`, no arrow keys (ExperiencePost.tsx:194-217). Caused failure on principle #8.
- Invisible date affordance: the opacity-0 native date input consumes 4 Tab stops, the 4th ringless (features.css:225-232). Caused failure on principles #4 and #8.
- Compact compose hiding its footnote: at 320×568 the "How privacy works" link (y529–565) sits behind the fixed nav (y494–558) (ComposePage.tsx:298-301; components.css:724). Caused failure on principle #8.
- Two labels, one action: "Sync" (TimetablePage.tsx:99-105 → api.sync) and "Sync now" (SettingsPage.tsx:117-142 → api.syncSeamless), both silently replaying stored school credentials (api/client.ts:137-147) with the disclosure only on the Settings card; Settings "Turn on" (:179) and "Reconnect" (:144) open the same ReconnectDialog; four connection verbs on one card. Caused failure on principles #10, #4 and #6.
- Same-screen duplicate CTAs: Mine renders "Share an experience" (MinePage.tsx:134) and "Share your first experience" (:157) together; header Share renders over the feed error banner beside Try again (FeedPage.tsx:32 error clause); header Share + interleaved invite co-render at ≥9 posts (:140-146). Caused failure on principle #10.
- Majority-state mark: "from your classes" on 14 of 19 Explore rows (every Teacher and Course, never a Place) appended to the link name without a separator or AT description (ExplorePage.tsx:152-155). Caused failure on principles #10 and #4.
- Key under three names: "ownership key" (MinePage.tsx:140,151; SettingsPage.tsx:216-217,349), "private control key" (ComposePage.tsx:141), "device-held key" (SettingsPage.tsx:220); "verified exposure" (ExperiencePost.tsx:128); "registry" (shared.tsx:69); "scrambled at rest" unglossed on Mine (MinePage.tsx:152). Caused failure on principle #4.
- Placeholder rendered as data: the room placeholder "Not selected" shown as a room on the Timetable block, the lesson modal and the History caption (TimetablePage.tsx:324-326,400-403; HistoryPage.tsx:126) while the registry hides it (entities.ts:84-86); delisted duplicate "213" (r_d48ff4b2f68a) says "no longer listed" with no link to the surviving r_360 (EntityPage.tsx:78; entities.ts:81-98). Caused failure on principles #4 and #6.
- Last off-ladder literals and orphan CSS: `.main` 14 px top (components.css:724), modal `20px 18px` (:919), un-reset UA `<p>` margins on `.caption`/`.text-4` (13 px rendered), UA button padding on `.daynav__arrow`/`.daynav__picker`, inline `margin:"6px 0 0"` (ComposePage.tsx:251), unrendered literals `.home-greeting` (features.css:72,75) and `.exp-card` (:583); orphan `.checkbox` (components.css:159-169), dead `.primary .action-cta` (foundations.css:295-299, 1.08:1 on night as written), unused `@keyframes pulse-soft` (:331-339), shadowed `.entity-row .caption` (features.css:574-576). Caused failure on principle #3.
- Duplicate work and dead code: second `useApi("entities")` on EntityPage.tsx:50 beside `useNames()` (shared.tsx:160-168) → `GET /api/entities` ×2 per entity page; orphaned `setConsent` (api/client.ts:121-123); stale consent comments (SchoolLoginForm.tsx:7,23-24; packages/backend/src/routes/auth.ts:26-29); leftover `refs` array (ExplorePage.tsx:49-52). Caused failure on principle #8.
- Manual SW cache naming: `CACHE = "honey-v2"` (public/sw.js:10) was not bumped for the fd522c3 wave, so previous hashed assets linger in the cache and are served cache-first (:34-47); `sw.js:42` caches any ok `/assets/` response although the origin answers a missing hash with a 200 HTML shell. Caused a resource defect relayed under principle #9 (anchor still met).

Top 5 moves from the audit (verbatim):
1. #6 honest + #8 thorough — Put the fd522c3 consent change and login back on the level. Replace migration 009's silent grant (`packages/backend/src/db/database.ts:244-253`) with a one-time on-screen notice for accounts that had declined — or re-ask them — and give every account a visible "Timetable import: on" line with a way to turn it off (the deleted `SettingsPage.tsx` toggle left only "Delete imported data", `:191-197`); restore the stay-connected caveat that `lib/portalCredentials.ts:16` assumes is shown ("(a browser is less protected than a phone's secure storage)") in `LoginPage.tsx:51-53` and `SettingsPage.tsx:163`; fix the 320×568 login by dropping `align-items:center` in the `100dvh` `.login` box (`features.css:8-17`) or giving the doorway a `min-height`-based layout so the wordmark top is reachable; move the "Stay connected" checkbox inside the `<form>` and before the submit button in Tab order (`LoginPage.tsx:44-55`); add the skip link / landmarks to /login; delete the stale consent comments (`SchoolLoginForm.tsx:7,23-24`, `packages/backend/src/routes/auth.ts:26-29`) and the orphaned `setConsent` (`api/client.ts:121-123`). Evidence: [F1][F2][F4][C10][S11].
2. #8 thorough — Give every error a way back and close the detail cluster. Add the same "Try again" control to Explore (`ExplorePage.tsx:78`), Home (`HomePage.tsx:76`), Timetable (`:131`), History (`HistoryPage.tsx:108`) and Mine (`MinePage.tsx:164`); after a retry or a privacy-link landing move focus to the new content (`FeedPage.tsx:108`, `EntityPage.tsx:89`, `SettingsPage.tsx:17-23`); make the composer treat an unknown/delisted key like the entity page does — "This entry is no longer listed." and no editor — instead of `entity?.name ?? (entities.data ? effectiveEntityKey : "Loading…")` (`ComposePage.tsx:86`) and caption a course target "Course" (`:87`); at 320×568 keep the compose footnote above the fixed nav (link y529–565 vs nav y494–558; `ComposePage.tsx:298-301`, `components.css:724`); bring the three ±12 px padded links to 44 (`features.css:1239-1244` → ±13 or `min-height`), the privacy link (102×18, `ComposePage.tsx:300`) and the Report item (110×35.5, `features.css:1016-1027`) to 44; give the ··· menu Escape/outside-click dismissal, `aria-haspopup`/`aria-controls` and initial focus (`ExperiencePost.tsx:194-217`); give the date picker a visible affordance and one Tab stop (`features.css:225-232`); drop the duplicate `useApi("entities")` (`EntityPage.tsx:50`). Evidence: [N8][N10][N2][N6][V7][N9][A5][N1].
3. #10 as little design as possible + #4 understandable — One action, one name, one control. Merge "Sync" (`TimetablePage.tsx:99-105`) and "Sync now" (`SettingsPage.tsx:117-142`) into one label calling one API, and disclose the silent re-auth where the button is; make Settings "Turn on" (`:179`) and "Reconnect" (`:144`) one control or two different ones; show one compose CTA on the empty Mine screen (`MinePage.tsx:134,157`); hide the header Share in the error state (`FeedPage.tsx:32`); decide the "from your classes" mark's job — mark only when it distinguishes (or mark the *unfamiliar* entities), separate it from the row name for AT (`ExplorePage.tsx:152-155`); use one name for the key everywhere ("ownership key" `MinePage.tsx:140,151`, `SettingsPage.tsx:216-217,349`; "private control key" `ComposePage.tsx:141`; "device-held key" `SettingsPage.tsx:220`); never render the "Not selected" placeholder as a room (`TimetablePage.tsx:324-326,400-403`, `HistoryPage.tsx:126`); link a delisted duplicate to its surviving entry (`EntityPage.tsx:78`; `entities.ts:81-98`). Evidence: [F3][S8][N7][N3][C8][S7][N5].
4. #3 aesthetic — Close the last rhythm residue and delete the orphans. Put `.main`'s 14 px top (`components.css:724`) and the modal's `20px 18px` (`:919`) on the ladder; reset the UA `<p>` margin on `.caption`/`.text-4` (`foundations.css`) so the 13 px value disappears; give `.daynav__arrow`/`.daynav__picker` explicit padding; remove the orphan `.checkbox` rule (`components.css:159-169`), the dead `.primary .action-cta` (`foundations.css:295-299`), the unused `@keyframes pulse-soft` (`:331-339`), the shadowed `.entity-row .caption` (`features.css:574-576`), the inline `margin:"6px 0 0"` (`ComposePage.tsx:251`) and the unrendered `.home-greeting`/`.exp-card` literals (`features.css:72,75,583`); comment the three legitimate literals as the documented exceptions. Evidence: [N11][N4][V1][V2].
5. #9 environmentally friendly (relay) — Make the service-worker cache self-evicting. Derive the cache name from the build hash (or bump it in the deploy script) so a deploy that touches only TSX still evicts the previous hashed assets (`public/sw.js:10`, `fd522c3` shipped with `honey-v2` unchanged and `index-OOE7rlZj.js` lingering cache-first), and guard `sw.js:42` so only JS/CSS/font content-types are cached under `/assets/` — the origin answers a missing hash with a 200 HTML shell. Evidence: [F5][W6].

Redesign principles in priority order:
1. #6 honest — success: no recorded user choice is changed without an on-screen notice; every account can see whether timetable import is on and turn it off; every claim about stored credentials states the caveat the code assumes; every "Sync" control discloses the re-auth it performs; no composer offers a Share the server will refuse; every "…try again." has something to press.
2. #8 thorough — success: every error surface has a recovery control that moves focus to the result; the login renders fully at 320×568 with the checkbox inside the form before the submit; every tappable element ≥44 px (re-measure the ±12 px links); the ··· menu dismisses on Escape and outside click; the date picker is visible and one Tab stop; `tsc --noUnusedLocals` stays at 0 and `GET /api/entities` fires once per entity page.
3. #10 as little design as possible — success: one Sync control per action, one dialog per control, one compose CTA per screen state, and a "from your classes" mark that only appears where it tells the student something.
4. #4 understandable — success: one name for the key across Mine/Settings/Compose; "verified exposure" and "registry" replaced with plain words; "Not selected" never shown as a room; a course target captioned "Course".
5. #3 aesthetic — success: a rendered spacing census at 390 returns only ladder values plus UA-free, documented shell clearances; zero orphan or duplicate rules in the four stylesheets; the login and compose screens fit 320×568 without clipping.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — the consent/import disclosure and control in Settings and on first sign-in, and the single Sync/Reconnect model across Timetable and Settings; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): first sign-in → timetable import disclosure → Home at 390×844 and 320×568, showing where the import status and its off-switch live; and feed error → Try again → focus landing.
- States checklist (empty, loading, error, success, focus, disabled) per screen, with the five text-only error banners redrawn with their control and the composer's unknown-key state drawn explicitly.
- Migration path for users currently on the old design: none for routes or data; for accounts whose import consent was flipped by migration 009, a one-time notice on next sign-in (or a re-ask) is part of this plan, not optional; bump or hash-derive the SW cache name with the deploy.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (login 320×568 unclipped, consent notice present, zero text-only error banners, all targets ≥44 px, one Sync control, rendered spacing census, getAnimations() 0, contrast on a genuine dark boot).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the raw-key fallback, the text-only banners and the ±12 px hit rule must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients, or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the spacing census and the dark-boot contrast probe, after every CSS change.
- Shipping a behaviour change (consent, sync) without its copy — fd522c3 removed the choice and its explanation in the same commit; the plan must land the disclosure with the behaviour.
- Claiming a change in the commit message that the diff does not contain — verify each move on the deployed app before reporting it done, and bump the SW cache name in the same deploy.
````
