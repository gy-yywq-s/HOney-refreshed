# 02 — Scorecard (r4; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `5d810d8`)

1. Good design is innovative — Score: 2/3 (r3 2, Δ0)
   Evidence: [C8] identity-free publish, no-author-column storage, browser-held ownership keys, exposure-verified provenance and no-free-text reports re-verified with zero drift since `3d91cb3`; every UI pattern this round touched (menu, date button, banners with retry, disclosure row) is conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing shipped this round is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1.

2. Good design makes a product useful — Score: 2/3 (r3 2, Δ0)
   Evidence: [W7] feed → typing is still 4 screens / 3 taps with `activeElement` BODY on landing; the composer decoy is gone ([S7] 0 Share buttons at an unlisted key); every error surface has a working recovery control ([V6] 7/7, +1 request each); Mine shows one CTA ([S1]); the date control is one named button ([S8]); but [N1] at 320×568 the compose "Cancel" is not hit-testable on landing (`elementFromPoint` = `nav.mobile-nav`) and [N3] the date picker has no reachable fallback if `showPicker` refuses; all three primary tasks complete on-screen ([S17][A3]).
   Justification: Every primary task completes and no path requires a wrong-screen detour (rules out 1); the chooser → History → editor path still adds steps and the smallest supported viewport hides a compose action until the student scrolls, so not "fewest possible steps" (rules out 3).

3. Good design is aesthetic — Score: 1/3 (r3 1, Δ0)
   Evidence: The r3 blocker is verifiably gone — [V2] 97.6 % on-ladder at 320/390/430 with **0 author-level off-ladder values on mobile**, [V1] 217 token usages / 6 literal lines, [V4] 43 colours byte-identical, [V3] type unchanged, [V5] lowest enabled 4.66 stone / 5.83 night. Rendered inconsistencies now: [N2] the daynav stepper lost its hairline dividers to a duplicate `.daynav__date` rule; [N3] the new date button's focus ring is clipped top and bottom by `overflow:hidden`; [N12] "No lessons today" renders on top of the "P3 Free"/"P4 Free" band rows on the primary screen; [V13] hero caption overlap 83×9; desktop 87.9 % from an un-reset `.settings-trigger` and an uncommented `.main` literal. Orphans: [N11] four named orphans removed, four duplicate/orphan constructs re-introduced, 27 consumer-less class names in a whole sweep.
   Justification: Anchor 1 is "3–5 inconsistencies OR one jarring violation" — the timeline overlay is text drawn over text on the timetable, and with the lost dividers, the clipped ring and the hero overlap the rendered count is four; anchor 2's "≤2 minor inconsistencies" and anchor 3's "no orphan styles" are both exceeded, so 1 by count, not by tie.

4. Good design makes a product understandable — Score: 1/3 (r3 1, Δ0) — tie 1↔2, settled lower
   Evidence: Fixed: [S8] date control one named button (r3's four ringless stops); [C7] one key name, "registry" gone, "scrambled at rest" glossed ×3, "Not selected" 0, course captioned "Course" [C6-E]; [C6-A] one Sync label. Remaining unclear controls: [N6] "Save school login…" opens a dialog titled "Reconnect school account" whose body says the session ended; [N10] "See 213" sits under an h1 "213"; [N9] a never-listed id renders "Teacher" as the entity's name and says it was delisted. Jargon present: [C7] "P1…P6" ×6 per day unexplained on the primary screen, "verified exposure" in the `not_eligible` note, "entities" in the search aria-label, four connection verbs on one card; [N17] `document.title` "HOney" on every route.
   Justification: Every primary action is identifiable without help (not 0); the count sits between anchor 2's "1 control needs a tooltip" and anchor 1's "2–3 controls unclear; jargon present" — two labels that mislead plus jargon on the timetable — and the tie-breaker settles it at 1.

5. Good design is unobtrusive — Score: 2/3 (r3 2, Δ0)
   Evidence: [W4][V12] 0 idle animations on every screen under both motion preferences; [W5] 0 interrupts on load, the disclosure row is a plain `.setting-row`; [V9][A7] overlays own the viewport; [S5] header Share hidden on error and empty; [S6] the majority-state Explore mark is gone (0/19); [S1] one Mine CTA; but [W4] the 460 ms `settle` still replays on every route and a primary-styled Share still sits in the stream header in the normal state ([S5]).
   Justification: Chrome is quiet and functional (anchor 2); per-route entrance motion and a primary button in the stream chrome keep the ground from fully receding (bars 3); nothing decorative competes with content (bars 1).

6. Good design is honest — Score: 2/3 (r3 1, Δ+1)
   Evidence: [C8] every privacy claim re-verified 1:1 against unchanged code; [C4] 0 dark patterns by the rubric list; [C3] one degree-word inflation ("strongly protected", `WhyPage.tsx:73`). The r3 dark-pattern condition — a recorded choice overridden while the switch and the disclosure were deleted — is replaced under the owner's product decision by an on-screen disclosure that verifies live: [C5] "Timetable import — On for every HOney account since 1 September 2026…" with a date claim consistent with the code, plus the login footnote "Your timetable comes along with it."; the stay-connected caveat is restored at 2 of 3 sites ([C3]); the composer no longer offers a Share the server refuses ([C6-D]); every "…try again." has a control ([C6-C]). Mismatches that remain: [N6] "The portal session ended." shown from the Save path on a connected account; [N8] "when you sign in" while import runs only at account creation; [N9] "no longer listed" for ids that never were; [N7] the Timetable re-auth clause 678 px from its button and conditional; [C6-H] "Open timetable" vs the "Tomorrow" chip (weak).
   Justification: Anchor 1 requires "2+ inflations OR one dark pattern" and neither holds — one inflation, and no forced-continuity, hidden-cost, scarcity or confirmshaming mechanic in any flow — while anchor 2's "≤1 minor inflation" is met; the four stale or displaced statements bar anchor 3's "every claim, badge, and label maps 1:1", so 2.

7. Good design is long-lasting — Score: 2/3 (r3 2, Δ0)
   Evidence: [V3][V4] single humanist face, one accent per surface, no gradients, no fad type, 43 colours unchanged; [W4] the staggered per-route entrance choreography and frosted translucent surfaces remain the one plausible trend marker.
   Justification: One dated marker matches anchor 2 exactly; no skeuomorph residue or trend typography (bars 1); the entrance signature bars 3.

8. Good design is thorough down to the last detail — Score: 1/3 (r3 1, Δ0)
   Evidence: Fixed and verified: Try again ×7 with `role=alert` [V6][A8], 0 sub-44 targets on 8 of 9 screens [V11], Report 44 px, privacy link 44 px, ··· menu with haspopup/controls/first-item focus/Escape/outside dismissal [S11], date button one stop [A2], login form order and 320×568 wordmark [V7], `/api/entities` ×1 [W2], `tsc` 0 [S4], privacy landing focuses its heading [S10]. Rough states on the worst instance: **error** — after retry focus lands on BODY on 6 of 7 surfaces and on a ringless container on the 7th ([N5]); **focus** — the date button's ring is clipped ([N3]), the menu survives Tab-out with a stale `aria-expanded` and drops focus on outside close ([N4]); **not-found** — never-listed ids render the kind label as the name ([N9]). Detail cluster: [N1] Cancel under the nav at 320×568; [N10] survivor link 52×22; [N13] login checkbox 18×18 and the submit 27 px below the fold at 320; [N6] `ReconnectDialog` missed the wave (checkbox outside the form, no caveat); [N18] editor mounts under "Loading…" before `unlisted` can resolve; [N11] duplicate rules; [N14] SW over-evicts the current font and Dash chunk on every activate.
   Justification: Two states are rough on their worst instance (error recovery that loses focus on six surfaces; a focus system with a clipped ring and a menu that leaks focus) and a third (not-found) is mislabelled, which is past anchor 2's "1 state missing or rough"; the state framework exists on every surface and most r3 defects verified fixed, so not 0.

9. Good design is environmentally friendly — Score: 3/3 (r3 3, Δ0)
   Evidence: [W1] app JS 87,022 B wire (84,469 br by curl), under 100 KB; Dash chunk 4,403 br fetched only on `/dash`; [W4] 0 idle animations on 11 screens, `infinite` bound only to the pull spinner and loading skeleton; [W9] prefers-color-scheme honoured pre-paint in six boot contexts with 0 later mutations; [W4][A10] prefers-reduced-motion → 0 on every route; [W2] 12 requests cold, 3 warm, 0 duplicate API calls. Relayed but outside the anchor: [N14] the SW's eviction set is built from `index.html` only, so every activate deletes the still-current font subset (28,740 B) and Dash chunk; eviction needs an `sw.js` byte change; 123,968 B of unused font subsets ship in `dist`.
   Justification: All four of anchor 3's conditions (<100 KB, no idle animation, dark mode honoured, reduced-motion respected) verify live for the second round running; the SW findings are resource defects relayed to the plan, but the anchor is met as written, so 3.

10. Good design is as little design as possible — Score: 2/3 (r3 1, Δ+1)
    Evidence: The four r3 same-screen removables are gone: [S1] Mine renders one CTA; [S5] header Share never co-renders with the error banner; [C6-A] one "Sync now" label calling one API; [S6] the "from your classes" mark no longer marks 14 of 19 rows. Remaining removable on one screen: [N6] Settings "Reconnect" and "Save school login…" are two entries into one identical dialog. Not live: [S5] header Share + interleaved invite co-render at ≥9 posts (code, 1 post live). Repeated-affordance families 13 ([S3]) — the additions are one component rendered per screen ("Try again" ×7) and CSS duplicates, not same-screen elements.
    Justification: One same-screen element could be removed without breaking any task (anchor 2's "≤2 removable elements"); no page is dominated by decoration or duplicated affordances (not 0); the dialog pair alone keeps it from "every element earns its place" (not 3).

**Total: 18/30** (r3 16/30, Δ+2; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #4. Principles scored with no tie: #1, #2, #3, #5, #6, #7, #8, #9, #10.
