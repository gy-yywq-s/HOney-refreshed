# 01 — Evidence (r4, consolidated from five verification subagent passes, all run on opus)

Audited live at https://honey.gaelisus.com (integration/product-v2, commit `5d810d8`; served `index-BQhfgddr.js` / `index-D7RZ4AWU.css`, matching `apps/web/dist` built from HEAD; live `sw.js` `honey-v3`). All dynamic evidence was measured by operating the app in Playwright/Chromium: 390×844 (primary), 320×568, 430×932, desktop 1280×800; "genuine dark boot" = fresh context, `colorScheme: dark`, no stored theme. Source paths relative to `/home/honey/HOney-refreshed/apps/web/src` unless noted; `BE` = `packages/backend/src`. Probe scripts `r4-*.js` (+ `.log`) and screenshots `r4-*.png` retained at `/root/claude-work/design-audit/`. Constraints honored: nothing published, no reports submitted, no reactions left set, no state-changing Settings actions pressed (dialogs opened and cancelled only), no secrets read or reproduced (`.session.json` expired mid-run and was re-minted through `lib.js`; never printed). `tsc --noEmit --noUnusedLocals -p tsconfig.json` → 0 errors. `git diff --stat fd522c3..5d810d8`: 19 web source files, `public/sw.js`, `BE/routes/auth.ts`, `BE/services/timetable.ts`; `BE/db/database.ts` (migration 009) **unchanged**; no dependency changes.

Anchors `[S…]` Structural, `[V…]` Visual, `[C…]` Copy & Honesty, `[W…]` Weight & Friction, `[A…]` Accessibility; `[N…]` = defects first seen this round. Owner's recorded product decision (00-scope): timetable import is part of the account — no consent gate, no switch; the disclosure is what is scored.

## r3 handoff verification (all passes)

| r3 move | Status | Evidence |
|---|---|---|
| M1 migration-009 silent grant → notice / re-ask | **NOT DONE** (superseded by owner decision; disclosure scored instead) | `git diff fd522c3..5d810d8 -- BE/db/database.ts` empty; `:244-253` unchanged; no notice string anywhere [C5-G] |
| M1 visible "Timetable import: on" line | **VERIFIED (disclosure only)** | `SettingsPage.tsx:195-203` static `.setting-row`, 0 controls; live text verbatim; row top 1384 px in a 3046 px page at 390 (≈1.6 viewports below the fold), 2.6 viewports at 320 [C5-A,B][S10][A6] |
| M1 stay-connected caveat restored | **PARTIAL (2 of 3 sites)** | `LoginPage.tsx:52-57`, `SettingsPage.tsx:169` live "(a browser is less protected than a phone's secure storage)"; **absent** in `ReconnectDialog.tsx:29-31` while `lib/portalCredentials.ts:16` says "the opt-in copy says so" [C3][C9][S16] |
| M1 login 320×568 clipping | **VERIFIED-FIXED** | `features.css:12-24` `align-items:center` → `flex-direction:column` + `.login__doorway{margin:auto}`; wordmark top **+32** at scrollTop 0 (r3 −14); scroller 681/568; centred at 390/430/1280 (gap 143/143, 205/205, 139/139) [V7] |
| M1 checkbox inside `<form>` before submit | **VERIFIED-FIXED** | `SchoolLoginForm.tsx:64` `{beforeSubmit}`; Tab order skip → username → password → checkbox → submit at 320/390/430; input still 18×18 in a 342×151 label [S2-1e][A2][V7] |
| M1 skip link / landmarks on /login | **PARTIAL** | "Skip to sign-in" → `#school-username` (`LoginPage.tsx:25-28`), Enter focuses the input; landmarks still `main` + `form` only; skip link rendered inside `<main>` [A5][A4] |
| M1 stale consent comments + `setConsent` | **VERIFIED-FIXED** (one miss) | `setConsent` gone; `SchoolLoginForm.tsx:28`, `BE/routes/auth.ts:26-27` rewritten; `packages/shared/src/api/contract.ts:12-16` still documents the deleted consent model [C9][C5-E] |
| M2 "Try again" on the 5 text-only banners | **VERIFIED-FIXED (7/7)** | `HomePage.tsx:79`, `HistoryPage.tsx:111`, `MinePage.tsx:169`, `ExplorePage.tsx:82`, `TimetablePage.tsx:148` + feed/entity; all `role=alert` + `btn--ghost btn--small` 77×44; +1 request per click on 6 surfaces; Mine only reachable with a seeded key [C6-C][V6][A3][A8] |
| M2 focus moves to the result after retry | **PARTIAL (1 of 7)** | Feed → `div.feed-stream` (tabIndex −1, `outline:none`, `:focus-visible` false); Entity → BODY whenever the refetch is empty (`EntityPage.tsx:108` ref on a node rendered only at `:118`); Home/Timetable/History/Explore/Mine call bare `reload()` → BODY [A3][C6-C][S2-2b] |
| M2 privacy landing focuses the heading | **VERIFIED-FIXED** | `SettingsPage.tsx:19-27` `el?.focus()`, `:218` `tabIndex={-1}`; `activeElement === h2#privacy` at 390 and 320, cold and from compose; heading top 16 px; UA default ring, not the 3 px accent [S10][A3] |
| M2 composer refuses unknown/delisted keys | **VERIFIED-FIXED** | `ComposePage.tsx:188-207`; `room:r_df12aeba9bb9`, `teacher:t_doesnotexist`, `course:c_bogus` → "This entry is no longer listed, so nothing can be shared about it." + one CTA "Find someone or something"; no editor, 0 Share buttons; "Loading…" preserved during a 4.5 s delayed registry [S7][C6-D][V6] |
| M2 course target captioned "Course" | **VERIFIED-FIXED** | `ComposePage.tsx:88-89`; live `?entityKey=course:c_55456` → "Course" [C6-E] |
| M2 compose footnote above the nav at 320×568 | **VERIFIED-FIXED — defect relocated** | `features.css:1287-1296` `@media (max-height:620px)` footnote order 1 / actions order 2; link `[16,311,280,62]` clear of nav (494–558); **"Cancel" `[16,484,288,44]` now under the nav**, `elementFromPoint` = `nav.mobile-nav` [N1] |
| M2 ±12 px links, privacy link, Report → 44 | **VERIFIED-FIXED** | `features.css:1240-1248` `calc(var(--sp-3) + 1px)` (commented exception); "Open timetable" 83×44, feed identity 111×44, context link 65×44, "How privacy works" 102×44, Report 110×44 (`:1281-1283`); 0 sub-44 interactive elements on 8 of 9 screens at 390 [V11][A6] |
| M2 ··· menu Escape / outside-click / haspopup / controls / initial focus | **VERIFIED-FIXED (4 of 6 behaviours)** | `ExperiencePost.tsx:104-127,220-232`; `aria-haspopup="menu"`, `aria-controls` matching `#id`, `aria-expanded` toggles; focus on open = "Report"; Escape closes and returns focus to `···`; outside pointer closes (focus → BODY); **Tab-out leaves the menu open with `aria-expanded="true"`**; no arrow keys [S11][A3][N4] |
| M2 date picker visible, one Tab stop | **VERIFIED-FIXED (ring clipped; opening unverifiable)** | `TimetablePage.tsx:72-95` `<button class="daynav__date" aria-label="Pick a date (Tue 1 Sept)">` 268×44, one stop, 3 px ring; input `tabIndex −1 aria-hidden opacity 0 pointer-events none` 5×4; `showPicker()` called with 0 errors, no picker observable headless; fallback `el.click()` targets the hidden input; **ring clipped top/bottom by `.daynav__stepper{overflow:hidden}`** [S8][A3][V16][N3] |
| M2 drop duplicate `useApi("entities")` | **VERIFIED-FIXED** | `EntityPage.tsx:34` `useNames()` only; live `GET /api/entities` ×1 on 2 cold + 2 SPA entity loads [W2][S2-2j] |
| M3 one Sync label, one API | **VERIFIED-FIXED** | `TimetablePage.tsx:118` and `SettingsPage.tsx:147` both "Sync now" → `api.syncSeamless()` → `client.ts:133-147` [C6-A][S3-5] |
| M3 disclose the re-auth where the button is | **PARTIAL** | Same sentence on both surfaces, gated on a saved login (absent live for this account): Settings 16 px above the button, same row (`SettingsPage.tsx:117-118`); Timetable inside the "Last synced" caption **678–722 px below** the sticky button and absent before a first sync or in the error branch (`TimetablePage.tsx:158-163`) [C6-A][S2-3b][N7] |
| M3 "Turn on"/"Reconnect" one control or two different ones | **REFUTED** | `SettingsPage.tsx:150` "Reconnect" and `:186` "Save school login…" both `setShowReconnect(true)` → one `ReconnectDialog` titled "Reconnect school account", body "The portal session ended. Sign in again to restore it…" — shown from the Save path on a Connected/`portalTokenValid` account; dialogs byte-identical at 390 and 320 [C6-B][S2-3c][N6] |
| M3 one compose CTA on empty Mine | **VERIFIED-FIXED** | `MinePage.tsx:133-137` `{!empty && …}`; live `["Share your first experience"]`; census 7/13 → 6/12 [S1][S2-3d] |
| M3 hide header Share in feed error | **VERIFIED-FIXED** | `FeedPage.tsx:33`; error → `headerShare: []`; empty → `[]`; loading → shown [S5][C6-G] |
| M3 mark only where it distinguishes + AT separator | **VERIFIED (code); 0 marks live** | `ExplorePage.tsx:114-115` `markable = some(mine) && some(!mine)`; Teachers 6/6, Courses 8/8 mine, Places 0/5 → 0 of 19 marked (r3 14/19); `<span class="visually-hidden">, </span>` verified only under an injected mixed section (accessible name "ChenJenny , from your classes") [S6][A9][C7] |
| M3 one name for the key | **VERIFIED-FIXED** | "ownership key" at 7 sites; "private control key" 0; "device-held key" 0 user-facing (`ComposePage.tsx:143`, `SettingsPage.tsx:234`) [C7] |
| M3 never render "Not selected" as a room | **VERIFIED-FIXED** | `BE/services/timetable.ts:31,139` `CASE WHEN lower(trim(r.name)) IN ('not selected','') THEN NULL`; 0 live occurrences on block, modal (no Room row), History ×24, compose detail; registry 5 places [S2-3i][C7][A9] |
| M3 link a delisted duplicate to its survivor | **VERIFIED-FIXED** | `EntityPage.tsx:78-92`; `/experiences/room/r_d48ff4b2f68a` → "See 213" → `/experiences/room/r_360`; link 52×22 [S9][A6][N10] |
| M4 `.main` 14 px + modal 20/18 px on the ladder | **VERIFIED-FIXED** | `components.css:711-714` `calc(var(--sp-4) + safe-area) var(--sp-4) calc(96px + safe-area)` with "96px: nav clearance (documented exception)"; `:909` `var(--sp-5) var(--sp-4) calc(var(--sp-6) + …)`; live `.main` top 16, sheet `20px 16px 24px` [V1][V9] |
| M4 UA `<p>` margin reset on `.caption`/`.text-4` | **VERIFIED-FIXED** | `foundations.css:174-178` `p.caption, p.text-4 { margin-block: 0 }`; no `<p>`-sourced 13 px in any census [V2] |
| M4 explicit padding on daynav arrow/picker | **VERIFIED-FIXED** | `features.css:194` `.daynav__arrow{padding:0}`; 6 px gone; 1 px remains on the aria-hidden 1×1 picker only [V2] |
| M4 remove the four orphans, the inline margin, `.home-greeting`/`.exp-card` literals | **VERIFIED-FIXED — new duplicates introduced** | `.checkbox`, `.primary .action-cta`, `pulse-soft`, duplicate `.entity-row .caption`, `.home-greeting` deleted; inline `6px 0 0` → `.compose-notice-alt{margin-top:var(--sp-2)}`; `.exp-card` → `var(--sp-6)`. New: `.daynav__date` ×2, `.post__menu button` ×2, `.visually-hidden` beside a now-consumerless `.sr-only`, dangling comment `features.css:1275`, `.login__consent` orphan, `.action-cta` base orphan [S4][V15][N11] |
| M4 comment the legitimate literals | **PARTIAL** | 2 of 6 remaining literal lines commented (`components.css:711-712`, `features.css:1245`); desktop `.main` `34px clamp(22px,5vw,76px) 110px` (`components.css:576`) and nudge sheet 18 px (`features.css:815`) uncommented [V1] |
| M5 SW self-evicting cache + typed `/assets/` caching | **PARTIAL** | `sw.js:10` `honey-v3` (manual bump); `:21-37` `evictStaleAssets()` on activate: seeded old names and old hashes evicted; `:64-65` content-type guard: missing hash returns `200 text/html` and is **not** cached. But eviction runs only from `activate` — a TSX-only deploy with byte-identical `sw.js` leaves seeded stale hashes through two reloads; the live-asset set is regex-matched from `index.html` only, so every activate **deletes the current font subset (28,740 B) and `DashPage-*.js` (13,413 B)**; navigation-shell cache unbounded (14 entries / 374,893 B after 10 loads); `sw.js` served `max-age=14400` [W6][N14] |

## Structural pass

- **[S1] Census @390 (visible/total; r3 in brackets)**: Home 9/15 [9/15]; Timetable **10/17** [9/16] (date button added, hidden input retained); Feed 15/21; Explore 26/32; Compose default 7/13, `?entityKey=` listed 10/16, `?lessonId=` 10/16, **unlisted 6/12** [10/16], editing 6/16; Entity 6/12; **Mine empty 6/12** [7/13]; Settings 19/26; History 8/14; Login 5/5 [4/4] (skip link). Nesting 10 in `main`, 14 in document — unchanged.
- **[S3] Repeated-affordance families: 13** (r3 11). Removed: Sync/Sync now split, Mine CTA pair, header-Share-over-error, duplicate `useApi("entities")`, duplicate `.entity-row .caption`. Added: "Try again" ×7 (one purpose, one component, each on its own screen), "Find someone or something" ×4 (`ExplorePage.tsx:59`, `FeedPage.tsx:46`, `ComposePage.tsx:200,207`), `.daynav__date` ×2, `.post__menu button` ×2, `.visually-hidden` + `.sr-only`, two wordings of "no longer listed" (`EntityPage.tsx:79`, `ComposePage.tsx:197`). Surviving: dual nav DOM; compose entry points 11 definition sites, max co-visible 1; "Reconnect"/"Save school login…" → one dialog; "Stay connected" ×3 sites with three different bodies; Keep-private ×3 defined, max 1 visible; Sign out/Settings/Dash/Appearance nav-vs-Settings; "Back to Experiences" ×4.
- **[S4] Dead code**: `tsc --noUnusedLocals` 0; `setConsent` 0 hits; `refs` array gone; whole-stylesheet orphan sweep: 237 class names, **27 with zero consumers** (`action-card, action-cta, action-grid, browse-grid, btn--secondary, card-copy, card-index, card-title, chip--danger, chip--muted, chip--ok, exp-card__spacer, hairline-list, home-exp-list, home-exp-row, home-exp-row__body, lift, login__consent, row-link, section-head__copy, segmented, sort-toggle, sort-toggle__btn, sort-toggle__btn--on, sr-only, stat-strip, text-2`), two created by this wave (`.login__consent` `features.css:61-65`; `.action-cta` base `foundations.css:286-299` left after `.primary .action-cta` was deleted).
- **[S5] Feed**: header Share hidden on error and empty, shown on loading (top 59 beside 7 skeleton rows); retry → 1 post + `activeElement = div.feed-stream`; interleaved invite at ≥9 posts unchanged (`FeedPage.tsx:140-148`, code only).
- **[S6] Explore**: 19 rows at 390 and 320; 0 marks; complete-set holds (`Teachers 6 / Courses 8 / Places 5 / Food 0`; `zzzz` → four "Nothing by that name." at count 0); error → Try again 77×44 with the four sections still rendered "Teachers 0 … Food 0" beneath. Non-wrap guard on the mark (`white-space:nowrap; flex:none`) was deleted with the duplicate rule — computed now `white-space:normal; flex:0 1 auto` (unobservable live).
- **[S7] Composer**: unlisted → 6/12, no editor; `course:c_55456` → "Course"; delayed registry → "Loading…" **with a full editor and enabled Share/Keep private rendered before the registry resolves** (`ComposePage.tsx:87,190-192`; transient); 320×568 order field 0 / footnote 1 / actions 2 → footnote 306–360, actions 380–528.
- **[S8] Timetable**: date `<button>` 268×44 one stop; Tab order Previous day → Pick a date → Next day → Back to today → History → Sync now → lesson block → nav; "Last synced 1 h ago" with no re-auth clause (no saved login); error → Try again 77×44.
- **[S9] Entity**: `r_d48ff4b2f68a` h1 "213" + "See 213" → `r_360`; `r_360` CTA "Share your experience", no note; `r_df12aeba9bb9` **h1 "Place"**, `t_doesnotexist` **h1 "Teacher"** + "What students have experienced in classes with Teacher." (`EntityPage.tsx:53-58` `KIND_TITLE[kind]` fallback); delisted/nonexistent empty copy "No experiences here."; `GET /api/entities` ×1.
- **[S10] Settings**: 4 sections + privacy; School connection verbs live `["Sync now","Reconnect","Disconnect","Save school login…"]`; Imported data {Timetable import (static), Delete imported data}; `/settings#privacy` → `activeElement === h2#privacy`.
- **[S11] ··· menu**: trigger 44×44 `aria-haspopup="menu"`, `aria-controls` = `#id`, `aria-expanded`; menu `[254,454,120,54]`, "Report" 110×44; open → focus Report; Escape → closed, focus `···`; outside → closed, focus BODY; **Tab → menu stays open, focus lands on `a.mobile-nav__item:Home`**, second Tab → "Experiences", still open; ArrowDown no-op.
- **[S17] Still-good**: scope tabs 2, `scrollWidth 239 == clientWidth 239`, 44 px; `.mobile-nav` `[10,770,370,64]` identical on 7 routes; `.view` settle 460 ms `backwards` per route; leave-and-return 0 refetch / 0 skeleton; cold `/timetable` 2 API calls, no duplicates.

## Visual pass

- **[V1] CSS spacing**: `var(--sp-*)` in margin/padding/gap 188 → **217**; literal-px spacing lines 9 → **6** (`foundations.css:94` `.sr-only` −1 px, now 0 consumers; `components.css:576` desktop `.main` 34/22–76/110 uncommented; `features.css:815` nudge 18 px uncommented; `features.css:1200` 90 px nav clearance; `features.css:1246-1247` 12+1 hit-area, commented). No literal-px inline spacing remains outside `DashPage.tsx`.
- **[V2] Rendered spacing (all margins/paddings/gaps > 0, 10 screens)**: @390 `1(1) 4(186) 8(171) 12(298) 13(10) 16(119) 20(72) 24(11) 96(10)` → **857/878 = 97.6 %** (r3 95.0 %); @320 97.6 % (90 replaces 96); @430 97.6 % (+44(10)); desktop `1(21) 6(20) … 34(10) 64(20) 110(10) 216(10) 249(20)` → 881/1002 = **87.9 %**. Off-ladder attribution: 13 ×10 = the commented 12+1 hit-area rule; 96/90 = nav clearance; 1 ×1 = UA padding on the aria-hidden 1×1 date input; desktop 1/6 ×41 = UA `<button>` padding on `.settings-trigger` (`components.css:537-552`, no padding reset) + picker; 34/64/110/216/249 = desktop `.main` literal + `--rail` + `.rail-nav{margin:auto}`. **Author-level off-ladder values on mobile: 0.** No 2/9/10/18/22/26/30 px value at any viewport.
- **[V3] Type**: @390 `{12,13,15,16,20,22,24,26,28}` (= r3); @320 `{12,13,15,16,20,24,26,28}`; @430 one fractional 27.95 (`.nextlesson__subject` `clamp(26px,6.5vw,34px)`, `features.css:106`); desktop `{12,13,15,16,20,28,30,32,34,36}`; login `{13,16,17}`. Nothing < 12. 12 off-ramp `font-size` declarations, identical set to `fd522c3`.
- **[V4] Colour**: **43** unique literals across the four stylesheets, byte-identical to `fd522c3` (0 added, 0 removed); one accent `#33667c`/`#8fc2d4`; warm only on danger/ok.
- **[V5] Contrast (live, alpha-composited; stone / dark boot)**: body 13.26 / 15.27; `--ink-2` 5.34 / 8.07; `--ink-3` (`.home-head__stale`) **4.66 / 5.83** (lowest enabled); `--muted`/`.text-4` 5.34 / 8.07; captions 5.34–5.66 / 7.26–8.07; accent links 5.81 / 9.28; scope tab on/off 6.30 & 5.34 / 9.27 & 8.07; nav on/off 13.26 & 5.34 / 15.27 & 8.07; `.nextlesson__state` **11.52 / 9.80**; error banner text 4.77 / 7.03 (home 5.02 / 6.25); "Try again" 11.80 / 13.48; daynav arrow 5.14 / 6.11; **date button 13.98 / 13.75**; Settings captions incl. disclosure rows 5.66 / 7.26; "no longer listed" 5.34–5.66 / 7.26–8.07; survivor link 5.81 / 9.28; Report menuitem 13.98 / 13.75; login label 11.78 / 14.09, stay-connected span (caveat) **4.87 / 6.92**, skip link 13.26 / 15.27. Disabled (exempt): ghost 4.49 / 7.01, primary 4.61 / 6.64, react-btn 2.92 / 4.57. **No enabled rendered text below 4.5:1 on either surface.** `.primary .action-cta` (r3's 1.08:1 dead rule) deleted.
- **[V6] States**: all seven error surfaces `role=alert .banner--danger` 316–358×130 + "Try again" 77×44, +1 request each (Mine via seeded key); focus after retry: Feed `.feed-stream`, others BODY. Loading: skeletons `role=status aria-label=Loading` (Home 2, Timetable/History/Entity 4, Explore 6, Mine 3 lines; feed 6–7 shimmer rows); compose "Loading…". Empty: Timetable "No lessons today", History "No lessons match.", Explore "Nothing by that name." with 4 sections, Feed "Nothing from your classes yet.", Entity "No experiences here yet…" / delisted "No experiences here.", Mine "Nothing here yet" + one CTA. Success: Keep-private page not exercised (would persist). Focus: `3px solid rgb(51,102,124)` offset 3 on every stop measured (Try again ×6, menu item, date button — clipped). Disabled: Share op 1 / Keep private op .65 on empty body, both enable on typing.
- **[V7] Login**: `.login` `height:100dvh; flex-direction:column; overflow-y:auto; padding:32px 24px`; doorway 617/559/521/521 at 320/390/430/1280; wordmark top +32/143/205/139; centred where it fits; checkbox 18×18 inside the form before submit; caveat verbatim; skip link `[12,-80,128,48]` → `#school-username`; 1 fixed descendant (skip link); 0 animations after settle; `scrollWidth == innerWidth`; landmarks `main` + `form`. **Submit `[24,547,272,48]` at 320×568 → bottom 595 vs 568: 21 px visible at scrollTop 0, reachable (maxScrollTop 113); also reachable at a simulated 320×308 keyboard viewport.**
- **[V8] Compose compact**: 320×568 footnote `[16,306,288,55]` order 1, link `[16,311,280,62]` hit; actions `[16,380,288,148]` order 2; nav `[10,494,300,64]`; 390/430 link 102×44, hit. See [N1].
- **[V9] Modal**: overlay = viewport at 390 and 1280; sheet `[0,459,390,385]` padding `20px 16px 24px`; `.view` transform none; `elementFromPoint` at nav centres → overlay/modal, `insideNav:false`; 2 finished animations while open, 0 after Esc; focus → `button.lesson-block`.
- **[V10] Menu geometry**: menu `[254,453.9,120,54]`, Report `[259,458.9,110,44]` `min-height:44px`; keyboard ring 3 px offset 3.
- **[V11] Touch @390**: 0 sub-44 on Home, Feed, Explore, Compose, Entity, Mine, Settings, History; Timetable 1 (aria-hidden picker 5×4, exempt); **survivor link 52×22** (`EntityPage.tsx:83-86`, not in the hit-area rule); login checkbox 18×18 (label 342×151 effective); `.lesson-block` `min-height:20px` (live block 282×56). @320×568 under-44 set empty on 5 screens.
- **[V12] Idle motion**: 0 at 2.5 s on 10 screens, both motion preferences; modal open 2 finished; `.view settle .46s backwards` per route, 1 µs under reduce; `infinite` exactly 2 (`ptr-rot` `components.css:627`, `skeleton-shimmer` `:939`); `@keyframes` total 6 (`pulse-soft` deleted).
- **[V13] Sweep 320/390/430/1280 × 10 screens**: `scrollWidth == innerWidth` everywhere; 0 per-element overflow; 0 wrapped button labels; only ellipsis `.post__provenance`; `.card--hero > .muted × .caption` overlap 83×9 on Home at all viewports (r3 83×8, pre-existing); feed `.page-head` baseline misalignment gone.
- **[V14]** = [N1]. **[V15]** = [N11]. **[V16]** = [N3]. **[V17]** = [N13]. **[V18]** `.timeline__empty` `position:absolute; inset:38px 0 0` centres "No lessons today" over the whole timeline; on a free-period day the icon + label render **on top of the "P3 Free"/"P4 Free" band rows** (`features.css:445-458`; `r4-vis-timetable-390.png`) — unchanged by `5d810d8`, unreported in r1–r3 = [N12]. **[V19]** Explore mark 0/19 live.

## Copy & Honesty pass

- **[C1] Strings changed since `fd522c3`** (29 items, all verified live unless noted): login caveat restored (`LoginPage.tsx:52-57`); "Skip to sign-in" (`:26-28`); Settings re-auth clause `" · Sync signs in again with your saved school login if the portal session expired."` (`SettingsPage.tsx:117-118`, only when `stayConnected`); Settings caveat (`:169`); Off-state "Saving it asks for your login once." (`:170`); "Turn on" → "Save school login…" (`:186`); Timetable import row (`:197-201`); "device-held key" → "ownership key" (`:234`); Timetable "Sync" → "Sync now" (`TimetablePage.tsx:118`); date button label "Pick a date (Tue 1 Sept)" (`:75`); Timetable re-auth clause (`:160-162`, when `portalCredentials.isAuthorized()`); Try again ×5; Mine header Share hidden when empty (`MinePage.tsx:133-137`); "scrambled at rest (unreadable without this device's key)" (`:154-155`); "private control key" → "one-time ownership key" (`ComposePage.tsx:143`); raw-key fallback removed (`:86`); "Course" (`:88-89`); unlisted copy + CTA (`:191-201`); "See {survivor.name}" (`EntityPage.tsx:88-90`); delisted empty "No experiences here." (`:112-114`); mark rule + separator (`ExplorePage.tsx:113,164-166`); "registry" → "isn't listed any more" (`shared.tsx:69`); reaction tooltip "students who have had the same class or place" (`ExperiencePost.tsx:24-25`); header Share hidden on error (`FeedPage.tsx:33`); backend room placeholder → NULL (`BE/services/timetable.ts:31,139`).
- **[C3] Inflations**: `WhyPage.tsx:73` "can be **strongly** protected" (degree word, no metric, live); `SettingsPage.tsx:224` "The detail, **honestly**:" (self-certifying framing, live; counted as framing, not inflation, as in r3); `WhyPage.tsx:63` "where possible" (hedge). Marketing superlatives across 16 routes: **0**.
- **[C4] Dark patterns (rubric list)**: forced continuity 0 (stay-connected `checked:false` default at 320/390); hidden cost 0 (Remove/Disconnect/Delete dialogs state cost); fake scarcity 0; confirmshaming 0.
- **[C5] Import disclosure**: (A) Settings row verbatim "Timetable import — On for every HOney account since 1 September 2026 — your timetable and history come from the school portal when you sign in. Delete them below at any time." (`SettingsPage.tsx:195-203`), static, in "Imported data" above "Delete imported data"; (B) not visible without scrolling at 390 (row top 1384 of 3046) or 320 (1504 of 3580); (C) date claim consistent with `BE/db/database.ts:244-253` ("Gary, 2026-09-01"), `BE/services/accounts.ts:66-69`, `BE/services/importer.ts:42-43`, `BE/routes/auth.ts:27`; live `/api/me` `consent.grantedAt 2026-09-01T16:51Z`; (D) login discloses import in one footnote below the submit and outside the form: "…created on first sign-in. Your timetable comes along with it." (`LoginPage.tsx:62-65`), reachable at 320 by scroll; (E) 0 user-facing hits for "turn it on"/"Import my timetable"/"consent"; residue: tests, dead `.login__consent`, `contract.ts:96 "no_consent"` wire type, and **`contract.ts:12-16` stating "no login payload can grant import consent — POST /api/consent is the single consent mutation path" — both halves now false**; (F) `/api/consent` still registered (`BE/routes/auth.ts:135-146`) and still writes `revoked_at` on `{timetable:false}` while `importer.ts:42-43` ignores it — surfaced nowhere; (G) migration 009 unchanged; no on-screen notice for previously-declined accounts (grep of pages/components/routes/services); the Settings row states the current state for every account.
- **[C6] Label→behaviour**: (A) "Sync now" ×2 → one API; disclosure adjacent on Settings (16 px above), 678 px below and conditional on Timetable; (B) "Save school login…"/"Reconnect" → identical `ReconnectDialog` ("Reconnect school account" / "The portal session ended. Sign in again to restore it — your HOney data stays as it is." / "…Your login is encrypted and kept only here." / submit "Reconnect"), md5-identical PNGs at 390 and 320; opened from a Connected + `portalTokenValid:true` account, so the premise was false; the Off-state caption "Saving it asks for your login once." is met but under the wrong title; (C) Try again 6/7 refetch (+1 request), Mine unreachable with 0 keys; (D) unlisted composer: 1 link, no textarea, no Share; (E) course caption "Course"; (F) `t_doesnotexist` → h1 "Teacher", "This entry is no longer listed." — a URL that never named anything is described as delisted; (G) header Share hidden on error; (H) Home "Open timetable" → today while the chip says "Tomorrow · 01:00" (harness TZ UTC; label→target relation TZ-independent); (I) Settings row "come from the school portal **when you sign in**" and Delete dialog "You can import again later." vs `BE/routes/auth.ts:29-33` import **only when `result.created`** — later sign-ins never import; Sync now / pull-to-refresh are the manual paths and neither copy names them; (J) login "Turn it off any time in Settings." true (`SettingsPage.tsx:174-183` "Turn off" renders with a saved login).
- **[C7] Jargon**: "ownership key" single name ×7; "private control key" 0; "device-held key" 0 UI; "verified exposure" survives once in the `not_eligible` runtime note (`ExperiencePost.tsx:152`); "registry" 0 UI; "entities" in `aria-label="Filter entities by name"` (`ExplorePage.tsx:73`) vs placeholder "Filter by name…"; "scrambled at rest" glossed at all 3 sites; "Not selected" 0; "P1…P6" ×6 per day unexplained (`TimetablePage.tsx:213-222`); connection verbs on one card 4; "from your classes" now 2 live referents (Home section, feed scope) with the Explore mark absent; Explore subline "…typing only narrows the list." still true.
- **[C8] Honesty-claims drift**: `git log fd522c3..5d810d8` on `client.ts`, `BE/routes/experiences.ts`, `WhyPage.tsx`, `contract.ts`, `database.ts` → only `setConsent` removed. Mapping re-confirmed: publish `auth:false` (`client.ts:229`) + `credentials:"omit"` (`:352-353`); body `{eligibilityToken, pass, body, rating?}` (`contract.ts:311-316`); `ownership_hash` only, no author column (`database.ts:103-121`); 6 = 6 report categories (`ExperiencePost.tsx:261-268` = `contract.ts:367-372` = `routes/experiences.ts:11-16`); "no free text is collected" ↔ `client.ts:248-250` `{category}`; HOney ID line `SettingsPage.tsx:72-75`; WhyPage `:75` "design boundary, not magic".
- **[C9] Stale comments**: fixed in `SchoolLoginForm.tsx:7-8`, `BE/routes/auth.ts:26-27`, `TimetablePage.tsx:41-42,79-81`; `lib/portalCredentials.ts:16` now true for 2 of 3 opt-in sites; `contract.ts:12-16` stale and false; `features.css:61-65 .login__consent` dead.
- **[C10] New copy defects**: (1) ReconnectDialog body false from the Save path; (2) Timetable disclosure displaced/conditional; (3) "when you sign in" inaccurate; (4) ReconnectDialog caveat missing; (5) "no longer listed" for never-existed keys on entity and composer; (6) `contract.ts:12-16`; (7) Explore mark absent for this account so Home/Feed vocabulary and the separator ship unobserved.

## Weight & Friction pass

- **[W1] Bundle**: `index-BQhfgddr.js` **87,022 B wire (CDP) / 84,469 br (curl) / 268,076 raw** (r3 86,394 / 264,874; +1.2 % raw); CSS 10,997 / 44,717; font latin 29,244; Dash `DashPage-CGVLT9aO.js` 4,403 br, fetched only on `/dash` (329 ms), never on 6 student routes cold or SPA; cold `/home` total 154,286 B incl. Cloudflare beacon 9,722; 123,968 B of six non-latin font subsets ship in `dist/assets` and are never requested; `cache-control: public, max-age=14400` on hashed assets (no `immutable`), `/` `max-age=0`, `sw.js` `max-age=14400`; no preload/preconnect.
- **[W2] Requests**: cold `/home` **12** (3 app-origin data); warm SPA → `/experiences` **3** (1); cold `/experiences` 11; cold `/timetable` 12; cold `/settings` 10; cold explore/mine 12; cold entity 13 with `/api/entities` ×1; SPA explore → entity re-issues `/api/directory` + `/api/entities` per the SWR contract (`useApi.ts:6-9,47-56`, pre-existing). No duplicate in-flight API calls anywhere.
- **[W3] Timing (quiet pass, medians of 5; init-script polling every 16 ms, TTI = max(bundle responseEnd, first nav item with React props, first data element))**: cold `/home` TTFB 52 / FCP 216 / **TTI 324**; cold `/experiences` 47 / 208 / 366; warm SW reload FCP 98 / TTI 125; `/login` fresh light 183–338, dark 161–321; **0 long tasks in 23 loads**; route transitions 72–209 ms. Not a safe delta vs r3 (load conditions differ; loaded r4 pass gave 407).
- **[W4] Idle**: 0 at settle and 2.5 s on Home, Timetable, Feed, Explore, Compose, Entity, Mine, Settings, Why, History, Login (cold and SPA); `/history?select=1` 1 finished `rise-in`; modal open 2 finished; menu open/closed, date picker opened 0. `infinite` 2 consumers, running only during a pull. Reduced motion → 0 on 11 routes + login.
- **[W5] Interrupts on load**: **0** visible dialogs/banners/toasts/badges on cold `/home`, `/experiences`, `/settings`, `/timetable`, `/experiences/explore`, `/experiences/mine`, `/login`; `.ptr__disc[role=status]` at opacity 0; Settings disclosure is a plain `.setting-row` (transparent, no border, no role, no dismiss); Timetable banners on load `[]`.
- **[W6] Service worker**: see verification table; (a) old names evicted; (b) seeded old hashes evicted at activate, stale URL then resolves to the origin shell not a cached JS body; (c) missing hash → `200 text/html` 1,850 B not cached; (d) `/api/` 0 entries; (e) navigations network-first, poisoned entry overwritten online, served offline (1,569 ms); (f) TSX-only deploy with byte-identical `sw.js` never runs eviction; live-asset set regex-matched from `index.html` only → current font + Dash chunk deleted on every activate (`r4-wf-sw4.log`); one extra `fetch("/", {cache:"no-store"})` per activate; navigation shells accumulate (~1.85 KB per distinct hard-loaded URL).
- **[W7] Compose path**: feed → Share → chooser → History (24 Select) → editor = **4 screens / 3 taps**, `activeElement` BODY on landing; Explore → row → entity → Share = 3 screens / 2 taps, BODY. Unchanged.
- **[W8] PTR**: feed spin 567 ms, timetable 601 ms, exactly 1 API call per pull, sub-threshold 0, no native reload.
- **[W9] Boot theme** (corrected probe): `data-surface` at 53–68 ms, before FCP (244–300) in six contexts incl. stored-choice-wins both ways and garbage fallback; 0 later `<html>` mutations in 2.6 s; head order boot script → module → stylesheet; static `theme-color #edf0f1` (`index.html:6`) is outside the boot palette and only visible if the boot script throws.

## Accessibility pass

- **[A1] Contrast**: table in [V5]; per-surface lowest enabled (stone/night): home 4.66/5.83, feed 5.34/8.07, explore 5.34/8.07, entity 5.34/8.07, compose 5.34/8.07, compose-unlisted 5.66/7.26, settings 5.66/7.26, timetable 5.14/6.11, error surfaces 4.77/6.25, login 4.87/6.92. Active nav label true value 13.26/15.27 (sibling `.mobile-nav__pill` background).
- **[A2] Focus order** (all stops `3px solid rgb(51,102,124)` + `:focus-visible`, no traps): Home skip → Open timetable → post preview → Share something → School Portal → nav; Feed skip → Share → Find… → Your notes → Why → tab/tab → context link → ± → ··· → nav; Explore skip → Back → search → 19 rows → nav; Compose editor skip → textarea → Cancel → How privacy works → nav; Timetable skip → Previous day → **date button (one stop)** → Next day → Back to today → History → Sync now → lesson block → nav; Settings skip → 4 themes → Sign out → Open Dash → Delete… → Sync now → Reconnect → Disconnect → Save school login… → Delete… → Import… → nav (disclosure row not a stop); Login skip → username → password → checkbox → submit.
- **[A3] Keyboard reachability**: YES for every primary action (lesson modal Enter/Tab/Esc → opener; scope tabs + arrows; ··· open/first-item/Escape; react ±; context link; Share → chooser → Explore → row → entity → composer; Keep private/Share after typing; Try again ×7; Explore search keeps the set; Settings 13 controls; skip link; privacy link → `h2#privacy`). NO: ··· arrow keys; ··· Tab-out dismissal. PARTIAL: date button (`showPicker` no error, picker not observable headless; fallback `el.click()` on an aria-hidden, `pointer-events:none`, 5×4 input — no reachable path if `showPicker` refuses). Skip link: `#main` has no tabindex → `activeElement` BODY on every page except Settings (`SettingsPage.tsx:19-27` focuses any hash → `main.main`) and Login (input).
- **[A4] Landmarks**: `role=banner` 0 on every page (every `<header>` is inside `<main>`); `contentinfo` 0 everywhere; nav 2 + main 1 on AppLayout pages; regions Home 2, Explore 4, Settings 5, Compose 1 (0 in the unlisted state); Feed tablist with **no tabpanel / no aria-controls**; /login `main` + `form`; `document.title` "HOney" on all 12 routes.
- **[A5] Skip link**: YES on 12/12 (r3 11/12); /login link rendered inside `<main id="main">`.
- **[A6] Touch @390**: 119 visible interactive elements measured across 10 screens; 0 genuine under-44 controls on 9; exceptions: aria-hidden date input 5×4, compose `<label>` 358×19.5 (non-interactive), **survivor link 52×22**, login checkbox 18×18; Settings disclosure row `DIV`, no role, no tabindex, 0 focusables, 115 px — correctly static.
- **[A7] Modal**: nav not hit-testable at 390/1280 for lesson and report dialogs; `aria-modal=true`, trap, Esc → opener; background carries no `inert`/`aria-hidden` (unchanged).
- **[A8] Semantics**: 7/7 error banners `role=alert` (feed's nested inside `aria-live=polite` `.feed-stream`); skeleton `role=status`; `.ptr__disc[role=status]` with no accessible name on every feed/timetable route; "no longer listed" (entity and composer) and composer "Loading…" are plain text, no live region; delisted empty copy differentiated.
- **[A9] Explore mark / lesson name**: 0/19 marked; separator source + shipped CSS only; lesson-block name "Activity P1 · 08:30–10:00 · 活动课老师" (separated, no placeholder); no room-bearing block reachable on any probed day (26 Aug–16 Sept); History captions "朱昂明 · 309".
- **[A10] Reduced motion**: 0 on 9 routes under both preferences.
- **[A11] New AT/keyboard defects**: (1) menu survives Tab-out with stale `aria-expanded="true"` (`ExperiencePost.tsx:107-127` handles only Escape and pointerdown); (2) outside-pointer close drops focus to BODY (`:118-120` lacks the `moreBtnRef.focus()` the Escape branch has); (3) feed retry focus target invisible (`outline:none`, `:focus-visible` false) and simultaneously `aria-live=polite`; (4) entity retry → BODY on an empty result; (5) survivor link 52×22; (6) Cancel under the nav at 320×568 and 320×600, clean at 390×620; (7) date input no reachable fallback; (8) skip-link behaviour page-dependent; (9) /login 0 nav/banner/contentinfo, `h1` text empty (`<img alt>`); (10) composer unlisted state landmarks reduce to `main` alone; (11) Explore renders `section[aria-label="Food"]` with 0 rows.

## New-defect register (first seen r4)

- **[N1] Compose "Cancel" under the fixed nav at 320×568**: `[16,484,288,44]` vs nav `[10,494,300,64]`, `elementFromPoint` = `nav.mobile-nav` on landing; 50 px of `main` scroll clears it; caused by the `@media (max-height:620px)` reorder (`features.css:1287-1296`) [S12][V14][A11-6].
- **[N2] Daynav stepper dividers lost**: `.daynav__date` declared twice — `features.css:207-220` sets `border-left/right:1px solid var(--line)`, `:221-225` sets `border:0` and wins; live border widths 0; A/B `crop-r3-vis-timetable-390.png` vs `crop-r4-vis-timetable-390.png` [S13][V15].
- **[N3] Date button focus ring clipped**: `.daynav__stepper{overflow:hidden}` (`features.css:190`) clips the 3 px offset-3 outline top and bottom; only the vertical segments render (`r4-vis-daynav-focus-clip-390.png`); `showPicker` fallback targets the hidden input [V16][A3][A11-7].
- **[N4] ··· menu incomplete**: no Tab-out/focusout dismissal (stale `aria-expanded`), outside-click drops focus to BODY, no arrow keys [S11][A11-1,2].
- **[N5] Retry focus**: BODY on Home/Timetable/History/Explore/Mine and on Entity when the refetch is empty; feed lands on a ringless container [A3][C6-C][A11-3,4].
- **[N6] ReconnectDialog untouched by the wave**: one dialog for "Reconnect" and "Save school login…", body "The portal session ended." shown from a connected account; checkbox outside the form (unused `beforeSubmit` slot); no browser-storage caveat while `portalCredentials.ts:16` says it is shown [C6-B][S16][C10-1,4].
- **[N7] Timetable re-auth disclosure displaced**: 678–722 px below the sticky "Sync now", gated on `lastSyncedAt`, absent in the error branch [C6-A][S2-3b].
- **[N8] Import row inaccurate**: "come from the school portal when you sign in" / "You can import again later." vs import on account creation only (`BE/routes/auth.ts:29-33`); manual paths (Sync now, pull) unnamed [C6-I].
- **[N9] Not-found rendered as delisted**: never-existed ids get "This entry is no longer listed" on the entity page and the composer; the kind label becomes the entity name ("Teacher"/"Place") in h1 and intro (`EntityPage.tsx:53-58`) [S14][C6-F][C10-5].
- **[N10] Survivor link**: "See 213" under h1 "213" (dedupe-by-name guarantees the collision); 52×22, outside the hit-area rule [S15][A6].
- **[N11] CSS duplicates/orphans re-introduced**: `.daynav__date` ×2, `.post__menu button` ×2 (`features.css:1017-1029` + `:1281-1283`), `.visually-hidden` (`:1297-1304`) beside 0-consumer `.sr-only` (`foundations.css:89-99`), dangling comment `features.css:1275`, `.login__consent` (`:61-65`), `.action-cta` base (`foundations.css:286-299`); whole sweep 27 orphan class names; `.settings-trigger` no padding reset (41 desktop UA values); desktop `.main` literal and nudge 18 px uncommented [S4][V1][V15].
- **[N12] `.timeline__empty` overlay collides with band labels** ("No lessons today" over "P3 Free"/"P4 Free"; `features.css:445-458`; pre-existing, first measured) [V18].
- **[N13] Login at 320×568**: submit 27 px below the fold (reachable by scroll); checkbox input 18×18 [V17][A6].
- **[N14] SW**: eviction only on `activate` (needs an `sw.js` byte change); over-evicts the current font subset and Dash chunk on every activate; navigation-shell cache unbounded; `sw.js` `max-age=14400` [W6].
- **[N15] Explore mark absent for this account** (0/19); separator and `markable` rule ship unobserved; Home/Feed "from your classes" vocabulary unchanged; mark non-wrap guard deleted [S6][A9][C7].
- **[N16] `contract.ts:12-16`** documents the deleted consent model as current; `/api/consent` still records an ignored `{timetable:false}` [C5-E,F].
- **[N17] Standing AT residue** (unchanged, no r3 move targeted it): `document.title` "HOney" on every route; 0 banner / 0 contentinfo; feed tablist without tabpanel; `.ptr__disc[role=status]` unnamed; modal background not `inert`; skip link inconsistent on Settings [A4][A5][A8][A11-8..11].
- **[N18] Composer transient**: during the registry fetch the editor and enabled Share render under the "Loading…" label before `unlisted` can resolve (`ComposePage.tsx:87,190-192`) [S7].

## Gaps (all passes)
Mine error branch unreachable without a seeded key (0 ownership keys); no opted-out account exists to observe migration 009's effect; Sync now / Reconnect / Save school login… / Disconnect / Delete / Turn off / Sign out / publish / report send / Keep private not exercised; saved-login disclosure rendered with a sentinel only; Explore mark unobservable on real data; no room-bearing timetable block in the probed window; `.nextlesson__wash` and the now-line never rendered (harness TZ UTC); `showPicker` unverifiable headless; screen-reader runtime untestable; headless LCP null; droplet-local timings; SW TSX-only redeploy simulated with seeded entries, not a real deploy; 430/desktop censuses taken by the Visual pass only.
