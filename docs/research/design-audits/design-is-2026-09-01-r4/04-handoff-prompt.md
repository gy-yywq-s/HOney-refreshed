# 04 — /make-plan handoff (r4)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 5d810d8). Current design failed audit at 18/30 with critical gaps in principles #3 aesthetic, #4 understandable, #8 thorough (all scored 1/3; no principle scored 0).

Verdict paragraph (quoted from 03-verdict.md):
> The r3 handoff verified as built almost everywhere it could be measured — the login fits 320×568 with its checkbox inside the form, the caveat is back, every error surface has a working "Try again", the composer refuses unlisted keys, the date is one named button, the ··· menu has menu semantics, every measured tap target is 44 px, "Not selected" and the raw keys are gone, the rendered spacing is 97.6 % on the ladder with zero author-level residue, and the import decision is disclosed on screen — so HOney moves for the first time, from 16 to 18/30, with #6 and #10 each gaining a point; but the score still says REDESIGN because the same wave shipped four new rendered defects on the primary screens (compose "Cancel" under the nav at 320×568, the day stepper's dividers lost to a duplicate rule, a clipped focus ring on the new date button, and a never-listed id rendered as an entity named "Teacher"), left `ReconnectDialog` untouched so "Save school login…" opens a dialog that says the session ended, and closed the r3 blockers on #3, #4 and #8 only to land on the next count boundary of each anchor — so the remaining work is again a short, enumerable finishing pass, and the audit reports the number it measured.

Why redesign and not refine: total 18/30 is below the REFINE threshold of 20; only one principle (#4) sat on a tie and granting it upward reaches 19, still below the threshold — so the verdict holds under any reading of the tie-break, and this prompt is the REDESIGN template even though every move is a finish-the-detail or merge-the-duplicate move on a layer whose tokens, type, colour, spacing, motion budget and trust architecture all verify.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decision, not up for re-litigation: timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), and the plan may only make that disclosure more accurate. Verify everything in a real browser against the deployed app, not from screenshots alone. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop})`; probe scripts `r4-*.js` with `.log` files) can be re-run to confirm each fix. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying.

Preserve from current design (MUST NOT be touched — these verified live on 5d810d8):
- Spacing system as it renders: 217 `var(--sp-*)` margin/padding/gap usages against 6 literal lines (apps/web/src/styles); rendered rhythm at 320/390/430 = 857 of 878 values on the 4/8/12/16/20/24/32/44 ladder (97.6 %), the only mobile off-ladder values being the commented 12+1 hit-area exception (features.css:1240-1248), the 96/90 px nav clearance (components.css:711-712, features.css:1200) and 1 px of UA padding on an aria-hidden input. `.main` top = `var(--sp-4)`; modal sheet `var(--sp-5) var(--sp-4) calc(var(--sp-6) + safe-area)` (components.css:909); `p.caption, p.text-4 { margin-block: 0 }` (foundations.css:174-178). Regression check: re-run the rendered spacing census at 390/320/430; on-ladder share must not drop below 97 % and no 2/9/10/14/18/22/26/30 px value may appear.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, no fractional at 390/320. Regression check: rendered-font-size census, nothing below 12 px.
- Palette and accent discipline: 43 colour literals, byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; `.nextlesson__state` on `var(--ink)` measuring 11.52:1 stone / 9.80:1 on a genuine dark boot; the new date button 13.98/13.75, Report menuitem 13.98/13.75, skip link 13.26/15.27. Regression check: rerun the live contrast probe on stone and on a fresh `colorScheme:dark` context with no stored theme; no enabled text below 4.5:1 on either (current floor 4.66 stone / 5.83 night).
- Zero idle motion: `document.getAnimations().length === 0` at 2.5 s after settle on every screen under both motion preferences; `infinite` keyframes only on the pull spinner (components.css:627) and loading skeleton (:939); 6 `@keyframes` total; prefers-reduced-motion collapses durations (foundations.css:362-375). Regression check: getAnimations() on all 11 screens with and without reduced-motion.
- Weight: app JS 87,022 B wire / 268,076 raw (index-*.js), CSS 10,997 wire, Dash chunk 4,403 br loaded only on /dash; 12 requests cold /home (3 app-origin data), 3 warm to /experiences, `GET /api/entities` exactly once per entity page (EntityPage.tsx:34 `useNames()` only); 0 long tasks; no new dependency. Regression check: curl br sizes; DashPage never fetched on student routes; request counts.
- Boot script honouring prefers-color-scheme pre-paint (index.html) with stored-choice-wins in both directions and 0 later `<html>` mutations; SW `honey-v3` content-type guard (public/sw.js:64-65) that refuses to cache the origin's 200-HTML shell under /assets/; `/api/` never cached; navigations network-first.
- Route-settle containment: `.view { animation: settle .46s … backwards }`; lesson modal overlay equals the viewport at 390 and 1280, nav not hit-testable beneath it, focus trap + Esc + opener restore on both dialogs.
- Focus system: 3 px accent outline offset 3 on 100 % of Tab stops measured across 12 routes; Timetable Tab order Previous day → date button (one stop, 268×44, `aria-label="Pick a date (…)"`, input `tabIndex -1 aria-hidden`) → Next day → Back to today → History → Sync now; scope tablist with arrow keys, exactly two tabs fully visible; `/settings#privacy` focuses `h2#privacy` (SettingsPage.tsx:19-27,218); report dialog and lesson modal return focus to their openers.
- Login: `.login { height:100dvh; flex-direction:column; overflow-y:auto }` + `.login__doorway { margin:auto }` (features.css:8-24) — wordmark top +32 at 320×568, centred at 390/430/1280; the "Stay connected" checkbox inside the `<form>` before the submit via `SchoolLoginForm`'s `beforeSubmit` slot (SchoolLoginForm.tsx:64, LoginPage.tsx:45-57) with the caveat "(a browser is less protected than a phone's secure storage)"; skip link "Skip to sign-in" → `#school-username` (LoginPage.tsx:25-28); footnote "…created on first sign-in. Your timetable comes along with it." (LoginPage.tsx:62-65).
- Settings: the static "Timetable import" disclosure row (SettingsPage.tsx:195-203) in "Imported data" above "Delete imported data" — keep it static, keep the date claim, correct only the "when you sign in" clause (see Discard); "Sync now" (:147) → `api.syncSeamless()` with its re-auth clause 16 px above the button (:117-118); one key name "ownership key" at all seven sites (SettingsPage.tsx:234,235,356,366; MinePage.tsx:142,153; ComposePage.tsx:143); "scrambled at rest (unreadable without this device's key)" glossed at all three sites; "Turn off" rendering with a saved login (:174-183).
- Timetable: "Sync now" (TimetablePage.tsx:118) → `api.syncSeamless()`; the date button + hidden input pattern (:72-95, features.css:218-233); backend placeholder rooms serialized as no room (packages/backend/src/services/timetable.ts:31,139) — no "Not selected" on the block, modal, History or compose detail; lesson-block accessible name "Activity P1 · 08:30–10:00 · 活动课老师".
- Error states: all seven surfaces render `role=alert .banner--danger` with the same `btn--ghost btn--small` "Try again" 77×44 that fires exactly one refetch (FeedPage.tsx:113, EntityPage.tsx:110, HomePage.tsx:79, HistoryPage.tsx:111, MinePage.tsx:169, ExplorePage.tsx:82, TimetablePage.tsx:148); feed header Share `feed.loading || (feed.error === null && feed.items.length > 0)` (FeedPage.tsx:33) — hidden on error and empty.
- Compose: unlisted keys render "This entry is no longer listed, so nothing can be shared about it." + "Find someone or something" with no editor (ComposePage.tsx:188-207) — keep the no-editor behaviour, refine only the never-listed wording (see Discard); course targets captioned "Course" (:88-89); "Loading…" as the lookup label; text-only notice `.compose-notice-alt { margin-top: var(--sp-2) }`; footer link "How privacy works" 102×44 → /settings#privacy; the `@media (max-height:620px)` footnote-above-actions order (features.css:1287-1296) — keep the order, fix the clearance (see Discard).
- Explore: complete-set rule (every section rendered with counts, search only filters, 19 rows, `Food 0` rendered); the `markable` rule (ExplorePage.tsx:114-115) and the `<span class="visually-hidden">, </span>` separator (:164-168) — keep both; "Find someone or something" at all four sites.
- Entity: delisted ids render no Share CTA, "This entry is no longer listed." and, for a same-name duplicate, a link to the survivor (EntityPage.tsx:78-92) — keep the link, relabel it (see Discard); delisted empty copy "No experiences here."
- Mine: header Share hidden when empty (MinePage.tsx:133-137) — one CTA "Share your first experience".
- Post ··· menu: `aria-haspopup="menu"`, `aria-controls`, `aria-expanded`, `role=menu`/`menuitem`, focus on "Report" on open, Escape closes and returns focus, outside pointer closes (ExperiencePost.tsx:104-127,220-232); Report item `min-height:44px`.
- Clean build: `tsc --noEmit --noUnusedLocals` = 0 errors; `setConsent` gone; stale consent comments gone from SchoolLoginForm.tsx and packages/backend/src/routes/auth.ts.
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:229,352-353; packages/shared/src/api/contract.ts:311-316), no author column (packages/backend/src/db/database.ts:103-121), 6 = 6 report categories, "no free text is collected" ↔ `{category}` only, the HOney ID line (SettingsPage.tsx:72-75), WhyPage "design boundary, not magic", the reaction tooltip "students who have had the same class or place" (ExperiencePost.tsx:24-25).

Discard (structural patterns causing the failures):
- Compact-height reorder that pushes the action column into the nav: `@media (max-height:620px) { .compose-editor > .text-4 { order:1 } .compose-actions { order:2 } }` (features.css:1287-1296) puts "Cancel" at y484–528 under the fixed nav at y494–558 at 320×568 — `elementFromPoint` at its centre resolves to `nav.mobile-nav` on landing. Caused failure on principles #8 and #2.
- Duplicate selector blocks that silently cancel each other: `.daynav__date` declared at features.css:207-220 (with `border-left/right: 1px solid var(--line)`) and again at :221-225 (`border: 0`) — the stepper's dividers are gone; `.post__menu button` at :1017-1029 and :1281-1283; `.visually-hidden` (:1297-1304) beside a now consumer-less `.sr-only` (foundations.css:89-99); dangling comment features.css:1275. Caused failure on principle #3.
- Focus ring inside an `overflow:hidden` pill: `.daynav__stepper { overflow:hidden }` (features.css:190) clips the date button's 3 px offset-3 outline top and bottom; the picker fallback `el.click()` (TimetablePage.tsx:81-82) targets an `aria-hidden`, `pointer-events:none`, 5×4 px input. Caused failure on principles #8 and #3.
- Retry without a landing: Home/Timetable/History/Explore/Mine "Try again" call bare `reload()` (HomePage.tsx:78, TimetablePage.tsx:147, HistoryPage.tsx:110, ExplorePage.tsx:81, MinePage.tsx:168) → focus BODY; Entity focuses `streamRef` which is not rendered when the result is empty (EntityPage.tsx:108,113-118); Feed lands on `.feed-stream` with `outline:none` (FeedPage.tsx:103,111). Caused failure on principle #8.
- Menu that leaks focus: `ExperiencePost.tsx:104-127` handles only `keydown Escape` and `pointerdown` — Tab from "Report" leaves the menu open with `aria-expanded="true"`, outside click drops focus to BODY, no arrow keys. Caused failure on principle #8.
- One dialog for two purposes with untouched copy: `SettingsPage.tsx:150` "Reconnect" and `:186` "Save school login…" both `setShowReconnect(true)` → `ReconnectDialog.tsx:19-33` titled "Reconnect school account", body "The portal session ended. Sign in again to restore it…", shown from a Connected/`portalTokenValid` account; its checkbox sits outside `SchoolLoginForm` (the `beforeSubmit` slot unused) and its copy "kept only here" lacks the caveat that lib/portalCredentials.ts:16 says every opt-in site shows. Caused failure on principles #4, #6, #10 and #8.
- Disclosure far from its control: the Timetable re-auth clause lives inside the "Last synced" caption (TimetablePage.tsx:158-163) 678–722 px below the sticky "Sync now", gated on `data?.lastSyncedAt`, absent in the error branch. Caused failure on principle #6.
- Import copy that overstates the trigger: Settings row "…come from the school portal when you sign in" (SettingsPage.tsx:199-200) and Delete dialog "You can import again later." (:285) while packages/backend/src/routes/auth.ts:29-33 imports only when the account is created and the manual paths are Sync now / pull-to-refresh. Caused failure on principle #6.
- Kind label as entity name and "no longer" for never: `EntityPage.tsx:53-58` falls back to `KIND_TITLE[kind]` for the h1 and intro ("What students have experienced in classes with Teacher."); both the entity page (:79) and the composer (ComposePage.tsx:188-201) say "no longer listed" for ids that never existed. Caused failure on principles #4 and #6.
- Survivor link that names the heading: "See 213" (EntityPage.tsx:86) under h1 "213", 52×22, outside the hit-area rule. Caused failure on principles #4 and #8.
- Composer editor mounted before the registry resolves: `ComposePage.tsx:87,190-192` renders the full editor with enabled Share under the "Loading…" label until `entities.data` arrives. Caused failure on principle #8.
- Login residue at 320×568: the 210 px `.stay-connected` label pushes the submit to y547–595 (27 px below the fold, reachable by scroll); the checkbox input is 18×18 (components.css:969-975). Caused failure on principle #8.
- Empty-day overlay over content: `.timeline__empty { position:absolute; inset:38px 0 0 }` (features.css:445-458) draws "No lessons today" on top of the "P3 Free"/"P4 Free" band rows; hero caption overlap `.card--hero > .muted × .caption` 83×9 on Home. Caused failure on principle #3.
- Orphan stylesheet mass: 27 class names with no consumer (`action-card, action-cta, action-grid, browse-grid, btn--secondary, card-copy, card-index, card-title, chip--danger, chip--muted, chip--ok, exp-card__spacer, hairline-list, home-exp-list, home-exp-row, home-exp-row__body, lift, login__consent, row-link, section-head__copy, segmented, sort-toggle, sort-toggle__btn, sort-toggle__btn--on, sr-only, stat-strip, text-2`); `.settings-trigger` with no padding reset (components.css:537-552, 41 desktop UA values); uncommented desktop `.main` literal (components.css:576) and nudge sheet 18 px (features.css:815). Caused failure on principle #3.
- Jargon and unnamed routes: "P1…P6" ×6 per day unexplained (TimetablePage.tsx:213-222); "verified exposure" in the `not_eligible` note (ExperiencePost.tsx:152); `aria-label="Filter entities by name"` (ExplorePage.tsx:73); `document.title` "HOney" on all 12 routes; feed tablist with no tabpanel; `.ptr__disc[role=status]` with no accessible name. Caused failure on principle #4.
- Stale contract comment: packages/shared/src/api/contract.ts:12-16 states "no login payload can grant import consent — POST /api/consent is the single consent mutation path", both halves false since fd522c3; `/api/consent` (routes/auth.ts:135-146) still records an ignored `{timetable:false}`. Caused a code-hygiene defect relayed under principle #6.
- Service-worker eviction scope: `sw.js:28` builds the live-asset set from `index.html` alone, so every activate deletes the current font subset (28,740 B) and `DashPage-*.js` (13,413 B); `evictStaleAssets` runs only from `activate` (:39-47), so a TSX-only deploy with a byte-identical `sw.js` never evicts; navigation shells accumulate unbounded; `sw.js` is served `max-age=14400`. Caused a resource defect relayed under principle #9 (anchor still met).
- Same-screen duplicate at scale (code only, 1 post live): header Share + interleaved invite co-render at ≥9 posts (FeedPage.tsx:140-148). Caused a latent failure on principle #10.

Top 4 moves from the audit (verbatim):
1. #8 thorough + #2 useful — Finish the interaction states the wave opened. After every "Try again" move focus to the content that replaces the banner (Home/Timetable/History/Explore/Mine call bare `reload()` — `HomePage.tsx:78`, `TimetablePage.tsx:147`, `HistoryPage.tsx:110`, `ExplorePage.tsx:81`, `MinePage.tsx:168`; Entity's `streamRef` is null on an empty result, `EntityPage.tsx:108,113-118`) and give the feed's landing container a visible ring or focus the first post instead (`FeedPage.tsx:103,111`, `.feed-stream` has `outline:none`); unclip the date button's ring (`.daynav__stepper{overflow:hidden}` `features.css:190` vs the 3 px offset-3 outline — use an inset outline or `clip-path` on the pill) and give the picker a reachable fallback when `showPicker` refuses (`TimetablePage.tsx:81-82` clicks an `aria-hidden`, `pointer-events:none` input); close the ··· menu on focusout/Tab-out, return focus to `···` on outside close, and add ArrowDown/Up (`ExperiencePost.tsx:104-127`); at 320×568 keep the whole action column above the fixed nav (`features.css:1287-1296` reorder pushes "Cancel" to y484–528 under the nav at y494–558 — add the nav clearance to `.compose-editor` under `max-height:620px` or let the editor scroll with a bottom inset); bring the survivor link (`EntityPage.tsx:83-86`, 52×22) into the hit-area rule and the login checkbox to ≥22 px (`components.css:969-975`, still 18×18); shorten the stay-connected block so the submit is above the fold at 320×568 (`LoginPage.tsx:45-57`, submit bottom 595 vs 568); do not mount the editor under "Loading…" before `unlisted` can resolve (`ComposePage.tsx:87,190-192`). Evidence: [N1][N3][N4][N5][N10][N13][N18].
2. #3 aesthetic — Merge the duplicates, delete the orphans, fix the two rendered collisions. Collapse `.daynav__date` into one block that keeps `border-left/right: 1px solid var(--line)` (`features.css:207-225` — the second block's `border:0` erased the stepper dividers); merge `.post__menu button` (`:1017-1029` + `:1281-1283`); keep one hidden-text utility (`.sr-only` `foundations.css:89-99` now has 0 consumers beside the new `.visually-hidden` `features.css:1297-1304`); delete `.login__consent` (`features.css:61-65`), the `.action-cta` base rule (`foundations.css:286-299`), the dangling comment (`features.css:1275`), and decide each of the 27 consumer-less class names (`action-card, action-grid, browse-grid, btn--secondary, card-copy, card-index, card-title, chip--danger, chip--muted, chip--ok, exp-card__spacer, hairline-list, home-exp-list, home-exp-row, home-exp-row__body, lift, row-link, section-head__copy, segmented, sort-toggle, sort-toggle__btn, sort-toggle__btn--on, stat-strip, text-2` + the four above); reset `.settings-trigger` padding (`components.css:537-552`, 41 desktop UA values); comment or tokenize the desktop `.main` literal (`components.css:576`) and the nudge sheet (`features.css:815`); stop `.timeline__empty` (`features.css:445-458`) drawing "No lessons today" over the "P3 Free"/"P4 Free" rows — render it in flow or hide the free bands when the day is empty; clear the hero caption overlap (`.card--hero > .muted × .caption`, 83×9). Evidence: [N2][N11][N12][V13][V1].
3. #4 understandable + #6 honest — One dialog per purpose, and copy that matches the code. Parameterize `ReconnectDialog` (title, body, submit label, `beforeSubmit` checkbox with the caveat — `ReconnectDialog.tsx:19-33`, `SchoolLoginForm.tsx:64`) so "Save school login…" (`SettingsPage.tsx:186`) opens "Save school login" / "Enter your school login once; it stays encrypted on this device (a browser is less protected than a phone's secure storage)." and "Reconnect" (`:150`) keeps "The portal session ended." only when `portalTokenValid` is false; render the Timetable re-auth clause next to "Sync now" (under the daynav, `TimetablePage.tsx:158-163`) whenever a login is saved, independent of `lastSyncedAt` and present in the error branch; correct the Settings row (`SettingsPage.tsx:199-200`) to "…imported when your account is created, and again whenever you press Sync now or pull to refresh" and the Delete dialog (`:285`) to "You can import again with Sync now."; distinguish never-listed from delisted — an id absent from both the registry and `/api/directory` gets "Nothing is listed at this address." with no kind-as-name h1 on the entity page (`EntityPage.tsx:53-58,79`) and the composer (`ComposePage.tsx:188-201`); relabel "See 213" (`EntityPage.tsx:86`) so it names something the heading does not ("Open the current entry for 213"); explain "P1…P6" once (`TimetablePage.tsx:213-222`); drop "verified exposure" from the `not_eligible` note (`ExperiencePost.tsx:152`) and "entities" from the search aria-label (`ExplorePage.tsx:73`); set `document.title` per route; delete the stale consent paragraph in `packages/shared/src/api/contract.ts:12-16`. Evidence: [N6][N7][N8][N9][N10][C7][N16][N17].
4. #9 environmentally friendly (relay; anchor met) — Make the service worker evict what is stale and only what is stale. Build the live-asset set from `index.html` plus the stylesheet's `url()`s plus the JS chunk graph (or Vite's `manifest.json`) instead of the index regex (`public/sw.js:28`) so the current font subset (28,740 B) and `DashPage-*.js` survive activate; run `evictStaleAssets` whenever a navigation fetch returns an index whose asset set differs from the cached one, not only from `activate` (`sw.js:39-47`), so a TSX-only deploy self-evicts without an `sw.js` byte change; cap navigation shells to `/`; serve `sw.js` with `cache-control: no-cache` (hostd manifest, out of the web tree). Evidence: [N14][W6].

Redesign principles in priority order:
1. #8 thorough — success: every "Try again" lands focus on the content that replaced the banner, with a visible ring; the date button's ring is a full ring and the picker has a reachable fallback; the ··· menu closes on Tab-out and returns focus on every close; at 320×568 every compose action is hit-testable on landing and the login submit is above the fold; every tappable element ≥44 px including the survivor link and the login checkbox's visible box ≥22 px; no editor renders under "Loading…"; `ReconnectDialog` carries the form-order and caveat the login already has.
2. #3 aesthetic — success: one rule per selector in the four stylesheets (no `.daynav__date` or `.post__menu button` twins), one hidden-text utility, zero consumer-less class names, the stepper's dividers back, `.timeline__empty` and the hero caption drawn beside content not over it, the rendered census still ≥97 % with no new value, desktop UA padding gone.
3. #4 understandable — success: "Save school login…" and "Reconnect" open dialogs whose titles and bodies describe what each does; a never-listed address is told apart from a delisted one and never wears its kind as a name; "See 213" names something the heading does not; P1–P6 explained once; no "verified exposure" or "entities" in user-facing or AT text; each route has its own `document.title`.
4. #6 honest — success: every re-auth disclosure sits beside the button it describes and appears whenever a login is saved; the import row and Delete dialog describe the real triggers (account creation, Sync now, pull-to-refresh); no dialog states a session ended when the token is valid; `contract.ts` no longer documents a consent path the code ignores.
5. #9 environmentally friendly (keep at 3) — success: the SW never deletes a current asset, evicts stale hashes on a TSX-only deploy without an `sw.js` edit, caps the navigation-shell cache, and the bundle stays under 100 KB wire with 0 idle animations.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — one `ReconnectDialog` component parameterized for two purposes (reconnect vs save), and the re-auth disclosure's placement on Timetable; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): compose at 320×568 from landing to "Cancel" without scrolling; error → Try again → focus landing on each of the seven surfaces; Settings "Save school login…" → its own dialog → "Turn off" row.
- States checklist (empty, loading, error, success, focus, disabled) per screen, with the retry focus landing drawn per surface, the not-found state drawn separately from the delisted state on entity and composer, and the composer's registry-loading state drawn without an editor.
- Migration path for users currently on the old design: none for routes or data; ship an `sw.js` byte change with this deploy so the corrected eviction runs once on every installed client.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (compose Cancel hit-testable at 320×568 on landing, stepper dividers rendered, date-button ring unclipped, retry focus on all seven surfaces, menu closes on Tab-out, survivor link ≥44 px, Save-login dialog with its own copy, Timetable disclosure adjacent to Sync now, zero duplicate selectors and zero consumer-less class names, rendered spacing census ≥97 %, getAnimations() 0, contrast on a genuine dark boot, SW keeps the current font and Dash chunk across activate).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the bare `reload()` retries, the shared `ReconnectDialog` copy and the `overflow:hidden` pill must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients, or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the spacing census, the dark-boot contrast probe and the 320×568 compose/login sweeps, after every CSS change.
- Appending a rule at the end of a stylesheet instead of editing the rule that already exists — that is how r3's fixes produced this round's duplicate `.daynav__date` and `.post__menu button` blocks and lost the stepper dividers.
- Fixing a surface without fixing its siblings — r3's caveat and form-order fixes reached Login and Settings but not `ReconnectDialog`; the plan must enumerate every site of a repeated affordance before changing one.
- Claiming a change in the commit message that the diff does not contain — verify each move on the deployed app before reporting it done, and make an `sw.js` byte change in the same deploy.
````
