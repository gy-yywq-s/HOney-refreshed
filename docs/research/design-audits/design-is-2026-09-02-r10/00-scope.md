# 00 — Scope (design-is audit, HOney, 2026-09-02, r10 — verification round)

## Audited artifact
- The DEPLOYED web app at https://honey.gaelisus.com — branch `integration/product-v2`, commit `41a01fe` ("design-is r9 handoff: arming contracts, landing contract, nav truth, one label per tick, details"), three commits on top of `eede644` (r9's audited commit): `e5aec3e` (product review pass: constitution, voice, find mode, chooser, counts), `eebf055` (type-error fix), `41a01fe`. Served bundle `index-CJ2b0Z4x.js` / `index-DOdf_Dgc.css` / `DashPage-DVaHpjSJ.js` — confirmed by `curl` of `/` and by `/home/honey/app` HEAD before fan-out. `git diff --stat eede644..41a01fe -- apps/web` touches 22 files, 355 insertions / 94 deletions. The commit message claims to implement the full r9 handoff (moves 1–5).
- Source at `/home/honey/HOney-refreshed/apps/web` (styles `src/styles/*.css`; pages `src/pages/**`; components `src/components/*`; `src/lib/*`; `src/features/experiences/*`; build gate `scripts/check-css.mjs`); backend read only for honesty mapping (`packages/backend/src/{app.ts,routes,services,db}`).

## Build drift during the audit (recorded at fan-out + ~50 min)
The live site was redeployed five times while the evidence passes ran: `41a01fe` (`index-CJ2b0Z4x.js`) until ~03:41 UTC, then `9c3b23f` ("Timetable: a native-style top bar, no captions, the canvas fits each phone"), `60c8672`, `979a42e` ("the canvas fills the frame and widens to early lessons"), `bd54519` ("Phones: gestures replace the timetable's History and Sync now buttons"), and from ~04:05 UTC `bc3b7c9` ("Pull labels read in order; constitution records the phone timetable decisions"; served `index-4oNbE07m.js` / `index-C9l5CGbD.css`). `git diff 41a01fe..bc3b7c9 -- apps/web` touches only `Modal.tsx`, `PullToHistory.tsx` (new), `PullToRefresh.tsx`, `lib/format.ts`, `lib/refresh.ts`, `TimetablePage.tsx`, and the three stylesheets (542 insertions / 216 deletions); every other file is byte-identical. Per rule 4g (live = latest green commit) and the skill's "score what ships", r10 scores the build that is live at scoring time, **`bc3b7c9`**: the r9-move verification rows measured on `41a01fe` stand for every surface the later commits did not touch, and every Timetable / pull-gesture / Modal / stylesheet measurement was re-taken on `bc3b7c9`; each evidence file attributes its measurements by build. `bc3b7c9` also amended the constitution §2: the timetable canvas fills the phone frame and never compresses below 560 px (desktop 656), widening the day range to the hour for lessons outside 09:00–20:00; on phones the timetable is a native-style screen with no captions and no utility buttons, the date bar under the status bar is the heading with the native date input invisibly over it, pull-to-refresh has two stages (release to refresh / pull further to sync, the label says so), a deliberate pull-up at the canvas end opens History, desktop keeps History and Sync now as buttons, and a keyboard on a phone gets a focus-revealed History link. These are owner decisions (Gary, 2026-09-02): the audit measures how they behave (discoverability, label honesty, keyboard/AT equivalents, thresholds, motion), not whether they exist. Decision (3) below is superseded accordingly.

## Round framing
ROUND 10. r1 16, r2 16, r3 16, r4 18, r5 19, r6 18, r7 19, r8 15, r9 18 (all REDESIGN by the <20 rule; r9 turned on two downward ties). r10 (a) verifies every r9 move on the live app, (b) hunts for NEW defects the fixes introduced (the forced-miss `reload()`; the Timetable landing now always on the first lesson incl. cached steps and gated on the lessons object; the Tab start-point reset after the landing; `?date` compared against `location.search` with bare `/timetable` staying bare; `matchPath`-derived nav current; the reconnect label bound to the tick; delete clearing the saved login; feed status per tab + "N more"; the shared date formatter; the one provenance map; "Could not load"; Explore hiding sections under a banner; compose `?lessonId` skeleton/error; one disabled treatment; the skip ring on the card radius + timetable bottom room; the wash stepped without a transition; Home min-heights; the SWR cap at 40; the widened CSS gate), plus everything the product-review pass (`e5aec3e`) added to the student surfaces (Find mode / search, composer chooser with the last 5 lessons, entity counts, new voice lines) — all of which is audited for the first time, and (c) re-measures the standard metric tables. Scores re-anchored from scratch against the skill's rubric; tie-breaker lower; score worst instance; no credit for unverified fixes.

Owner's product decisions (standing, not up for re-litigation): (1) timetable import is part of the account — no consent gate, disclosed, not asked; (2) the four surfaces Home / Timetable / History / Experiences stay; (3) the 620 px timetable canvas density on normal heights stays; compact notches (540 / 450), static daynav and once-per-date landing at ≤620 heights are the deliberate degradation — audit how it behaves, not whether it exists; (4) rule 4f: every finite selectable set fully displayed, search only filters. iOS out of scope. The constitution (`docs/product/product-and-style-constitution.md`) is the single product/style authority.

## Primary user / tasks
A high-school student on their phone (installed PWA), 390×844 typical, must hold to 320×568. Tasks: (1) check today's timetable / next lesson; (2) read the Experiences stream; (3) share an experience.

## Evidence mode
Dynamic operation of the live app in a real browser (playwright harness `/root/claude-work/design-audit/`, `authedPage({viewport, desktop, reducedMotion, timezoneId})`), device matrix 320×568 / 375×667 / 390×844 / 393×852 / 402×874 / 430×932 / 440×956 / 360×800 / 412×915 plus compact 320×600 / 360×620 / 390×620 and desktop 1280×800; lesson measurements in `Asia/Shanghai` (2026-09-02 and 2026-08-24 are 3-lesson days; 2026-08-25 sparse; 2026-08-22 / 2026-09-06 empty). Five Opus evidence subagents (structural, visual, copy & honesty, weight & friction, accessibility); scoring by the orchestrator only. Hard limits: no post published, no report submitted, no persistent Settings change, secrets never read or printed, reactions only if undone, no repo edits.

## Out of scope
iOS app, admin dash internals, backend moderation logic, portal internals, the public edge header rewrite (relay only).
