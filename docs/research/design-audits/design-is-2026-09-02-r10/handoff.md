# handoff — /make-plan (r10)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit daff183 after the site was redeployed ten times during the audit — 41a01fe → bc3b7c9 → daff183). Current design failed audit at 16/30 with critical gaps in principles #3 aesthetic (1/3), #4 understandable (1/3), #7 long-lasting (1/3), #8 thorough (1/3) and #10 as little design as possible (1/3); no load-bearing principle scored 0; two ties (#7 1↔2, #10 1↔2) were settled downward and granting both upward gives 18 — still REDESIGN, so unlike r9 the verdict does not turn on the ties.

Verdict paragraph (quoted from scores.md):
> REDESIGN — by the Phase 3 rule (total 16 < 20; no principle at 0). As in r3–r9 the word overstates the scope: tokens, type, spacing, colour, motion budget, weight, service worker, trust architecture and now the idle budget all verify. What the score names is a contract problem, not a style problem: the app ships new surfaces and restyles without carrying the contracts the previous round established (states on every branch, a landing that keeps the heading, one formatter, one truth per channel, an affordance for every action), and it does so mid-audit.

Why redesign and not refine: total 16/30 is below the REFINE threshold of 20. The r9 handoff's own moves largely verified (48/48 retry landings, 21/21 dialog exits, ?date= right on 15/15 transitions, one disabled recipe, the wash animation gone so #9 returned to 3) — but the product-review surfaces that shipped beside them (Find mode, the composer chooser, entity counts) arrived with none of the states contract, and the Timetable restyle + navigation hierarchy that deployed DURING the audit created 8 of the 12 rendered inconsistencies, regressed the one-formatter fix within the day, removed the bottom-room fix, took the day heading off every non-today compact load, and removed every visible sync/history affordance and the freshness indicator from phones (while also fixing the 404 nav truth, the touch spotlight ring, the pull pill and the canvas fit). Every move below is contract-finishing work on a layer whose tokens verify for the eighth round; none is structural.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844; must hold to 320×568; matrix 320×568 / 375×667 / 360×800 / 412×915 / 390×844 / 393×852 / 402×874 / 430×932 / 440×956 plus compact 320×600 / 360×620 / 390×620 and desktop 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. The single product/style authority is docs/product/product-and-style-constitution.md — read it first; it now records (Gary, 2026-09-02) that on phones the Timetable is a native-style screen with NO captions and NO utility buttons, the date bar under the status bar IS the heading with the native date input invisibly over it, pull-to-refresh is two-stage (release to refresh / pull further to sync, the label says so), a deliberate pull-up at the canvas end opens History, desktop keeps History and Sync now as buttons, the canvas fills the phone frame and never compresses below 560 px (desktop 656), the app is a hierarchy (lib/navigation.ts tree: back bar on every non-root screen, root-ancestor tab lit, tabs replace history, left-edge swipe = up). These are owner decisions: fix how they behave, never whether they exist. Other standing decisions: timetable import is part of the account (no consent gate; disclosed, and the disclosure must name exactly the triggers that exist); the four surfaces Home/Timetable/History/Experiences stay; rule 4f — every finite option set fully displayed, search only filters; quiet-humanist language — Source Sans 3 only, one accent #33667c (night #8fc2d4), warm hues only for danger/ok, ramp 12/13/15/16/17/20/22/28, ladder 4/8/12/16/20/24/32/44, zero idle animation; honest privacy copy that never overclaims; iOS wire compatibility — propose NO API changes. Verify everything on the deployed app by computed style, geometry, document.activeElement, live-region text and request logs, never from the diff. The audit harness at /root/claude-work/design-audit/ (playwright; lib.js exports authedPage({viewport, desktop, reducedMotion}) and now defaults to Asia/Shanghai with standalone-inset options; probes r10-{struct,vis,copy,wf,a11y,final}-*.js with .log files and r10*-*.png screenshots; matrix.js = device regression) reproduces every finding: r10-struct-landing.js / r10-struct-bar.js (compact landing, Tab 1, sparse day), r10-final-css2.js (320 Tab order), r10-vis-* + r10c-vis-* (states, overlaps, now-line), r10-struct-new*.js / r10-a11y-find*.js (Find mode, chooser), r10-wf-cls2.js (Home CLS with a delayed hero), r10-wf-offline.js, r10-struct-gaps.js (/api/me), r10-copy-tt-bc3b7c9.js (Timetable copy), r10-final-nav.js (nav truth). In-page today during r10 was 2026-09-02 (3 lessons); 2026-08-24 a 3-lesson control day; 2026-08-25 a one-late-lesson sparse day; 2026-08-22 / 2026-09-06 empty. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report, never press Sign out or Sync now in a probe (intercept POST /api/sync with page.route), reactions only if undone or intercepted. Deploy discipline for this pass: do NOT deploy unrelated restyles while the re-audit runs; the audit scores what is live.

Preserve from current design (MUST NOT be touched — these verified live):
- Retry landing contract: every "Try again" arms on the union of every loading flag its handler reloads (8/8 useRetryFocus consumers; MinePage.tsx:60 `mine.loading || namesLoading`; ExplorePage.tsx:43 `entities.loading || directory.loading`; the Timetable region wrapping every branch TimetablePage.tsx:205-215), useApi.reload() as a forced miss (useApi.ts:62-69,107-111) — 48/48 landings, ring 25.9–28.4 %, 0 px scroll. Regression check: r10-a11y-landing.js, r10-struct-retry.js — 48/48.
- Modal opener contract: ExperiencePost.tsx:264 focuses the persistent `···` trigger before the menu unmounts; Modal.tsx reads onClose through a ref with `[]` deps; createPortal to body + `#root[inert]` — 21/21 exits restore the opener, traps 0/60. Regression check: r10-struct-dialogs.js, r10-a11y-dialogs.js.
- Pending pill: aria-disabled on the tapped pill only, pointer-events auto, one POST for three activations, focus kept, status "Saving your reaction…" — keep the mechanism; clear the note (see Discard). Regression check: r10-struct-react2/3.js.
- Timetable ?date= contract: TimetablePage.tsx compares against window.location.search; bare /timetable stays bare on today; 15/15 transitions correct; invalid dates replaced; history.length unchanged. Regression check: r10-struct-date.js.
- Nav truth (daff183): lib/navigation.ts tree; AppLayout.tsx:21-24 tabIndex by rootOf(); tabs as `<Link replace>` with className/aria-current from the index (AppLayout.tsx:146-157, 213-224) — 0 aria-current / 0 is-active on 6/6 404 paths at 390 and 1280; every real route lights its root ancestor; document.title from titleOf(). Regression check: r10-final-nav.js.
- Back bar: `nav.pagebar[aria-label="Up one level"] > a.pagebar__back` 44 px tall, sticky at ≤960, second Tab stop after the skip link, POP when the parent is the previous entry, REPLACE on a cold deep link, never a parent/child loop, never leaves the app; present on all 10 non-root screens, absent on the 3 roots, never beside the daynav. Regression check: r10-final-back.js, r10-final-hist.js.
- Canvas fit (bc3b7c9): min-height 560 base / 540 at ≤620 h / 450 at ≤620 h ∧ ≤360 w / 656 desktop; rangeFor() widening to the hour (TimetablePage.tsx:283-292); every notched iPhone in standalone fits with zero page scroll; 0 clipped blocks; 0 horizontal overflow on 21/21 cells. Regression check: r10-struct-canvas.js.
- Pull ladder (daff183): "Pull to refresh" → "Release to refresh · pull further to sync" → "Release to sync with school" on /timetable; three stages on /experiences; REFRESH_AT 64 / SYNC_AT 132 damped px at damping 0.45; the sync stage only while a sync handler is registered (refresh.ts:27); the one-pill indicator (.ptr__pill, radius 99, contrast 5.63 stone / 7.27 dark / 6.13 sync). Pull-up for History: SHOW_AT 24 / OPEN_AT 110 / MIN_MS 220 with the three guards holding. Regression check: r10-final-gestures.js, r10-struct-gestures.js (POST /api/sync intercepted).
- Skip ring: `.main:focus-visible > .view` and `.section-title[id]:focus-visible` (a35dc89) — 0 ringed after a plain tap 12/12, ring on the keyboard path, radius 17 = the card radius, 0 intruders in the band on /timetable at 390. Regression check: r10-final-css.js.
- Rest-state outline census 0/115 on 60 loads; rest ≠ focused 114/114 Tab stops; mobile sheet grammar 42/42 (`20px 20px 0 0`, top border only, safe-area padding, sheet-up, flush bottom); dark-boot palette through the portal. Regression check: r10c-vis-outline.js, r10c-vis-dialogs.js.
- Type/spacing/colour: one face; live sizes {12,13,15,16,17,20,22,24,26,28}, floor 12; spacing on-ladder 97.8–98.0 % mobile / 92.9 % desktop; 43 colour literals; tokens.css unchanged except --inset-top/--inset-bottom; one accent per surface; warm hue only --danger; contrast floor 4.77 stone / 7.03 night, 0 failures on 50 surface-measurements. Regression check: r10c-vis-typespace.js ×4, r10c-vis-contrast.js, r10-a11y-contrast.js.
- Idle and motion budget: getAnimations() 0 on 44/44 cells DURING and between lessons (the wash steps without a transition, features.css:78-84); 6 keyframes, 2 infinite loading-only; reduced motion 106/106 to 1e-06s incl. every new bar/gesture element; scroll-behavior auto. Regression check: r10-wf-anim.js, r10-wf-wash2.js, r10-a11y-motion-bc3b7c9.js.
- Weight: JS 88,932 B br (daff183), CSS 9,722 br, Dash 0/15 student loads, cold /home 10 requests, warm SPA 3; SW md5 f7513f02194664efa4e96d50773ec83d, battery green, /api/ never cached, offline document 144–165 ms. Regression check: curl br size <100 KB; r10-wf-net.js; r10-wf-sw1.js mode real.
- Copy that verified: Reconnect "Reconnect only" / "Reconnect and save login", Save "Save login" / "Sign in without saving" (ReconnectDialog.tsx:39-48); one provenance map (shared.tsx:16-21 → ExperiencePost.tsx); "Could not load" region over the "Could not reach…" alert; feed status per tab and "N more" on load-more (FeedPage.tsx:36-51); 8/12 canonical constitution lines identical (login hero, Experiences hero, composer prompt + actions, nudge, cooling, empty feed, empty entity); 0 forbidden phrasings; 0 all-caps titles; 0/7 dark patterns; the nine privacy claims ↔ code (identity-free publish api/client.ts:241, no author column db/database.ts:103-121, ownership_hash, 6=6=6 categories, free_text_not_accepted routes/experiences.ts:226, coarse day format.ts:73-79, the import disclosure SettingsPage.tsx:200-204 / LoginPage.tsx:70-71 ↔ routes/auth.ts:32 + routes/data.ts:17, the delete confirm body SettingsPage.tsx:309 ↔ :316).
- Rule 4f on Explore: 19/19 registry entities rendered in four named sections with no "N more"; typing filters locally. Keep — and extend it to the directory-failure branch (see Discard).

Discard (structural patterns causing the failures):
- A surface shipped without the states contract its siblings carry. ExplorePage.tsx:36-40 calls useApi(() => api.search(searchQ), [searchQ]) with no cache key, no debounce and consumes neither loading nor error, and :135 gates the results section on `search.data.experiences.length > 0` — so a 9 s delay, an abort and an empty result all render identically to the un-searched page (0 skeletons, 0 alerts, 0 status), one uncached GET per keystroke ("mathematics" = 10 requests, retyping = 10 more), and no announcement of results (0 live-region mutations while 19 rows become 3). ComposePage.tsx:100-106 fetches /api/history for the chooser and :332-352 renders the five rows only on success — under a 5 s delay or an abort the "Your recent lessons" list silently vanishes (0 skeletons, 0 alerts, 0 retry), and the chooser's one-tap path to the editor lands activeElement on `<body>`. EntityPage.tsx:53,140-149 appends the count silently and never says a number is missing. Caused failure on principles #8 and #2.
- An error surface outside every contract: AppLayout.tsx:62-73 replaces the whole app with div.fullscreen-note when /api/me fails — no shell, no nav, no skip link, no #main, no scroll owner, no role=alert, a "Retry" that is not wrapped in useRetryFocus (activeElement = body on fail AND success), and offline the entire PWA is that one 197-character card on 3/3 routes although the SW served the document, JS and CSS. Caused failure on principles #8 and #2.
- Actions with no affordance for some users (phone Timetable, bc3b7c9): features.css:253,280-282 hides .daynav__row — "Synced N ago", History and Sync now — under `@media (max-width: 700px)`, a WIDTH query, while both replacement gestures are touch-only (PullToRefresh.tsx:123-126, PullToHistory.tsx:82-85). Consequences measured: on a phone no control of any kind reaches Sync now from the Timetable and no freshness indicator is visible at rest; a mouse user in a ≤700 px window cannot sync at all and reaches History only via a 1×1 clipped link; both gesture ladders live inside aria-hidden containers for their whole duration (PullToRefresh.tsx:136 `aria-hidden={!busy}`, PullToHistory.tsx:95-96) so AT hears nothing until release; the focus-revealed a.daynav__history-sr is 72×32 (features.css:283-296), the one sub-44 control; the native input[type=date] laid over the h1 (features.css:213-231) costs four consecutive Tab stops, the fourth with no ring. The pull-to-sync gesture is a THIRD import trigger (PullToRefresh.tsx:113 emitSync → refresh.ts:29-46 → TimetablePage.tsx:103-106 → POST /api/sync) that the disclosure (SettingsPage.tsx:200-204, LoginPage.tsx:70-71) does not name — the one constitution §2 drift found — and the caption that disclosed the sync's portal sign-in (api.syncSeamless, client.ts:135-145) was deleted from the screen where the action now happens. Caused failure on principles #4, #6, #2 and #8.
- A landing that still cannot be honoured and a focus reset that lands one stop too far: TimetablePage.tsx (landing effect, bc3b7c9 :349-365) scrolls `visible[0]` into view with `scroll-margin-top: calc(var(--sp-8)*2 + var(--sp-3))` (features.css:360 = 100 px) but nothing pads the canvas, so a day whose first lesson is late clamps to max scroll (h1 −77/−25/−115 at 320×568/360×620/390×620, the third rewrite in which [S-R10-9] survives); the bar is `position: static` at ≤620 heights and grows 58 → 106 px on non-today dates ("Back to today" row), so the heading now leaves on EVERY non-today compact load (−13/−13/−13 on the 3-lesson control day; today is a no-op only because block 1 already sits at y 67); a step onto a cached date resets the owner to 0 and never lands (TimetablePage.tsx:74-77 vs the comment at :65-69 that says "cached or not"); and `skip?.focus({preventScroll:true}); skip?.blur()` (bc3b7c9 :362-363) puts the sequential-focus start point AT the skip link so Tab 1 is the daynav arrow on 9/9 compact cells and the skip link is stepped over — reproduced 3/3 on daff183 at 320×568. Caused failure on principles #4, #8, #5 and #3.
- Chrome and clock lines over content (bc3b7c9): the fixed nav.mobile-nav covers 22 / 33 / 60 px of the day canvas and the last hour label(s) at rest on 320×568 / 375×667 / 390×620 — the `.timetable-screen { padding-bottom }` that gave bottom room on 41a01fe was removed by the restyle; div.timeline__now (7 px, --danger) crosses hour and ghost labels by 1–7 px on 7/8 clock×viewport cells (12:05 → "12:00" by 6 px at 320×568; the line bisects "Lunch Break"). Caused failure on principles #3 and #5.
- One truth per channel, breached again on new and old surfaces: format.ts:35-40 formatDayTitle ("Wednesday, 2 September") for the Timetable h1 beside format.ts:43-48 formatDayHeading ("Wednesday, 2 September 2026") on Home — the r9 one-formatter fix regressed within the day; ExperiencePost.tsx:156-166 sets "Saving your reaction…" and :181-184 `finally` clears busy/pendingValue but not the note (still on screen 5.8 s after the response); FeedPage.tsx:36-51 returns early on error so a stale "12 experiences" sits over the alert, and on a tab change to a larger tab the "more" branch wins first ("4 more" then "7 experiences"); ExplorePage.tsx:75 "Teachers, courses, places and food are all listed below" renders while :120/:124 render null for all four sections under `entities.error || directory.error` — a directory-only failure blanks a complete listing that /api/entities delivered (rule 4f); ExplorePage.tsx:84-91 names the box "Filter by name" and the page says "typing only narrows the list" while it also runs a server search and adds an "Experiences that mention …" section whose aria-label (:136 "…this") is not its heading (:137), whose section counts show the filtered number, and whose "Recent" list (:107-120, the last 5 entity pages opened on this device — recentContexts.ts) sits outside the landing region under a name that collides with the chooser's "Your recent lessons"; useComposer.ts:44 EDIT_REQUIRED leads every edit_required lane with "Remove the insult or private detail" even when the reason is `expression:uncertain` (shared.tsx:52's "We couldn't understand part of this…" then follows as a bullet); useComposer.ts:45 + ComposePage.tsx:402 say "keep it for yourself" and "keep it as a private note" back to back; SettingsPage.tsx:96-98 (delete-account row) omits the device clearance the confirm body :309 promises, and :316 clears portalCredentials BEFORE :317 awaits the delete; the `···` trigger "More options" opens a menu "Post options" holding one item "Report" (ExperiencePost.tsx:249,258); five sentence shapes for the one storage-caveat fact (LoginPage.tsx:59-63, SettingsPage.tsx:171, :255-259, ReconnectDialog.tsx:29-31); WhyPage.tsx:73 "strongly protected" (the one inflation, unchanged since r9); the back link's accessible name is only the parent's title (AppLayout.tsx:184-200 — the landmark carries the only "up" wording); navigation.ts:56-57 gives every unknown route the parent Home, so a 404 renders "‹ Home" above a page whose only action is "Go home"; navTabs.tsx:17 DESKTOP_TABS has no Settings entry so /settings and /dash light nothing and expose no aria-current at ≥961 px; packages/backend/src/experiences/service.ts:936-956 entityStats is the one read path without the HIDE_PUBLIC_EXPERIENCES guard (relay — no API change: add the guard, same shape). Caused failure on principles #4, #6 and #10.
- Rendered inconsistencies and removables: `.daynav__date:has(.daynav__picker:focus-visible)` at `outline-offset: -3px` where every other ring is +3; four identical "Nothing by that name." under an empty filter; the entity count run onto the intro paragraph; the provenance line ellipsised at 320 and 390 (`.post__provenance` scrollWidth 305 vs clientWidth 122/192); the widest pull label at 7 px side clearance at 320 (`white-space: nowrap`); chooser rows 48/49/49/73/49 from one wrapping name; dangling trailing comments 9 (components.css:527,759,918,967; features.css:84,1282,1388,1410,1416 — target ≤6); a dead `.doc__footer` rule (features.css:1190) and a rule-less `ptr__label` span (PullToRefresh.tsx still emits it; its CSS was deleted); the audit log `apps/web/src/r10-struct-narrowdesk.log` committed by a35dc89; scripts/check-css.mjs:56 dead (`&& false`), :33 a fresh `seen` Map per at-rule (the cross-@media duplicate still passes), :44-47 rejecting the legal `.btn,.btn:hover{}` and `.nav a:hover,.nav a[aria-current="page"]{}`. Caused failure on principles #3, #8 and #10.
- Home's CLS reservation on the wrong boxes: features.css:1177 `.home-foot{min-height:44px}` renders at 110 px (a no-op) and :1341 `.home-voices{min-height:132px}` matches; the boxes that actually resolve late are `.home-head` (63 → 85 px when lastSyncedAt wraps the greeting, HomePage.tsx:56-58) and `.nextlesson` (110 → 209 px when the hero settles, HomePage.tsx:71-95), both `min-height:auto` — CLS 0.185 with /api/next-lesson delayed 2.5 s, 0.046 on Slow 3G, 0.086/0.064 under contention. Caused failure on principle #5 (relay #9).
- Relay, anchor #9 met: one uncached GET per Find keystroke; one GET per cached date step; PullToRefresh.tsx:114 apiCache.clear() for every screen on any pull; 123,968 B of never-requested font subsets and 1,201 B of admin CSS in the student payload; the edge-injected Cloudflare RUM beacon (10,175 B / 2 requests per cold load, 2 per SPA navigation — a Cloudflare setting, out of repo); the edge rewriting sw.js to max-age=14400 (out of repo); no keyboard equivalent to load-more or pull-to-refresh on any route.

Top 5 moves from the audit (verbatim):
1. #8 thorough + #2 useful — States and landings for the surfaces that shipped without them. Find mode (ExplorePage.tsx:36-40): give the search a cache key (`search:${q}`), a ~250 ms debounce and the useApi loading/error; render a skeleton while loading, the standard banner + "Try again" inside the landing region on error, and one empty line ("No experiences mention “q”") on an empty result; announce results in a pre-mounted role=status ("N names, M experiences"). Chooser (ComposePage.tsx:100-106, 332-352): skeleton rows while /api/history loads, the banner + "Try again" (armed on history.loading) on failure, and land the composer on its Editor region on arrival from a chooser tap AND from History "Select" (the same landing ComposePage already performs for the ?lessonId= error branch). Shell (AppLayout.tsx:62-73): render the /api/me failure INSIDE the app frame (nav, skip link, #main, one role=alert, "Try again" on useRetryFocus(loading)), and offline show the shell plus whatever the SWR cache holds. Entity count (EntityPage.tsx:53): keep silent on failure but mark the request non-blocking (no change to the intro until the count exists). Evidence: [V-R10-11][A-R10-12][S-R10-17][S-R10-19][S-R10-2][W-R10-8][A-R10-11][S-R10-18].
2. #4 understandable + #2 useful + #8 thorough — The phone Timetable within the owner's decisions. (a) Hide .daynav__row by POINTER, not width: `@media (max-width:700px) and (pointer: coarse)` (features.css:253,280-282) so a no-touch narrow window keeps History and Sync now (desktop-only buttons stay a constitution decision; a mouse window is not a phone). (b) Surface freshness somewhere a phone student can read it without a caption: the pull pill's first stage label carries it ("Pull to refresh · synced 13 min ago", PullToRefresh.tsx label at stage "pull"), and the lesson dialog or Settings School-connection keeps the full sentence; flag to Gary that at rest a phone shows no freshness (open product question, not a move). (c) Give the gesture ladders to AT: keep `.ptr`/`.pullup` aria-hidden at idle but flip to visible from the first stage (PullToRefresh.tsx:136 `aria-hidden={stage === "idle" && !busy}`, PullToHistory.tsx:95 likewise) so "Release to sync with school" is announced before release. (d) One Tab stop for the date: keep input.daynav__picker invisibly over the h1 for taps (constitution) but take it out of the Tab order (`tabIndex={-1}`, aria-hidden) and make the visible date pill a `<button>` inside the h1 that calls `picker.showPicker()` — one ringed stop, `outline-offset: 3px` like every other ring (drop the −3px `:has()` rule, features.css:213-231). (e) Keep the heading on screen in every clock state and on every date: make header.daynav `position: sticky` at ≤620 heights too (it is 58 px; the 540/450 notches already fit) and keep "Back to today" from growing the bar (place it in the stepper row or as a 44 px pill that overlays the canvas top) — then the landing can never take the day name away; pad `.timetable-screen` bottom at ≤620 heights by the nav clearance so scrollIntoView can be honoured on a sparse day and the nav never covers the last hour (this also fixes 320×568 / 375×667 / 390×620 at rest); make the cached-date step land like a cold load or delete the "cached or not" comment (TimetablePage.tsx:65-77). (f) Tab 1 = the skip link: replace `el.scrollIntoView()` + `skip.focus(); skip.blur()` (TimetablePage.tsx:349-365) with a direct `owner.scrollTop = targetOffset` (setting scrollTop does not move the sequential-focus start point), verified at 320×568 / 360×620 / 390×620 on today, 2026-08-24 and 2026-08-25 — Tab 1 = a.skip-link 9/9. (g) The revealed History link (features.css:283-296) at 44 px tall when focused. (h) Start the now-line to the right of the hour column and give ghost labels ("Lunch Break", "P2 Free") a surface ground so the line passes behind them (features.css .timeline__now / .timeline__hour). Evidence: [S-R10-24][S-R10-25][S-R10-26][A-R10-20][C-R10-35][C-R10-39][V-R10-4][V-R10-5][S-R10-9][S-R10-31][S-R10-10][A-R10-6][F-R10-22][V-R10-2][V-R10-3][V-R10-7][V-R10-8].
3. #4 understandable + #6 honest + #10 as little design — One truth per channel, round three. One date formatter: delete format.ts:35-40 formatDayTitle or make Home use it — decide whether the year shows, and render the SAME string on Home and the Timetable h1 (the stepper's "Wed 2 Sept" is a second, deliberate grammar; not a third). Disclose the third import trigger: SettingsPage.tsx:200-204 and LoginPage.tsx:70-71 → "…when your account is created, and again whenever you press Sync now or pull the timetable down to sync." — the constitution requires the disclosure to name exactly the triggers that exist. Name the sign-in where it happens: the sync result banner reads "Signed in again and synced N lessons." when syncSeamless replayed the saved login (TimetablePage.tsx:79-98 knows), otherwise "Synced N lessons from the school portal."; keep the Settings caption. Clear the reaction note in the `finally` (ExperiencePost.tsx:181-184 → setNote(null)) and pre-mount the role=status node. Feed status (FeedPage.tsx:36-51): set the text to "" on error, and compute the scope-change branch BEFORE the "more" branch (compare scope first, then count). Explore under a directory-only failure (ExplorePage.tsx:120/:124): render the four sections whenever entities loaded and put the directory banner above them; hide the "all listed below" helper (:75) only when the sections are hidden. Find naming (ExplorePage.tsx:84-91, 136-137): aria-label "Search names and experiences", placeholder to match, the helper sentence to say what typing does ("narrows the list and finds experiences that mention it"), the results section `aria-labelledby` its heading, section counts as "N of M" or the total, "Recent" → "Recently opened", and the Recent list inside the landing region. Moderation lanes: useComposer.ts:44-45 map the lead sentence per reason (`expression:uncertain` → shared.tsx:52's line as the lead), and drop ComposePage.tsx:402's appended sentence when the lane text already says it. Delete account: SettingsPage.tsx:96-98 row adds "the school login saved on this device is cleared"; swap :316/:317 so the credential is cleared only after a successful delete. `···` → a direct 44 px "Report" button (ExperiencePost.tsx:249-258; the menu contract can go). One caveat sentence for the five storage caveats (LoginPage.tsx:59-63, SettingsPage.tsx:171, :255-259, ReconnectDialog.tsx:29-31) — write it once in shared.tsx and reuse. WhyPage.tsx:73 "strongly protected" → "protected by design, not by magic" (or drop "strongly"). Back link name: AppLayout.tsx:184-200 → visually-hidden "Back to " prefix inside the link. 404: navigation.ts:56-57 returns no parent for unknown routes (the page's "Go home" is the way out) — or name the bar "Go home"; desktop Settings: navTabs.tsx:17 / AppLayout.tsx rail foot — mark the account/settings control `aria-current="page"` + `is-active` when rootOf() is /settings. entityStats guard (service.ts:936-956): return zeros under HIDE_PUBLIC_EXPERIENCES, same response shape. Evidence: [C-R10-34][C-R10-41][C-R10-38][C-R10-16][C-R10-13][C-R10-12][C-R10-14][V-R10-10][S-R10-17][S-R10-18][C-R10-3][C-R10-4][C-R10-15][C-R10-17][C-R10-10][C-R10-8][C-R10-6][C-R10-1][F-R10-7][F-R10-4][F-R10-3][C-R10-20].
4. #3 aesthetic + #10 as little design + #8 thorough — Rendered details and removables. One empty sentence for an empty filter (ExplorePage.tsx: when every section is empty under a query, render one "Nothing by that name." above the sections, not four); the entity count as its own `p.caption` line (EntityPage.tsx:140-149); `.post__provenance` wraps instead of ellipsising at 320/390 (features.css — drop `overflow:hidden; text-overflow:ellipsis`, allow two lines); the pull pill label allowed to wrap or shortened at ≤360 ("Release to refresh · pull on to sync"); chooser rows a consistent min-height with the name clamped to two lines; delete the dead `.doc__footer` rule (features.css:1190); either restore a `.ptr__label` rule or drop the span (PullToRefresh.tsx); dangling trailing comments 9 → ≤6 (move each explanatory comment ABOVE the declaration it explains: components.css:527,759,918,967; features.css:84,1282,1388,1410,1416); `git rm apps/web/src/r10-struct-narrowdesk.log`; scripts/check-css.mjs: delete the dead `&& false` line (:56), share one `seen` Map keyed by at-rule head so identical `@media` blocks compare (:33), and change the dangling-member rule (:44-47) to flag a stateless member only when its BASE selector differs from every stateful member's base (`.card, a:focus-visible` → flag; `.btn, .btn:hover` → pass; `.nav a:hover, .nav a[aria-current]` → pass). Evidence: [V-R10-12][V-R10-13][V-R10-17][F-R10-16][V-R10-14][F-R10-25][F-R10-24][S-R10-15][F-R10-28][S-R10-14][W-R10-11].
5. #5 unobtrusive + #9 environmentally friendly (relay; keep the payload) — Reserve the boxes that shift: `.home-head` and `.nextlesson` get min-heights equal to their settled heights at 320/390 (features.css; HomePage.tsx:56-58, 71-95), remove the no-op `.home-foot{min-height:44px}`; verify CLS 0 with /api/next-lesson delayed 2.5 s and on Slow 3G. Find mode debounce + cache key (move 1). Pull: clear only the current route's keys (PullToRefresh.tsx:114 → apiCache.invalidate(prefix)) or document the global clear. Fonts: ship only the latin subset (vite font plugin config) so 123,968 B of woff2 leave dist; move admin.css out of the student bundle (1,201 B). Out of repo, relay to Gary: the Cloudflare RUM beacon (Web Analytics setting) and the edge's sw.js max-age=14400. Evidence: [W-R10-6][W-R10-14][W-R10-7][W-R10-15][W-R10-13][W-R10-18][W-R10 relay].

Redesign principles in priority order:
1. #8 thorough — success: Find mode and the chooser each show skeleton / banner + landing retry / empty on the deployed app (delay 5 s, abort, empty body); the /api/me failure renders inside the shell with a landing retry in both outcomes; offline shows the shell; History "Select" and a chooser tap land on the Editor region; the reaction note clears on settle; the date control is one ringed Tab stop; Tab 1 = the skip link on 9/9 compact Timetable cells; dangling comments ≤6; no dead rule, no rule-less class; the stray log gone; the gate rejects the class and passes `.btn,.btn:hover{}`.
2. #4 understandable — success: one string for the day on Home and the Timetable; the disclosure names three triggers; the sync banner names the sign-in when it happened; the feed status is "" on error and never says "N more" on a tab change; Explore keeps its listing under a directory failure; the Find box is named for what it does and its results are announced; each moderation lane leads with its own sentence; a direct "Report"; the back link says "Back to"; the 404 offers no false parent; Settings lights on desktop; a phone user can read the timetable's freshness somewhere without a caption.
3. #3 aesthetic — success: the heading in view on 9/9 compact cells on today, the control day and the sparse day; the nav clears the canvas foot at rest on all 13 viewports; the now-line crosses no label at 12:05 / 12:30 / 18:05 / 10:35 at 320 and 390; one ring offset; one empty sentence; the count on its own line; the provenance line unclipped; the rest-state census still 0/115, sheets 42/42, spacing ≥97.5 % mobile.
4. #10 as little design as possible — success: ≤2 removable elements on any screen (the one-item menu and the duplicate sentence gone; four empties → one).
5. #7 long-lasting — success: no new chrome; the phone-Timetable and hierarchy decisions unchanged; the contracts above enumerated per consumer so the next surface inherits them.
6. #2, #5, #6, #9 (hold at 2/2/2/3) — success: retry 48/48, dialogs 21/21, ?date= 15/15, canvas fit, pull ladder and pull-up guards, nav truth, back bar POP/REPLACE, idle 0/44 during a lesson, JS <100 KB br, dark boot 6/6, reduced motion 100 %, 0/7 dark patterns, one inflation → zero.

Deliverables for the plan:
- New information architecture (not derived from old): none — the tree in lib/navigation.ts, the four roots, the phone Timetable and the gesture set are owner decisions and stay. What changes are contracts: (1) a states contract enumerated for EVERY useApi consumer (loading skeleton, error banner + landing retry armed on the flag the handler reloads, empty sentence, success) with Find mode, the chooser and the shell added to the list; (2) a landing contract for the Timetable (sticky bar at every height, bottom padding so the request can be honoured, owner.scrollTop instead of scrollIntoView, cached steps land or the comment says not); (3) an affordance contract (every action reachable by touch, keyboard and pointer at every width; gesture ladders visible to AT from the first stage); (4) one-formatter / one-sentence contracts listed per string.
- New primary flow (low-fi, labeled, compared side-by-side to current): the phone Timetable at 320×568 on 2026-08-25 from cold load — heading in view, Tab 1 = skip link, Tab 2 = ‹, Tab 3 = the date button, Tab 4 = ›, Tab 5 = History (44 px), then lessons; a pull with the ladder announced; /experiences/explore → type "phys" → skeleton → results announced → abort → banner + Try again → landing; /experiences/compose → chooser (skeleton → rows) → tap → Editor focused; /api/me down → shell + banner + retry landing; Settings → Delete account → row and body say the same thing.
- States checklist (empty, loading, error, success, focus, disabled/pending, selected) per screen incl. Find mode, the chooser, the shell and offline; the compact-height column (today / control day / sparse day at 320×568 / 360×620 / 390×620: heading y, bar y, block 1 y, nav vs canvas foot, Tab 1); the announcement column for every status (feed per tab / load-more / error / tab change; Find results; pull stages; reaction); the pointer column (coarse phone / fine ≤700 px window / desktop: History and Sync now reachable?).
- Migration path for users currently on the old design: none for routes or data; no sw.js change is required — if sw.js is edited, re-run r10-wf-sw1.js mode real and r10-wf-sw3.js.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically: Find mode and the chooser show all three states; the shell survives /api/me failure and offline; Tab 1 = skip link 9/9 compact cells; heading in view 9/9; nav clears the canvas foot 13/13; now-line crosses 0 labels; one day string on Home and Timetable; three triggers disclosed; feed status "" on error; Explore listing kept under a directory failure; direct "Report"; ≤6 dangling comments; 0 dead rules; the log removed; the gate passes `.btn,.btn:hover{}`; CLS 0 with a delayed hero; and every Preserve check above still green — measured on a build that does NOT change again until the audit reports.

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the width-query hide, the scrollIntoView landing, the CSS-only ring offset and the keyless useApi call must be replaced by their contracts, not patched on the cell that was measured.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new chrome, gestures, gradients or type faces; the phone Timetable and the hierarchy are settled; do not add captions or utility buttons back on phones.
- Treating the Preserve list as optional — run every regression check named above after any CSS, focus, landing, data-layer or nav change; in particular the rest-state outline census, the sheet census, the retry-landing sweep in both outcomes, the dialog-exit sweep, the compact-height sweep on three dates, the pull-ladder probe with POST /api/sync intercepted, the nav-truth probe, and the idle census during a lesson.
- Shipping a surface without its siblings' contracts — Find mode and the chooser reproduced, on the same day the r9 handoff landed, exactly the missing-states defect that handoff closed on Compose-?lessonId=. Before adding any data-bound view, enumerate the states contract and measure each branch.
- Restyling a measured surface while it is being measured — ten deployments landed during r10; three of them undid r9 fixes (the one-formatter fix, the bottom room, the heading in view). Freeze deploys for the duration of the re-audit, or tell the auditor the commit to pin.
- Fixing the instance, not the class — the skip-link reset moved Tab 1 from a lesson block to the daynav arrow, one stop short of the contract; the CLS reservation reserved the boxes that were named rather than the boxes that move; the gate rejected the spelling it was shown and now rejects a legal one. State the invariant ("Tab 1 is the skip link"; "no box on Home resolves late"; "a stateless member beside a stateful one with a different base") and measure the invariant.
- Claiming a change in the commit message that the deployed app does not show — verify each move on the deployed app in every state before reporting it done.
````
