# 04 — /make-plan handoff (r8)

````
/make-plan Redesign the HOney web app presentation layer (student PWA at https://honey.gaelisus.com, source apps/web/src on branch integration/product-v2, audited at commit 13cbcee). Current design failed audit at 15/30 with critical gaps in principles #3 aesthetic (0/3), #4 understandable (1/3), #5 unobtrusive (1/3), #8 thorough (1/3) and #10 as little design as possible (1/3); no load-bearing principle scored 0.

Verdict paragraph (quoted from 03-verdict.md):
> The r7 handoff verified as built almost everywhere it aimed — the pending reaction dims to 0.7 on both boots and keeps focus through one POST; `ReconnectDialog` renders one four-word label for both purposes with the caveat in both bodies; `p.caption.timetable-note` finally computes its margin (12/8 px) so the note clears the canvas by 8 px and touches no hour label; "Back to Experiences" is a 170×44 pill on `/why` as on Explore and the touch census reads 162/162 ≥44 at 390 and 320; the compact-height landing is one instant step inside a rAF, fires on retry 0 of 24 times, survives SPA re-entry, and with the daynav static at ≤620 px heights the overlap census reads 0/0/0 where r7 read 8/8/5 while every lesson block clears the fixed nav by 220–274 px and a 40-minute lesson no longer clips; the feed carries 0 `aria-live` and announces 13–14 characters on arrival instead of 1,120; the compose retry that fails twice lands on a ringed region by mouse and keyboard; `aria-describedby` resolves on 7/7 dialogs; the 404 has its own title matching its h1, an `id="main"` and a skip link; an impossible `?date=` is replaced by the day shown; the Timetable speaks one locale; the P1–P6 clause renders only where a P label does; the delete-data copy no longer claims to remove data it leaves; the login footnote names both import triggers and both data types; `Reveal`, `.reveal`, `.is-in`, `.option-grid span`, the unterminated comment and the duplicate `a` rule are gone with `tsc` at 0 and consumer-less classes at 0; a pull during an in-flight request now issues a new request; the entity page has an error branch; the plain chooser issues 1 data request; the origin sends `no-cache` for `sw.js`, `immutable` for `/assets/*` and `must-revalidate` for the rest; the seven privacy claims, the two-trigger disclosure and the service-worker battery re-verify with zero drift — but the same wave shipped two parser-level CSS breakages that no screen escapes: deleting `button, a { -webkit-tap-highlight-color }` left `button,` glued to the `:focus-visible` selector list, so every `<button>` in HOney now paints the 3 px accent focus ring at rest (24 of 111 Tab stops ringed with nothing focused; 37,094 accent pixels on an untouched Timetable; rings colliding in the theme grid and between lesson blocks; disabled buttons ringed; focus no longer distinguishable from rest on any button), and a nested `@media` opened inside `.modal{}` orphaned the phone sheet's radius, borders, safe-area padding and `sheet-up` entrance, so every mobile dialog is a four-cornered, four-bordered desktop card with `rise-in` flush to the viewport bottom while the compose safety sheet still slides up as a sheet; and the wave's own new code reproduced the defect it was written to cure — the entity page's registry-error retry arms `useRetryFocus` on the wrong loading flag and drops focus to `<body>` on a second failure, hides a feed that loaded and offers "Share your experience" beside "Could not reach the HOney server"; `reload()` evicts the in-flight promise but the superseded fetch still writes its stale body into the cache last, so the next SPA entry silently shows pre-refresh data; the `?date=` correction runs once on mount and the address bar lies after any stepper tap including "Back to today"; the static daynav removed the overlap by scrolling itself, the heading and the note off-screen on arrival, and a date step never lands at all; the status sibling says "1 experiences", "0 experiences" over "Share the first one" and "Could not load" over a different visible sentence; both reaction pills dim and announce disabled when one is pending; the 404's "Skip to content" skips to its only control; the one-locale fix follows the browser while all copy stays English ("No lessons on 9月6日周日" under `zh-CN`) and Home still hard-codes `en-GB` — so HOney falls from 19 to 15/30: #3 drops to 0 on active visual noise, #4, #5 and #10 each lose a point to the same two breakages and the new contradictions, #8 stays at 1 with two state families missing on their worst instance, #1, #2, #6, #7 and #9 hold, and the audit reports the number it measured.

Why redesign and not refine: total 15/30 is below the REFINE threshold of 20 and #3 scored 0; two ties (#3 0↔1, #10 1↔2) were settled downward and granting both gives 17, still REDESIGN — the verdict does not turn on a tie. This prompt is therefore the REDESIGN template even though the two largest failures are one-line CSS repairs and every move below is a finish-the-detail move on a layer whose tokens, type, colour, spacing, motion budget, weight, trust architecture and service worker verify live for the sixth round running.

Context the next session needs (it has no access to the audit): HOney is a high-school student PWA (primary viewport 390×844, must hold to 320×568; also checked at 320×600, 360×620, 390×620, 430×932 and 1280×800). Primary tasks: check today's timetable / next lesson; read the Experiences stream; share an experience. Constraints: quiet-humanist language — single font Source Sans 3, narrow cool palette, ONE muted blue-teal accent #33667c (night #8fc2d4), no warm tones except semantic danger/ok; no small ALL-CAPS titles; every finite option set fully displayed (search only filters); honest privacy copy that never overclaims anonymity; iOS wire compatibility — propose NO API changes. Owner's product decisions, not up for re-litigation: (1) timetable import is part of the account — no consent gate, no switch; it is disclosed (Settings "Timetable import" row + login footnote), the disclosure is accurate (exactly the two `syncTimetable` callers that exist, both data types, both triggers), and the plan must keep it so; (2) the 620 px timetable canvas density on normal-height screens stays; the compact-height notches (540 px at ≤620 px height, 450 px at ≤360 px width) and the static daynav at ≤620 px heights are the deliberate degradation — fix how the degradation behaves (a visible day name on arrival, a consistent landing contract, the 3 px shortfall at 390×620), not whether it exists. Verify everything in a real browser against the deployed app by computed style and geometry, never by reading the diff. The audit harness at /root/claude-work/design-audit/ (playwright; `lib.js` exports `authedPage({viewport, desktop, reducedMotion})` returning `{browser, context, page}`; probe scripts `r8-{struct,vis,copy,wf,a11y}-*.js` and `r8-orch-check.js` with `.log` files and `r8-*.png` screenshots; `matrix.js` = 9-device regression) can be re-run to confirm each fix — `r8-orch-check.js` alone reproduces both CSS breakages in under a minute. IMPORTANT: the harness runs in UTC, where this account's lessons fall outside the 09:00–20:00 timeline window and the Timetable always looks empty — build contexts by hand with `timezoneId: "Asia/Shanghai"` (see `r8-struct-timetable.js`, `r8-vis-compact.js`, `r8-a11y-compact.js`) whenever a measurement involves lesson blocks; in-page "today" during r8 was 2026-09-02 (3 lessons), 2026-08-24 is a 3-lesson control day, 2026-08-22 and 2026-09-06 are empty days. The harness's cached access token expires after ~30 minutes while `lib.js` only checks the refresh expiry — if a probe lands on /login, delete `.session.json` and re-run. Never read /home/honey/.secrets/, never print tokens, never publish a post or submit a report while verifying; a reaction tap is allowed only if undone in the same script. The test account has 0 ownership keys and a connected, valid portal token — seed `HOney.experiences.keys` / `honey.portal.cred` sentinels in localStorage to reach Mine's populated/error branches and the Save-login UI; rewrite the `/api/me` body to `portalTokenValid:false` to reach the Reconnect branch; the real feed holds one post, so fulfil `/api/experiences/feed` with `{items, nextCursor, headCursor}` to reach load-more and the ≥9-post state; abort `/api/entities` and, separately, `/api/directory` to reach the registry-error branches on Compose, Entity, Explore and Mine; delay `/api/experiences/*/react` 2 s to observe the pending reaction; delay `/api/entities` 3 s with distinguishable bodies and pull-to-refresh at ~1.9 s, then SPA-navigate away and back, to observe which body the cache serves.

Preserve from current design (MUST NOT be touched — these verified live on 13cbcee):
- Spacing system as it renders: 214 spacing declarations, 176 on `var(--sp-*)`, 2 literal-px lines (foundations.css:91 `margin:-1px`, features.css:1201 `padding-bottom:90px`); rendered rhythm at 320/390/430 = 97.6–97.8 % on the 4/8/12/16/20/24/32/44 ladder, desktop 92.6 %; `p.caption.timetable-note { margin-block: var(--sp-3) var(--sp-2) }` (features.css:226-229) computing 12/8 px at every viewport with 8 px note→canvas and 0 note×hour overlaps; `.timeline__empty { margin: var(--sp-2) 0 }`. Regression check: re-run `r8-vis-typespace.js` at 320/390/430/1280; on-ladder share ≥97 % mobile / ≥92 % desktop; `r8-vis-extra.js` note margin 12/8 and 0 note×hour pairs.
- Type ramp `--fs-micro…--fs-display` (tokens.css); live sizes {12,13,15,16,20,22,24,26,28} px at 390, floor 12, one fractional (27.95 at 430); 10 off-ramp declarations (11 with admin). Regression check: rendered-font-size census, nothing below 12 px, no new off-ramp declaration.
- Palette and accent discipline: 43 colour literals, `tokens.css` blob 1d70d963a91a5bfbeb17928b599185286ad8f836 byte-identical since fd522c3; one accent per surface; warm hues only on --danger/--ok; contrast floor 4.66 stone / 5.83 night on `.home-head__stale`, 0 enabled text below 4.5:1 on either boot (24 surfaces); focus rings 5.81–6.30 stone / 8.36–9.28 night against adjacent ground; disabled text 4.61–5.66 stone at `opacity 1` / `color: var(--muted)`; placeholders 5.34–5.63. Regression check: rerun `r8-a11y-contrast.js` / `r8-vis-contrast.js` on stone and on a fresh `colorScheme:dark` context with no stored theme; 0 enabled text below 4.5:1; every focus ring ≥3:1.
- Zero idle motion: `document.getAnimations().length === 0` at 2.5 s on 22/22 screen × preference cells; `infinite` keyframes only on the pull spinner and the skeleton; 6 `@keyframes` (`fade-in, ptr-rot, rise-in, settle, sheet-up, skeleton-shimmer`); `.view` ONE rule carrying `settle .46s var(--ease) backwards`; prefers-reduced-motion collapses durations to 1e-06 s and `scroll-behavior` to `auto`; CLS 0 on cold /timetable, /home, /experiences in both timezones at 390×844, 360×620 and 320×568, on tab change, load-more, dialog open and the compact landing. Regression check: `r8-wf-anim.js`, `r8-wf-cls.js`; `grep -n '\.view' apps/web/src/styles/*.css` returns one `.view {` block.
- Weight: app JS 86,192 B br / 88,733 B zstd / 275,493 raw, CSS 9,292 br / 40,849 raw, Dash chunk 4,406 br loaded on 0/14 student loads; 12 requests cold /home (3 data), 3 warm to /experiences (1,803 B), plain /compose 10 requests / 1 data; no new dependency; exactly one `GET /api/entities` per view that needs it and 0 duplicate API paths on 14 cold views; 5 pulls in 923 ms → 1 refresh round. Regression check: curl br sizes under 100 KB; DashPage never fetched on student routes; `r8-wf-net.js` counts unchanged; `r8-wf-inflight.js` dupes `[]`.
- Service worker as it is (public/sw.js, 4,828 B, md5 f7513f02194664efa4e96d50773ec83d, cache `honey-v3`): `liveAssetSet` resolving Vite's relative chunk imports; `activate` → `evictStaleAssets()`; `/__index-sig` persisted and compared whole; content-type guard refusing 200-HTML under /assets/; `/api/` never cached; navigations network-first; one `/` shell. Verified r8: 10/10 current kept, 5/5 stale evicted, `honey-v2` deleted, 0 B refetched after `ServiceWorker.stopWorker` ×3, JS-only and CSS-only mismatches evict, exact match does no work, cache 5 entries / 347,347 B after 10 hard loads, offline shell 71–88 ms. Regression check: re-run `r8-wf-sw1.js` (mode real), `r8-wf-sw3.js`, `r8-wf-sw4.js` after any sw.js change (note the signature sort order changes with every build — update the literal); keep the cache name unless a deliberate flush is intended.
- Backend static headers at the origin (packages/backend/src/app.ts:99-108): `sw.js` `no-cache`, `/assets/*` `public, max-age=31536000, immutable`, everything else `public, max-age=0, must-revalidate` — verified by curling `127.0.0.1:8871` directly. Keep; the edge rewrite (below) is outside this repo.
- Boot script honouring prefers-color-scheme pre-FCP in six contexts with stored-choice-wins, 0 later `<html>` mutations, and theme-color rewritten to the surface colour before first paint (index.html:14-33).
- Modal semantics: 7/7 dialogs `role=dialog`, `aria-modal`, named from the h2, `aria-describedby` resolving (`lesson-dialog-body` TimetablePage.tsx:468-469, `appearance-dialog-body` ThemeControls.tsx:37,46, `report-dialog-body`, `school-dialog-body`, `confirm-dialog-body` Modal.tsx:94), focus trap 0/7 escapes, Esc + Cancel/× restore the opener 7/7, nav beneath not hit-testable 7/7, every dialog button 44 px, overlay = viewport; `.modal { max-height: 100dvh }` at ≤620 px heights is the ONE intended declaration of the nested block — keep it, as a sibling rule (see Discard); `ReconnectDialog` one-line label "Stay connected on this device." for both purposes with the caveat in both bodies (ReconnectDialog.tsx:25-51), `submitLabel` per purpose, checkbox before the submit, checked by default when saving (a device-convenience choice restating the row the user pressed, described by the body — not pre-ticked consent).
- Focus system: 3 px accent outline offset 3 on `:focus-visible` (foundations.css:66-74 — keep the rule, remove the bare `button,` from its selector list, see Discard); `useRetryFocus` (lib/useRetryFocus.ts:9-21) arming only from the "Try again" handlers, setting `data-landed` on the element it focuses, clearing it on `blur` `{once:true}`, focusing with `{preventScroll:true}` — landings never focus on a normal load, never in the Tab order, scroll 0 px on retry (24/24 incl. the Timetable at 320×568 in Asia/Shanghai), ring 0/16 on ordinary mouse and touch taps; `.focus-landing[data-landed]:focus`; named landing regions "Now and next" (HomePage.tsx:66), "Day timeline" (TimetablePage.tsx), "Lessons" (HistoryPage.tsx), `group` "Everything listed" (ExplorePage.tsx), "Your notes & posts" (MinePage.tsx), "<name> experiences" (EntityPage.tsx:87, in both branches), "Editor" on the compose editor (ComposePage.tsx:306 — keep here, rename the error branch, see Discard); the compose error branch's `section.focus-landing` (ComposePage.tsx:214-233) — keep the landing, rename it; `#main[tabIndex=-1].main` with `.main:focus { outline: 3px solid var(--accent); outline-offset: -3px }` — keep the ring, stop it drawing over content (see Discard); the `···` guard, `aria-haspopup="menu"`, `aria-controls`, `aria-expanded`, Escape → trigger, Tab-out close, ArrowUp/Down; reactions `aria-disabled={busy || undefined}` with the `if (busy) return` guard (ExperiencePost.tsx:162,226,238) and `.react-btn[aria-disabled="true"] { opacity: .7; cursor: default }` + `.react-btn:hover:not([aria-disabled="true"])` (components.css:247-256) — keep the style, scope the attribute to the tapped pill (see Discard).
- Timetable: `.timeline__empty` `<p role="status">` "No lessons today" / "No lessons on <day>" only on genuinely empty days; the `?date=` real-calendar guard plus the mount-time `history.replaceState` (TimetablePage.tsx:41-47) — keep, extend to every `setDate` (see Discard); `formatStepperDate` in the same locale as `formatDayHeading` (TimetablePage.tsx:22-27, `undefined`) — keep the single source, fix Home to match and decide the language (see Discard); the P1–P6 clause conditional on lessons (TimetablePage.tsx:170-173); `.timeline { min-height: 620px }` with the 540 / 450 notches (features.css:759-790) — owner's decision, keep; `.daynav { position: static }` at ≤620 px heights (features.css:770-778) — owner's decision, keep; `.lesson-block { scroll-margin-top: var(--sp-2) }` (features.css:355) — adjust per Discard; `.lesson-block--compact { min-height: 36px }`; the cold-landing gate `settledDates` + `coldLanding` + `land` prop + `requestAnimationFrame(() => el.scrollIntoView({ block: "start", behavior: "instant" }))` (TimetablePage.tsx:56-62,205-210,324-336) — keep instant, keep 0 px on retry, keep re-entry; make a date step obey the same contract (see Discard); `button.lesson-block[data-lesson]`; the lesson modal containing without scrolling at all six viewports; `hidden={pickerVisible}` on `.daynav__date` and the conditional `aria-label` on the hidden input; the "Sync now" clause shown only when a credential exists; all 7 daynav controls at compact heights with names and roles intact.
- Compose: the registry-pending skeleton; `useNames(!!effectiveEntityKey)` on the chooser (ComposePage.tsx:101) so plain /compose issues 1 data request; `useApi(…, effectiveEntityKey ? "entities" : undefined)` sharing the key with `useNames()`; the delisted branch "This entry is no longer listed." (shared.tsx:69 = EntityPage.tsx:106 = ComposePage.tsx:257) and the never-listed pair; `.compose-actions` two 44 px rows at ≤620 px heights; the `.nudge` safety sheet with `sheet-up` and safe-area padding (its grammar is the one the dialogs must return to).
- `useApi` (lib/useApi.ts): the in-flight map with `.finally` eviction; `invalidate()`/`clear()` dropping `inflight` and `reload()` deleting its key (lines 20, 24, 90) — keep; guard the cache write (see Discard); `useNames(enabled)` returning `error`/`loading`/`reload` (shared.tsx:160-186) — keep, consume on every caller (see Discard).
- Feed: `#feed-panel` / `div.feed-stream[role=tabpanel][tabindex=-1][aria-labelledby=<tab id>]` (named "Your classes" / "Around school"), the `role=alert` banner and the skeleton outside any live region, 0 `[aria-live]` on the page, the `p.sr-only[role=status]` sibling (FeedPage.tsx:159-161) 1×1 `clip: rect(0,0,0,0)`, 0 mutations on idle, 13–14 characters on arrival — keep the mechanism, fix the strings (see Discard); header Share suppressed on the true-empty feed; "Share the first one" as the single empty-feed action; `SHARE_PROMPT_EVERY = 8`.
- Entity: never-listed ids → h1 "Nothing is listed at this address." + "Find someone or something" 44 px; delisted ids → "This entry is no longer listed." + `.entity-survivor`; the new registry-error branch's `role=alert` + "Try again" 77×44 inside the named region (EntityPage.tsx:130-138) — keep the branch, fix its arming, order and copy (see Discard).
- Settings: "Reconnect" rendered only when `connection.connected && !connection.portalTokenValid`; the import row "On for every HOney account since 1 September 2026 — your timetable and history are imported from the school portal when your account is created, and again whenever you press Sync now. Delete them below at any time." (SettingsPage.tsx:200-204) — the two named triggers are the only two `syncTimetable` callers (packages/backend/src/routes/auth.ts:32, routes/data.ts:17); the delete-data row "Removes your imported lessons — your timetable and history. Shared teacher, course and room entries stay." and body (SettingsPage.tsx:210-212,288) — keep, add the lesson rows to the "what stays" list (see Discard); the theme picker `[aria-pressed="true"]` recipe; `.chip` before `.chip--*`; `.btn:disabled { opacity: 1; color: var(--muted); border-color: var(--line) }` (components.css:66-72) — keep the text recipe, unify the fill (see Discard).
- Login: `.login { height:100dvh; flex-direction:column; overflow-y:auto }`; `.login .btn` and `.login .input` `min-height: 44px`; `label.stay-connected { min-height: 44px }` (components.css:876) as the checkbox's hit area (342×131.5 / 272×151 on /login, 340×59 in the dialog; all corners hit the label); skip link "Skip to sign-in" → `#school-username`; title "Sign in · HOney"; footnote "There is no separate sign-up — your school account is your HOney account, created on first sign-in. Your timetable and history come along with it, and again whenever you press Sync now." (LoginPage.tsx:65-66); login checkbox unchecked by default.
- Titles: per-route `document.title` "<Name> · HOney" on all 10 AppLayout routes, entity titles, "Sign in · HOney", the 404 "Page not found · HOney" = h1 "Page not found" (NotFoundPage.tsx:6,14), `main#main[tabIndex=-1]` on the 404 (NotFoundPage.tsx:9) — keep, make the skip link use it (see Discard).
- PTR: idle disc unnamed inside `aria-hidden="true"`, `aria-label="Refreshing"` only while refreshing; 1 `GET` per pull, never `POST /api/sync`; `refreshingRef` + `MIN_SPIN_MS=650` absorbing pull spam.
- Explore: complete-set rule (four sections with counts at 390 and 320, "Food 0" rendered, search only filters); `aria-label="Filter by name"`; `role=group` "Everything listed"; its registry-error branch with a landing (ExplorePage.tsx:29-31,84) — keep, add the directory failure (see Discard).
- Touch floor: 162/162 ≥44 in a `hasTouch` context at 390×844 and 320×568 across 15 routes; the coarse-pointer rule `@media (pointer: coarse) { .btn--small, .react-btn, .usermenu__button { min-height: 44px } }`; `/why` "Back to Experiences" `a.btn.btn--ghost` 170×44 (WhyPage.tsx:101). Regression check: `r8-a11y-targets.js` sub-44 list stays empty.
- Clean build: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters -p tsconfig.json` = 0; 0 whole-head duplicate selectors; consumer-less classes 0 (adjusted); 0 unterminated comments; `lib/motion.tsx` exporting exactly `prefersReducedMotion, staggerStyle, useNowTick, Skeleton`.
- All honesty copy and the code it maps to: identity-free publish (api/client.ts:227-229,349-353), no author column (packages/backend/src/db/database.ts mig. 003), `ownership_hash` sha256, 6 = 6 = 6 report categories, "no free text is collected" ↔ `free_text_not_accepted` (routes/experiences.ts:208), coarse `publishedDay` (lib/format.ts:69-79), the HOney ID line, WhyPage "design boundary, not magic", contract.ts:12-16, the Save/Reconnect caveat matching lib/portalCredentials.ts:20-21,78-88,115-124, the ownership-key caveat "the key sits on this device too" (SettingsPage.tsx:255-259), the refreshed comments ReconnectDialog.tsx:14-18, TimetablePage.tsx:22, :107-109.

Discard (structural patterns causing the failures):
- A selector left behind when its rule was deleted: `foundations.css:64` — `5c0cec9` removed `button, a { -webkit-tap-highlight-color: transparent }` (moving the property onto `a` alone) and left `button,` on the line above the "Focus" comment, so the served rule is `button,button:focus-visible,a:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible,summary:focus-visible{outline:3px solid var(--accent);outline-offset:3px}`; every `<button>` computes `outline: solid 3px rgb(51,102,124)` (night `rgb(143,194,212)`) with nothing focused — Feed 5/5, Timetable 7–8/8, Settings 12/12, 24/111 Tab stops on the 10 primary routes; 21,876 accent pixels on an unfocused Settings, 37,094 on the Timetable; `restOutline === afterProgrammaticFocus` on the same element; adjacent rings collide in the 2×2 theme grid and between 61 px lesson blocks; disabled buttons wear the ring with no hover/focus delta; `<a class="btn">` is unaffected so "Sign out" (button) is ringed beside "Open Dash" (link) in one row; buttons also lost the tap-highlight suppression. Caused failure on principles #3 (0), #5, #4 and #8.
- A brace closed early to nest a media query: `components.css:814-829` — `13cbcee` inserted `}` + `@media (max-height: 620px) { .modal { max-height: 100dvh } …` after `max-height: 92dvh`, so `border-radius: 20px 20px 0 0`, `border-left/right/bottom: none`, `padding: var(--sp-5) var(--sp-4) calc(var(--sp-6) + env(safe-area-inset-bottom))` and `animation: sheet-up .42s var(--ease) both` are bare declarations inside an at-rule (209 dead bytes in the served CSS; `git show 213707d:apps/web/src/styles/components.css` has all six inside `.modal{}`); on 18/18 mobile opens every dialog computes `border-radius 20px` on all four corners, 1 px borders on all four sides, `padding 24px`, `animation-name rise-in`, flush to `bottom = innerHeight` with no safe-area term; Reconnect at 320×568 fills `[0,0,320,568]` with four radii and four borders and still overflows 6 px (572/566); `@keyframes sheet-up` has one live consumer (`.nudge`), so the compose safety sheet and the dialogs are two sheet grammars on one phone. Caused failure on principles #3 and #8.
- A retry wired to the wrong loading flag, copied from the fix it was modelled on: `EntityPage.tsx:48` `landing = useRetryFocus(feed.loading)` while the new registry-error "Try again" (`:133`) calls `reloadNames()`, which toggles `directory/entities.loading`, never `feed.loading`; a second failure leaves `activeElement=(body)`, `data-landed=null`, 3 ring px, and `armed.current` stuck `true` so a later unrelated feed settle would fire a spurious landing (6/7 retry surfaces land; this one does not). In the same branch `namesError` is the first ternary arm (`:130`), so a registry outage hides a feed that returned 200 (`posts=0`), while `listed = !entities || …` (`:54`) keeps "Share your experience" (`:95-102`) and the present-tense intro (`:126-128`) on screen beside "Could not reach the HOney server". `useNames().error/loading/reload` (shared.tsx:167-178) is consumed by 1 of 3 callers: `MinePage.tsx:49` and `ComposePage.tsx:101` destructure `{ names }` only, so Mine renders success with the registry down and Explore ignores a `directory` failure. `ComposePage.tsx:214-219` names the error-branch region "Editor" (contains only the banner) — the same name as the real editor at `:306`. Caused failure on principles #8, #2 and #4.
- A cache write above its own cancellation guard: `lib/useApi.ts:67-70` — `cache.set(key, data)` runs one line above `if (!cancelled)`, so when a refresh replaces an in-flight request the request that finishes LAST writes the cache; with the replaced request slower (the realistic case) the pre-refresh body is cached, and the next SPA entry renders it with `loading=false`, no skeleton, no request. The r8 `inflight.delete` (lines 20/24/90) evicts the promise but nothing cancels the old fetch's write. Caused failure on principle #2 (a refresh that does not stick) and #8.
- A URL correction scoped to mount: `TimetablePage.tsx:41-47` replaces an impossible `?date=` once; `setDate` (Next, Previous, Back to today, the picker) never writes back, so from `/timetable?date=2026-08-24` "Back to today" shows "Wednesday, September 2, 2026" over `?date=2026-08-24` and a reload of that URL shows August 24 — the invariant the fix was written for holds only for the first paint. Caused failure on principle #4.
- A landing contract the effect does not implement, and an arrival with no day name: the comment at `TimetablePage.tsx:56-57` says "the first settle (success OR error) of a date is its cold landing", but `DayTimeline`'s effect runs on `[date, visible.length]` and `useApi` holds the previous day's lessons while a stepped-to date loads, so `visible.length` does not change and the effect fires only on the loading render where `land` is false — a date step never lands (4/4 steps to a never-seen 3-lesson day → `scrollTop` 0) while cold load, cached reload and SPA re-entry do. With `.daynav { position: static }` and `.lesson-block { scroll-margin-top: var(--sp-2) }` (features.css:355,770-778), the landing parks the daynav at −140 / −192 / −198 px, `h1.schedule-header` off-screen on 5/6 compact rows and `.timetable-note` on 4/6 — r7's eight overlaps were removed by removing the header from the viewport, and the r7 deliverable "with the day heading visible on arrival" is unmet; at 320×568 and 360×620 the landing is the document's maximum scroll; at 390×620 block 1 lands at y=−6 (`scrollTop` 225 of a 228 maximum). Caused failure on principles #4 and #8 (and #5: motion that hides the chrome the student needs next).
- A status string with no plural and two channels that disagree: `FeedPage.tsx:159-161` `` `${feed.items.length} experiences` `` renders "1 experiences" on the real feed and "0 experiences" over the visible "Nothing has been shared yet… Share the first one"; on error the status says "Could not load" while the `role=alert` says "Could not reach the HOney server. Check your connection and try again." — one event announced twice in two wordings; load-more announces the cumulative total ("24 experiences"), tab change announces twice (emptied, refilled) with no tab name; three unnamed `role=status` nodes co-exist during load. Caused failure on principles #4 and #10.
- A pending state applied to both pills: `ExperiencePost.tsx:226,238` set `aria-disabled={busy || undefined}` on both reactions from one `busy`, so during 👍's 2 s POST 👎 also computes `aria-disabled="true"`, `opacity 0.7`, `cursor default` — assistive technology hears both disabled and sight sees both dim; a second activation inside the window is accepted (`pointer-events: auto`), produces no POST and changes nothing measurable (`if (busy) return`, `:162`). Caused failure on principles #4 and #8.
- A skip link that skips nothing, on a page outside the shell: `NotFoundPage.tsx:9-17` renders `<a class="skip-link" href="#not-found-home">Skip to content</a>` inside `main#main[tabindex=-1]` and targets the "Go home" link — the page's only other focusable element and already the second Tab stop; the same commit added `id="main"` and did not use it; the page renders outside `AppLayout` with 0 `nav` and 1 unnamed landmark. Caused failure on principles #4 and #10.
- A submit label that contradicts an unticked box: `ReconnectDialog.tsx:40` `submitLabel="Save login"` while `:53-55` runs `portalCredentials.clear()` when `stayConnected` is false — the Save dialog opens ticked; untick it and "Save login" deletes any stored credential (read from source; not pressed). Caused failure on principle #4 (and #6 as a label wider than the code).
- A locale moved, not unified, and a language that changes with the date: `TimetablePage.tsx:26` now `toLocaleDateString(undefined, …)` while `HomePage.tsx:43,47` still hard-codes `"en-GB"` — Home "Wednesday 2 September" beside Timetable "Wednesday, September 2, 2026" on the same day; under `zh-CN` (a Chinese school; every directory name is Chinese) the empty-day sentence renders "No lessons on 9月6日周日" inside an English frame. Caused failure on principle #4.
- Copy one clause short of the code: the delete-data row and body name `teachers`/`courses`/`rooms` as what stays but omit the shared `lesson_instances` rows that also survive (`services/accounts.ts:238-240` deletes only `user_lesson_exposures`; `importer.ts:100`, `db/database.ts` mig. 002 have no `honey_id`); the delete-account row "everything imported with it" (`SettingsPage.tsx:96-98`) and its confirm body (`:309`) carry no "what stays" clause at all (the two delete flows are now asymmetric); four storage-caveat sentences say "encrypted on this device" without saying the key shares the store (`portalCredentials.ts:10-16,20-21,115-124`), which the notes bullet (`SettingsPage.tsx:255-259`) does say; two provenance registers (`shared.tsx:15-19` "Verified — has taken this over time" vs `ExperiencePost.tsx:18-22` "from someone who has taken this over time"); the login checkbox's accessible name is the whole 199-character label vs 30 in the dialog; the empty-day note begins with a space; `packages/backend/src/app.ts:104-106` asserts "The service worker must be re-checked on every load" while the wire delivers `max-age=14400`. Caused failure on principles #6 and #4.
- Residue unchanged for three rounds: no dialog sets `inert`/`aria-hidden` on `.app-frame` (0/7, Modal.tsx); the `#main` inset ring band draws over content rows on Settings, History, Explore, Why and Dash (5/12); the report dialog's `describedBy` id exists only on the form branch (`ExperiencePost.tsx:321` vs `:336`) and dangles after submit; the lesson dialog's `aria-describedby` resolves to its whole unpunctuated `<dl>` ("Time09:00–10:30TopicIELTS-Speaking…") read twice; the `···` menu wraps the single item "Report" with no name; two disabled fills in the compose row (`.btn--primary:disabled` grey fill vs transparent ghost, components.css:85-90); no keyboard equivalent to pull-to-refresh anywhere; Enter on a History "Select" row lands the composer with `activeElement=(body)`; the `.swatch` literals; two classless `<div>`s and a `null` ternary arm around every feed post (FeedPage.tsx:155-164); dangling trailing comments 9 (three new at components.css:820,876, features.css:80); `.react-btn:disabled` now unstyled (latent). Caused failure on principle #8 (and #3, #10).
- Relay, anchor #9 met: the public edge (hostd gateway / Cloudflare, outside this repo) rewrites `sw.js` from the origin's `no-cache` to `max-age=14400` and `/icon-192.png`/`/favicon-h.png` to `max-age=14400, must-revalidate` (uniform on every edge-cached file with origin TTL <4 h), strips the ETag on `/` (`If-None-Match` → 200), leaves `/assets/*` immutable and `/` DYNAMIC; the SWR `cache` Map grows ≈106 KB of heap per date visited, unbounded per session (+2.23 MB over 20 steps); 123,968 B of never-requested font subsets in `dist` and 1,201 B admin CSS in the student payload; `liveAssetSet` still trusts cached bodies; 11/25 cold loads carry a 51–110 ms boot task; `/home` shifts 0.044 once under 5-way CPU contention (slow-data hazard on `home-voices`/`home-foot`).
- Latent same-screen duplicate (code only, 1 post live): header Share + the interleaved "Share an experience" invite co-render at ≥9 posts (`SHARE_PROMPT_EVERY = 8`); a fulfilled 12-item page with `nextCursor` renders no load-more control in the harness.

Top 4 moves from the audit (verbatim):
1. #3 aesthetic + #5 unobtrusive + #4 understandable + #8 thorough — Repair the two parser-level CSS breakages and gate the build so a dropped selector or brace cannot ship again. (a) `foundations.css:64`: delete the dangling `button,` before the `:focus-visible` list and restore `button, a { -webkit-tap-highlight-color: transparent }` as its own rule (buttons lost the suppression too); verify by computed style that 0 `<button>` elements have `outline-style != none` at rest on 10 routes × 320/390/1280 × both boots, that a Tab stop's outline differs from its rest outline on 111/111 stops, and that edge-band accent pixels before any interaction are 0/0/0/0 as r7 measured. (b) `components.css:814-829`: close the nested `@media (max-height:620px){ .modal{max-height:100dvh} }` as a sibling rule AFTER the complete `.modal{…}` sheet block so `border-radius: 20px 20px 0 0`, `border-left/right/bottom: none`, `padding: var(--sp-5) var(--sp-4) calc(var(--sp-6) + env(safe-area-inset-bottom))` and `animation: sheet-up` are back inside `.modal{}`; verify at 320×568, 360×620, 390×844 on all seven dialogs that `.modal` computes `border-radius 20px 20px 0 0`, four `border-*-style none` except top, `animation-name sheet-up`, `padding` carrying the safe-area term, and that the served CSS contains 0 declarations outside a selector block. (c) Add a served-CSS parse gate to the build (stylelint `no-invalid-position-at-import-rule`/`block-no-empty` plus a script that parses `dist/assets/*.css` with a real CSS parser and fails on any orphan declaration, any selector list containing a bare element selector alongside pseudo-class selectors, and any in-context duplicate selector) so a brace or a selector cannot regress silently. Evidence: [O-R8-1][O-R8-2][S-R8-14][S-R8-15][V-R8-8][V-R8-9][V-R8-11][A-R8-18][A-R8-19][A-R8-30][W-R8-16].
2. #8 thorough + #2 useful — Finish every sibling of the r7 fixes by the same contract, and verify the contract, not the branch. Entity (`EntityPage.tsx:48,130-138`): arm the landing on the flag the retry toggles (`useRetryFocus(feed.loading || namesLoading)` or arm/settle on `useNames().loading`) so a second registry failure lands on the named region with a ring (today `<body>`, `armed` stuck true); render the names error as a banner ABOVE a loaded feed instead of instead of it, and suppress "Share your experience" and the present-tense intro while `entities === null`; consume `useNames().error/loading/reload` on Mine and on Explore's directory failure exactly as on Entity (today 1/3 callers); name the compose error region for what it holds ("Couldn't load", not "Editor") so one name maps to one content. `useApi.ts:67-70`: move `cache.set` below the `cancelled`/superseded check (or stamp each run with a generation and write only if it is the latest) so the refreshed body always wins the cache — today the request the refresh replaced writes last and the next SPA entry serves pre-refresh data with `loading=false`. Reactions (`ExperiencePost.tsx:226,238`): set `aria-disabled` and the dim only on the pill whose POST is in flight, and give the swallowed second tap a visible or announced response (or `pointer-events:none` while pending). Dialogs: `inert` on `.app-frame` while any of the 7 is open (0/7 for three rounds); the report dialog's `describedBy` only on the branch that renders the id; the lesson dialog's description a punctuated summary (or `describedBy` dropped) rather than the unpunctuated `<dl>` read twice; the `#main` ring band off content on Settings/History/Explore/Why/Dash (5/12 for three rounds). Evidence: [S-R8-17b][S-R8-18b][S-R8-4][S-R8-19b][S-R8-27][C-R8-N1][C-R8-N2][A-R8-22][W-R8-15][A-R8-20][A-R8-2][V-R8-12][A-R8-4][A-R8-6][A-R8-26][A-R8-13].
3. #4 understandable + #8 thorough — Make the Timetable's URL, landing and day name agree at every moment. Write `?date=` on every `setDate` (Next, Previous, Back to today, the picker) with `history.replaceState`, not only on mount, so the address bar names the day shown (today `?date=2026-08-24` over "Wednesday, September 2, 2026" after "Back to today"); make the landing contract one thing — either every first settle of a date lands (cold load, re-entry AND a date step; today a step never lands because `visible.length` does not change while the previous day's lessons are held during loading) or only cold loads do — and state it in the comment that today claims the former; fix the 3 px shortfall that parks block 1 at y=−6 at 390×620 (the 540 canvas + `scroll-margin-top` vs a 848 px document); and give the compact arrival a visible day name — the owner's static daynav stays, but the r7 deliverable "with the day heading visible on arrival" is unmet on 5/6 rows: land with `scroll-margin-top` equal to the heading + note height (≈60–70 px) so `h1.schedule-header` and the note sit above the first block while the daynav scrolls away, or keep a one-line day label in view at ≤620 heights; measure `h1` in-view on 6/6 compact rows, daynav reachable by one upward swipe, and 0 overlaps. Evidence: [S-R8-16][W-R8-17][V-R8-15][V-R8-7][W-R8-6][W-R8-20][S-R8-25][V-R8-10][A-R8-28][W-R8-18].
4. #4 understandable + #6 honest + #10 as little design — One truth per channel, and no element that does nothing. Feed status (`FeedPage.tsx:159-161`): pluralise ("1 experience"), make the empty and error texts match the visible sentence or say nothing on error (the `role=alert` already speaks — today an error is announced twice), and announce "N more" on load-more rather than the cumulative total. 404 (`NotFoundPage.tsx:9-17`): point "Skip to content" at `#main` or drop the link (today it skips to the page's only control), and render the page inside the shell so it has the nav (0 nav, 1 landmark today). `ReconnectDialog.tsx:40,53-55`: the submit label must not read "Save login" when unticking makes it clear the login — bind the label to the tick or split the action. Locale: one source for every date in the app (`HomePage.tsx:43,47` still `en-GB` beside the Timetable's browser locale) and one language per sentence (force `en` for dates while all copy is English, or localise the frame — today "No lessons on 9月6日周日"). Copy: delete-account confirm gets the "what stays" clause the delete-data body has, and both name the shared lesson rows (`lesson_instances`) that survive; the four storage caveats say the key shares the store, as the notes bullet already does; one provenance register (`shared.tsx:15-19` vs `ExperiencePost.tsx:18-22`); the login checkbox named by its first sentence with the caveat in `aria-describedby` (199-char name today); trim the empty-day note's leading space; the one-item `···` menu becomes a direct "Report" or gets a name; refresh the false `app.ts:104-106` comment to say the origin sends `no-cache` and the edge is outside this repo. Relay (anchor #9 met): edge `sw.js` `max-age=14400` (hostd gateway / Cloudflare, out of repo); cap the SWR map (≈106 KB per date, unbounded); 209 dead bytes go with move 1. Evidence: [C-R8-10][C-R8-N5][A-R8-21][W-R8-11][C-R8-N6][A-R8-24][A-R8-25][S-R8-23][C-R8-N8][C-R8-N3][C-R8-N4][C-R8-1][C-R8-2][C-R8-7][C-R8-8][A-R8-23][C-R8-N9][S-R8-18][C-R8-N7][W-R8-12][W-R8-35].

Redesign principles in priority order:
1. #3 aesthetic + #5 unobtrusive — success: 0 `<button>` elements compute an outline at rest on every route at 320/390/1280 on both boots; edge-band accent pixels before interaction 0/0/0/0; every mobile dialog computes `border-radius 20px 20px 0 0`, no side/bottom border, safe-area padding and `animation-name sheet-up` at 320×568 / 360×620 / 390×844; the served CSS parses with 0 orphan declarations and 0 bare element selectors in a pseudo-class list; one disabled fill; the Reconnect sheet does not scroll at 320×568; the rendered spacing census stays ≥97 % mobile / ≥92 % desktop with 43 colours and the type set unchanged; 0 idle animations; the compact landing stays one instant step.
2. #8 thorough — success: the entity retry that fails twice leaves `activeElement` on the named region with a ring, and `armed` is false after every settle; a names error renders above a loaded feed with no "Share your experience" while `entities === null`; Mine and Explore show the same banner under either registry failure; after a pull-to-refresh with the replaced request finishing last, the next SPA entry renders the refreshed body; only the tapped reaction pill is `aria-disabled`/dim and a second tap has a measurable response; `inert` true on `.app-frame` 7/7; `describedBy` resolves on the report dialog's done branch; the lesson description is punctuated or absent; the `#main` band overlaps 0 content rows on 12/12; `tsc` 0; 0 orphan declarations; dangling comments ≤6.
3. #4 understandable — success: `location.search` equals the day shown after every stepper action and on load; the landing contract in the code comment is the one the effect implements, and a date step behaves like a cold load or the comment says it does not; `h1.schedule-header` is in view on 6/6 compact rows after landing and block 1 is never above y=0; the feed status reads "1 experience" / matches the visible empty and error sentences or is silent on error; the 404 skip link lands on `#main` (or is gone) and the page has the shell nav; "Save login" never clears; Home and Timetable print one date grammar from one locale source, and no English sentence wraps a non-English date; one region name maps to one content; the login checkbox's name is one sentence.
4. #6 honest + #10 as little design — success: both delete flows carry a "what stays" clause naming the shared lesson rows; the four storage caveats say where the key lives; one provenance register; the false backend comment is corrected; the one-item menu is a direct action or a named menu; the error-branch status does not duplicate the alert; the header Share and the invite never co-render (or the duplicate is a decision recorded in the code); the import disclosure stays exactly as it is.
5. #2 useful — success: the three primary tasks keep their step counts (1/0, 1 tap, 4 screens / 3 taps); a refresh always fetches AND sticks; every lesson block hit-tests to itself and clears the nav at all six viewports on a 3-lesson day in Asia/Shanghai; a keyboard user arriving at the composer from History lands on a control, not `<body>`.
6. #9 environmentally friendly (keep at 3) — success: bundle under 100 KB wire (br and zstd), 0 idle animations, dark boot honoured pre-FCP, reduced-motion 25/25; the SW battery (`r8-wf-sw1/3/4.js`) still passes; the origin headers stay per file class; the SWR map is capped; 0 dead CSS bytes.

Deliverables for the plan:
- New information architecture (not derived from old): only where the discard list forces it — the served-CSS parse gate in the build; the entity/Mine/Explore registry-error contract (one banner, one arming flag, one order: error above content); the `useApi` write contract (latest generation wins the cache); the Timetable landing contract (stated once, implemented once, `?date=` written on every change); the feed announcement strings; the 404 inside the shell; everything else keeps the current IA.
- New primary flow (low-fi, labeled, compared side-by-side to current): a lessons day at 320×568 from cold load to tapping the 13:30 block with the day heading visible on arrival and no ring on any untouched button; Settings → "Save school login…" at 320×568 as a bottom sheet with a flat bottom edge and safe-area padding; Explore → pull-to-refresh during a slow request → tab away → tab back with the refreshed names; Entity with the registry down → "Try again" fails → focus on a ringed region, the loaded feed still visible; Feed → tap 👍 with 👎 unchanged → tap 👍 again with a response; `/timetable?date=2026-08-24` → Back to today → URL says today.
- States checklist (empty, loading, error, success, focus, disabled/pending, selected) per screen, with the focus column split into keyboard / pointer-retry / pointer-plain-tap / retry-that-fails-again / rest (every control's outline at rest must be `none`), the touch-target column listing every control's height in a `hasTouch` context, the pending column for BOTH reaction pills, the announcement column for every `role=status` (what text, when, and whether a `role=alert` speaks the same event), the sheet column for every dialog at 320/360/390 (radius, borders, padding, animation), and the compact-height column (scroll samples, overlaps, `h1` in view, block 1 ≥ y=0, daynav reachable) for the Timetable at 320×568 / 360×620 / 390×620.
- Migration path for users currently on the old design: none for routes or data; ship a `sw.js` byte change only if the SW is edited, and re-run the seeded-eviction battery with real bodies (`r8-wf-sw1.js` mode real) and the cold-start probe (`r8-wf-sw3.js`) before calling it done.
- Cutover criteria: a re-run of the design-is audit on the deployed commit scores ≥20 with no principle at 0 — specifically the live measurements above (0 buttons ringed at rest on 10 routes × 3 widths × 2 boots; every mobile dialog a sheet with `sheet-up`; served CSS parses clean; entity double-fail lands; refresh sticks in the cache; one pill pending; `inert` 7/7; `?date=` follows every step; `h1` in view on 6/6 compact rows; block 1 ≥ y=0 at 390×620; "1 experience"; 404 skip → `#main` with nav; "Save login" never clears; one date grammar app-wide; delete copy names the lesson rows; rendered spacing census ≥97 %; `getAnimations()` 0; contrast on a genuine dark boot; SW battery green; `tsc` 0; 0 orphan declarations).

Anti-patterns to guard against (specific to REDESIGN):
- Porting old structure under new styling — the dangling selector, the mis-nested media block, the wrong-flag arming, the unguarded cache write, the mount-only URL fix and the unpluralised status must be replaced, not wrapped.
- Keeping both designs behind a flag indefinitely.
- Redesigning to follow a trend rather than the principles above — no new motion, gradients or type faces; the palette, font, spacing ladder and type ramp are fixed and verified.
- Treating the Preserve list as optional — every Preserve item has a stated regression check; run them, especially the rest-state outline census (new this round), the mobile-sheet computed-style census on all seven dialogs, the touch-target census in a `hasTouch` context, the plain-tap ring test on all eight landings, the spacing census, the dark-boot contrast probe, the 320×568/360×620/390×620 timetable sweep in Asia/Shanghai with scroll sampling, the overlap census and `h1` in-view, the feed MutationObserver probe, the refresh-then-re-enter cache probe, and the SW seeded-eviction battery, after every CSS, focus, data-layer or SW change.
- Editing a CSS rule without re-reading its neighbours — the ring shipped because a deleted rule's first selector was left on the line above the next rule; the sheet died because a brace was closed to nest a media query. After ANY stylesheet edit, parse the served CSS and diff the computed style of the affected selectors on the deployed app; never trust the source diff or the commit message.
- Copying a fix to a sibling without copying its contract — the entity branch copied the compose landing's markup but not the flag it arms on; enumerate every `useRetryFocus` consumer and the loading flag its retry toggles before adding one.
- Evicting a promise without cancelling its write — `inflight.delete` is not a cancellation; guard every cache write with the generation that requested it.
- Fixing an invariant at one moment — a URL, a landing or a label that is correct on mount and wrong after one tap is not fixed; test every state transition, not the first paint.
- Removing an overlap by removing the content — if the heading must be visible on arrival, measure `h1` in view, not just "0 overlaps".
- Adding an announcement without deciding what it competes with — every `role=status` string must be checked against the `role=alert` and the visible copy on the same branch, at n = 0, 1 and many.
- Measuring the Timetable in UTC — the harness's default clock hides every lesson block; any measurement involving lessons must use `timezoneId:"Asia/Shanghai"`.
- Claiming a change in the commit message that the deployed app does not show — `13cbcee`'s message says "compact sheets use the full height"; the deployed sheets lost their skin. Verify each move on the deployed app before reporting it done.
````
