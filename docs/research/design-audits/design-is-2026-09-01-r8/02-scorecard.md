# 02 — Scorecard (r8; orchestrator-scored per skill anchors; tie-breaker: lower; score worst instance; no credit for anything that did not verify live on `13cbcee`)

1. Good design is innovative — Score: 2/3 (r7 2, Δ0)
   Evidence: [C-R8-25] identity-free publish, no-author-column storage, browser-held ownership keys and category-only reports re-verified with zero drift for the seventh round (`api/client.ts:227-229,349-353`, `db/database.ts` mig. 003, `routes/experiences.ts:208`); every pattern this round touched (a per-mount settled-dates gate, an in-flight map eviction, a `sr-only` status sibling, an `enabled` flag on a data hook, an `[aria-disabled]` pending style, per-file-class cache headers) is conventional.
   Justification: The trust architecture still "refreshes an existing pattern with a clear improvement"; nothing shipped is a pattern absent from 5+ peer products, so 2, not 3; nothing is copied wholesale, so not 1. No tie.

2. Good design makes a product useful — Score: 2/3 (r7 2, Δ0)
   Evidence: [S-R8-35] next lesson 1 screen / 0 taps; stream 1 tap; share 4 screens / 3 taps to the editor (+1 to a cursor) — unchanged. Every r7 blocker on this principle verified gone: [S-R8-36][A-R8-14] 18/18 blocks hit-test to themselves and clear the nav by 220–274 px at all six viewports (r7 −6 / −30); [A-R8-16][W-R8-7] retry moves 0 px on 24/24 landings incl. the Timetable at 320×568 (r7 77); [W-R8-1][S-R8-5] a pull during an in-flight request now issues a new request; [S-R8-2][W-R8-13] the chooser costs 1 data request. New detours on adjacent surfaces: [W-R8-15] the refreshed body loses the cache to the request it replaced, so the next SPA entry serves pre-refresh data with no skeleton; [S-R8-18b][C-R8-N2] a registry outage hides a feed that loaded and offers "Share your experience" beside the error; [S-R8-16][W-R8-17] the URL names a different day than the page after any stepper tap, so a copied link is the wrong day; [V-R8-10][A-R8-28] at compact heights the student arrives with no visible day name or stepper; [A-R8-22] the entity retry that fails twice drops focus to `<body>`.
   Justification: All three primary tasks complete on-screen with no wrong-screen detour (rules out 1); the chooser → History → editor path still adds steps, a cursor costs an extra tap, and a refresh can be silently undone by the cache, so not "fewest possible steps; no decoy actions" (rules out 3). No tie.

3. Good design is aesthetic — Score: 0/3 (r7 1, Δ−1) — tie 0↔1, settled lower
   Evidence: The system underneath holds: [V-R8-16] 97.6–97.8 % on-ladder mobile / 92.6 % desktop; [V-R8-18] 43 colours, `tokens.css` byte-identical; [V-R8-17] type unchanged; [V-R8-19] 0 genuine contrast failures; [S-R8-33] button-height families 10→9, the 22 px outlier gone; [V-R8-4] note gap 8 px, 0 note×hour overlaps; [V-R8-3] one label grammar in `ReconnectDialog`; [V-R8-5] one shape for "Back to Experiences". Rendered on `13cbcee`: [O-R8-1][V-R8-8][A-R8-18] every `<button>` on every surface carries a permanent 3 px accent ring — 21,876 accent pixels on an unfocused Settings, 37,094 on the Timetable, rings colliding in the 2×2 theme grid and between 61 px lesson blocks, "Sign out" ringed beside a bare "Open Dash" in one row, disabled buttons ringed [V-R8-11], the scope switch double-outlined and three ringed circles on the stream (`r8-orch-outline-feed.png`); [O-R8-2][V-R8-9] every mobile dialog is a fully-rounded, four-bordered desktop card with `rise-in` sitting flush to the viewport bottom, while the compose `.nudge` still slides up as a sheet — two sheet grammars on one phone; [V-R8-2] two disabled fills in one row; [V-R8-3] the Reconnect sheet scrolls 6 px at 320×568. Orphans: [V-R8-24][W-R8-16] six orphaned `.modal` declarations (209 dead bytes) and a `sheet-up` keyframe with no consumer in its intended block; [V-R8-13] `.react-btn:disabled` unstyled; [S-R8-34] dangling comments 8→9.
   Justification: Anchor 1 is "3–5 inconsistencies OR one jarring violation"; the permanent ring alone is the jarring violation, and the count of rendered inconsistencies (ring on buttons but not on `.btn` links, two sheet grammars, two disabled fills, a sheet that scrolls, rings on dead controls) exceeds five. Anchor 0 is "no visible system OR active visual noise": the system is visible, but 25 thick accent outlines per screen that mean nothing, colliding with each other and with the pressed and focused states they were designed for, are visual noise that is actively rendered on every surface before any interaction. Uncertain between 0 (noise) and 1 (violation); the tie-breaker settles it at 0.

4. Good design makes a product understandable — Score: 1/3 (r7 2, Δ−1)
   Evidence: Fixed and verified: [S-R8-7][C-R8-5] an impossible `?date=` no longer stays in the bar over today; [C-R8-5] one locale inside the Timetable; [S-R8-9] the P1–P6 note only where P labels render; [C-R8-4] the 404's title matches its h1 and differs from the never-listed entity's; [A-R8-9] the feed tabpanel is named; [A-R8-3] 7/7 dialogs described; [C-R8-8] one "delisted" sentence; [C-R8-3] the login footnote and the Settings row name the same triggers and data. Against, on `13cbcee`: [A-R8-18][A-R8-30] the accent ring now means "focused" on a link and "is a button" on a button — focus is indistinguishable from rest on 24/111 Tab stops, including both reaction pills; [A-R8-20][V-R8-12] when one reaction is pending both pills dim and announce disabled; [A-R8-2] a second tap is accepted and changes nothing; [C-R8-N8] the primary button "Save login" deletes the saved login when the box is unticked (`ReconnectDialog.tsx:40/55`); [C-R8-N6][A-R8-24] "Skip to content" on the 404 skips to the page's only control; [C-R8-N1][S-R8-27] a region named "Editor" that contains no editor, beside one that does; [C-R8-10][C-R8-N5] "1 experiences", "0 experiences" over "Share the first one", "Could not load" over a different visible sentence; [C-R8-N2] "Share your experience" and a present-tense intro rendered with "Could not reach the HOney server"; [S-R8-16] the address bar names yesterday after "Back to today"; [V-R8-10][A-R8-28] on a compact phone the student lands on blocks with no day named and the stepper 26–84 px above the fold; [C-R8-N3][C-R8-N4] "No lessons on 9月6日周日" under `zh-CN`, and Home in `en-GB` beside a Timetable in the browser locale; [C-R8-23] jargon / unclear labels 6, [C-R8-24] label→behaviour mismatches 4.
   Justification: Anchor 1 is "2–3 controls unclear; jargon present": three controls now contradict their own label or state — "Save login" that clears, "Skip to content" that skips nothing, and the reaction pair whose focus and pending channels no longer say which one is live — and six jargon/unclear labels are counted; that is past anchor 2's "1 control needs a tooltip". Every primary action remains identifiable without help, so not 0. No tie.

5. Good design is unobtrusive — Score: 1/3 (r7 2, Δ−1)
   Evidence: For: [W-R8-29] 0 dialogs / toasts / badges / alerts / live regions on load across 24 cells; [W-R8-28][V-R8-21] 0 idle animations on 22/22; [W-R8-30] CLS 0 in 18/18 cold combinations, on tab change, load-more and dialog open; [W-R8-11][A-R8-8] the feed announces 13–14 characters on arrival, not 1,120; [W-R8-4] the compact landing is one instant step, never on retry; [A-R8-17] 0 rings on 16/16 ordinary taps. Against: [O-R8-1][V-R8-8][A-R8-18] the accent ring — the design's loudest signal, reserved for focus and retry landings — is painted on every button on every screen before any interaction (37,094 accent pixels on the Timetable at rest; edge-band accent pixels top 3 / right 817 where r7 measured 0/0), competing with post text on the stream and with the lesson blocks on the canvas; [O-R8-2] mobile dialogs carry a desktop card's borders and radii against the viewport edge; [W-R8-5][W-R8-18] the compact landing still moves the viewport 156–216 px +79…+410 ms after first paint, on 6/6 compact rows, and scrolls the day nav off-screen; [W-R8-11] load-more announces a cumulative total, tab change announces twice with no tab name, and an error is announced twice (status + alert).
   Justification: Anchor 1 is "decoration competes with content": a permanent accent outline on ~25 controls per screen has no function and competes with the content on the stream, the canvas and Settings; the chrome does not dominate the content (post text and lesson blocks remain the figure), so not 0. Anchor 2's "chrome visible but quiet" is contradicted by the pixel counts. No tie.

6. Good design is honest — Score: 2/3 (r7 2, Δ0)
   Evidence: [C-R8-25] nine privacy claims re-mapped 1:1 with zero drift (no touched file among `api/client.ts`, `format.ts`, `db/database.ts`, `routes/experiences.ts`, `services/*`); [C-R8-3] the import disclosure now names both triggers and both data types at the point of decision and in Settings, and the two `syncTimetable` callers are still the only two; [C-R8-22] 0 dark patterns across seven categories (the Save-login pre-tick restates the row the user just pressed and is described by the dialog body); [C-R8-8] one "delisted" sentence; [C-R8-9] the three stale comments are true. Against: [C-R8-21] one degree-word inflation ("strongly protected", `WhyPage.tsx:73`); [C-R8-1] the delete-data copy's "what stays" list omits the shared `lesson_instances` rows it leaves behind — true at the visibility level, incomplete at the row level; [C-R8-2] the delete-account body still says nothing about what stays; [C-R8-7] four storage-caveat sentences say "encrypted" without saying the key shares the store, which the sibling notes bullet does say; [C-R8-N8] "Save login" can clear the login; [C-R8-N7] a new backend comment promises a header the wire does not carry (not user-facing).
   Justification: Anchor 1 requires "2+ inflations OR one dark pattern" — one inflation and zero dark patterns; the deletion and storage copy are imprecisions about what stays and where the key lives, not inflated value or deceptive flow, so they bar anchor 3's "every claim maps 1:1" rather than adding to the inflation count; 2, no tie.

7. Good design is long-lasting — Score: 2/3 (r7 2, Δ0)
   Evidence: [V-R8-17][V-R8-18] single humanist face, one accent per surface, 43 colours held for five commits, spacing 97.6 % tokenised; [V-R8-23] trend markers rendered: the per-route `settle` entrance and the floating, translucent (`blur(12px)`, `color(srgb … / 0.95)`) detached pill nav with a sliding indicator — one soft-app-shell signature, as in r1–r7 — plus pill radii, 17–20 px cards with one soft shadow recipe and `color-mix()` tints; 0 gradients outside the loading shimmer; largest mobile type 28 px. Durability facts from this wave: two rules shipped broken because a brace and a selector were edited without a parse check ([O-R8-1][O-R8-2]) and survived build, deploy and the commit message's claims; the r7 compose-landing defect was reproduced verbatim in the entity branch written to fix it [A-R8-22]; a cache write sits above its own cancellation guard [W-R8-15].
   Justification: The entrance choreography and the translucent floating shell are one dated signature, which matches anchor 2 exactly; the two broken rules are process facts, not trend markers; no skeuomorph residue or trend typography (bars 1); the signature bars 3. No tie.

8. Good design is thorough down to the last detail — Score: 1/3 (r7 1, Δ0)
   Evidence: Fixed and verified: [A-R8-1] pending visual; [A-R8-3] `describedBy` 7/7; [A-R8-7] compose double-fail lands; [A-R8-11] 162/162 ≥44 at both widths; [V-R8-4] note gap; [V-R8-6] 0 compact overlaps, no clipping, blocks clear the nav; [W-R8-4][W-R8-7] instant landing, 0 px on retry; [S-R8-11][S-R8-12] `Reveal`, `.option-grid span`, the unterminated comment gone; [S-R8-34] `tsc` 0, consumer-less classes 0; [W-R8-12] origin headers per file class. Worst instances on `13cbcee`: **focus** — [A-R8-18] indistinguishable from rest on every `<button>` (24/111 stops; Settings 11/17), [A-R8-22] the new entity retry drops focus to `<body>` with `armed` stuck true, [A-R8-31] History "Select" → composer lands on `<body>`, [A-R8-4] `inert` 0/7, [A-R8-13] the `#main` band over content 5/12, [A-R8-6] the report `describedBy` dangling after submit; **disabled/pending** — [V-R8-11] disabled buttons ringed with no hover/focus delta, [V-R8-2] two fills, [A-R8-20] the untapped pill dims and announces disabled, [A-R8-2] a second tap swallowed silently, [V-R8-13] `.react-btn:disabled` unstyled; **error** — [S-R8-4] Mine renders success with the registry down, Explore ignores a directory failure, [S-R8-18b][C-R8-N2] Entity hides a loaded feed and offers "Share your experience" beside the alert, [V-R8-20] Explore's banner plus four "Nothing here yet."; **refresh** — [W-R8-15] the refreshed body loses the cache to the request it replaced; **edge/spacing** — [O-R8-2] the phone sheet skin dead on 18/18 mobile opens, [V-R8-3] Reconnect 572/566 at 320×568, [S-R8-25] block 1 at y=−6 at 390×620, [V-R8-10] heading off-screen on arrival 5/6, [V-R8-7] a date step that never lands; **copy/AT** — [A-R8-21] "1 experiences", [A-R8-26] an unpunctuated dialog description read twice, [A-R8-23] a 199-character checkbox name, [S-R8-16] a URL that lies after one tap; **hygiene** — [W-R8-16] 209 orphaned bytes, [S-R8-34] dangling comments 9, [C-R8-N7] a false comment.
   Justification: Two state families are missing on their worst instance (focus as a distinguishing state on every button; an error state on Mine and on Explore's directory failure) and four more are rough (disabled/pending, error on Entity, refresh, the mobile sheet), which is anchor 1's "2–3 states missing"; nothing is default-browser and no fourth family is absent, so not 0; far past anchor 2's "1 state missing or rough". No tie.

9. Good design is environmentally friendly — Score: 3/3 (r7 3, Δ0)
   Evidence: [W-R8-25] app JS 86,192 B br / 88,733 B zstd as delivered to Chromium, under 100 KB either way (+205 B br); [W-R8-28][V-R8-21] 0 idle animations on 22/22 screens × preferences, 6 keyframes, the 2 infinite ones loading-only; [W-R8-31] prefers-color-scheme honoured pre-FCP in 6/6 boot contexts with 0 later `<html>` mutations and `theme-color` rewritten; [W-R8-32] prefers-reduced-motion → 1e-06 s on 25/25, `scroll-behavior:auto`, the compact landing one discrete step; [W-R8-33] SW battery green (10/10 kept, 5/5 evicted, 0 B cold-start refetch, exact match no work, offline shell 71–88 ms, `/api/` never cached); [W-R8-30] CLS 0/18; [W-R8-26] warm navigation 3 requests / 1,803 B, dupes 0/14, plain compose 1 data request (r7 3); [W-R8-27] boot long tasks 11/25 (r7 21). Relayed, outside the anchor: [W-R8-12] the edge rewrites `sw.js` to `max-age=14400` over the origin's `no-cache`; [W-R8-15] the last-writer-wins cache; [W-R8-35] +2.23 MB heap over 20 date steps, unbounded; [W-R8-34] 123,968 B unrequested font subsets, 1,201 B admin CSS, +209 dead bytes; [W-R8-33] `liveAssetSet` still trusts cached bodies.
   Justification: All four of anchor 3's conditions verify live for the sixth round running and the one r7 resource item inside this repo (the origin's `sw.js` header) is repaired at the origin; the remaining relay items do not touch the anchor, so 3.

10. Good design is as little design as possible — Score: 1/3 (r7 2, Δ−1) — tie 1↔2, settled lower
    Evidence: [S-R8-30] census delta 0 on 16/17 surfaces; [S-R8-33] button families 10→9; [S-R8-34] consumer-less classes 2→0, unterminated comments 1→0, `tsc` 0; [S-R8-9] the P-clause no longer renders where nothing wears a P label (r7's second removable resolved). Removable elements on one screen now: [S-R8-18] the `···` menu wrapping the single item "Report" (unchanged); [S-R8-23][A-R8-24] the 404's skip link, which skips one step to the page's only other control — a no-op element added by this wave; [C-R8-N5][W-R8-11] the status node on the error branch, which announces "Could not load" beside a `role=alert` that already announces the error — a duplicated announcement. Latent: [S-R8-19] header Share + interleaved invite at ≥9 posts; [S-R8-26] two classless `<div>`s and a `null` ternary arm around every post; [S-R8-27] two regions named "Editor"; [V-R8-24] a `sheet-up` keyframe consumed only by `.nudge`. Decoration: [O-R8-1] ~25 meaningless rings per screen.
    Justification: No page is dominated by decoration or duplicated affordances (not 0); two elements are firmly removable without breaking a task (the one-item menu, the no-op skip link) and a third — the error-branch status that duplicates the alert — is arguable, which puts the count on the boundary between anchor 2's "≤2 removable elements" and anchor 1's "3–5"; uncertain, the tie-breaker settles it at 1.

**Total: 15/30** (r7 19/30, Δ−4; r6 18/30; r5 19/30; r4 18/30; r3 16/30; r2 16/30; r1 16/30)

Tie-break register (principles settled downward by the lower-tie-breaker rule): #3 (0↔1), #10 (1↔2). Principles scored with no tie: #1, #2, #4, #5, #6, #7, #8, #9. Sensitivity, stated honestly: granting both ties upward gives 17/30 — still below the REFINE threshold, and #3 at 1 would still leave the verdict unchanged; the verdict is not sensitive in the upward direction. Downward: none of the no-tie scores was close to its lower anchor except #8, where a fourth missing state family (Visual's unreconciled loading claim) would give 0 — it was not counted because two passes contradict it (§6).

Why r8 dropped by 4: #1, #2, #6, #7, #8, #9 held; #3, #4, #5 and #10 each lost a point to defects the r7 fix wave introduced — above all two parser-level CSS breakages ([O-R8-1] a dangling `button,` selector that puts the focus ring on every button at rest; [O-R8-2] a misplaced brace that drops the phone bottom-sheet skin) that no pass could avoid measuring on every surface — while every r7 blocking condition the wave actually targeted (note margin, compact overlaps, animated landing, retry scroll, two-grammar dialog, silent pending state, whole-feed announcement, compose landing, refresh that fetches nothing, invalid-date URL, P-clause, orphan styles, unterminated comment, `tsc`, touch floor) verified fixed. The app is scored as it ships.
