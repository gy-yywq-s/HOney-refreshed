# 01 — Evidence (r8, consolidated from five Opus evidence subagents plus two orchestrator-verified findings; scoring withheld to 02)

Anchor IDs: `[S-R8-n]` Structural, `[V-R8-n]` Visual, `[C-R8-n]` Copy & Honesty, `[W-R8-n]` Weight & Friction, `[A-R8-n]` Accessibility, `[O-R8-n]` orchestrator (probe `r8-orch-check.js` / `.log`, screenshots `r8-orch-*.png`). All measurements on the deployed `13cbcee` (`index-KkxCm-lK.js` / `index-Ddv96bIG.css`), 2026-09-02, harness `/root/claude-work/design-audit/`; lesson-block measurements in `Asia/Shanghai`. Every finding cites a file:line, a probe log, a screenshot or a measured value; nothing was scored by a subagent.

## 0. Verification of the r7 handoff (moves 1–4) — live

**Move 1 (finish the half-done states):** (a) pending reaction — CONFIRMED [V-R8-1][A-R8-1][C-R8-11]: `.react-btn[aria-disabled="true"]{opacity:.7;cursor:default}` computes on both boots, focus kept, one POST; residue: the untapped pill dims and announces disabled too [A-R8-20][V-R8-12], a second tap gives no feedback [A-R8-2]. (b) `ReconnectDialog` one label — CONFIRMED [C-R8-7][V-R8-3]: "Stay connected on this device." for both purposes, caveat in both bodies, one line at 320/390/1280; residue: the Reconnect sheet at 320×568 still scrolls 6 px (572/566) [V-R8-3]. (c) note spacing — CONFIRMED [V-R8-4][S-R8-9]: `p.caption.timetable-note` computes `margin-block 12px 8px` everywhere, 8 px note→canvas, 0 note×hour overlaps. (d) `/why` pill 170×44 and `.stay-connected` label ≥44 — CONFIRMED [A-R8-11][A-R8-12][V-R8-5][S-R8-20]: touch census 162/162 ≥44 at 390 and 320, sub-44 list empty (the nested checkbox element is 22×22 inside its label). (e) one disabled treatment — PARTIAL [V-R8-2][A-R8-29]: one text recipe (`opacity 1`, `--muted`, 4.61–5.66), two fills (grey primary vs transparent ghost).

**Move 2 (quiet, once, correct compact landing):** CONFIRMED on its stated contract [W-R8-4..9][V-R8-7][A-R8-14..16][S-R8-36]: one instant step inside a rAF (+79…+410 ms after FCP), retry 0 px on 24/24 landings incl. the Timetable at 320×568, SPA re-entry lands, `.daynav` static at ≤620 heights, 0 daynav×heading/note overlaps on 6/6 rows (r7 8/8/5), last block clears the nav at all six viewports (220–274 px), `.lesson-block--compact` 36 px, a synthesised 40-min lesson no longer clips. Deviations: the overlap was removed by scrolling the daynav (−140/−192/−198 px), the heading (5/6 rows) and the note (4/6) off-screen on arrival [V-R8-10][A-R8-28][W-R8-18]; a date step does not land even on a never-seen 3-lesson day [V-R8-7][W-R8-6] although the code comment defines "the first settle of a date" as its cold landing; at 390×620 block 1 lands at y=−6 [S-R8-25]; the r7 handoff's own success criterion "with the day heading visible on arrival" is not met.

**Move 3 (AT says only what changed, never drops focus):** feed — CONFIRMED [S-R8-17][A-R8-8][W-R8-10][W-R8-11]: `[aria-live]` 0, a 1×1 `role=status` sibling, arrival 13–14 chars (r7 1,120), tabpanel named from its tab [A-R8-9]; residue: "1 experiences" / "0 experiences" / "Could not load" [C-R8-10][C-R8-N5][A-R8-21]. Compose double-fail — CONFIRMED [S-R8-1][A-R8-7]: lands on the "Editor" region with a 26.5 % ring by mouse and keyboard; residue: the region is named "Editor" and contains no editor [C-R8-N1][S-R8-27]. Dialogs — `aria-describedby` 7/7 CONFIRMED [A-R8-3]; `inert` 0/7 REFUTED, unchanged [A-R8-4]; lesson description is the whole unpunctuated `<dl>` [A-R8-26]. 404 — title/`#main`/skip link CONFIRMED present [S-R8-10][C-R8-4]; the skip link targets "Go home", not `#main`, and the page has 0 nav [A-R8-10][A-R8-24][A-R8-25][S-R8-23]. `#main` band over content — REFUTED, unchanged 5/12 [A-R8-13].

**Move 4 (one truth per surface, no silent work):** invalid `?date=` replaced — CONFIRMED [S-R8-7][C-R8-5]; but the URL is stale after every stepper action incl. "Back to today" [S-R8-16][W-R8-17][V-R8-15]. One locale on the Timetable — CONFIRMED [C-R8-5]; but Home still hard-codes `en-GB` [C-R8-N4] and under `zh-CN` the English sentence wraps a Chinese date [C-R8-N3]. P-clause conditional — CONFIRMED [S-R8-9][C-R8-6] (leading-space residue [C-R8-N9]). 404 title = h1 — CONFIRMED [C-R8-4]. Delete-data copy — PARTIAL [C-R8-1] (overstatement gone; `lesson_instances` omitted from "what stays"); delete-account body unchanged [C-R8-2]. Login footnote — CONFIRMED [C-R8-3]. Provenance — REFUTED, two registers [C-R8-8]; "delisted" — CONFIRMED unified. Stale comments — CONFIRMED refreshed; one new false comment (`app.ts:104-106` vs the wire) [C-R8-9][C-R8-N7]. `useApi` refresh bypass — CONFIRMED for the request [W-R8-1][S-R8-5]; REFUTED for the cache (last-to-arrive wins, pre-refresh body served silently on re-entry) [W-R8-2][W-R8-15]. `useNames` error surfaced — CONFIRMED on Entity [S-R8-3], REFUTED on Mine and on Explore's directory failure [S-R8-4][S-R8-19b]; the new entity branch drops focus to `<body>` on a second failure and hides a loaded feed [S-R8-17b][A-R8-22][S-R8-18b][C-R8-N2]. Chooser 0 registry requests — CONFIRMED [S-R8-2][W-R8-13]. `Reveal`/`.reveal`/`.is-in`/`.option-grid span`/unterminated comment — CONFIRMED gone [S-R8-11][S-R8-12][W-R8-14]. `sw.js` `no-cache` — CONFIRMED at the origin, REFUTED on the wire (edge `max-age=14400`) [W-R8-12].

**Shipped by the wave, not asked for (both parser-level CSS breakages, found independently by the orchestrator and by four of five subagents):**
- **[O-R8-1]** = [S-R8-14][V-R8-8][A-R8-18]: `foundations.css:64` — deleting `button, a { -webkit-tap-highlight-color }` left `button,` glued to the focus-visible selector list; served `button,button:focus-visible,…{outline:3px solid var(--accent);outline-offset:3px}`; every `<button>` computes `solid 3px rgb(51,102,124)` (night `rgb(143,194,212)`) at rest — Feed 5/5, Timetable 7–8/8, Settings 12/12; 24/111 Tab stops ringed with nothing focused; 21,876 / 37,094 accent pixels on unfocused Settings / Timetable; adjacent rings collide (theme grid, lesson blocks); `<a class="btn">` unaffected so one row shows "Sign out" ringed and "Open Dash" bare; `button` also lost its tap-highlight suppression (`r8-orch-outline-feed.png`, `r8-orch-check.log`).
- **[O-R8-2]** = [S-R8-15][V-R8-9][A-R8-19][W-R8-16]: `components.css:814-829` — the nested `@media (max-height:620px)` closes `.modal{}` early, orphaning `border-radius: 20px 20px 0 0`, `border-left/right/bottom: none`, `padding … env(safe-area-inset-bottom)`, `animation: sheet-up` (209 dead bytes in the served CSS; `git show 213707d` has them inside `.modal{}`); every mobile dialog (18 opens) is a fully-rounded, four-bordered desktop card with `padding 24px` and `rise-in`, flush to the viewport bottom; Reconnect at 320×568 fills `[0,0,320,568]` with four radii and four borders; `sheet-up`'s only consumer is now `.nudge`, so the compose safety sheet and the dialogs use two sheet grammars on one phone (`r8-orch-sheet-320x568.png`, `r8-orch-check.log`).

## 1. Structural [S] (opus; probes `r8-struct-{census,branches,useapi,ptr,ptr2,date,feed,timetable,css,touch,tasks,landings,regress,depth}.js` + `.log`; screenshots `r8-struct-*.png`)

r7-move verification (live, `13cbcee`):
- **[S-R8-1]** CONFIRMED — compose registry-error branch carries the "Editor" landing: `/compose?entityKey=teacher:t_23348879d1b4` with `/api/entities` aborted, "Try again" (still aborted) → `activeElement = section.focus-landing[role=region][aria-label="Editor"][tabindex=-1]`, `data-landed=""`, outline `3px solid rgb(51,102,124)` (`ComposePage.tsx:209-235`).
- **[S-R8-2]** CONFIRMED — plain `/experiences/compose` cold: `/api/entities` 0, `/api/directory` 0, total API 1 (`GET /api/me`) (`ComposePage.tsx:55-58`, `shared.tsx:160-170`).
- **[S-R8-3]** CONFIRMED text / PARTIAL behaviour — entity page under `/api/entities` or `/api/directory` abort renders `role=alert` "Could not reach the HOney server. Check your connection and try again." + "Try again" (110×44); retry re-requests both; but focus never lands (`activeElement=body`, `[data-landed]` = []) — see [S-R8-17b] (`EntityPage.tsx:130-138`).
- **[S-R8-4]** REFUTED (inconsistent siblings) — Explore under `entities` abort: banner + retry + landing fires; Explore under `directory` abort: no banner (renders 6 teachers / 8 courses silently); Mine under either abort: no banner, no retry — `useNames().error` unused (`ExplorePage.tsx:29-31,84`; `MinePage.tsx:49`).
- **[S-R8-5]** CONFIRMED — `reload()` mid-flight re-fetches: Explore `/api/entities` delayed 3 s, "Try again" during the flight → 1 new request, no duplicate; Timetable delayed 3 s + PTR mid-flight: `/api/timetable` 1→2, new=1 (`useApi.ts:18-25,89-92`).
- **[S-R8-6]** CONFIRMED with caveat — only `apiCache.clear()` (PTR path, `PullToRefresh.tsx:77-78`) drops the in-flight map; a bare `honey:refresh` event mid-flight yields new=0 for the in-flight key (`useApi.ts:96-100`).
- **[S-R8-7]** CONFIRMED — `?date=2026-13-45` / `2026-02-30` / `abcd` → `location.search="?date=2026-09-02"`, h1 "Wednesday, September 2, 2026"; valid `?date=2026-08-24` not rewritten; `history.length` unchanged across the rewrite (`TimetablePage.tsx:41-47`).
- **[S-R8-8]** REFUTED — "Back to today" (104×44) shows Sep 2 but leaves `?date=2026-08-24` in the bar; see [S-R8-16].
- **[S-R8-9]** CONFIRMED — 2026-08-24 note "P1–P6 are the school's six lesson periods. Last synced 6 h ago."; 2026-08-22 note " Last synced 6 h ago." (leading space in the text node); `p.caption.timetable-note` computes `margin-block 12px 8px` (`TimetablePage.tsx:170-173`).
- **[S-R8-10]** CONFIRMED — `/nonexistent-route`: title "Page not found · HOney", h1 "Page not found", `main.login#main[tabindex=-1]`, `a.skip-link[href="#not-found-home"]` → "Go home" (`NotFoundPage.tsx:5-24`); census 2/2 (r7 1/1).
- **[S-R8-11]** CONFIRMED — `Reveal` gone; `motion.tsx` exports exactly `prefersReducedMotion, staggerStyle, useNowTick, Skeleton`; served CSS `.reveal` 0, `.is-in` 0, `.option-grid span` 0.
- **[S-R8-12]** CONFIRMED — unterminated `/*` openers 0 (r7 1).
- **[S-R8-13]** CONFIRMED with consequence — `a` no longer duplicated; individual repeated selectors 4 (r7 5): `button`, `.caption`, `.feed-identity a`, `.post__context a`; the `button` pair is the broken rule of [S-R8-14].
- **[S-R8-17]** CONFIRMED — `/experiences`: `[aria-live]` 0 on every state; `[role=status]` 2 (`div.ptr__disc` always mounted + `p.sr-only` 1×1, `clip: rect(0,0,0,0)`); text "1 experiences" (real feed) / "12 experiences" / "0 experiences" (empty) / "Could not load" (error); tabpanel name "Your classes" via `aria-labelledby=tab-my-classes` (`FeedPage.tsx:107-160`).
- **[S-R8-18]** CONFIRMED (unchanged) — `div.post__menu[role=menu]` with no name, 1 menuitem "Report" 110×44.
- **[S-R8-19]** CONFIRMED (unchanged) — fulfilled 12-item feed: header Share `btn--primary btn--small` [16,59,59,44] and `.feed-invite` "Share an experience" [16,1739,141,44] co-render (`FeedPage.tsx:18,164-172`).
- **[S-R8-20]** CONFIRMED — the r7 `a` "Back to Experiences" 133×22 row is gone; sub-44 controls 4→2 (login `input[type=checkbox]` 22×22 at 390 and 320) (`WhyPage.tsx:101`).

New defects:
- **[S-R8-14]** Every `<button>` paints a permanent 3 px accent ring at rest. `foundations.css:64-66`: the `button, a { -webkit-tap-highlight-color }` rule was deleted but its `button,` line stayed and joined the focus selector list; served CSS verbatim `button,button:focus-visible,a:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible,summary:focus-visible{outline:3px solid var(--accent);outline-offset:3px}`. `getComputedStyle` at 390×844 with `activeElement=BODY`: Settings 12/12 buttons `3px solid rgb(51,102,124)` offset 3, Timetable 8/8, Feed 5/5; anchors 0, inputs 0 (`r8-struct-btnoutline-{settings,timetable,feed}.png`).
- **[S-R8-15]** The phone bottom-sheet treatment for `.modal` is dead. `components.css:814-825`: the new `@media (max-height:620px)` was opened inside `.modal`'s declaration list, so `border-radius: 20px 20px 0 0`, `border-left/right/bottom: none`, the `sp-5/sp-4/sp-6+safe-area` padding and `animation: sheet-up` are orphan declarations. Served CSS: `.modal{max-width:none;max-height:92dvh}@media (max-height: 620px){.modal{max-height:100dvh}border-radius: 20px 20px 0 0;…animation: sheet-up .42s var(--ease) both}`. Lesson modal at 390×844 / 390×620 / 320×568: `border-radius 20px` all corners, 1 px border all four sides, `padding 24px`, `animation-name rise-in`, overlay still `align-items:flex-end; padding:0` so the fully-rounded, fully-bordered card sits flush at `bottom = innerHeight`; desktop unaffected (`r8-struct-modal-*.png`).
- **[S-R8-16]** The timetable URL is stale after every stepper action: `replaceState` runs once on mount; `setDate` never touches the URL. From `/timetable?date=2026-08-24`: Next day → h1 Aug 25, search `?date=2026-08-24`; Previous → Aug 24; **Back to today → h1 Sep 2, search `?date=2026-08-24`**; a reload of that URL shows Aug 24. `history.length` 2 throughout (`TimetablePage.tsx:41-47,138-150`).
- **[S-R8-17b]** EntityPage's new registry-error retry never lands focus: `landing = useRetryFocus(feed.loading)` (`EntityPage.tsx:48`) but the retry calls `reloadNames()` (`:133`), which changes `namesError`, not `feed.loading`; measured across 7 error surfaces: history, feed, explore, compose, entity-feedfail land with `data-landed` + ring; entity-registryfail → `BODY`, no ring; Mine renders no "Try again" at all. 6/7 (`lib/useRetryFocus.ts:11-20`).
- **[S-R8-18b]** The registry error hides a feed that loaded fine: `namesError` is the first ternary arm (`EntityPage.tsx:130`); with `/api/entities` aborted and the feed served 200, the page shows only the banner, `posts=0`.
- **[S-R8-19b]** `useNames` error/loading/reload consumed by 1 of 3 callers (`EntityPage.tsx:35`); `MinePage.tsx:49` and `ComposePage.tsx:101` destructure `{ names }` only (`shared.tsx:167-178`).
- **[S-R8-20b]** `useNames(enabled=false)` — no undefined-name consumer (`names` read only inside the `unlisted` branch, `ComposePage.tsx:244-246`, reachable only when enabled). No defect.
- **[S-R8-21]** Cold-load request table (fresh context, networkidle + 2.5 s): explore entities 1 / directory 1 / API 3; entity 1/1/4; `compose?entityKey=` 1/1/3 (two hooks on key "entities", one request); `compose?lessonId=` 0/0/2; mine 1/1/3; plain compose 0/0/1; feed 0/0/2.
- **[S-R8-22]** Back button after the date rewrite intact: `/home` → `/timetable?date=2026-13-45` (rewritten) → Back → `/timetable` → Back → `/home`.
- **[S-R8-23]** The 404 skip link is a no-op: `#not-found-home` is the page's only other focusable element and already the second Tab stop; `#main[tabindex=-1]` exists but is not the target; no content bypassed.
- **[S-R8-24]** Compact-height daynav keeps all 7 controls (Previous 44×44, Pick a date 198–308×44, `input[type=date]` 5×4, Next 44×44, Back to today 104×44, History 67×44, Sync now 79×44) at all six viewports; `position: static` at ≤620 heights, `sticky` at 844/932; sticky/fixed inventory at compact heights: skip-link (fixed, off-screen), `div.ptr` (sticky, 0 px), `nav.mobile-nav` (fixed).
- **[S-R8-25]** At 390×620 the cold landing parks block 1 at `y=-6` (6 px above the viewport top): `scrollTop=225`, max 228 (`848−620`); other viewports land block 1 at y=29/61/81/219/247.
- **[S-R8-26]** Two classless `<div>`s wrap every feed post (`FeedPage.tsx:161` ex-live wrapper, `:164`); `:155-157` is a ternary arm rendering literal `null`; post path `main>div.view>div.feed-screen>div.feed-stream>div>div>article.post>…`.
- **[S-R8-27]** Two regions named "Editor": `ComposePage.tsx:212-219` (the error banner) and `:306` (the editor), same `landing.ref`.

Required fields:
- **[S-R8-30]** Interactive census 390×844 (visible/total), r8 = r7 on 16/17 surfaces: Home 9/15, Timetable today 13/20, lessons 14/21, empty 11/18, History 8/14, History select 32/38, Settings 18/25, Feed 15/21, Explore 26/32, Compose chooser 7/13, editor 10/16, Entity 6/12, Mine empty 6/12, Mine seeded 7/13, Why 6/12, Login 5/5, **404 2/2 (r7 1/1, +skip link)**. `activeElement` = body on landing on all 17.
- **[S-R8-31]** Depth from `#main`: feed post deepest node 10, deepest interactive 9 (r7 9); lesson block deepest 9, interactive 6; Settings row deepest 6, interactive 7 — unchanged.
- **[S-R8-32]** Repeated-pattern families 16 (same boundary): "Try again" 9 (r7 8, `EntityPage.tsx:134`), `useRetryFocus` 8 / `.focus-landing` 9 (r7 8, `ComposePage.tsx:215`), `<Skeleton>` 9, `role=alert` 13 (r7 12), `className="card"` 19, `.card-actions` 10, `<Modal>` 4, `<ConfirmDialog>` 5, `<ReconnectDialog>` 2, `h1.page-title` 14, compose entry points 11, pill grammar, hairline row 4, empty state 9, filter/search 3.
- **[S-R8-33]** Button families by rendered height at 390×844 `hasTouch`: **9** (r7 10) — 44 (15 classes), 45, 46, 48, 49, 50, 73, 81, 84; the 22 px family is gone. At 320×568: 10 (r7 11). Touch census 520 boxes (r7 518); sub-44 in either dimension **2** (r7 4), both the login checkbox 22×22.
- **[S-R8-34]** `tsc --noEmit --noUnusedLocals --noUnusedParameters` → 0. Served CSS 40,849 B (r7 40,914), 372 rules (r7 374), 222 classes (r7 225); served JS 275,493 B raw (r7 274,090). Whole-head duplicate selectors 0; individual repeated selectors 4 (r7 5). Consumer-less classes 14 raw / **0 adjusted** (r7 2; the 14 are 7 interpolated family members + 7 admin-only). TSX→CSS reverse sweep 12 raw / 5 real, unchanged: `home`, `home-head`, `home-voices` (`HomePage.tsx:55,56,110`), `login__card` (`ErrorBoundary.tsx:23`), `post__dot` (`ExperiencePost.tsx:196`). Dangling trailing comments 9 (r7 8; new at `components.css:820,876`, `features.css:80`). Unterminated openers 0 (r7 1).
- **[S-R8-35]** Primary tasks (390×844 cold): next lesson 1 screen / 0 taps (`.nextlesson` [16,95,358,217]); stream 1 tap; share via History 4 screens / 3 taps to the editor, 4 to a cursor; via Explore 5 / 4 / 5; textarea never auto-focused. Unchanged.
- **[S-R8-36]** Lesson-block hit-testing 2026-08-24 Asia/Shanghai: 18/18 blocks hit-test to themselves at 320×568 / 320×600 / 360×620 / 390×620 / 390×844 / 430×932; `scroll-margin-top 8px`; landing `scrollTop` 208/176/156/225/0/0; last-block-bottom → nav-top clearance 220/220/220/258/214/274 px (r7: −6 at 320×568, −30 at 390×620); 0 clipped text; lesson modal contains without scrolling at all six. Empty day: `scrollTop` 0 everywhere; the empty canvas runs under the fixed nav by 213/181/161/251/107/47 px.
## 2. Visual [V] (opus; probes `r8-vis-{outline,react,dialogs,compact,scroll,scroll2,shortlesson,extra,typespace,css,css2,contrast,idle,states,states2,surface,ring,shots}.js` + `.log`; screenshots `r8-vis-*.png`; served CSS `r8-served.css`; reaction undone in-script)

r7-move verification (computed style and geometry):
- **[V-R8-1]** CONFIRMED — `.react-btn[aria-disabled="true"]{opacity:.7;cursor:default}` live (`components.css:251`); stone at 100/500/1500 ms `opacity 0.7`, `cursor default`, `aria-pressed` flips at 100 ms; genuine dark boot identical; count text lags ("0" until settle); `pointer-events auto` throughout; hover rule `.react-btn:hover:not([aria-disabled=true])` present (`:247`).
- **[V-R8-2]** PARTIAL — `.btn:disabled{cursor:default;opacity:1;color:var(--muted);border-color:var(--line)}` (`components.css:66-72`): all 3 live disabled buttons compute opacity 1 and text `rgb(92,103,112)` — one text recipe; but `.btn--primary:disabled{background:color-mix(in srgb,var(--ink) 8%,var(--surface));border-color:transparent}` (`:85-90`) gives a grey fill `color(srgb .891 .901 .907)` while ghosts stay transparent with a `rgba(35,43,49,.14)` border — two fills; disabled text vs own fill 4.61 (primary "Share anonymously") / 5.34 (ghost "Keep private") / 5.66 (Settings "Export") stone, 6.64 / 8.07 / 7.26 night; no `.btn--danger:disabled` rule (latent); login submit never `disabled`.
- **[V-R8-3]** PARTIAL — label one line, 20 px, "Stay connected on this device." in both purposes at 320/390/1280 (r7: 4 lines / 78 px on reconnect); caveat in both bodies; submit bottom ≤ viewport everywhere (Save 543/819/623, Reconnect 549/819/635); `.modal` `max-height` 568 px (100dvh) at 320×568, 776.48 (92dvh) at 390×844, 704 at 1280; `scrollHeight ≤ clientHeight` fails once — **Reconnect at 320×568: 572 vs 566, scrolls 6 px** (Save 543/543 ✓; r7 539/522) (`ReconnectDialog.tsx:29-33,46-51`; `components.css:818-821`).
- **[V-R8-4]** CONFIRMED — `p.caption.timetable-note` computes `margin-block 12px / 8px` at 320×568, 360×620, 390×620, 390×844 and on the empty day (r7 0/0); note-bottom → canvas-top 8 px on a lessons day (r7 0), 47 px on an empty day; note × every `.timeline__hour`: 0 intersecting pairs at all viewports (r7 27×6 vs `09:00`) (`features.css:226-229`).
- **[V-R8-5]** CONFIRMED — `/why` "Back to Experiences" `A.btn.btn--ghost` 170×44, `min-height 44px`, `padding 0 20px`, `border-radius 99px` at 390 and 320 — byte-for-byte Explore's 170×44 (`WhyPage.tsx:101`); login `.stay-connected` label 342×132 / 272×151, `min-height 44px` (`components.css:876`); checkbox element 22×22 inside it.
- **[V-R8-6]** CONFIRMED — `.daynav` `position: static` at 320×568 / 360×620 / 390×620, `sticky` at 390×844; overlap census after landing 0 / 0 / 0 / 0 (r7 8 / 8 / 5 / 1) on 2026-08-24, 2026-09-02 and the empty day; last block bottom vs nav top 274≤494, 326≤546, 299≤546, 556≤770 (r7 −6 / −30); `.lesson-block--compact` `min-height` 36 px at ≤620 h (20 at 844); synthesised 40-min lesson on the 450 canvas at 320×568: block 36 px, subject box [91,33,80,13], meta box [91,46,192,13], `clipY:false`, `bodyClip:false` (r7 27 px / 7 px meta, clipped) (`features.css:355,770-779`).
- **[V-R8-7]** CONFIRMED with one deviation — owner `.main` (`scroll-behavior: smooth`); 16 ms sampling navigation→1600 ms on 2026-08-24: exactly one step, no intermediate values, in all four legs — 320×568 no-pref 0→208 at 476 ms (77 samples, distinct {−1,0,208}); 390×620 no-pref 0→220 at 251 ms; 320×568 reduce 0→208 at 521 ms; 390×620 reduce 0→211 at 460 ms (r7's ~120 ms ramp gone). Retry (`/api/timetable` aborted once → Try again): 118 samples over 2 s, 0 changes, `scrollTop` 0→0, 3 blocks rendered (r7 0→77). SPA re-entry (Timetable → Home tab → Timetable tab): `scrollTop` 208 at 320×568, 214 at 390×620 — the landing survives re-entry (r7 lost). **Date step: next/prev ×4 → `scrollTop` 0 on every step, daynav top 16 — the landing does not run on a date change even for a never-seen 3-lesson date (Wed Aug 26)**, although the code comment `TimetablePage.tsx:56-57` defines "the first settle of a date" as its cold landing (`:58-62,328-335`).

New defects:
- **[V-R8-8]** Every `<button>` paints a permanent 3 px accent ring: `5c0cec9` pulled `a` out of `button, a { -webkit-tap-highlight-color }` and left `button,` glued to the focus-visible list (`foundations.css:64`; absent at 213707d per `git show`); served `button,button:focus-visible,…{outline:3px solid var(--accent);outline-offset:3px}`; `3px solid rgb(51,102,124)` at rest on every `<button>` at 320×568 / 390×844 / 1280×800 on five routes; night `rgb(143,194,212)`; `<a class="btn">` unaffected (`3px none`) — one button grammar split in two on the same row (Settings "Sign out" ringed, "Open Dash" bare); instances: 4 theme cards (adjacent rings collide in the 2×2 grid), Sign out, Delete account…, Sync now, Disconnect, Save school login…, Delete imported data…, Export, Import…; Timetable `‹` date `›`, Sync now, all 3 lesson blocks (rings of adjacent 61 px blocks overlap); Feed both scope tabs, both reactions, `···`; every `.modal__close`; edge-band accent pixels before any interaction top 3 / right 817 on Timetable, right 455 on Feed (r7 0/0/0/0 on 16/16) (`r8-vis-shot-settings-390x844.png`, `r8-vis-shot-tt-landing-320x568.png`, `r8-vis-modal-lesson-390x844.png`).
- **[V-R8-9]** The mobile bottom-sheet skin is dead: `13cbcee` inserted `}` + `@media (max-height:620px){ .modal{…}` mid-rule (`components.css:818-828`), orphaning `border-radius: 20px 20px 0 0`, `border-left/right/bottom: none`, `padding: var(--sp-5) var(--sp-4) calc(var(--sp-6) + env(safe-area-inset-bottom))`, `animation: sheet-up .42s`; 18 opens (6 dialogs × 320/390/1280): `border-radius 20px` all corners, borders 1/1/1/1, `padding 24px`, `animation-name rise-in` — the desktop card, with the overlay still `align-items:flex-end; padding:0`, so every mobile dialog is a fully-rounded, fully-bordered card flush to the viewport bottom (`touchesBottom:true` 12/12 mobile opens), no `env(safe-area-inset-bottom)`; r7's sheet padding 20/16/24 is now flat 24; worst: Reconnect at 320×568 `[0,0,320,568]` — the whole screen with four 20 px radii and four 1 px borders, still overflowing 6 px; `@keyframes sheet-up` has exactly one live consumer, `.nudge` — the compose safety sheet slides up with safe-area padding while every dialog on the same phone does not (two sheet grammars).
- **[V-R8-10]** At compact heights the static daynav is scrolled entirely off-screen on arrival: after landing on 2026-08-24 `.daynav` rect [16,−192,288,114] at 320×568 (bottom 78 px above the viewport), [16,−140,328,114] at 360×620 (26 px above), [16,−198,358,114] at 390×620 (84 px above); `h1.schedule-header` top −62 / −10 / −68; `.timetable-note` half-visible at 320×568 ("…6 h ago." only), fully off at 390×620; at 320×568 and 360×620 the landing equals the document's maximum scroll (208 = 776−568; 156 = 776−620); recovery is scroll-up or re-tapping the Timetable tab (resets to today). r7's 8 overlaps were removed by removing the header from the viewport (`features.css:773-775`; `TimetablePage.tsx:328-335`; `r8-vis-shot-tt-landing-320x568.png`).
- **[V-R8-11]** A disabled button wears the focus ring: rest, hover and focus on the disabled "Share anonymously" / "Keep private" all compute `outline: solid 3px rgb(51,102,124)`, `opacity 1`, `transform none` — hover/focus indistinguishable from rest, and the ring makes the dead control the loudest thing in the row (`r8-vis-disabled-hover-*.png`).
- **[V-R8-12]** The pending state bleeds onto the untapped pill: with 👍 tapped, 👎 also computes `aria-disabled=true`, `opacity 0.7`, `cursor default` at 100/500/1500 ms on both boots; `···` does not (`ExperiencePost.tsx:226,238`).
- **[V-R8-13]** `.react-btn:disabled` is now unstyled (a native-`disabled` `.react-btn` computes opacity 1 / cursor pointer) — latent, the component uses `aria-disabled` (`components.css:251`).
- **[V-R8-14]** Empty-day gap under the note doubled: note-bottom → `.timeline__empty` top 16 px (r7 8; `.timeline__empty{margin:8px 0}` + the note's new 8 px); no overlap.
- **[V-R8-15]** Address bar contradicts the page after a date step: `?date=2026-08-24` + 2 × next → page "Wed, Aug 26", `location.search` still `?date=2026-08-24` (4/4 steps, both viewports) (`TimetablePage.tsx:42-47`).
- Checked clean: body-copy links intact after the `Reveal` removal (`a{color:var(--accent);text-decoration:none;-webkit-tap-highlight-color:transparent}` + `a:hover{underline}` served; "How privacy works" `rgb(51,102,124)` at 320 and 390); `.stay-connected` alignment sound (`align-items:flex-start`, checkbox [46,437,22,22] vs span [83,437,162,20]).

Required fields:
- **[V-R8-16]** Spacing scale: 320×568 {1,4,8,12,13,16,20,24,90} 859/878 = 97.84 %; 390×844 {…,84,96,130} 859/880 = 97.61 %; 430×932 97.61 %; 1280×800 893/964 = 92.63 % — identical to r7; off-ladder attribution unchanged (`.main` nav clearance 90/96/110 documented; 13 px ×8 from `calc(var(--sp-3) + 1px)`; 1 px hidden picker; `auto`/`clamp` resolutions). Author level: 214 spacing declarations, 176 on `var(--sp-*)`, 2 literal px (`foundations.css:91 margin:-1px`, `features.css:1201 padding-bottom:90px`) — identical to r7.
- **[V-R8-17]** Type scale: 390 {12,13,15,16,20,22,24,26,28}; 430 {…,27.95,28}; 1280 {12,13,15,16,20,28,30,32,34,36}; floor 12; fractional 1 (`.nextlesson__subject` 27.95 at 430); off-ramp `font-size` declarations 11 incl. admin (10 excl., = r7).
- **[V-R8-18]** Colours: 43 unique literals (unchanged); `tokens.css` `git hash-object` = `1d70d963a91a5bfbeb17928b599185286ad8f836` (byte-identical to 213707d); rendered unique computed colours Feed+Timetable+Settings 17 stone / 19 night (unchanged); non-token intrusions unchanged (`.swatch` border `rgba(0,0,0,0.18)`, four `.swatch--*` hexes, `components.css:787-801`).
- **[V-R8-19]** Contrast: 306 nodes × 9 routes + error surface, both boots; lowest enabled 4.66 stone / 5.83 night (`.home-head__stale`); 9/9 reported "failures" at exactly 1.00 are the r7-documented ancestor-walk artefact on the active nav label (true 13.26/15.27); 0 genuine failures; next lowest error-banner text 4.77 / 7.03, note and hour labels 5.34 / 8.07; focus ring vs ground 5.81–6.30 / 8.36–9.28 — but the ring is now permanent on every `<button>` [V-R8-8]; pressed theme card `rgb(35,43,49)`/`rgb(244,246,247)`; scope tab 6.30; chips not rendered this round (0 stored keys; carried from r7 unverified).
- **[V-R8-20]** States checklist at 390: empty ✓ Timetable/Explore/Mine/Entity; loading ✓ Home 2 blk/4 rows, Timetable, Feed, Explore, History — **missing (0 skeleton blocks) on Mine, Entity, Compose** with the endpoint delayed 5 s; error ✓ 44 px retry on Home/Timetable/Feed/History, Explore banner + 4 "Nothing here yet." at once, **missing on Mine, Entity (renders success copy "No experiences here yet — yours could be the first." with its endpoint aborted), Compose**; success ✓ Feed (1 post, 0 banner); focus — **permanent on every `<button>`, no longer a state**; disabled ✓ Compose (2 fills), Settings Export; pending ✓ (bleeds to sibling); selected ✓ scope tab, theme card (`r8-vis-state*-*.png`).
- **[V-R8-21]** Idle animations: `document.getAnimations().length` at 2.5 s = 0 on 11/11 screens × no-preference and 0 on 11/11 × reduce (unchanged); 6 `@keyframes` (`fade-in, ptr-rot, rise-in, settle, sheet-up, skeleton-shimmer`); `skeleton-shimmer` only while mounted.
- **[V-R8-22]** Modal containment: 6 dialogs × 3 viewports = 18 opens (theme dialog unreachable — inline picker on mobile, rail trigger not found at 1280, as r7): overlay = viewport 18/18; nav hit-tests → `DIV.modal-overlay` 72/72; `scrollHeight ≤ clientHeight` 17/18 (Reconnect 320×568 572/566); buttons ≥44 53/53, of which 51 at 44 and 2 wrap-induced 47 px families at 320×568 (Delete-data Cancel/confirm; Report options 4–5); sheet geometry per [V-R8-9].
- **[V-R8-23]** Trend markers rendered: gradients 1 (`.skeleton__row` linear, loading only); blur/translucency 3 rules (`.modal-overlay blur(8px)`, `.rail blur(12px)`, `.mobile-nav blur(12px)`; `.mobile-nav` bg `color(srgb … / 0.95)`, `.card` `rgba(255,255,255,0.72)` stone / `.043` night); drop shadows 9 (6 on `var(--shadow) = 0 14px 40px rgba(22,30,34,.1)` + 3 literals `.ptr__disc`, `.feed-new`, `.post__menu`); radius families 13 distinct (7,10,11,12,13,14,16,17,18,20,50%,99px,999px + three asymmetric); entrance animations `settle .46s/.5s backwards` per route, `rise-in .3–.5s` (now every mobile dialog too), `fade-in` overlay, `sheet-up .42s` (`.nudge` only), `ptr-rot` while dragging; counter-markers: no radial gradients, no icon-only nav, largest mobile type 28 px, `.timeline__now` static, 0 idle animations; `.reveal` removed.
- **[V-R8-24]** Orphan styles: NEW — six orphaned `.modal` sheet declarations (`components.css:822-827`, in the served bytes, consumed by nothing); `@keyframes sheet-up` with one consumer (`.nudge`) and zero for its intended `.modal`; `.react-btn:disabled` path unstyled. FIXED — `.option-grid span` deleted; the unterminated comment at `components.css:41-45` gone. Remaining declared-but-never-literal classes 7 (`.chip--ok/danger/muted`, `.swatch--stone/white/mist/night`), all composed dynamically — not true orphans. Dangling-comment detector 7 hits, 0 true deletion scars (r7 1). In-context duplicate selectors 0 across five stylesheets.
- **[V-R8-25]** Additional #3 facts: button-height families at 320×568 include 47 px (wrap-induced) beside 44 and 81; the `.react-btn` "on" recipe differs by boot (stone ink-on-tint `rgb(35,43,49)` / `rgba(35,43,49,.05)`; night accent-on-tint `rgb(143,194,212)` / `rgba(255,255,255,.06)`); 9 shadow declarations across 4 recipes.
- **[V-R8-26]** #8 worst instances: loading — Mine/Entity/Compose 0 skeleton blocks at 5 s delay (`r8-vis-state2-loading-*.png`); error — Entity with its feed endpoint aborted renders the success copy and no banner, Compose renders neither banner nor empty; empty — Explore banner + four "Nothing here yet." together; focus — indistinguishable from rest on any `<button>`; disabled — ringed, no hover/focus delta, two fills; pending — sibling dims, count lags; selected — clean; success — Feed only.
## 3. Copy & Honesty [C] (opus; probes `r8-copy-{1..7}.js` + `.log`, `r8-copy-census.log`; screenshots `r8-copy-*.png`; every reaction POST intercepted; no write path exercised)

r7-move verification (live + source + backend):
- **[C-R8-1]** PARTIAL — delete-imported-data row `SettingsPage.tsx:210-212` "Removes your imported lessons — your timetable and history. Shared teacher, course and room entries stay."; body `:288` "Your imported lessons — timetable and history — are removed from HOney. Shared teacher, course and room entries stay. You can import again with Sync now." ↔ `services/accounts.ts:238-240` one `DELETE FROM user_lesson_exposures WHERE honey_id = ?`. The r7 overstatement ("all … data imported from the school portal") is gone; new understatement: the "what stays" list omits `lesson_instances` (school-wide rows, no `honey_id`, `importer.ts:100`, mig. 002) and organic `entity_registry` rows — "removes your imported lessons" is true at the visibility level (every read passes the exposure join, `services/timetable.ts:35-39,45-48`), not at the row level.
- **[C-R8-2]** REFUTED (unchanged) — delete-account row `SettingsPage.tsx:96-98` "Removes your HOney account and everything imported with it. Published experiences stay…" ↔ `accounts.ts:191-193` `DELETE FROM honey_users` + cascades (`honey_sessions`, `school_connections`, `import_consents`, `user_lesson_exposures`); shared rows stay; confirm body `:309` "This permanently removes your account and imported data. This cannot be undone." still omits "Published experiences stay" and carries no "what stays" clause — the two delete flows are now asymmetric.
- **[C-R8-3]** CONFIRMED — login footnote `LoginPage.tsx:65-66` "…Your timetable and history come along with it, and again whenever you press Sync now." ↔ Settings row `SettingsPage.tsx:200-204` "…your timetable and history are imported from the school portal when your account is created, and again whenever you press Sync now. Delete them below at any time." — two data types and two triggers match; `syncTimetable` callers still exactly two (`routes/auth.ts:32` guarded `if (result.created)`, `routes/data.ts:17`). Reachability note: "Sync now" names a control (`SettingsPage.tsx:159`, `TimetablePage.tsx:160`) the login reader has not yet seen; the elided verb ("…come again whenever…") is grammatical.
- **[C-R8-4]** CONFIRMED — 404 title "Page not found · HOney" = h1 "Page not found" (`NotFoundPage.tsx:6,14`); never-listed entity keeps "Not found · HOney" (`EntityPage.tsx:70`) with h1 "Nothing is listed at this address." (`:76`) — distinct from the 404, still ≠ its own h1.
- **[C-R8-5]** CONFIRMED — six malformed `?date=` inputs → `location.search="?date=2026-09-02"`, h1 "Wednesday, September 2, 2026", no notice, no "Back to today"; back button lands on `/home`. Locale: `TimetablePage.tsx:26` `undefined`; en-US: h1 "Saturday, August 22, 2026" / stepper "Sat, Aug 22" / "No lessons on Sat, Aug 22"; September "Sep"; en-GB: "Wednesday, 2 September 2026" / "Wed 2 Sept" — one locale per page in both.
- **[C-R8-6]** CONFIRMED with residue — lessons day note "P1–P6 are the school's six lesson periods. Last synced 6 h ago."; empty day " Last synced 6 h ago." (leading space, `TimetablePage.tsx:170-173`); with `lastSyncedAt === null` on an empty day the paragraph would render empty.
- **[C-R8-7]** CONFIRMED, accurate — Save: title "Save school login", body "Enter your school login once; it stays encrypted on this device (a browser is less protected than a phone's secure storage) so routine portal time-outs reconnect on their own.", label "Stay connected on this device.", checked true, submit "Save login". Reconnect (`/api/me` `portalTokenValid:false`): title "Reconnect school account", body "The portal session ended. Sign in again to restore it — your HOney data stays as it is. If you keep the login on this device it stays encrypted here (a browser is less protected than a phone's secure storage).", same label, checked false, submit "Reconnect" (`ReconnectDialog.tsx:25-51`). Against `lib/portalCredentials.ts:20-21,78-88,115-124`: AES-GCM ciphertext and its key both in `localStorage`; true as stated; no surface says the key shares the store (the sibling private-notes bullet `SettingsPage.tsx:255-259` does say "the key sits on this device too").
- **[C-R8-8]** SPLIT — delisted wording unified: `shared.tsx:69` "This entry is no longer listed." = `EntityPage.tsx:106` = `ComposePage.tsx:257` (live on a synthesised delisted teacher). Provenance NOT unified: `shared.tsx:15-19` `PROVENANCE_LABELS` ("Verified — has taken this over time", Mine `MinePage.tsx:246`) vs `ExperiencePost.tsx:18-22` `PROVENANCE_LINE` ("from someone who has taken this over time", Feed/Entity).
- **[C-R8-9]** CONFIRMED — the three r7 stale comments are now true (`ReconnectDialog.tsx:14-18`, `TimetablePage.tsx:22`, `:107-109`); new comments at `useApi.ts:24,90`, `shared.tsx:179`, `TimetablePage.tsx:56-57,329-330` true. One new false comment: [C-R8-N7].
- **[C-R8-10]** CONFIRMED present, ungrammatical at 1 — `FeedPage.tsx:159-161` `role=status`: "1 experiences" (real feed), "12 experiences", "0 experiences" (empty), "" (loading), "Could not load" (error); `[aria-live]` 0 on every branch.
- **[C-R8-11]** CONFIRMED — pending reaction: `aria-disabled="true"`, `aria-pressed="true"`, opacity 0.7, cursor default, class `react-btn react-btn--on`; name "Matches my experience" and visible "0" unchanged; a second tap inside the window produces zero feedback (live texts identical, POST attempts 1); failure message "Could not save that reaction. Please try again." (`ExperiencePost.tsx:181`) only after the round trip.
- **[C-R8-12]** CONFIRMED present — "strongly protected" (`WhyPage.tsx:71-76`), bounded by "anonymity is a design boundary, not magic."

New defects:
- **[C-R8-N1]** `role=region aria-label="Editor"` on a branch with no editor (`ComposePage.tsx:214-219`): live regions `[{aria:"Editor", text:"Could not reach the HOney server… Try again"}]`, `hasEditor:false`; the same name labels the real editor.
- **[C-R8-N2]** Entity page contradicts itself with the registry down: `listed = !entities || …` (`EntityPage.tsx:54`) so with `entities === null` it renders "Share your experience" (`:95-102`), the present-tense intro "What students have experienced in classes with ChenJenny. No single Experience is the whole picture." (`:126-128`) and the `role=alert` "Could not reach the HOney server…" together; live `ctas: ["Share your experience","Try again"]`, `posts: 0`.
- **[C-R8-N3]** The one-locale fix follows the browser locale while all copy stays English: under `zh-CN` the empty-day sentence renders "No lessons on 9月6日周日", heading "2026年9月6日星期日", stepper "9月6日周日" (`r8-copy-7.log`); this is a Chinese school (every directory name is Chinese).
- **[C-R8-N4]** The hard-coded locale moved, not removed: `HomePage.tsx:43,47` still `"en-GB"` → Home "Wednesday 2 September" vs Timetable "Wednesday, September 2, 2026" on the same date in the same session.
- **[C-R8-N5]** Feed AT channel ≠ sight channel: error → status "Could not load" vs alert "Could not reach the HOney server. Check your connection and try again."; empty → "0 experiences" vs "Nothing has been shared yet. A short thought is enough to begin."
- **[C-R8-N6]** 404 "Skip to content" (`NotFoundPage.tsx:10-12`) targets `#not-found-home` ("Go home"), rendered inside `#main`; `#main[tabindex=-1]` (`:9`) added and unused.
- **[C-R8-N7]** `packages/backend/src/app.ts:104-106` comment "The service worker must be re-checked on every load…" with `no-cache` set at `:106`; live `/sw.js` → `cache-control: max-age=14400` on `cf-cache-status: REVALIDATED` and on a forced MISS; `/assets/*` immutable and `/` must-revalidate do hold.
- **[C-R8-N8]** "Save login" can clear the saved login: `ReconnectDialog.tsx:40` `submitLabel="Save login"` vs `:53-55` `if (stayConnected) authorize(creds); else portalCredentials.clear()`; the box opens ticked; untick and the same primary label deletes any stored credential (read from source; not pressed).
- **[C-R8-N9]** Empty-day note leading space / can render empty (see [C-R8-6]).
- **[C-R8-N10]** Register drift (pre-existing, not introduced): one data set named "Delete imported data" / "your imported lessons — your timetable and history" (Settings), "History" (nav, h1), `aria-label="Lessons"` + "Search lessons…" (History page); "school portal" / "School Portal" / "the portal" / "school login"; "experiences" / "posts" / "Experience". Unreachable copy introduced: 0.

Required fields:
- **[C-R8-20]** Strings: 492 distinct (405 student surface, 87 Dash). Delta: 9 changed (`SettingsPage.tsx:211,288`, `LoginPage.tsx:66`, `NotFoundPage.tsx:6`, `ReconnectDialog.tsx:32,48-50`, `shared.tsx:69`, `TimetablePage.tsx:171,26`), 2 visible + 3 accessible-only added ("Skip to content" `NotFoundPage.tsx:11`; `role=status` texts `FeedPage.tsx:159-161`; `aria-label="Editor"` `ComposePage.tsx:219`; `describedBy` on theme `ThemeControls.tsx:37,46` and lesson `TimetablePage.tsx:468-469`), 1 removed (old reconnect label).
- **[C-R8-21]** Inflations: 1 ("strongly protected"); 0 added. Watch item "Share anonymously" (`ComposePage.tsx:373`), qualified at `:389`, `SettingsPage.tsx:231-234`, `WhyPage.tsx:73-75`.
- **[C-R8-22]** Dark patterns: 0/7 (forced continuity, hidden cost, scarcity, confirmshaming, pre-checked consent, nagging, roach motel). Save-login pre-tick = device-convenience restating the row the user just pressed ("Save school login…"), body bound as `aria-describedby`; login checkbox unchecked by default (`LoginPage.tsx:22`); reconnect mirrors stored state (live false); invite aside exactly 1 at 12 posts (`SHARE_PROMPT_EVERY = 8`).
- **[C-R8-23]** Jargon / unclear labels: 6 — two provenance registers; "clearing site data" (`SettingsPage.tsx:374`); "press Sync now" on the login page; "1 experiences"; "Editor" on the error branch; "Skip to content" → Go home.
- **[C-R8-24]** Label→behaviour mismatches: 4 — "Save login" ↔ `clear()` (`ReconnectDialog.tsx:40/55`); "Skip to content" ↔ `#not-found-home` (`NotFoundPage.tsx:11/16`); `aria-label="Editor"` ↔ banner only (`ComposePage.tsx:219/221-232`); status "Could not load" ↔ alert text (`FeedPage.tsx:161/120-131`).
- **[C-R8-25]** Privacy-claim map: 9 claims (no author column `db/database.ts` mig. 003; `auth:false`/`credentials:"omit"` `api/client.ts:227-229,349-353`; `ownership_hash` sha256; `formatDayBucket` `format.ts:69-79`; `free_text_not_accepted` `routes/experiences.ts:208`; ownership-key caveat `lib/ownershipKeys.ts:185-186,240,257`; portal credential store `portalCredentials.ts:20-21,78-88,115-124`; two `syncTimetable` callers; "design boundary, not magic" ↔ `contract.ts:12-16`) — `git diff --stat 213707d..13cbcee` on `api/`, `format.ts`, backend routes/db/services = empty; 0 drift.
- **[C-R8-26]** Title map (15 routes): 13 match or intentionally differ; 404 fixed; 1 mismatch (never-listed entity "Not found · HOney" vs h1 "Nothing is listed at this address."); 404/never-listed collision resolved; `/login` "Sign in · HOney" (wordmark, no h1 text).
- **[C-R8-27]** Two-truths-on-one-surface: 5 (Settings adjacent rows `:118` + `:171` on reconnect; Save body + label; feed empty visible vs status; feed error visible vs status; entity registry-down intro + CTA + alert). One clause on four surfaces: "(a browser is less protected than a phone's secure storage)" ×4 (`LoginPage.tsx:57-58`, `SettingsPage.tsx:171`, `ReconnectDialog.tsx:31,32`) in four different sentences ("kept only here" / "kept only on this device" / "stays encrypted on this device" / "stays encrypted here"). One-sentence-three-surfaces: 0. Date formats live across the app: 7 ("Wednesday, September 2, 2026", "Wed, Sep 2", "Wednesday 2 September" (Home, en-GB), "Tue, Sep 1", "Sep 1, 2026", "September 2026", "6 h ago"); within the Timetable one locale; across Home/Timetable two locale sources.
## 4. Weight & Friction [W] (opus; probes `r8-wf-{inflight,ptrpoint,cachectl,tt,live,live2,modal,swr,interrupt,net,perf,perf2,anim,boot,cls,sw1,sw3,sw4,sw5,ring}.js` + `.log`, `r8-wf-cls2.log`; origin curled directly at `127.0.0.1:8871`)

r7-move verification:
- **[W-R8-1]** CONFIRMED — `/api/entities` delayed 3 s, PTR committed at t=1,904 ms → second `GET /api/entities` at 1,904 ms + `GET /api/directory` at 1,906 ms (r7 0); one spinner window 606 ms; screen after settle = the post-refresh body (`useApi.ts:20,24,90`; `PullToRefresh.tsx:77-78`).
- **[W-R8-2]** REFUTED for the cache — SPA re-entry first frame (0 skeletons = cache hit) renders the PRE-refresh body when the replaced request finishes last; control with delays swapped → post-refresh body; rule = last-to-arrive wins (`useApi.ts:69` `cache.set` above the `cancelled` guard at `:70`).
- **[W-R8-3]** CONFIRMED — 14 cold views, duplicate API paths 0; `/api/entities` max 1 (explore, entity, `compose?entityKey`, mine), 0 on the other 10.
- **[W-R8-4]** CONFIRMED — 12 combinations (320×568 / 360×620 / 390×620 × 2026-09-02 / 2026-08-24 × no-preference / reduce): exactly two `scrollTop` transitions per run — `null→0`, then one `0→156/208/211/214/216`; no intermediate samples at 16 ms; owner `scroll-behavior` `smooth` / `auto` (reduce), step discrete under both (`TimetablePage.tsx:327-334`).
- **[W-R8-5]** CONFIRMED with caveat — the step lands +79…+410 ms after FCP (12/12 after FCP, median ≈ +174); FCP 152–436, step 254–735 ms.
- **[W-R8-6]** CONFIRMED — date step: next 208→0, prev 0→0; no re-landing (`TimetablePage.tsx:58-62,208`).
- **[W-R8-7]** CONFIRMED — retry (timetable route aborted → Try again): `scrollTop` 0 before / 0 after, landed element `section.focus-landing[data-landed]`.
- **[W-R8-8]** CONFIRMED — SPA re-entry lands (208 → /home → 208); `settledDates` is a per-mount `useRef`.
- **[W-R8-9]** CONFIRMED — cold reload of a cached date lands (0→208 at 229 ms, FCP 128, +101 ms).
- **[W-R8-10]** CONFIRMED — `[aria-live]` 0 on the whole page in every feed state (real, empty, 8-item, 12-item, error); live regions: `div.ptr__disc[role=status]` (empty) + `p.sr-only[role=status]` 1×1 (`FeedPage.tsx:159-161`).
- **[W-R8-11]** CONFIRMED, measured — arrival 1 mutation / 13 chars ("1 experiences"); 8-item 1/13; 12-item 1/14; load-more 1 `characterData` / 14 chars, cumulative total ("16 experiences", "24 experiences"); tab change 2 mutations (emptied then refilled, 13 chars net, no tab name); error 1/14 ("Could not load") plus the separate `role=alert` 64-char banner.
- **[W-R8-12]** PARTIAL — origin (curled at `127.0.0.1:8871`) honours `app.ts:99-108` exactly: `/sw.js` `no-cache`; `/assets/*` `public, max-age=31536000, immutable`; `/`, `/manifest.webmanifest`, `/icon-192.png` `public, max-age=0, must-revalidate`. Edge (`server: cloudflare`): `/sw.js` → `max-age=14400` (REVALIDATED); `/assets/*` unchanged (HIT, age 258/124); `/` and manifest DYNAMIC, unchanged; `/icon-192.png`, `/favicon-h.png` → `public, max-age=14400, must-revalidate`. Pattern: every edge-cached file with origin TTL <4 h is rewritten to 14400. `/` revalidation: `If-Modified-Since` → 304; `If-None-Match` → 200 (edge strips the ETag `W/"73e-1a05f8903eb"`); `/sw.js` `If-None-Match` → 304.
- **[W-R8-13]** CONFIRMED — plain `/experiences/compose` cold: 10 requests, 1 data (`GET /api/me`); `?entityKey=`: exactly 1 `GET /api/entities` + 1 `GET /api/directory`.
- **[W-R8-14]** CONFIRMED — served JS `IntersectionObserver` ×1 (`useLoadMoreSentinel`, `rootMargin:"600px 0px"`); "reveal" ×2 both React internals; served CSS `.reveal` 0, `is-in` 0; 6 `@keyframes`.

New defects:
- **[W-R8-15]** The refresh's own result is discarded by the request it replaced: gen1 delayed 3,000 ms / gen2 120 ms with distinguishable bodies, PTR at ~1.9 s, then SPA `/why` → `/explore` first frame → `onScreen=GEN2`, `cacheServes=GEN1` (0 skeletons, `loading=false`); control swapped → GEN2. Cause `useApi.ts:67-70`; `inflight.delete` evicts the promise, nothing cancels the old fetch's cache write. Consequence: pull, see fresh data, tab away and back → pre-refresh data with no skeleton and no in-progress signal.
- **[W-R8-16]** `13cbcee` broke the mobile bottom-sheet rule block: `components.css:818-828` opened the nested `@media` after `.modal{}` closed at `:817`; `git show 213707d:…components.css:815-824` has all six declarations inside `.modal{}`; served CSS carries 1 orphan-declaration site, 209 dead un-minified bytes; live on the lesson dialog and the Appearance sheet at 390×844 / 360×620 / 320×568: `borderRadius "20px/20px"`, borders 1 px, `padding "24px"` (no `env(safe-area-inset-bottom)`), `animationName "rise-in"`; the intended `max-height` does work (776.48 at 844; 620 / 568 = 100dvh at compact heights).
- **[W-R8-17]** The date stepper does not update `?date=`: `/timetable?date=2026-09-02` → Next day → blocks 3→2 while `location.search` stays `?date=2026-09-02` (`TimetablePage.tsx:42-46` mount-only; `setDate` never writes back).
- **[W-R8-18]** At compact heights the landing scrolls the day stepper off-screen: `.daynav` top −140 px (360×620), −192 px (320×568); +16 sticky at 390×844 (`features.css:770-778`).
- Clean: **[W-R8-19]** refresh spam 5 pulls / 923 ms → 1 round / 2 requests (`refreshingRef` + `MIN_SPIN_MS=650`, `PullToRefresh.tsx:57,74,82`); **[W-R8-20]** cold-landing gate: production bundle (StrictMode no double-effects), return to a landed date `08-24 (208) → 08-25 (0) → 08-24 (0)`, first lesson top 237 in view; **[W-R8-21]** `100dvh` modal CLS 0 on open (3 viewports × 2 dialogs), never scrollable 6/6; **[W-R8-22]** static daynav CLS 0 before/after the landing; **[W-R8-23]** status sibling 0 mutations over 5 s idle.

Required fields:
- **[W-R8-25]** App JS `index-KkxCm-lK.js` br **86,192** (r7 85,987, +205) / zstd **88,733** (88,461, +272) / gzip 85,008 / raw **275,493** (+1,403); CSS br 9,292 (+11) / zstd 10,006 / raw 40,849 (−65); Chromium receives 88,579–89,166 B JS (CDP `encodedDataLength`); Dash chunk br 4,406 / raw 13,413, loads on 0/14 student views; no new dependency; `dist/` 665,536 B.
- **[W-R8-26]** Cold requests (total / data / wire B): /home 12/3/155,747; /timetable 12/2 (Shanghai), 10/2 (UTC); /experiences 11/2; /compose 10/1; /compose?entityKey= 12/3; /compose?lessonId= 11/2; /explore 10/3; /teacher/… 13/4; /mine 12/3; /history 12/3; /settings 10/1; /why 10/1. Warm SPA /home→/experiences 3 / 1 / 1,803 B; warm reload /experiences 9 / 1,979 B (JS/CSS from the SW, 0 wire); dupes 0/14; 1 font subset 14/14.
- **[W-R8-27]** 25 cold loads (unthrottled, 4 vCPU droplet, `PerformanceObserver buffered:true`): 11 long tasks ≥50 ms (r7 21), 51–110 ms, all at t=179–440 ms (boot), none after settle; FCP median /home 220, /timetable 208, /experiences 200, explore 188, mine 172; LCP /home 392, /timetable 788, /experiences 388; TTI proxy /home 305, /experiences 400 (r7 317 / 362); input latency 0.3–0.7 ms handler, 32–51 ms route commit.
- **[W-R8-28]** Idle animations 0 on 22/22 route × preference cells (only `/history?select=1` opt-in banner 1 `finished`); `@keyframes` 6; `infinite` 2 (`skeleton-shimmer` loading-only, `ptr-rot` during a 650 ms refresh); `.view` one rule `settle .46s` (1e-06 under reduce).
- **[W-R8-29]** Interrupts on load, 12 routes × 2 preferences: dialogs 0/24, toasts 0/24, badges 0/24, `role=alert` 0/24, `[aria-live]` 0/24; `role=status` PTR disc 24/24 (empty), feed "1 experiences" 2/24, `/history?select=1` opt-in banner 2/24.
- **[W-R8-30]** CLS 0 in 18/18 contention-free cold combinations (3 routes × 2 timezones × 3 viewports); one 0.044 shift on /home under 5-way CPU contention (`home-voices` + `home-foot`, slow-data hazard); tab change 0; load-more 0 (12→24→30); dialog open 0; landing scroll 0.
- **[W-R8-31]** Dark pre-FCP 6/6 (surface at 63–236 ms, FCP 284–1,272 ms); 0 later `<html>` mutations; `theme-color` rewritten 6/6.
- **[W-R8-32]** Reduced motion: 25/25 sampled elements 1e-06 s; owner `scroll-behavior auto` under reduce 12/12; compact landing one discrete step under reduce 6/6.
- **[W-R8-33]** SW: `public/sw.js` byte-identical to r7 (md5 `f7513f02194664efa4e96d50773ec83d`, 4,828 B, `honey-v3`, last touched `03875ed`); seeded eviction mode real: current kept 10/10, stale evicted 5/5, `honey-v2` deleted, `/__index-sig` rewritten; mode fake (1-byte bodies) 8/10 current evicted (`sw.js:33-34` trusts bodies, unchanged); cold start after `stopWorker` ×3: 0 refetches / 0 B; JS-only and CSS-only mismatches evict, exact match no work (signature sort order flipped this build — probe literal corrected); 10 hard loads → 5 entries / 347,347 B, `/api/` entries 0, content-type guard holds; offline shell /timetable 71 ms, /experiences 88 ms.
- **[W-R8-34]** Unrequested bytes: 7 `@font-face` blocks (2,155 B CSS text per payload), 1 subset fetched (28,740 B), 123,968 B across 6 subsets never requested; admin CSS 11 rules / 1,201 B in the student payload; +209 dead bytes (orphaned sheet declarations).
- **[W-R8-35]** SWR Map: 20 forward date steps → 21 `timetable:<date>` keys; back-steps 0 new keys, rendered from cache at ~60 ms with 0 skeleton frames; JS heap 2,860,833 → 5,092,574 B (+2.23 MB) over 20 steps, ~106 KB retained per date; unbounded per session.
## 5. Accessibility [A] (opus; probes `r8-a11y-{ring,react,contrast,dialogs,dialogs2,landing,landing2,compact,404,feedstatus,semantics,skip,targets,targets2,tab,reach,reach2,disabled}.js` + `.log`; screenshots `r8-a11y-*.png`; reactions undone 4/4; no write path exercised)

r7-move verification:
- **[A-R8-1]** CONFIRMED — pending reaction (POST delayed 2,000 ms): tapped pill `opacity 1→0.7`, `cursor pointer→default`, `aria-disabled null→"true"`, `aria-pressed false→true`, `+react-btn--on`; identical on stone and a genuine dark boot; focus stays on the button through the round trip by Enter, Space and pointer (4/4); 1 POST per pass (`components.css:251-256`).
- **[A-R8-2]** REFUTED (no feedback) — a second Enter/Space/pointer at +400 ms: POST count stays 1; diff of every attribute + computed property on the pill `[]`; no status/live update; `pointer-events: auto`, so the tap is accepted and swallowed by `ExperiencePost.tsx:162`.
- **[A-R8-3]** CONFIRMED — `aria-describedby` 7/7 (lesson `lesson-dialog-body` `TimetablePage.tsx:468-469`; theme `appearance-dialog-body` `ThemeControls.tsx:37,46`; report; Save/Reconnect `school-dialog-body`; two confirms `confirm-dialog-body` `Modal.tsx:94`); all 7 `role=dialog`, `aria-modal`, named from the h2, trap holds (0/7 escapes over 9 Tab + 4 Shift+Tab), Escape closes, opener restored 7/7 by Escape and by Cancel/×.
- **[A-R8-4]** REFUTED, unchanged — `inert`/`aria-hidden` on `.app-frame` 0/7 (`Modal.tsx` sets neither).
- **[A-R8-5]** CONFIRMED — nav beneath 7/7 hit-tests to `DIV.modal-overlay`; dialog buttons ≥44 (× 44×44, Cancel 86×44, confirm 179×44, Save login 340×44, theme cards 97×81); 0 sub-44.
- **[A-R8-6]** REFUTED, unchanged (static) — report dialog `describedBy="report-dialog-body"` passed unconditionally (`ExperiencePost.tsx:321`); the id exists only on the form branch (`:336`), so it dangles on the `done` branch (`:322-333`); not exercised (hard limit).
- **[A-R8-7]** CONFIRMED — compose double-fail (`/api/entities` aborted throughout): after a "Try again" that fails again, mouse and keyboard, `activeElement = section[role=region][aria-label="Editor"]`, `data-landed=""`, outline 3 px accent, 11,629/43,842 ring px (26.5 %), scroll 0→0 (`ComposePage.tsx:214-233`).
- **[A-R8-8]** CONFIRMED — feed `[aria-live]` 0 (r7 1); `[role=status]` 3 during load (`ptr__disc` empty, `skeleton` empty, `p.sr-only`), 2 after settle; announced "1 experiences" (13 chars) / "12 experiences" (14 chars, t=386 ms) / tab change "6 experiences" — r7's 1,120-char whole-list announcement gone (`FeedPage.tsx:159-161`).
- **[A-R8-9]** CONFIRMED — `div.feed-stream#feed-panel[role=tabpanel][aria-labelledby="tab-my-classes"]` → "Your classes"; after tab change → "Around school".
- **[A-R8-10]** PARTIAL — 404 skip link present, first Tab stop, ringed; `href="#not-found-home"` lands on "Go home" (106×44, 2,863 ring px), not on `#main` (`mainFocused:false`, 0 ring px); title "Page not found · HOney" = h1. Skip-link tally 13/13 present, 12/13 reach `#main`.
- **[A-R8-11]** CONFIRMED — `hasTouch`, 15 routes × 2 viewports: 162/162 ≥44 at 390×844 and 162/162 at 320×568; `/why` "Back to Experiences" `a.btn.btn--ghost` 170×44 at both widths (r7 133×22).
- **[A-R8-12]** CONFIRMED with residue — `label.stay-connected` `min-height:44px` (`components.css:876`) renders 342×131.5 / 272×151 on `/login` and 340×59 / 270×59 in the Save dialog; all four inset corners + midpoint `elementFromPoint` → the label; the nested `input[type=checkbox]` element is still 22×22 (the only sub-44 element on `/login`, 1/6), but its hit area is the label.
- **[A-R8-13]** REFUTED, unchanged — `#main` inset ring on 12/12 (27,005 px light / 22,584 dark, 5.81 / 9.28); content still under the 3 px band at the sampled edges on Settings (`DIV.setting-row`), History, Explore, Why, Dash — 5/12, both boots.
- **[A-R8-14]** CONFIRMED — compact heights 6 rows (320×568, 360×620, 390×620 × 2026-09-02 and 2026-08-24): `.daynav` `position:static` 6/6 (`features.css:774`); `daynav×h1 = null`, `daynav×note = null` 6/6 (r7 8/8/5 overlaps); last block bottom ≤ nav top 6/6 (396≤494, 274≤494, 448≤546, 326≤546, 443≤546, 300≤546); 3/3 blocks hit-test to themselves 6/6.
- **[A-R8-15]** CONFIRMED — after the cold landing (`scrollTop` 208/156/216 today, 208/156/213 on 0824) Shift+Tab from the first block reaches Sync now → History → (Back to today) → Next day → Pick a date → Previous day → skip link, each scrolled into view (`inView:true` every stop, 6/6); every daynav control keeps its name and role; AT order daynav `group "Choose a day"` → h1 → note → `region "Day timeline"` → blocks.
- **[A-R8-16]** CONFIRMED — 7 landing surfaces × mouse/touch/keyboard: `data-landed=""`, 3 px ring, 10,269–22,494 px, scroll 0→0 (21/21 + compose = 24/24); Timetable retry sampled every 100 ms for 3 s: distinct `scrollTop` samples `[0]` at 320×568, 360×620, 390×620, 390×844, mouse and keyboard — r7's 77 px gone (`TimetablePage.tsx:56-60,205-210,324-334`).
- **[A-R8-17]** CONFIRMED — ordinary mouse/touch taps on 8/8 landings: `data-landed` absent, `outline none`, ring px ≤3; click-away clears; retry twice rings twice; retry then Tab moves on with the attribute cleared; navigate away + back `anyLanded=0`; landings out of the Tab order 8/8.

New defects:
- **[A-R8-18]** SHIPPED REGRESSION — every `<button>` paints the focus ring at rest (`foundations.css:64-74`, served `button,button:focus-visible,…{outline:3px solid var(--accent);outline-offset:3px}`). With nothing focused: Timetable 7/7 visible buttons, Feed 5/5, Settings 12/12; same-element `restOutline` = `afterProgrammaticFocus` = "rgb(51,102,124) solid 3px" (`same:true`); 24/111 Tab stops on the 10 primary routes (21.6 %) already ringed — Settings 11/17, Timetable 7/14, Feed 5/15, Compose editor 1/8; accent pixels on an unfocused screen: Settings 21,876, Timetable 37,094 (`r8-a11y-restring-*.png`); links unaffected 10/10 — so the ring means "focused" on a link and "is a button" on a button.
- **[A-R8-19]** SHIPPED REGRESSION — mobile bottom-sheet declarations orphaned inside the nested `@media` (`components.css:814-829`): lesson modal at 390×844 `borderRadius "20px"` all corners, border 1 px left/bottom, `padding 24px`, `animationName "rise-in"`, rect [0,398,390,446]; 320×568 same, `maxHeight 568px`, rect [0,122,320,446]; only `max-height:100dvh` survives (`r8-a11y-modal-sheet-{390,320}.png`).
- **[A-R8-20]** The untapped reaction pill also goes `aria-disabled="true"`, `opacity 0.7`, `cursor default` during the other pill's pending window (`ExperiencePost.tsx:226,238` `aria-disabled={busy || undefined}` on both) — AT hears both disabled, sight sees both dim, 4/4 passes both themes.
- **[A-R8-21]** Three unnamed `role=status` nodes co-exist on the feed during load (`ptr__disc` 36×36 visible 0 chars, `skeleton` 0 chars, `p.sr-only`), 2 after settle; none named, no `aria-atomic`; `p.sr-only` correctly 1×1 `clip: rect(0,0,0,0)` on both themes (`foundations.css:86-96`); fires on every branch — error "Could not load", empty "0 experiences" while the visible copy says "Share the first one"; "1 experiences" unpluralised; nearest landmark the unnamed `main`.
- **[A-R8-22]** The new entity `namesError` branch drops focus to `<body>` on a retry that fails again (abort `/api/entities` or `/api/directory`, `/experiences/teacher/t_23348879d1b4`, "Try again" 77×44 inside the named region "ChenJenny experiences"): `activeElement=(body)`, `data-landed=null`, 3 ring px; cause `EntityPage.tsx:48` arms `useRetryFocus(feed.loading)` while `:133` `reloadNames()` toggles only `directory/entities.loading`; `armed.current` stays `true`, so a later unrelated feed settle would fire a spurious landing — the r7 compose defect reproduced in the code written to fix it.
- **[A-R8-23]** Login checkbox accessible name = the whole 4-sentence label (199 chars) vs 30 chars for the same control in the Save dialog (`LoginPage.tsx`, `ReconnectDialog.tsx:44-54`).
- **[A-R8-24]** 404 skip link → `#not-found-home`; Tab order skip → Go home → `(body)`; `#main[tabindex=-1]` never focused (`NotFoundPage.tsx:9-17`).
- **[A-R8-25]** 404 has 0 `nav`, landmarks `{main:1}` unnamed vs `{navigation:2, main:1,…}` on every authenticated route (`NotFoundPage.tsx:11` outside `AppLayout`); a keyboard user has 2 stops and one CTA.
- **[A-R8-26]** Lesson dialog `aria-describedby` resolves to the whole `<dl>` as one unpunctuated run ("Time09:00–10:30TopicIELTS-SpeakingTeacherChenJenny…Room213"), the same text then read as content (twice); theme dialog description "Saved on this device and applied before the page paints." — a footnote, not the four choices.
- **[A-R8-27]** No daynav control lost name or role at compact heights; the hidden `input.daynav__picker` (5×4, `aria-hidden`) is never in the Tab sequence — no defect.
- **[A-R8-28]** The compact landing hides the heading and note by scrolling rather than overlaying: after landing `h1.schedule-header` off-screen on 5/6 compact rows (`y=−62/−10/−70/−67`), `.timetable-note` off-screen 4/6, daynav at `y=−192/−140/−200`.

Required fields:
- **[A-R8-29]** Contrast: 24 surfaces × 2 boots (stone; genuine dark boot `colorScheme:dark`, no stored theme → `data-surface=night`, root `rgb(20,23,26)`): 0 failures; floors 4.66 stone / 5.83 night (`.home-head__stale`, 15 px/400), identical to r7; enabled text <4.5 = 0; focus rings vs adjacent ground `.focus-landing` 5.81–6.13 / 8.36–9.28, `#main` 5.81 / 9.28, `.daynav__date` 6.13 / 8.36 (7/7 surfaces); disabled text (recorded) compose Share 5.34 / 8.07, Keep private 5.34 / 8.07, Settings Export 5.66 / 7.26 — all `opacity:1`, `color: var(--muted)` (`components.css:66-71`; r7 `opacity:.65` gone; one disabled text treatment, though Share keeps a soft grey fill and Keep private is transparent with a border); placeholders 5.34–5.63 / 7.27–8.07; nav active 13.26 / 15.27.
- **[A-R8-30]** Focus order: 202/202 Tab stops paint a visible ring across 14 route states (r7 201/201) — but 24/111 on the 10 primary routes already paint it unfocused ([A-R8-18]), so the indicator distinguishes focus on 87/111. Sequences: Home 9, Timetable 13 (skip → Previous → Pick a date → Next → History → Sync now → 3 blocks 282×84 → 4 nav), Feed 15, Compose editor empty 8 / typed 10 (Share anonymously #3, Keep private #4), Settings 18, Login 5.
- **[A-R8-31]** Keyboard reachability: 22 reachable / 1 unreachable (compose Share/Keep private are `disabled` until text — by design) / 0 absent; Enter opens + Escape closes on Delete account, Delete data, Save login, lesson, `···`→Report with nothing confirmed; PTR keyboard alternative: none (`.ptr` not focusable, no in-page refresh control on any route); after Enter on a History "Select" row, `activeElement=(body)` on arrival at the composer.
- **[A-R8-32]** Landmarks: authenticated routes nav 2 ("Primary" in the `display:none` rail, "Primary, mobile") + unnamed main + named regions (Home "Now and next"/"From your classes"; Explore group "Everything listed" ⊃ 4; Timetable group "Choose a day" + region "Day timeline"; Settings 5 regions + group; History "Lessons"; Mine "Your notes & posts"; Entity "<name> experiences" in OK and error branches; Compose editor "What this is about" + "Editor"); 404 `{main:1}` unnamed, 0 nav; Login `{main:1}`, unnamed form; duplicate unnamed landmark: the unnamed `main` on every route; banner/contentinfo 0; Explore's search outside any `search` landmark.
- **[A-R8-33]** Skip link: present 14/14 surfaces (r7 12/13); lands on `#main` with the inset ring 12/13 (27,005 / 22,584 px); 404 → "Go home"; Login "Skip to sign-in" → `#school-username`; 134×48 when focused.
- **[A-R8-34]** Touch census (`hasTouch`, coarse, Asia/Shanghai): 390×844 162/162 ≥44; 320×568 162/162; sub-44 list empty (r7 1 per viewport); all eight "Try again" 77×44; reactions 49×44, `···` 44×44, `.modal__close` 44×44, nav items 85×46 / 67.5×46, theme cards 154×81 / 119×81; outside the census `/login` 6 interactive, 1 sub-44 element (the nested checkbox 22×22 inside the 342×131.5 / 272×151 label); desktop 1280×900 `hasTouch` 1 sub-44 `a.home-voices__body` 936×22.5 (unchanged).
## 6. Cross-pass reconciliations

- **Rest-state ring** — found independently by the orchestrator ([O-R8-1], computed style on 5/5 feed buttons), Structural (25/25 on three routes), Visual (five routes × three viewports, night colour, adjacent-ring collisions) and Accessibility (24/111 Tab stops, pixel counts, `restOutline === afterProgrammaticFocus`). Weight & Friction's `r8-wf-ring.js` did not contradict. Consensus: shipped in `5c0cec9`, absent at `213707d`.
- **Dead sheet skin** — found independently by all five passes and the orchestrator ([O-R8-2]); Weight & Friction adds the regression proof (`git show 213707d`) and the 209-byte count; Visual adds the 18-open census and the two-sheet-grammar fact; Structural/Accessibility add 320×568 geometry. Consensus.
- **Compact landing** — Visual, Accessibility and Weight & Friction agree the step is instant, once, 0 px on retry, survives re-entry, and that the daynav/heading are scrolled off-screen on arrival. Visual and Weight & Friction agree a date step does not land (Structural did not test it); the orchestrator's read of `TimetablePage.tsx:56-62,324-336` explains why: `useApi` keeps the previous day's lessons during the step, so `visible.length` does not change and the `[date, visible.length]` effect runs only on the loading render where `land` is false — the "first settle of a date" contract in the comment is not what the effect implements. Recorded as a contract deviation, not re-litigated as a design choice.
- **Entity registry error** — Structural and Accessibility independently measured the second failure landing on `<body>` and diagnosed the same cause (`useRetryFocus(feed.loading)` vs `reloadNames()`); Copy adds the contradictory copy on the same screen; Structural adds that a loaded feed is hidden. Consensus.
- **Feed status text** — Structural, Copy, Accessibility and Weight & Friction all read "1 experiences"; Copy and Accessibility flag "0 experiences" / "Could not load" against the visible copy; Weight & Friction adds the double announcement on error and the cumulative total on load-more. Consensus.
- **URL after a date step** — Structural, Visual and Weight & Friction all measured `location.search` unchanged after Next/Previous/Back to today. Consensus.
- **Disabled buttons** — Visual and Accessibility agree: one text recipe, two fills; Visual adds that disabled buttons now wear the rest-state ring.
- **Loading states on Mine / Entity / Compose** — Visual reports 0 skeleton blocks with "the endpoint" delayed 5 s [V-R8-20]; the r7 Preserve list verified a registry-pending skeleton on Compose and Structural/Accessibility observed `<Skeleton lines={4}>` inside Entity's `feed.loading` branch. The delayed endpoint is not identified in the Visual log, so this is recorded as unreconciled and NOT used for scoring.
- **`sw.js` header** — Copy observed `max-age=14400` on the wire and flagged the new backend comment; Weight & Friction curled the origin directly (`127.0.0.1:8871`) and confirmed `no-cache` there, attributing the rewrite to the edge (uniform 14400 on every edge-cached file with origin TTL <4 h). Scored as the origin's intent; relayed as an edge fact.
- **Mine / Explore under registry failure** — Structural measured Mine silent under either abort and Explore silent under a directory abort; Visual's states census independently recorded Entity/Mine rendering success copy with the endpoint aborted. Consensus that `useNames().error` is consumed by one of three callers.
- **Touch targets** — Structural (520 boxes, sub-44 = 2 nested checkboxes) and Accessibility (162/162 per viewport, sub-44 list empty because the label is the hit area) agree on the fact: the only sub-44 element is the login checkbox inside its ≥44 label.
- **Contrast** — Visual and Accessibility agree: 0 genuine failures on both boots, floors 4.66 / 5.83; Visual's nine 1.00 readings are the documented ancestor-walk artefact on the active nav label (true 13.26 / 15.27).
- **Idle motion / weight** — Visual (0/22) and Weight & Friction (0/22, 6 keyframes, 2 infinite loading-only) agree; bundle +205 B br vs r7, under 100 KB by both encodings.
- **Unchanged r7 residue confirmed by ≥2 passes** — `inert` 0/7 [A-R8-4]; `#main` band 5/12 [A-R8-13]; report `describedBy` dangling on the done branch [A-R8-6]; two provenance registers [C-R8-8]; one-item `···` menu without a name [S-R8-18]; header Share + invite at ≥9 posts [S-R8-19]; `.swatch` literals [V-R8-18]; 123,968 B unrequested fonts + 1,201 B admin CSS [W-R8-34]; `liveAssetSet` trusting bodies [W-R8-33]; unbounded SWR map [W-R8-35]; no keyboard refresh [A-R8-31].
