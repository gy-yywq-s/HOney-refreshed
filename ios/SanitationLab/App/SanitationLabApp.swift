//
//  SanitationLabApp.swift
//  SanitationLab — a standalone app around the credential-image pipeline.
//  Not HOney: no login, no feed, one screen and a harness.
//

import SwiftUI

@main
struct SanitationLabApp: App {
    var body: some Scene {
        WindowGroup {
            LabRootView()
        }
    }
}
