# HOney iOS (Swift)

Built and tested via GitHub Actions macOS-runner CI (no local Xcode in the dev box).
Populated in milestone M6. Access + Timetable Day-view carry the legacy design *whole*
(modernized); everything else is redesigned. No legacy code is copied.
