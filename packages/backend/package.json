{
  "name": "@honey/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "db:reset:dev": "tsx src/scripts/db-reset-dev.ts",
    "issuer:keygen": "tsx src/scripts/issuer-keygen.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/blindrsa-ts": "0.4.6",
    "fastify": "^5.1.0",
    "@honey/shared": "workspace:*",
    "@honey/portal-connector": "workspace:*",
    "@fastify/static": "^8.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "vitest": "^2.1.5",
    "@types/node": "^22.9.0"
  }
}
