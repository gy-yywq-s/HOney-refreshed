# HOney design system & brand

New brand identity (wordmark/marks/icon) is generated via `codex exec` → imagegen (M4);
the legacy serif wordmark is not carried forward. Shared design tokens (color/type/space/
radius/motion/components) are consumed by both iOS and Web.
